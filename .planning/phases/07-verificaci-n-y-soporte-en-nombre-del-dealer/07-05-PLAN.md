---
phase: 07-verificaci-n-y-soporte-en-nombre-del-dealer
plan: 05
type: execute
wave: 2
depends_on: ["07-03"]
files_modified:
  - mi-pagina.html
  - assets/mi-pagina.js
autonomous: false
requirements: [ADMIN-04]
user_setup: []

must_haves:
  truths:
    - "Cumple las decisiones D-10, D-12, D-14, D-15, D-16 de 07-CONTEXT.md."
    - "`mi-pagina.html?org=<id>` abierto por un administrador carga la página de esa empresa por `/admin/organizaciones/<id>/pagina` y todas las escrituras van a ese prefijo."
    - "En modo soporte no se ven «Publicar página» ni «Despublicar»; sí la franja «Está editando la página de <nombre> en su nombre» y el campo de motivo, que viaja como `motivo` en cada escritura."
    - "Un no-admin que abre `?org=` ve la página de «no encontrada», nunca el editor de otro."
    - "El dealer, en modo normal, ve «El equipo de MercaMaquinarias editó su página el <fecha>» si `editadaPorSoporte`."
    - "El logotipo se sigue subiendo con `reducir(…, true)` (conserva el alfa) en los dos modos."
  artifacts:
    - path: "assets/mi-pagina.js"
      provides: "modo soporte"
      contains: "admin/organizaciones"
---

<objective>
Que el personal use el mismo editor que el dealer para arreglar su página, sabiendo en todo
momento que lo hace en su nombre, y que el dealer vea que alguien la tocó.

Purpose: ADMIN-04 en pantalla (criterio 4).
Output: modo soporte en `mi-pagina.js` y los huecos en `mi-pagina.html`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/07-verificaci-n-y-soporte-en-nombre-del-dealer/07-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Tarea 1: modo soporte del editor</name>
  <files>mi-pagina.html, assets/mi-pagina.js</files>
  <read_first>assets/mi-pagina.js entero, mi-pagina.html entero</read_first>
  <action>
  HTML: añadir (hidden) `#franjaSoporte` (`section.panel` con `p.acceso__aviso` y `#nombreSoporte`,
  `label.campo-v` con `input#motivoSoporte`), `#noEncontrada` (panel-vacío «La dirección que abrió no
  existe o no tiene acceso a ella»), `#notaSoporte` (`p.panel__meta` bajo `#estadoPagina`), e ids en la
  miga y el título para cambiarlos en modo soporte.
  JS: `const ORG = new URLSearchParams(location.search).get('org')`; `BASE = ORG ? '/admin/organizaciones/' + encodeURIComponent(ORG) + '/pagina' : '/mi-pagina'`;
  sustituir TODAS las rutas `/mi-pagina…` por `BASE + …`; en modo soporte, `llamar()` añade
  `motivo` del campo a `opciones.cuerpo` en métodos distintos de GET (crear cuerpo `{}` si falta).
  Arranque en modo soporte: exige sesión; `cargar()` con `silencioso`; si null → `#noEncontrada`.
  Pintado: esconder `btnPublicar`/`btnDespublicar` y no conectar sus escuchas; texto de estado según
  UI-SPEC §3; migas «Inicio › Revisión › Página de <nombre>» con enlace a `admin.html`; título
  «Página de <nombre>» (con `textContent`, no innerHTML). Modo normal: si `estado.editadaPorSoporte`,
  `#notaSoporte` con la fecha en `es-DO`.
  </action>
  <verify><automated>node --check assets/mi-pagina.js && npm run check:encoding && npm run dealer:probar</automated></verify>
  <acceptance_criteria>
  - `assets/mi-pagina.js` contiene `admin/organizaciones` y ya no contiene literales `'/mi-pagina/` fuera de la definición de BASE.
  - `mi-pagina.html` contiene `id="franjaSoporte"` y `id="notaSoporte"`.
  - `reducir(archivo` sigue llamándose con `true` para el logotipo.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="non-blocking">
  <name>Tarea 2: recorrido en navegador</name>
  <action>Recorrido con puppeteer: admin abre `mi-pagina.html?org=`, cambia la descripción, el dealer la ve en su editor con la nota de soporte; capturas en claro y oscuro. La mirada queda pendiente de Victor.</action>
  <verify><automated>script de recorrido en .tmp/fase-07/</automated></verify>
  <acceptance_criteria>El recorrido sale sin fallos; capturas guardadas.</acceptance_criteria>
</task>

</tasks>

<threat_model>
| ID | Amenaza | Mitigación |
|---|---|---|
| T-07-09 | Un no-admin fuerza `?org=` | la API responde 404; el editor enseña «no encontrada» |
| T-07-10 | Nombre de empresa con HTML | `textContent` |
</threat_model>

<verification>`node --check assets/mi-pagina.js`, dealer:probar, check:encoding.</verification>

<success_criteria>El personal edita la página de un dealer desde el mismo editor, sin publicar ni despublicar.</success_criteria>

<output>`.planning/phases/07-verificaci-n-y-soporte-en-nombre-del-dealer/07-05-SUMMARY.md`</output>

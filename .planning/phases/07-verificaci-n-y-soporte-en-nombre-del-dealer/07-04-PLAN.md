---
phase: 07-verificaci-n-y-soporte-en-nombre-del-dealer
plan: 04
type: execute
wave: 2
depends_on: ["07-01", "07-02", "07-03"]
files_modified:
  - admin.html
  - assets/admin.js
autonomous: false
requirements: [ADMIN-02, ADMIN-03]
user_setup: []

must_haves:
  truths:
    - "Cumple las decisiones D-01, D-03, D-05 de 07-CONTEXT.md."
    - "La consola tiene la sección «Empresas» con filtros por estado, búsqueda, sello dar/retirar (retirar pide motivo) y enlace «Editar su página» para dealers aprobados."
    - "La consola tiene la sección «Números de serie» con filtros, la serie, las fotos para leer la placa, el aviso de repetidos y los botones Coincide / Con observaciones (pide nota) / Volver a pendiente."
    - "El botón de sello de la cola de «Aprobadas» pide motivo al retirar."
    - "Tras cada escritura se recargan la sección y la bitácora; la bitácora pinta los rótulos de `anuncio.serie` y `pagina.editar`."
    - "Sin `style=` en línea ni colores literales nuevos; todo texto de usuario pasa por `esc()`."
  artifacts:
    - path: "admin.html"
      provides: "secciones t-empresas y t-series"
      contains: "t-empresas"
    - path: "assets/admin.js"
      provides: "montarEmpresas, cargarEmpresas, montarSeries, cargarSeries"
      contains: "montarSeries"
---

<objective>
Poner en la consola las pantallas de ADMIN-02 y ADMIN-03 sobre las rutas de 07-01 y 07-02, y
la entrada a la edición en nombre del dealer (07-05).

Purpose: criterios 1 y 2 de la fase, desde el sitio.
Output: dos secciones nuevas en `admin.html` y su lógica en `assets/admin.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/07-verificaci-n-y-soporte-en-nombre-del-dealer/07-UI-SPEC.md
@.planning/phases/07-verificaci-n-y-soporte-en-nombre-del-dealer/07-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Tarea 1: sección «Empresas» y motivo en la cola</name>
  <files>admin.html, assets/admin.js</files>
  <read_first>admin.html (secciones t-cola, t-bandeja, t-bitacora), assets/admin.js (solicitudHTML, pedirMotivo, alternarVerificada, montarAdmin, bandeja, bitácora: ROTULOS, valorBitacora, cambioBitacora)</read_first>
  <action>
  HTML según UI-SPEC §1 tras «Cola de revisión», con ids `t-empresas`, `filtrosEmpresas`
  (`data-empresas-estado` aprobada|pendiente|rechazada|''), `buscarEmpresas`, `avisoEmpresas`,
  `listaEmpresas`, `empresasVacio`, `metaEmpresas`.
  JS: `EMPRESAS_ESTADO='aprobada'`, `cargarEmpresas()` (GET `/admin/organizaciones?estado=&q=`),
  `empresaHTML(e)`, delegación de clics: `sello` (si retira, caja `sol__motivo` con motivo obligatorio;
  si da, motivo opcional en la misma caja con botón «Confirmar»), POST a `/admin/organizaciones/:id/verificar`
  con `{ verificada, motivo }`; tras éxito `cargarEmpresas()`, `cargar()` y `cargarBitacora()`.
  Búsqueda con retardo de 250 ms. Enlace `mi-pagina.html?org=<id>` solo si `estado_revision === 'aprobada'`.
  Cola: `alternarVerificada` pide motivo con `pedirMotivo`-equivalente cuando retira (texto propio:
  «Por qué se retira el sello. Queda en la bitácora.»), y reutiliza la misma función de envío.
  Bitácora: añadir rótulos para las claves `revision` («Serie»), `nota` («Nota»), y las de página
  (`descripcion`, `nombre`, `lema`, `web`, `correoPublico`, `telefonoPublico`, `logo`, `banner`,
  `seccion`, `orden`, `foto`, `enlaces`) si `cambioBitacora` usa un mapa; si un valor es objeto o lista,
  `valorBitacora` lo resume (JSON corto o recuento) sin romper.
  </action>
  <verify><automated>node --check assets/admin.js && npm run check:encoding && npm run bitacora:probar</automated></verify>
  <acceptance_criteria>
  - `admin.html` contiene `id="t-empresas"` y `data-empresas-estado`.
  - `assets/admin.js` contiene `function montarEmpresas` y `mi-pagina.html?org=`.
  - `grep -c 'style="' admin.html` no aumenta respecto a antes.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Tarea 2: sección «Números de serie»</name>
  <files>admin.html, assets/admin.js</files>
  <read_first>assets/admin.js tras la tarea 1</read_first>
  <action>
  HTML según UI-SPEC §2 tras «Empresas»: `t-series`, `filtrosSeries` (`data-serie-estado`
  pendiente|conforme|observada|''), `avisoSeries`, `listaSeries`, `seriesVacio`, `metaSeries`.
  JS: `cargarSeries()` (GET `/admin/series?estado=`), `serieHTML(s)` con serie en `code` y clase `num`,
  «Repetido en N anuncio(s)» si `repetidos > 0`, miniaturas enlazadas a la foto completa en pestaña
  nueva, «Ver la ficha» (`equipo.html?id=`), «Revisada el … por …» y nota; botones
  `data-serie-accion` conforme / observada (pide nota obligatoria en `sol__motivo`) / pendiente.
  POST `/admin/anuncios/:id/serie`; tras éxito recargar series, empresas y bitácora.
  `montarEmpresas()` y `montarSeries()` se llaman desde `montarAdmin()` tras `montarBandeja()`.
  </action>
  <verify><automated>node --check assets/admin.js && npm run check:encoding</automated></verify>
  <acceptance_criteria>
  - `admin.html` contiene `id="t-series"`; `assets/admin.js` contiene `function montarSeries` y `/admin/series`.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="non-blocking">
  <name>Tarea 3: verificación visual (pendiente de Victor)</name>
  <action>Recorrido automatizable con puppeteer contra un servidor propio y capturas en `.tmp/fase-07/`; la mirada en claro y oscuro queda pendiente de Victor.</action>
  <verify><automated>npm run auditar (con servidor de demo en 8080)</automated></verify>
  <acceptance_criteria>Capturas guardadas; lista de pasos para Victor en el SUMMARY.</acceptance_criteria>
</task>

</tasks>

<threat_model>
| ID | Amenaza | Mitigación |
|---|---|---|
| T-07-08 | XSS por nombre de empresa, serie o nota | todo por `esc()` |
</threat_model>

<verification>`node --check assets/admin.js`, `npm run check:encoding`, `npm run auditar`.</verification>

<success_criteria>El personal da/retira el sello y revisa series desde la consola.</success_criteria>

<output>`.planning/phases/07-verificaci-n-y-soporte-en-nombre-del-dealer/07-04-SUMMARY.md`</output>

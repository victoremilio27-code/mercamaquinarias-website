---
phase: 07-verificaci-n-y-soporte-en-nombre-del-dealer
plan: 06
type: execute
wave: 2
depends_on: ["07-02"]
files_modified:
  - publicar.html
  - assets/panel.js
  - assets/app.js
autonomous: true
requirements: [ADMIN-03, CONF-01]
user_setup: []

must_haves:
  truths:
    - "Cumple las decisiones D-07, D-09 de 07-CONTEXT.md."
    - "`publicar.html` ya no dice «solo visible para el equipo de verificación» a secas: dice que no se publica, que se coteja con la placa de las fotos y con los demás anuncios, que la ficha lo indicará si cuadra, y que no se consultan registros de robo."
    - "El panel del vendedor enseña, en cada anuncio con serie, «Serie: pendiente de revisión», «Serie cotejada» o «Serie con observaciones: <nota>»."
    - "La ficha pública enseña «Serie cotejada» con el texto de límites solo si `serie_cotejada`."
  artifacts:
    - path: "publicar.html"
      provides: "texto de CONF-01"
      contains: "No consultamos registros de robo"
    - path: "assets/app.js"
      provides: "nota de serie cotejada en la ficha"
      contains: "serie_cotejada"
    - path: "assets/panel.js"
      provides: "estado de la serie en la lista de anuncios"
      contains: "serie_revision"
---

<objective>
Cerrar CONF-01: que lo que el vendedor lee al publicar sea exactamente lo que el personal hace,
y que el resultado se vea en su panel y en la ficha.

Purpose: criterios 2 y 3.
Output: texto de `publicar.html`, línea en el panel, nota en la ficha.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/07-verificaci-n-y-soporte-en-nombre-del-dealer/07-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Tarea 1: texto de publicar y ficha</name>
  <files>publicar.html, assets/app.js</files>
  <read_first>publicar.html:140-160, assets/app.js (bloque de nota-verificado ~987 y su entorno)</read_first>
  <action>
  `publicar.html`: placeholder y ayuda exactos de UI-SPEC §7, con comentario HTML del porqué
  (la promesa anterior no tenía diligencia detrás y la serie además salía por la API).
  `assets/app.js`: junto a la nota de anunciante verificado, si `e.serie_cotejada` (o el nombre
  del campo en el objeto de la ficha: comprobar cómo se mapea el anuncio → `e`; si hay un mapeo
  tipo línea 2132, añadir `serieCotejada: !!a.serie_cotejada`), pintar UI-SPEC §6.
  </action>
  <verify><automated>node --check assets/app.js && npm run check:encoding && npm run check</automated></verify>
  <acceptance_criteria>
  - `publicar.html` contiene `No consultamos registros de robo` y no contiene `solo visible para el equipo de verificación`.
  - `assets/app.js` contiene `Serie cotejada`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Tarea 2: estado de la serie en el panel</name>
  <files>assets/panel.js</files>
  <read_first>assets/panel.js (pintado de la lista de anuncios, avisos de tren motriz)</read_first>
  <action>
  En la tarjeta de cada anuncio, si `a.tiene_serie`: una línea con la clase de meta que ya use la
  tarjeta: `serie_revision === 'conforme'` → «Serie cotejada por MercaMaquinarias»; `'observada'` →
  «Serie con observaciones: <nota escapada>»; nulo → «Serie: pendiente de revisión».
  </action>
  <verify><automated>node --check assets/panel.js && npm run check:encoding</automated></verify>
  <acceptance_criteria>`assets/panel.js` contiene `serie_revision` y `Serie con observaciones`.</acceptance_criteria>
</task>

</tasks>

<threat_model>
| ID | Amenaza | Mitigación |
|---|---|---|
| T-07-04 | Prometer más de lo que se hace | texto literal acotado; «no consultamos registros de robo» |
</threat_model>

<verification>`node --check`, check:encoding, check, auditar.</verification>

<success_criteria>El texto de publicar, el panel y la ficha dicen lo mismo que la diligencia real.</success_criteria>

<output>`.planning/phases/07-verificaci-n-y-soporte-en-nombre-del-dealer/07-06-SUMMARY.md`</output>

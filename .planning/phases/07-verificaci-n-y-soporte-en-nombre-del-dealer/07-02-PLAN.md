---
phase: 07-verificaci-n-y-soporte-en-nombre-del-dealer
plan: 02
type: execute
wave: 1
depends_on: ["07-01"]
files_modified:
  - tools/db.js
  - tools/api.js
  - tools/probar-bitacora.js
  - tools/probar-seguridad.js
  - tools/auditar-permisos.js
autonomous: true
requirements: [ADMIN-03, CONF-01]
user_setup: []

must_haves:
  truths:
    - "Cumple las decisiones D-04, D-05, D-06, D-07, D-08, D-17, D-18 de 07-CONTEXT.md."
    - "La migración `2026-09-serie-revision` va al FINAL de `MIGRACIONES` y añade `serie_revision`, `serie_revisada`, `serie_revisada_por`, `serie_nota` a `anuncios`."
    - "`GET /api/admin/series?estado=` lista los anuncios con serie declarada, con empresa, hasta 4 fotos, y cuántos OTROS anuncios repiten la serie normalizada; solo admin."
    - "`POST /api/admin/anuncios/:id/serie` con `conforme`, `observada` (nota obligatoria) o `pendiente` escribe por `conAdminEnNombreDe('anuncio.serie')` y deja fila sin el número de serie."
    - "`GET /api/anuncios/:id` a quien no es dueño NO devuelve `serie`, `serie_nota`, `serie_revisada_por`, `serie_revisada` ni `serie_revision`; sí `serie_cotejada` (booleano)."
    - "`GET /api/mis-anuncios` devuelve por anuncio `tiene_serie`, `serie_revision` y `serie_nota` para que el vendedor vea el resultado."
  artifacts:
    - path: "tools/db.js"
      provides: "migración, ACCIONES_BITACORA['anuncio.serie'], seriesParaRevisar, anotarRevisionSerie, columnas en anunciosDeOrganizacion"
      contains: "2026-09-serie-revision"
    - path: "tools/api.js"
      provides: "listarSeries, revisarSerie, rutas, PRIVADOS_DEL_ANUNCIO ampliado, serie_cotejada"
      contains: "anuncio.serie"
  key_links:
    - from: "tools/api.js revisarSerie"
      to: "db.enNombreDe"
      via: "conAdminEnNombreDe('anuncio.serie')"
      pattern: "conAdminEnNombreDe\\('anuncio\\.serie'"
---

<objective>
Construir el circuito del número de serie: el personal lo ve, deja el resultado de su revisión
por la bitácora, y el resultado llega al vendedor y (si cuadra) a la ficha. De paso, cerrar la
fuga por la que la ficha entregaba la serie a cualquiera.

Purpose: ADMIN-03 y la parte de datos de CONF-01 (criterios 2, 3 y 5).
Output: migración, consultas, dos rutas de admin, privacidad del anuncio, pruebas.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/07-verificaci-n-y-soporte-en-nombre-del-dealer/07-CONTEXT.md
@.planning/phases/07-verificaci-n-y-soporte-en-nombre-del-dealer/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Tarea 1: migración, catálogo y consultas de la serie</name>
  <files>tools/db.js</files>
  <read_first>tools/db.js (MIGRACIONES y su final, ACCIONES_BITACORA, enNombreDe, anuncio, anunciosDeOrganizacion, organizacionesAdmin)</read_first>
  <action>
  Al FINAL de `MIGRACIONES`, con comentario del porqué: `['2026-09-serie-revision', [ 'ALTER TABLE anuncios ADD COLUMN serie_revision TEXT', '… serie_revisada TEXT', '… serie_revisada_por TEXT', '… serie_nota TEXT' ]]`.
  En `ACCIONES_BITACORA` añadir `'anuncio.serie': 'Número de serie revisado'`.
  Función `normalizarSerie(s)` = mayúsculas sin espacios, guiones, puntos ni barras.
  `seriesParaRevisar({ estado })`: anuncios con `TRIM(serie) <> ''`, estado de anuncio distinto de
  'borrador'; filtro `pendiente` (`serie_revision IS NULL`), `conforme`, `observada`, o todos;
  columnas id, marca, modelo, anio, estado, serie, serie_revision, serie_revisada, serie_revisada_por,
  serie_nota, organizacion_id, empresa (o.nombre), hasta 4 fotos (`COALESCE(miniatura,url)` como
  `miniatura` y `url`); `repetidos` calculado en JS: cuántos OTROS anuncios del conjunto completo
  con serie comparten `normalizarSerie`. Pasar por `conNombres` para marca/categoría si aplica.
  Orden: pendientes primero, luego por `publicado DESC`. Límite 300.
  `anotarRevisionSerie(idAnuncio, { resultado, nota, nombreAdmin })`: UPDATE de las cuatro columnas;
  con `pendiente` las deja en NULL. Devuelve `changes > 0`.
  `anuncioSerie(idAnuncio)`: id, organizacion_id, serie, serie_revision, serie_nota.
  En `anunciosDeOrganizacion` añadir `(TRIM(COALESCE(a.serie,'')) <> '') AS tiene_serie, a.serie_revision, a.serie_nota`.
  En `organizacionesAdmin` sustituir el cálculo condicional de 07-01 por la subconsulta directa de
  `series_pendientes` (la columna ya existe). Exportar lo nuevo.
  </action>
  <verify><automated>node --check tools/db.js && npm run bitacora:probar</automated></verify>
  <acceptance_criteria>
  - La última entrada de `MIGRACIONES` es `2026-09-serie-revision`.
  - `tools/db.js` contiene `'anuncio.serie'`, `function seriesParaRevisar`, `function anotarRevisionSerie`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Tarea 2: rutas de admin y privacidad de la ficha</name>
  <files>tools/api.js</files>
  <read_first>tools/api.js (conAdminEnNombreDe, verificarOrganizacion, PRIVADOS_DEL_ANUNCIO, verAnuncio, RUTAS)</read_first>
  <action>
  `listarSeries = conAdmin(…)` → `{ series: db.seriesParaRevisar({ estado }) }` con `estado` de la consulta
  (valores válidos `pendiente|conforme|observada`, otro = todos).
  `revisarSerie = conAdminEnNombreDe('anuncio.serie', async (req, res, ctx, idAnuncio) => …)`: leer
  cuerpo; `resultado` ∈ `conforme|observada|pendiente` si no 400; `nota = texto(c.nota, 500)`;
  `observada` sin nota → 400 «Escriba qué no cuadra: es lo que lee el vendedor»; `db.anuncioSerie`
  inexistente o sin serie → 404; `ctx.enNombreDe(a.organizacion_id, { objetoTipo: 'anuncio', objetoId: idAnuncio, motivo: nota }, () => { db.anotarRevisionSerie(…, { nombreAdmin: ctx.usuario.nombre || ctx.usuario.correo }); return { antes: { revision: a.serie_revision || 'pendiente', nota: a.serie_nota || null }, despues: { revision: resultado, nota } }; })`.
  Responder `{ serie: fila actualizada de seriesParaRevisar? }` — basta `{ ok: true, resultado }`.
  Rutas: `['GET', /^\/api\/admin\/series$/, listarSeries]`, `['POST', /^\/api\/admin\/anuncios\/([\w-]+)\/serie$/, revisarSerie]`.
  Privacidad: añadir a `PRIVADOS_DEL_ANUNCIO` `'serie', 'serie_nota', 'serie_revisada_por', 'serie_revisada', 'serie_revision'`
  con comentario (la promesa de publicar.html y la fuga encontrada); en `verAnuncio`, ANTES de borrar
  privados, poner `a.serie_cotejada = a.serie_revision === 'conforme'` (para todos).
  </action>
  <verify><automated>node --check tools/api.js && npm run bitacora:probar && npm run seguridad:probar</automated></verify>
  <acceptance_criteria>
  - `tools/api.js` contiene `conAdminEnNombreDe('anuncio.serie'`, `api\/admin\/series`, `serie_cotejada`.
  - `PRIVADOS_DEL_ANUNCIO` incluye `'serie'`.
  - La guarda de `probar-bitacora.js` clasifica la ruta nueva sin tocar `ESCRITURAS_ADMIN_PROPIAS`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Tarea 3: pruebas de la serie</name>
  <files>tools/probar-bitacora.js, tools/probar-seguridad.js, tools/auditar-permisos.js</files>
  <read_first>tools/probar-bitacora.js, tools/probar-seguridad.js (bloque de precio_minimo y cómo crea anuncios)</read_first>
  <action>
  `probar-bitacora.js`, bloque «Número de serie»: sembrar dos anuncios con la misma serie escrita
  distinta ("CAT-0320 x" / "cat0320x") y uno sin serie, directamente con `db.crearAnuncio`/el
  helper que exista (leer cómo lo hace probar-seguridad). Comprobar: la lista de pendientes trae
  los dos con `repetidos === 1` y no el que no tiene serie; normal = 404; `observada` sin nota = 400
  sin fila; `conforme` = 200 con fila `anuncio.serie` sobre la organización del anuncio, y la fila
  no contiene la serie (`JSON.stringify(fila)` no incluye `CAT`); `?estado=conforme` lo trae;
  `pendiente` lo devuelve a pendientes; resultado inválido = 400; anuncio inexistente = 404;
  `GET /api/mis-anuncios` del dueño trae `serie_revision` y `tiene_serie`.
  `probar-seguridad.js`: un visitante sin sesión y otra cuenta piden `GET /api/anuncios/:id` de un
  anuncio con serie → no hay `serie` ni `serie_nota`, sí `serie_cotejada` booleano; el dueño sí ve `serie`.
  `auditar-permisos.js`: `GET /admin/series` y `POST /admin/anuncios/x/serie` con cuenta normal → 404.
  </action>
  <verify><automated>npm run bitacora:probar && npm run seguridad:probar</automated></verify>
  <acceptance_criteria>
  - Ambos scripts terminan en 0 mal; `probar-bitacora.js` contiene `Número de serie`.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
| ID | Amenaza | Mitigación |
|---|---|---|
| T-07-03 | Revisión de serie sin rastro | `conAdminEnNombreDe('anuncio.serie')` y guarda sobre RUTAS |
| T-07-04 | La serie (dato que ayuda a clonar papeles de una máquina) sale por la ficha pública | privados ampliados; prueba en seguridad:probar |
</threat_model>

<verification>
`npm run bitacora:probar`, `npm run seguridad:probar`, `npm run dealer:probar`, `npm run facturas:probar`, `npm run pagos:probar`.
</verification>

<success_criteria>
El personal puede revisar cada serie declarada con prueba en la bitácora, el vendedor recibe el resultado y la serie deja de ser pública.
</success_criteria>

<output>
`.planning/phases/07-verificaci-n-y-soporte-en-nombre-del-dealer/07-02-SUMMARY.md`
</output>

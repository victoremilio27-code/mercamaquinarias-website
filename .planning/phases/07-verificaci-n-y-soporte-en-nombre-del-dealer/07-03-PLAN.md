---
phase: 07-verificaci-n-y-soporte-en-nombre-del-dealer
plan: 03
type: execute
wave: 1
depends_on: ["07-02"]
files_modified:
  - tools/db.js
  - tools/api.js
  - tools/probar-pagina-dealer.js
  - tools/auditar-permisos.js
autonomous: true
requirements: [ADMIN-04]
user_setup: []

must_haves:
  truths:
    - "Cumple las decisiones D-11, D-12, D-13, D-14, D-17, D-18 de 07-CONTEXT.md."
    - "El personal lee la página de un dealer con `GET /api/admin/organizaciones/:id/pagina` (misma forma que `/api/mi-pagina` más `organizacion`) y la edita con PATCH, secciones, orden, galería y enlaces bajo el mismo prefijo."
    - "Cada escritura del personal deja una fila `pagina.editar` con el trozo que cambió antes y después."
    - "El dealer ve el cambio en `GET /api/mi-pagina` y `estado_pagina` no cambia, esté en borrador o publicada."
    - "No existen `/api/admin/organizaciones/:id/pagina/publicar` ni `/despublicar`: un POST ahí no responde 2xx."
    - "Un usuario normal o sin sesión recibe 404/401 en todas las rutas nuevas; una organización particular o inexistente, 404."
    - "`GET /api/mi-pagina` incluye `editadaPorSoporte` (fecha de la última fila `pagina.editar`, sin datos del empleado)."
  artifacts:
    - path: "tools/api.js"
      provides: "núcleo compartido de la página y rutas /api/admin/organizaciones/:id/pagina…"
      contains: "pagina.editar"
    - path: "tools/db.js"
      provides: "SAVEPOINT en ordenarSecciones y guardarEnlaces; ACCIONES_BITACORA['pagina.editar']; ultimaAnotacion"
      contains: "SAVEPOINT ordenar_secciones"
    - path: "tools/probar-pagina-dealer.js"
      provides: "bloque «Edición en nombre del dealer»"
      contains: "en nombre del dealer"
  key_links:
    - from: "tools/api.js (rutas admin de página)"
      to: "db.enNombreDe"
      via: "conAdminEnNombreDe('pagina.editar')"
      pattern: "conAdminEnNombreDe\\('pagina\\.editar'"
---

<objective>
Dar al personal una vía de servidor para editar la página de un dealer en su nombre, con la
misma validación que usa el dealer, cada escritura en la bitácora y sin tocar su estado de
publicación.

Purpose: ADMIN-04 (criterios 4 y 5).
Output: núcleo de página compartido, rutas de admin, transacciones anidables, pruebas.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/07-verificaci-n-y-soporte-en-nombre-del-dealer/07-CONTEXT.md
@.planning/phases/07-verificaci-n-y-soporte-en-nombre-del-dealer/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Tarea 1: base anidable y catálogo</name>
  <files>tools/db.js</files>
  <read_first>tools/db.js (ordenarSecciones, guardarEnlaces, ACCIONES_BITACORA, bitacora)</read_first>
  <action>
  `ordenarSecciones` y `guardarEnlaces`: sustituir `BEGIN/COMMIT/ROLLBACK` por
  `SAVEPOINT ordenar_secciones` / `RELEASE` / `ROLLBACK TO` + `RELEASE` (y `guardar_enlaces`),
  con comentario: dentro de `enNombreDe` un BEGIN anidado lanza.
  `ACCIONES_BITACORA['pagina.editar'] = 'Página del dealer editada en su nombre'`.
  `ultimaAnotacion(idOrg, accion)` → `creada` de la fila más reciente o null. Exportar.
  </action>
  <verify><automated>node --check tools/db.js && npm run dealer:probar && npm run bitacora:probar</automated></verify>
  <acceptance_criteria>
  - `tools/db.js` contiene `SAVEPOINT ordenar_secciones` y `SAVEPOINT guardar_enlaces` y ya no `d.exec('BEGIN')` en esas dos funciones.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Tarea 2: núcleo compartido y rutas del personal</name>
  <files>tools/api.js</files>
  <read_first>tools/api.js (reglasDePagina, conPagina, verMiPagina … guardarMisEnlaces, conAdminEnNombreDe, RUTAS)</read_first>
  <action>
  Extraer de cada manejador de `mi-pagina` su cuerpo a funciones puras de la forma
  `(org, c) → { codigo, datos }` o que lancen `{ codigo }`: `leerPagina(org)`, `cambiosDePagina(c)`
  (la validación de `editarMiPagina`, devuelve `{ datos }` o `{ error }`), `nuevaSeccion`,
  `cambiosDeSeccion`, `validarGaleria`, `validarEnlaces`. Los manejadores del dueño siguen
  respondiendo EXACTAMENTE igual (las 49 comprobaciones de dealer:probar en verde).
  Añadir `orgDealerParaAdmin(idOrg)` = fila de organizaciones con tipo dealer o error 404
  «Esa empresa no tiene página».
  Rutas del personal, todas `conAdminEnNombreDe('pagina.editar', …)` salvo la lectura (`conAdmin`):
  GET `/api/admin/organizaciones/:id/pagina` → `{ organizacion: { id, nombre }, pagina, …reglas, direccion }`;
  PATCH `…/pagina` (campos básicos; `antes` = valores previos solo de las claves cambiadas);
  POST `…/pagina/secciones`; PATCH `…/pagina/secciones/orden` (ANTES que la genérica);
  PATCH `…/pagina/secciones/:idSeccion`; DELETE `…/pagina/secciones/:idSeccion`;
  POST `…/pagina/galeria`; DELETE `…/pagina/galeria/:idFoto`; PUT `…/pagina/enlaces`.
  Cada escritura: `ctx.enNombreDe(idOrg, { objetoTipo: 'pagina'|'seccion'|'galeria'|'enlaces', objetoId, motivo: texto(c.motivo, 300) }, (org) => { …; return { antes, despues, resultado } })`.
  Errores de validación: lanzarlos DENTRO del callback con `codigo` 400/404 para que no quede fila.
  Las respuestas tienen la misma forma que las del dueño. Ubicar estas rutas en `RUTAS` junto a
  `/api/admin/organizaciones/…/verificar`, con las de `/secciones/orden` antes que `/secciones/:id`.
  `verMiPagina` añade `editadaPorSoporte: db.ultimaAnotacion(org.id, 'pagina.editar')`.
  Actualizar la nota del comentario de `conAdminEnNombreDe` que hablaba de la fase 7.
  </action>
  <verify><automated>node --check tools/api.js && npm run dealer:probar && npm run bitacora:probar</automated></verify>
  <acceptance_criteria>
  - `tools/api.js` contiene `conAdminEnNombreDe('pagina.editar'` y la ruta `pagina\/secciones\/orden` de admin aparece antes que `pagina\/secciones\/([\w-]+)` de admin.
  - No hay rutas `api\/admin\/organizaciones\/([\w-]+)\/pagina\/publicar` ni `despublicar`.
  - dealer:probar y bitacora:probar en 0 mal.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Tarea 3: pruebas de la edición en nombre</name>
  <files>tools/probar-pagina-dealer.js, tools/auditar-permisos.js</files>
  <read_first>tools/probar-pagina-dealer.js entero</read_first>
  <action>
  Bloque «Edición en nombre del dealer»: crear admin (`db.marcarAdmin`), usar el dealer que ya
  cumple las reglas. Casos: GET admin 200 con reglas y organización; PATCH descripción → fila
  `pagina.editar` con antes/después y motivo; el dealer ve la descripción nueva en `/api/mi-pagina`
  y `editadaPorSoporte` no nulo; `estado_pagina` igual (probar con página en borrador y con publicada);
  crear sección, reordenar (dos secciones), editar, borrar, galería añadir/quitar, enlaces → cada
  una añade exactamente una fila y responde 2xx; web inválida → 400 sin fila; sección inexistente → 404 sin fila;
  POST `…/pagina/publicar` y `…/despublicar` como admin → no 2xx y estado igual; usuario normal → 404
  sin fila; sin sesión → 401/404; organización particular → 404.
  `auditar-permisos.js`: `GET /admin/organizaciones/x/pagina` y `PATCH` con cuenta normal → 404.
  </action>
  <verify><automated>npm run dealer:probar && npm run bitacora:probar</automated></verify>
  <acceptance_criteria>
  - `tools/probar-pagina-dealer.js` contiene `en nombre del dealer`; sale 0 mal.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
| ID | Amenaza | Mitigación |
|---|---|---|
| T-07-05 | Un admin (o cuenta robada) cambia la página sin rastro | toda escritura por `conAdminEnNombreDe`; guarda sobre RUTAS |
| T-07-06 | El personal publica una página que el dealer dejó en borrador | sin rutas de publicar/despublicar de admin; prueba |
| T-07-07 | Contenido no validado en la página pública | mismo núcleo de validación que el dealer |
</threat_model>

<verification>
dealer:probar, bitacora:probar, seguridad:probar en verde.
</verification>

<success_criteria>
El personal puede editar la página de un dealer por la API con la misma validación, cada escritura queda en la bitácora y el estado de publicación se respeta.
</success_criteria>

<output>
`.planning/phases/07-verificaci-n-y-soporte-en-nombre-del-dealer/07-03-SUMMARY.md`
</output>

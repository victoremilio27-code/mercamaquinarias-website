---
phase: 07-verificaci-n-y-soporte-en-nombre-del-dealer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/db.js
  - tools/api.js
  - tools/probar-bitacora.js
  - tools/auditar-permisos.js
autonomous: true
requirements: [ADMIN-02]
user_setup: []

must_haves:
  truths:
    - "Cumple las decisiones D-01, D-02, D-03, D-17, D-18 de 07-CONTEXT.md."
    - "`GET /api/admin/organizaciones` devuelve las empresas dealer con estado de revisión, sello, estado de la página, anuncios activos y series pendientes, filtrable por `?estado=` y `?q=`; a quien no es administrador le responde 404."
    - "Conceder el sello a un dealer no aprobado responde 409 y no deja fila en la bitácora (D-02)."
    - "Retirar el sello sin motivo responde 400 y no deja fila; con motivo deja la fila con ese motivo (D-03)."
    - "El listado no incluye el RNC entero ni el correo de la cuenta."
  artifacts:
    - path: "tools/db.js"
      provides: "organizacionesAdmin({ estado, q })"
      contains: "function organizacionesAdmin"
    - path: "tools/api.js"
      provides: "listarOrganizacionesAdmin y la ruta GET /api/admin/organizaciones; reglas nuevas en verificarOrganizacion"
      contains: "listarOrganizacionesAdmin"
    - path: "tools/probar-bitacora.js"
      provides: "Bloque «Empresas y sello» con los casos de D-01..D-03"
      contains: "Empresas y sello"
  key_links:
    - from: "tools/api.js RUTAS"
      to: "listarOrganizacionesAdmin"
      via: "['GET', /^\\/api\\/admin\\/organizaciones$/, …]"
      pattern: "api\\\\/admin\\\\/organizaciones\\$"
---

<objective>
Que el personal pueda dar y retirar el sello de verificada a cualquier empresa dealer aprobada
sin depender de encontrarla en la cola de solicitudes, y que retirar el sello deje siempre un
motivo en la bitácora.

Purpose: ADMIN-02 (criterios 1 y 5 de la fase).
Output: consulta `organizacionesAdmin`, ruta de lectura del directorio, reglas 409/400 en
`verificarOrganizacion`, pruebas.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/07-verificaci-n-y-soporte-en-nombre-del-dealer/07-CONTEXT.md
@.planning/phases/07-verificaci-n-y-soporte-en-nombre-del-dealer/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Tarea 1: directorio de empresas y reglas del sello</name>
  <files>tools/db.js, tools/api.js</files>
  <read_first>tools/db.js (marcarVerificada, dealersPublicos, bitácora, module.exports), tools/api.js (conAdmin, conAdminEnNombreDe, verificarOrganizacion, listarBitacora, RUTAS)</read_first>
  <action>
  En `tools/db.js` añadir `organizacionesAdmin({ estado, q } = {})`: SELECT de `organizaciones`
  con `tipo = 'dealer'`, columnas `id, nombre, slug, verificada, estado_revision, estado_pagina,
  perfil_publico, creada`, subconsulta `activos` (anuncios `estado = 'activo'`) y `series_pendientes`
  (anuncios con `serie` no vacía y `serie_revision IS NULL`; esta columna nace en 07-02, así que
  aquí se calcula SOLO si existe: comprobar con `PRAGMA table_info(anuncios)` una vez y cachear,
  o devolver 0). Filtro `estado` ∈ `aprobada|pendiente|rechazada` (otro valor = todas); `q` con
  `nombre LIKE ? COLLATE NOCASE` (escapar `%` y `_`). Orden por nombre. Límite 500. Sin `rnc` ni
  `correo`. Convertir `verificada` y `perfil_publico` a booleanos. Exportarla.

  En `tools/api.js`: `listarOrganizacionesAdmin = conAdmin((req, res, ctx, consulta) => …)` que
  responde `{ empresas }`; ruta `['GET', /^\/api\/admin\/organizaciones$/, listarOrganizacionesAdmin]`
  junto a la de verificar. En `verificarOrganizacion`, dentro del callback de `ctx.enNombreDe`
  (para que la fila no se escriba si falla): si `verificada && org.tipo !== 'dealer'` o
  `verificada && org.estado_revision !== 'aprobada'` lanzar error con `codigo: 409` y mensaje
  «Solo se verifica una empresa dealer con el alta aprobada»; si `!verificada && org.verificada && !motivo`
  lanzar `codigo: 400` «Escriba el motivo para retirar el sello». Traducir esos códigos en el catch
  como ya se hace con el 404.
  </action>
  <verify><automated>node --check tools/api.js && node --check tools/db.js && npm run bitacora:probar</automated></verify>
  <acceptance_criteria>
  - `tools/db.js` contiene `function organizacionesAdmin` y la exporta.
  - `tools/api.js` contiene `/^\/api\/admin\/organizaciones$/` y `Escriba el motivo para retirar el sello`.
  - `npm run bitacora:probar` sale 0 (las comprobaciones de la fase 4 siguen verdes).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Tarea 2: pruebas del directorio y del sello</name>
  <files>tools/probar-bitacora.js, tools/auditar-permisos.js</files>
  <read_first>tools/probar-bitacora.js (bloqueApi, siembra), tools/auditar-permisos.js (sección 4)</read_first>
  <action>
  En `probar-bitacora.js` añadir `bloqueEmpresas()` (título «Empresas y sello») llamado tras
  `bloqueApi()`: GET como admin trae empresas con los campos pactados y sin `rnc`/`correo`;
  `?estado=aprobada` solo aprobadas; `?q=` filtra por nombre; como usuario normal 404.
  Dar el sello a un dealer pendiente = 409 sin fila; aprobar uno (db.resolverSolicitud) y darlo
  = 200 con fila; retirarlo sin motivo = 400 sin fila; con motivo = 200 y la fila lleva ese motivo.
  En `auditar-permisos.js` añadir `GET /admin/organizaciones` a la comprobación de rutas ocultas
  a una cuenta normal (espera 404).
  </action>
  <verify><automated>npm run bitacora:probar</automated></verify>
  <acceptance_criteria>
  - `tools/probar-bitacora.js` contiene `Empresas y sello`.
  - `npm run bitacora:probar` termina con `0 mal`.
  - `tools/auditar-permisos.js` contiene `/admin/organizaciones'`.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
| ID | Amenaza | Mitigación |
|---|---|---|
| T-07-01 | Un no-admin enumera empresas | `conAdmin` responde 404; prueba y auditoría |
| T-07-02 | Sello retirado sin rastro de por qué | 400 sin motivo; motivo en la fila |
</threat_model>

<verification>
`npm run bitacora:probar`, `npm run seguridad:probar`, `npm run dealer:probar` en verde.
</verification>

<success_criteria>
El personal puede listar empresas dealer y cambiar el sello de cualquiera aprobada, con motivo al retirarlo, y todo queda en la bitácora.
</success_criteria>

<output>
`.planning/phases/07-verificaci-n-y-soporte-en-nombre-del-dealer/07-01-SUMMARY.md`
</output>

---
phase: 08-moneda-y-disponibilidad-en-el-cat-logo
plan: 03
type: execute
wave: 3
depends_on: [08-02]
files_modified:
  - tools/db.js
  - tools/api.js
  - equipos.html
  - assets/app.js
  - tools/probar-catalogo.js
  - tools/auditar-publico.js
autonomous: true
requirements: [CAT-03, CAT-02]
user_setup: []

must_haves:
  truths:
    - "`?permuta=1` devuelve solo anuncios que aceptan permuta; `?itbis=1`, solo los de precio con ITBIS incluido; combinados, la intersección."
    - "equipos.html tiene el selector «Dónde está el equipo» y las casillas «Acepta permuta» y «Precio con ITBIS incluido»; al cargar con esos parámetros aparecen marcados y con su chip."
    - "La auditoría del navegador falla si faltan los filtros, si `?permuta=1` no marca la casilla o si la ficha no dice la disponibilidad."
  artifacts:
    - path: "equipos.html"
      provides: "Selector de disponibilidad y casillas de condiciones"
      contains: "name=\"permuta\""
    - path: "tools/auditar-publico.js"
      provides: "Comprobaciones de los filtros nuevos y de la ficha"
      contains: "permuta=1"
  key_links:
    - from: "assets/app.js CAMPOS_BUSQUEDA"
      to: "tools/api.js catalogo"
      via: "parámetros permuta, itbis, disponibilidad"
      pattern: "'permuta'"
---

<objective>
Que permuta e ITBIS incluido se puedan usar como filtro, y poner en la pantalla del
catálogo los tres filtros nuevos (con la disponibilidad del plan 02).

Purpose: CAT-03 y la parte visible del criterio 3.
Output: SQL y API de los filtros, controles en equipos.html, chips y casillas en app.js,
pruebas de API y de navegador.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/08-moneda-y-disponibilidad-en-el-cat-logo/08-CONTEXT.md
@.planning/phases/08-moneda-y-disponibilidad-en-el-cat-logo/08-UI-SPEC.md
@.planning/phases/08-moneda-y-disponibilidad-en-el-cat-logo/08-02-SUMMARY.md

<interfaces>
- `filtrosCatalogo(f)`: añadir `a.permuta = 1` y `a.itbis_incluido = 1` sin parámetros.
- `catalogo()` en api.js: `permuta: v('permuta') === '1'`, `itbis: v('itbis') === '1'`.
- app.js `CAMPOS_BUSQUEDA`, `ROTULO_FILTRO`, rama del valor del chip, restauración de
  campos en `montarResultados` (las casillas necesitan `checked`).
- `tools/auditar-publico.js` usa puppeteer contra `http://127.0.0.1:8080` sembrado.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarea 1: Filtros en base, API y prueba</name>
  <files>tools/db.js, tools/api.js, tools/probar-catalogo.js</files>
  <action>
Condiciones fijas en `filtrosCatalogo` cuando `f.permuta` / `f.itbis` son verdaderos;
la API solo los activa con el valor exacto `'1'`. Prueba: cada filtro, la combinación con
disponibilidad y precio, y que `permuta=si` no filtra.
  </action>
  <verify><automated>npm run catalogo:probar && npm run seguridad:probar</automated></verify>
  <done>Criterio 4 demostrado a nivel de API.</done>
</task>

<task type="auto">
  <name>Tarea 2: Controles del catálogo y auditoría del navegador</name>
  <files>equipos.html, assets/app.js, tools/auditar-publico.js</files>
  <action>
Según UI-SPEC: selector y fieldset en el formulario de filtros; `CAMPOS_BUSQUEDA`,
rótulos y valores de chip; casillas restauradas con `checked`. En auditar-publico:
comprobar que existen `select[name=disponibilidad]`, `input[name=permuta]` e
`input[name=itbis]`; que `equipos.html?permuta=1` deja la casilla marcada, pinta el chip y
no da error; y que la ficha abierta contiene «República Dominicana» o «Bajo pedido» en
`.detalle__disponibilidad`.
  </action>
  <verify><automated>node --check assets/app.js && npm run db:demo && (npm start &) && node tools/esperar-servidor.js --url http://127.0.0.1:8080/ && npm run auditar && npm run check</automated></verify>
  <done>El comprador filtra por permuta, ITBIS y disponibilidad desde la pantalla.</done>
</task>

</tasks>

<threat_model>
| ID | Amenaza | Mitigación |
|---|---|---|
| T-08-05 | Parámetros manipulados | Solo `'1'` activa; condiciones fijas sin interpolar nada de la petición |
</threat_model>

<verification>
Suite completa con navegador en verde.
</verification>

<success_criteria>
Criterios 3 y 4 del ROADMAP visibles en la pantalla y comprobados en CI.
</success_criteria>

<output>
`.planning/phases/08-moneda-y-disponibilidad-en-el-cat-logo/08-03-SUMMARY.md`
</output>

---
phase: 08-moneda-y-disponibilidad-en-el-cat-logo
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - tools/db.js
  - tools/api.js
  - assets/app.js
  - assets/publicar.js
  - assets/panel.js
  - publicar.html
  - tools/seed.js
  - tools/probar-catalogo.js
autonomous: true
requirements: [CAT-02]
user_setup: []

must_haves:
  truths:
    - "Existe `anuncios.disponibilidad` ('en-pais' | 'bajo-pedido', NOT NULL, por defecto 'en-pais') creada por una migración AL FINAL de MIGRACIONES; ninguna migración anterior cambia."
    - "Publicar acepta `disponibilidad`; cualquier valor que no sea 'bajo-pedido' se guarda como 'en-pais'."
    - "La ficha dice «Ya está en República Dominicana» o «Bajo pedido…», y la tarjeta del catálogo marca «Bajo pedido»."
    - "El dueño cambia la disponibilidad desde su panel (`PATCH /api/anuncios/:id/disponibilidad`); a otra organización le responde 404."
    - "`GET /api/anuncios?disponibilidad=bajo-pedido` filtra; un valor desconocido se ignora."
  artifacts:
    - path: "tools/db.js"
      provides: "Migración 2026-09-anuncios-disponibilidad, crearAnuncio con disponibilidad, guardarDisponibilidad, filtro"
      contains: "2026-09-anuncios-disponibilidad"
    - path: "tools/api.js"
      provides: "editarDisponibilidad y ruta antes de la genérica"
      contains: "disponibilidad"
  key_links:
    - from: "tools/api.js RUTAS"
      to: "editarDisponibilidad"
      via: "PATCH /api/anuncios/:id/disponibilidad antes de PATCH /api/anuncios/:id"
      pattern: "disponibilidad\\$/"
---

<objective>
Que cada anuncio diga si la máquina ya está en el país o es bajo pedido: se declara al
publicar, se cambia desde el panel, se ve en la ficha y la tarjeta, y se filtra.

Purpose: CAT-02 (y la mitad «se puede filtrar» del criterio 3).
Output: migración, API, publicar, ficha/tarjeta, panel, siembra y pruebas.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/08-moneda-y-disponibilidad-en-el-cat-logo/08-CONTEXT.md
@.planning/phases/08-moneda-y-disponibilidad-en-el-cat-logo/08-UI-SPEC.md
@.planning/phases/08-moneda-y-disponibilidad-en-el-cat-logo/08-01-SUMMARY.md

<interfaces>
- `crearAnuncio(datos)` INSERT explícito de columnas (tools/db.js ~2633).
- `editarTrenMotriz` + `guardarTrenMotriz`: patrón de PATCH del dueño.
- `buscarAnuncios` SELECT de la tarjeta (sin `a.*`), `anunciosDeOrganizacion` SELECT del panel.
- `anuncioDeApi(a)` en app.js; `avisoHTML`, `fichaTecnicaHTML`, bloque de `montarDetalle`.
- publicar.js: `estadoInicial().equipo`, `leerPaso('equipo')`, `volcarEstadoAlFormulario`, carga útil (~1427), vista previa (~1575).
- panel.js: `filaAnuncio(a)`, manejador de clics sobre `#filasAnuncios`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarea 1: Base, API y prueba</name>
  <files>tools/db.js, tools/api.js, tools/probar-catalogo.js, tools/seed.js</files>
  <action>
- Migración `2026-09-anuncios-disponibilidad` al final de `MIGRACIONES` con el ALTER de
  CONTEXT D-03 y su porqué (anuncios existentes quedan en el país).
- `DISPONIBILIDADES = ['en-pais', 'bajo-pedido']` exportada; `crearAnuncio` guarda
  `datos.disponibilidad` normalizada; `guardarDisponibilidad(idAnuncio, idOrg, valor)`;
  `igual('disponibilidad', ...)` en `filtrosCatalogo` solo con valor de la lista; SELECT
  de la tarjeta y del panel con `a.disponibilidad`.
- api.js: `publicar` pasa `disponibilidad`; `catalogo` pasa `disponibilidad`;
  `editarDisponibilidad = conSesion` (404 si no es suyo, 400 si el valor no es de la lista),
  ruta ANTES de `PATCH /api/anuncios/:id`.
- seed.js: `disponibilidad`, `moneda`, `permuta`, `itbisIncluido` opcionales por entrada
  del inventario; marcar al menos un anuncio en US$, uno bajo pedido, uno con permuta y
  uno con ITBIS incluido.
- Prueba: la ficha trae el campo, filtro, valor inválido ignorado, PATCH del dueño y del
  ajeno, y una base con la migración aplicada deja los anuncios previos en 'en-pais'.
  </action>
  <verify><automated>npm run catalogo:probar && npm run seguridad:probar && npm run dealer:probar</automated></verify>
  <done>Criterio 3 demostrado a nivel de API.</done>
</task>

<task type="auto">
  <name>Tarea 2: Publicar, ficha, tarjeta y panel</name>
  <files>publicar.html, assets/publicar.js, assets/app.js, assets/panel.js</files>
  <action>
Según UI-SPEC: selector en el paso 1 de publicar (estado, borrador, carga útil, vista
previa); `anuncioDeApi` con `disponibilidad`; marca en `avisoHTML`; párrafo en la ficha y
fila en la ficha técnica; botón en el panel que llama al PATCH y refleja el cambio en
memoria. Todo texto dinámico pasa por `esc()`.
  </action>
  <verify><automated>node --check assets/app.js && node --check assets/publicar.js && node --check assets/panel.js && npm run catalogo:probar</automated></verify>
  <done>El vendedor declara y cambia la disponibilidad; el comprador la ve.</done>
</task>

</tasks>

<threat_model>
| ID | Amenaza | Mitigación |
|---|---|---|
| T-08-03 | Cambiar la disponibilidad de un anuncio ajeno | `conSesion` + comprobación de organización + `WHERE organizacion_id = ?` |
| T-08-04 | XSS por el valor | Lista blanca en la API y la base (CHECK); el texto que se pinta es fijo |
</threat_model>

<verification>
`catalogo:probar`, `seguridad:probar`, `dealer:probar`, `bitacora:probar` en verde.
</verification>

<success_criteria>
La ficha dice si el equipo está en el país o es bajo pedido, y la API filtra por ello.
</success_criteria>

<output>
`.planning/phases/08-moneda-y-disponibilidad-en-el-cat-logo/08-02-SUMMARY.md`
</output>

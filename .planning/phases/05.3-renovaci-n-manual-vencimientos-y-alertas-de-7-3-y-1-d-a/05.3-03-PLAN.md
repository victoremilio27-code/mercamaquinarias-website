---
phase: 05.3-renovaci-n-manual-vencimientos-y-alertas-de-7-3-y-1-d-a
plan: 03
type: execute
wave: 2
depends_on: [05.3-01]
files_modified:
  - tools/correo.js
  - tools/probar-correo.js
  - tools/tareas.js
  - tools/db.js
  - tools/probar-recordatorios.js
  - package.json
  - .github/workflows/desplegar.yml
  - .env.example
autonomous: true
requirements: [MOD-10, MOD-11]
comprobante_ncf: false  # tareas, correo y el registro de recordatorios; no toca aprobarPago, pagos.js ni facturas.js

must_haves:
  truths:
    - "La tanda diaria de `node tools/tareas.js` pasa a `vencida` las suscripciones con fin pasado ANTES de caducar anuncios y apagar perfiles, así que en la misma pasada sus anuncios dejan de publicarse y la página del dealer se apaga."
    - "Un anuncio activo recibe el aviso de 7 días, el de 3 días y el de 24 horas, cada uno una sola vez por ciclo de `vence`, aunque la tarea corra dos veces o dos pasadas coincidan."
    - "Un anuncio vendido, pausado, vencido o renovado (su `vence` cambió) no recibe el aviso del ciclo viejo; tras renovar, el ciclo nuevo vuelve a tener sus tres avisos."
    - "Un correo que falla queda anotado como 'fallido' y se reintenta una sola vez en la pasada siguiente; uno enviado queda 'enviado' y no se repite."
    - "Cada aviso lleva equipo, fecha exacta de vencimiento, plan y el botón «Renovar anuncio» a `panel.html?renovar=<id>`; asuntos «Tu anuncio vence en 7 días», «Tu anuncio vence en 3 días», «Tu anuncio vence mañana»."
    - "El aviso único de 5 días deja de escribirse: `aviso_por_vencer` queda NULL tras la tarea (la columna no se borra); el aviso al vencer sigue igual."
  artifacts:
    - path: "tools/correo.js"
      provides: "asuntoRecordatorio(tipo), enviarRecordatorioVencimiento; sale enviarAnuncioPorVencer"
      contains: "Tu anuncio vence mañana"
    - path: "tools/tareas.js"
      provides: "tarea 'suscripciones' (vencerMembresias), 'por-vencer' → avisarRecordatorios; module.exports y guarda require.main"
      contains: "require.main === module"
    - path: "tools/probar-recordatorios.js"
      provides: "arnés `npm run recordatorios:probar` que corre las tareas dos veces"
  key_links:
    - from: "tools/tareas.js avisarRecordatorios"
      to: "db.recordatoriosPendientes → db.reservarRecordatorio → correo.enviarRecordatorioVencimiento → db.anotarRecordatorio"
      via: "reservar antes de enviar"
      pattern: "reservarRecordatorio"
    - from: "tools/tareas.js TAREAS"
      to: "db.vencerSuscripciones"
      via: "clave 'suscripciones', primera de la tanda diaria"
      pattern: "suscripciones: vencerMembresias"
---

<objective>
Lo que corre solo cada madrugada: vencer de verdad las suscripciones y mandar los avisos de 7, 3 y 1 día,
una sola vez por anuncio y ciclo, con el registro idempotente que dejó el 05.3-01. Todo sobre
`tools/tareas.js` y su temporizador de siempre: nada de infraestructura nueva.

**Toca la ruta del comprobante/NCF: NO.**

Purpose: MOD-10 (una suscripción vencida deja de sostener anuncios y la página del dealer) y MOD-11
(avisos 7/3/1 registrados, idempotentes y anulados si se vendió, venció o renovó).
Output: plantilla nueva en `tools/correo.js`, tareas nuevas en `tools/tareas.js` (requerible sin
ejecutarse), `anunciosPorVencer` retirada de `tools/db.js`, arnés `tools/probar-recordatorios.js` con su
script y su paso de CI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.3-renovaci-n-manual-vencimientos-y-alertas-de-7-3-y-1-d-a/05.3-CONTEXT.md
@.planning/phases/05.3-renovaci-n-manual-vencimientos-y-alertas-de-7-3-y-1-d-a/05.3-01-SUMMARY.md
@CLAUDE.md

<interfaces>
De tools/db.js tras el 05.3-01:
- `vencerSuscripciones()` → `{ vencidas: n }`, idempotente.
- `recordatoriosPendientes(momento = ahora())` → `[{ id, marca, marca_nombre?, modelo, anio, vence, tipo: '7d'|'3d'|'1d', correo, nombre, plan_nombre }]`, sin los ya anotados (salvo 'fallido').
- `reservarRecordatorio({ idAnuncio, tipo, vence })` → id de la fila o null (anuncio ya no activo, `vence` distinto o ya reservado).
- `anotarRecordatorio(idRecordatorio, resultado)` ('enviado' | 'fallido').
- `apagarPerfilesSinPlan()` y `caducarAnuncios()` (ya existen; el 05.3-01 hizo que la primera ignore las suscripciones con fin pasado).
- `anunciosPorVencer(dias)` (~4007) y su export: los retira este plan (única llamada: `avisarPorVencer` de tareas.js).
- `marcarAviso(idAnuncio, cual)`: se queda (la usa `avisarVencidos`); la rama 'por-vencer' deja de usarse.

tools/tareas.js hoy: `require('./entorno')`, `SECO`, `DIAS_AVISO = Number(process.env.MERCA_DIAS_AVISO) || 5`,
`anotar(tarea, mensaje)`, `caducar`, `avisarPorVencer` (~60), `avisarVencidos` (~82), `apagarPerfiles` (~472),
`TAREAS` (~481, el orden de las claves es el orden de la tanda diaria, sin las `informe-*`) y una IIFE final
que ejecuta y hace `process.exit`.

tools/correo.js: `enviar({ para, responderA, asunto, texto, html })`, `envoltura({ titulo, saludo, parrafos, extra, accion, nota })`,
`tarjeta(html)`, `esc`, `fecha(iso)`, `SITIO`, `BUZONES.anuncios`, `enviarAnuncioPorVencer` (~951), `enviarAnuncioVencido` (~977).
Con `MERCA_CORREO=archivo` el correo se escribe en `.tmp/correos` y devuelve `{ entregado: true }`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarea 1: la plantilla de los tres avisos</name>
  <files>tools/correo.js, tools/probar-correo.js</files>
  <read_first>
    - tools/correo.js líneas 1-130 (transporte, BUZONES, SITIO, esc, fecha), 280-330 (enviar al archivo), 915-1000 (enviarAnuncioPublicado, enviarAnuncioPorVencer, enviarAnuncioVencido) y el module.exports
    - tools/probar-correo.js líneas 50-110 (la lista de plantillas que se envían a mano)
  </read_first>
  <behavior>
    - `asuntoRecordatorio('7d')` = «Tu anuncio vence en 7 días», `('3d')` = «Tu anuncio vence en 3 días», `('1d')` = «Tu anuncio vence mañana»; un tipo desconocido lanza.
    - `enviarRecordatorioVencimiento({ para, nombre, equipo, idAnuncio, vence, tipo, plan })` con `MERCA_CORREO=archivo` devuelve `{ entregado: true }`; el HTML contiene el equipo escapado, `fecha(vence)`, el plan y un enlace a `/panel.html?renovar=<idAnuncio codificado>` con el texto «Renovar anuncio».
    - Un `equipo` con `<script>` sale escapado en el HTML.
  </behavior>
  <action>
    Per D-09, en tools/correo.js sustituye `enviarAnuncioPorVencer` por:
    - `asuntoRecordatorio(tipo)`: tabla `{ '7d': 'Tu anuncio vence en 7 días', '3d': 'Tu anuncio vence en 3 días', '1d': 'Tu anuncio vence mañana' }`; tipo desconocido → `throw new Error(...)`. Los asuntos van exactamente así (decisión D-09) seguidos de ` · MercaMaquinarias` como todos los demás correos del sitio.
    - `enviarRecordatorioVencimiento({ para, nombre, equipo, idAnuncio, vence, tipo, plan })` con `responderA: BUZONES.anuncios`, `asunto: asuntoRecordatorio(tipo) + ' · MercaMaquinarias'`, texto plano y `envoltura` con el mismo molde que el aviso viejo: tarjeta con el equipo, «Deja de publicarse el» + `fecha(vence)` y «Plan» + `plan` (si viene), párrafo «Renuévalo y sigue en el catálogo con las mismas fotos, el mismo anuncio y sus visitas acumuladas. El nuevo período se suma al final del actual.», acción `{ texto: 'Renovar anuncio', url: SITIO + '/panel.html?renovar=' + encodeURIComponent(idAnuncio) }` y nota «Si ya lo vendiste, márcalo como vendido en el panel y no te volvemos a escribir por este equipo.». Tuteo en todo el correo para que case con el asunto que fijó D-09 (el resto de correos sigue en usted; no se tocan). Nunca un teléfono.
    - Comentario encima: sustituye al aviso único de 5 días (D-08); quién llama se encarga de mandarlo una sola vez por ciclo con la tabla `recordatorios`.
    - Exporta las dos y quita `enviarAnuncioPorVencer` del export (única llamada: tareas.js, que se cambia en la tarea 2). `enviarAnuncioVencido` no cambia (D-08).
    tools/probar-correo.js: cambia la entrada «aviso de vencimiento» por tres entradas (7d, 3d, 1d) con `enviarRecordatorioVencimiento` y datos de ejemplo.
  </action>
  <verify>
    <automated>node -e "process.env.MERCA_CORREO='archivo';const c=require('./tools/correo');const a=['7d','3d','1d'].map(c.asuntoRecordatorio);if(a[0]!=='Tu anuncio vence en 7 días'||a[1]!=='Tu anuncio vence en 3 días'||a[2]!=='Tu anuncio vence mañana')process.exit(1);c.enviarRecordatorioVencimiento({para:'x@ejemplo.com',nombre:'Ana',equipo:'<b>2019 CAT 320</b>',idAnuncio:'a-1',vence:new Date().toISOString(),tipo:'1d',plan:'Destacado'}).then(r=>process.exit(r&&r.entregado?0:1))" && node --check tools/probar-correo.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Tu anuncio vence mañana" tools/correo.js` = 1 y `grep -c "Tu anuncio vence en 7 días\|Tu anuncio vence en 3 días" tools/correo.js` = 2.
    - `grep -c "panel.html?renovar=" tools/correo.js` ≥ 1.
    - `grep -c "enviarAnuncioPorVencer" tools/correo.js tools/probar-correo.js` = 0 (tras la tarea 2 también en tareas.js).
    - `git diff tools/correo.js | grep '^+' | grep -ci "tel:\|teléfono\|whatsapp"` = 0 (ningún teléfono en la plantilla nueva).
    - El comando del verify termina con 0.
  </acceptance_criteria>
  <done>Hay una plantilla para los tres avisos con los asuntos fijados, el equipo, la fecha, el plan y el enlace de renovar.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarea 2: vencer membresías y los avisos 7/3/1 en la tanda diaria, probados corriendo dos veces</name>
  <files>tools/tareas.js, tools/db.js, tools/probar-recordatorios.js, package.json, .github/workflows/desplegar.yml, .env.example</files>
  <read_first>
    - tools/tareas.js entero (533 líneas: sobre todo 1-110 y 465-533)
    - tools/db.js líneas 3999-4040 (anunciosPorVencer, anunciosVencidosSinAvisar, marcarAviso) y el module.exports (~4740)
    - tools/probar-renovacion.js líneas 1-120 (entorno antes del require, helpers; lo dejó el 05.3-01) como molde del arnés nuevo
    - .env.example líneas 104-110 (MERCA_DIAS_AVISO)
    - package.json (scripts) y .github/workflows/desplegar.yml (paso «Pruebas de renovación» que añadió el 05.3-01)
  </read_first>
  <behavior>
    - `TAREAS` empieza por 'suscripciones', antes de 'caducar' y 'perfiles'; la tanda diaria (claves sin `informe-`) la incluye.
    - Dealer con Premium `activa`, fin de ayer, página encendida y un anuncio `activo` con `vence` = ese fin: tras `TAREAS.suscripciones()`, `TAREAS.caducar()` y `TAREAS.perfiles()` → suscripción `vencida`, anuncio `vencido`, `perfil_publico = 0`; la segunda pasada no cambia nada y ninguna fila se borra.
    - Tres anuncios activos con `vence` a 6,5 días, 2,5 días y 20 horas: `avisarRecordatorios()` manda tres correos con tipos 7d, 3d y 1d; la segunda llamada manda cero; `recordatorios` tiene tres filas 'enviado'.
    - `Promise.all([avisarRecordatorios(), avisarRecordatorios()])` sobre un anuncio nuevo en ventana → un solo correo.
    - Vendido, pausado o vencido en ventana → cero correos.
    - El doble de correo devuelve `{ entregado: false }` → fila 'fallido'; la pasada siguiente con el correo sano manda uno y la fila pasa a 'enviado'; la tercera, cero.
    - Renovar (`db.renovarSinCosto` con `precios.desglose(0)` + referencia) un anuncio con su aviso de 3 días ya enviado → su `vence` sale de la ventana y no se manda nada; al poner a mano un `vence` nuevo a 2,5 días (ciclo siguiente) vuelve a salir un '3d' y quedan dos filas (la vieja se conserva).
    - `aviso_por_vencer` sigue NULL en todos los anuncios tras las pasadas; `anunciosVencidosSinAvisar`/`avisarVencidos` siguen funcionando.
    - `require('./tareas')` no ejecuta nada ni sale del proceso.
  </behavior>
  <action>
    RED primero: crea tools/probar-recordatorios.js con el molde de probar-renovacion.js (variables de entorno ANTES de cualquier require de db.js o tareas.js: `MERCA_DB`, `MERCA_FACTURAS`, `MERCA_FOTOS`, `MERCA_VIDEOS` en `.tmp/prueba-recordatorios` limpia, `MERCA_CORREO = 'archivo'`, `MERCA_SECRETO`; borra `MERCA_TRANSFERENCIA*`, `MERCA_CARDNET` y `MERCA_DIAS_AVISO`), `ok()` con contador y `process.exit(fallos ? 1 : 0)`. Requiere `./db`, `./correo`, `../assets/precios.js` y `./tareas`; sustituye `correo.enviarRecordatorioVencimiento` por un doble que anota la llamada y devuelve `{ entregado: true }` (o `false` cuando la prueba lo pida). Prepara organizaciones, planes, suscripciones y anuncios con UPDATE/INSERT directos por una conexión `DatabaseSync` aparte, como el arnés del 05.3-01. Casos: los de <behavior>, en secciones «1 · vencer en la tanda», «2 · avisos una sola vez», «3 · anulados y reintentos», «4 · ciclo nuevo tras renovar». Script `"recordatorios:probar": "node tools/probar-recordatorios.js"` en package.json y paso de CI «Pruebas de recordatorios» con `run: npm run recordatorios:probar` justo después de «Pruebas de renovación» (el .yml es LF: edítalo con la herramienta de edición, nunca con un heredoc). Commit `test(05.3-03): …`.

    GREEN, en tools/tareas.js, per D-06 a D-08:
    - `vencerMembresias()`: con `SECO` cuenta las `activa` con `fin IS NOT NULL AND fin < ahora` y lo anota; si no, `db.vencerSuscripciones()` y `anotar('suscripciones', n + ' membresía(s) vencidas')`. Comentario: antes nada pasaba una suscripción a vencida (auditoría §1.9) y un plan pagado una vez seguía «activo» para siempre; va primera para que `caducar` y `perfiles` vean el estado de hoy.
    - `avisarRecordatorios()` sustituye a `avisarPorVencer`: `db.recordatoriosPendientes()`; por cada uno, con `SECO` solo cuenta; si no, `idRec = db.reservarRecordatorio({ idAnuncio: a.id, tipo: a.tipo, vence: a.vence })`, si es null sigue; `r = await correo.enviarRecordatorioVencimiento({ para: a.correo, nombre: a.nombre, equipo, idAnuncio: a.id, vence: a.vence, tipo: a.tipo, plan: a.plan_nombre })` dentro de try/catch (un fallo del proveedor no detiene al resto); `db.anotarRecordatorio(idRec, r && r.entregado ? 'enviado' : 'fallido')`. `equipo` con `marca_nombre || marca`, como `pagos.nombreDeEquipo` (no requieras pagos.js: arma la cadena aquí). Llama SIEMPRE como `correo.enviarRecordatorioVencimiento(...)` (propiedad del módulo, no desestructurada: el arnés la sustituye). Anota «N de M aviso(s) de vencimiento (7d: a, 3d: b, 1d: c)». Comentario: reservar antes de enviar es lo que impide dos correos si dos pasadas coinciden; no hay preferencia de «no quiero avisos» en la base y no se inventa (D-10).
    - `TAREAS`: `suscripciones: vencerMembresias` como PRIMERA clave; `'por-vencer': avisarRecordatorios` (se conserva el nombre de la clave para que `node tools/tareas.js por-vencer` siga valiendo). Quita `avisarPorVencer` y `DIAS_AVISO`.
    - La IIFE final pasa a ejecutarse solo con `if (require.main === module)`, y el archivo exporta `module.exports = { TAREAS, vencerMembresias, avisarRecordatorios }`. Actualiza el comentario de cabecera: los avisos quedan anotados en `recordatorios`.
    tools/db.js: borra `anunciosPorVencer` y su nombre del `module.exports` (comentario en `marcarAviso`: la rama 'por-vencer' ya no se escribe desde la 05.3, la columna se conserva por historial, D-08). No toques migraciones.
    .env.example: quita `MERCA_DIAS_AVISO` y su comentario (las ventanas 7/3/1 son fijas por D-07).
    Commit `feat(05.3-03): …`. Commits en español con rutas explícitas.
  </action>
  <verify>
    <automated>npm run recordatorios:probar && npm run renovacion:probar && MERCA_DB=.tmp/tareas-seco.db MERCA_CORREO=archivo node tools/tareas.js --seco && npm run publicacion:probar && npm run dealer:probar && npm run metricas:probar</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "require.main === module" tools/tareas.js` = 1 y `grep -c "module.exports" tools/tareas.js` = 1.
    - `grep -c "correo.enviarRecordatorioVencimiento(" tools/tareas.js` = 1 y `grep -c "reservarRecordatorio" tools/tareas.js` = 1.
    - `grep -rn "anunciosPorVencer\|enviarAnuncioPorVencer\|DIAS_AVISO" tools/ .env.example` sin resultados.
    - En `TAREAS`, `suscripciones` aparece antes que `caducar` (`grep -n` de las dos claves).
    - `grep -c "recordatorios:probar" package.json` = 1 y `grep -c "recordatorios:probar" .github/workflows/desplegar.yml` = 1.
    - `MERCA_DB=.tmp/tareas-seco.db MERCA_CORREO=archivo node tools/tareas.js --seco` termina con 0 (base desechable, nunca la de desarrollo ni la de producción) y lista 'suscripciones'.
    - `npm run recordatorios:probar` 0 fallos y el resto del verify en verde.
  </acceptance_criteria>
  <done>La tanda diaria vence membresías antes de caducar y apagar perfiles, y manda cada aviso 7/3/1 una sola vez por ciclo, probado corriendo la tarea dos veces y en paralelo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| temporizador → tareas.js → base | pasadas repetidas o simultáneas; proceso que muere a mitad |
| tareas.js → proveedor de correo | fallos y respuestas ambiguas |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.3-13 | Repudiation | avisos duplicados | mitigate | reserva síncrona con `UNIQUE(anuncio_id, tipo, vence)` antes del `await` del envío; prueba en paralelo y corriendo dos veces |
| T-05.3-14 | Information disclosure | correo de aviso | mitigate | destinatario del anuncio o del propietario (consulta de la base, no de la petición); `esc` en todo lo que va al HTML; sin teléfono |
| T-05.3-15 | Tampering | vencer membresías | mitigate | UPDATE condicionado a `estado = 'activa' AND fin < ahora`, nada se borra, idempotente |
| T-05.3-16 | Denial | proveedor caído | accept | el fallo queda 'fallido' y se reintenta al día siguiente; un proceso muerto tras reservar deja 'enviando' y no reenvía (mejor perder un aviso que duplicarlo) |
</threat_model>

<verification>
`npm run recordatorios:probar`, `renovacion:probar`, `publicacion:probar`, `dealer:probar`, `metricas:probar`, `transferencia:probar`, `pagos:probar`, `seguridad:probar`: todo en verde; `MERCA_DB=.tmp/tareas-seco.db node tools/tareas.js --seco` corre sin fallos.
</verification>

<success_criteria>
Las membresías vencen de verdad en la tanda diaria, sus anuncios dejan de publicarse y la página del dealer se apaga; los avisos de 7 días, 3 días y 24 horas salen una sola vez por anuncio y ciclo, anulados al vender, vencer o renovar, y el arnés lo prueba corriendo la tarea dos veces.
</success_criteria>

<output>
Create `.planning/phases/05.3-renovaci-n-manual-vencimientos-y-alertas-de-7-3-y-1-d-a/05.3-03-SUMMARY.md` when done
</output>

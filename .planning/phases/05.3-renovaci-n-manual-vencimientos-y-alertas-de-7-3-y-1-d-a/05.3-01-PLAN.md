---
phase: 05.3-renovaci-n-manual-vencimientos-y-alertas-de-7-3-y-1-d-a
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/db.js
  - db/schema.sql
  - tools/pagos.js
  - tools/probar-renovacion.js
  - package.json
  - .github/workflows/desplegar.yml
autonomous: true
requirements: [MOD-09, MOD-10, MOD-11, MOD-12]
comprobante_ncf: true   # toca aprobarPago y la línea del comprobante en pagos.js: ejecutar con Opus

must_haves:
  truths:
    - "Una suscripción con `fin` pasado deja de contar en `suscripcionesDe` (y por tanto en `suscripcion`, `suscripcionConHueco` y el panel); `fin` NULL sigue contando (cuentas internas)."
    - "`db.vencerSuscripciones()` pasa a `estado = 'vencida'` solo las `activa` con `fin < ahora`, es idempotente (la segunda pasada cambia 0) y no borra nada."
    - "`apagarPerfilesSinPlan` apaga la página de un dealer cuya única Premium tiene `fin` pasado, aunque su fila siga `activa`."
    - "Aprobar un pago con `intencion.tipo = 'renovacion'` (solo por `pagos.confirmarPago` → `db.aprobarPago`) suma `dias` a `max(fin, ahora)`, deja la suscripción `activa`, rehace `vence`/`destacado_hasta` de sus anuncios y devuelve a `activo` en la MISMA fila los `vencido` que quepan; nunca crea un anuncio."
    - "Una suscripción solo admite un pago de renovación pendiente (índice único parcial en la base); confirmar dos veces el mismo pago no suma dos periodos; dos pagos aprobados distintos suman dos."
    - "El comprobante de una renovación cuadra (subtotal + ITBIS = total) y su periodo va del fin anterior (o de hoy si ya venció) al fin nuevo; un rechazo no consume NCF."
    - "La tabla `recordatorios` con `UNIQUE(anuncio_id, tipo, vence)` existe y `reservarRecordatorio` no deja reservar dos veces el mismo anuncio + tipo + ciclo."
  artifacts:
    - path: "tools/db.js"
      provides: "migración 2026-09-renovacion; aplicarRenovacion, rama 'renovacion' de aprobarPago, renovarSinCosto, vencerSuscripciones, suscripcionesRenovablesDe, suscripcionRenovable, pagoPendienteDeRenovacion, guardarRenovacionAutomatica, recordatoriosPendientes, reservarRecordatorio, anotarRecordatorio"
      contains: "'2026-09-renovacion'"
    - path: "tools/pagos.js"
      provides: "confirmarPago pasa a emitir el periodo renovado"
      contains: "periodo"
    - path: "tools/probar-renovacion.js"
      provides: "arnés `npm run renovacion:probar`, secciones 1-3"
  key_links:
    - from: "tools/pagos.js confirmarPago"
      to: "db.aprobarPago rama 'renovacion' → aplicarRenovacion"
      via: "la única transición pendiente → aprobado, dentro del SAVEPOINT aprobar_pago"
      pattern: "tipo === 'renovacion'"
    - from: "tools/db.js suscripcionesDe"
      to: "suscripciones.fin"
      via: "filtro (s.fin IS NULL OR s.fin > ahora)"
      pattern: "s\\.fin IS NULL OR s\\.fin >"
---

<objective>
La base de la fase: que una suscripción venza de verdad, que la renovación sea una intención más de
`aprobarPago` (la única transición), y que exista el registro idempotente de recordatorios. Todo en
`tools/db.js`, con su arnés nuevo.

**Toca la ruta del comprobante/NCF: SÍ** (rama nueva en `aprobarPago` y el periodo de la línea del
comprobante en `pagos.confirmarPago`). `tools/facturas.js` y la emisión del NCF NO se tocan.

Purpose: MOD-09 (renovar extiende el mismo anuncio), MOD-10 (vencer de verdad), MOD-11 (registro por
anuncio + tipo + ciclo) y MOD-12 (columnas de la renovación automática, nunca marcadas).
Output: migración `2026-09-renovacion`, funciones nuevas de `db.js` exportadas, `pagos.js` con el
periodo renovado, `tools/probar-renovacion.js` con script y paso de CI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.3-renovaci-n-manual-vencimientos-y-alertas-de-7-3-y-1-d-a/05.3-CONTEXT.md
@.planning/phases/05.2-publicar-este-equipo-borrador-pago-y-activaci-n-del-particul/05.2-01-SUMMARY.md
@CLAUDE.md

<interfaces>
Lo que ya existe en tools/db.js (leer las funciones, no reescribirlas salvo lo indicado):
- `MIGRACIONES` (línea ~51); la última es `'2026-09-borradores'` (~1071). `migrar()` tolera «duplicate column».
- `ahora()`, `hoy()`, `id()`, `sumarDias(dias, desde = new Date())` (~1122-1131).
- `apagarPerfilesSinPlan()` (~2693), `suscripcionesDe(idOrg)` (~2813), `suscripcion(idSusc, idOrg)`,
  `suscripcionConHueco`, `ESTADOS_QUE_OCUPAN = "('activo', 'pausado')"`.
- `anotarPago(d, { idOrg, idSusc, idAnuncio, cobro, t })` (~2866), `encenderPerfilSiProcede(d, idOrg, plan, t)`,
  `otorgarCompra` (~2924), `activarBorrador` (~2947), `publicarBorradorSinCosto` (~2963, patrón a copiar).
- `registrarCobro({ idOrg, idSusc, idAnuncio, cobro, intencion })` (~2993): traduce el UNIQUE a 409 solo con idAnuncio.
- `aprobarPago(idPago)` (~3051): SAVEPOINT aprobar_pago, ramas 'compra' | 'ampliacion' | 'publicacion'.
- `refrescarAnunciosDe(idSusc)` (~3290): hoy actualiza TODOS los anuncios de la suscripción y nadie la llama.
- `cambiarEstadoAnuncio(idAnuncio, idOrg, estado)` (~3975), `caducarAnuncios` (~3994), `marcarAviso` (~4037).
- `anunciosDeOrganizacion(idOrg)` (~3679): `pago_pendiente` y `pendiente_pago: !!a.pago_pendiente`.
- Esquema: `suscripciones.estado IN ('activa','vencida','cancelada','impaga')`, `fin` NULL = sin caducidad.

tools/pagos.js: `emitir(pago, intencion, membresia)` usa `membresia.inicio/fin` como periodo de la línea;
`confirmarPago(idPago, { envolver })` desestructura `{ pago, membresia, yaEstaba }` de `db.aprobarPago`.
assets/precios.js: `precioRenovacion({ precioUnitario, cupo, dias })` = `precioCompra` → `desglose`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarea 1: migración, vencimiento de verdad y arnés nuevo</name>
  <files>tools/db.js, db/schema.sql, tools/probar-renovacion.js, package.json, .github/workflows/desplegar.yml</files>
  <read_first>
    - tools/db.js líneas 51-60, 1040-1120 (final de MIGRACIONES y migrar), 2693-2835, 3285-3300, 3975-3998
    - db/schema.sql líneas 494-520 (suscripciones) y 610-630 (avisos de anuncios)
    - tools/probar-publicacion.js líneas 1-215 (orden de variables de entorno ANTES de require, helpers ok/consulta/ejecuta/prepararOrganizacion/cuentaCon/pedir, cómo cuenta NCF)
    - package.json (scripts) y .github/workflows/desplegar.yml (paso «Pruebas de publicación del particular»)
  </read_first>
  <behavior>
    - Una suscripción `activa` con `fin` de ayer no sale en `suscripcionesDe`/`suscripcionConHueco`; una con `fin` NULL sí.
    - `vencerSuscripciones()` devuelve `{ vencidas: n }`; la segunda llamada devuelve 0; el número de filas de `suscripciones` no cambia.
    - `apagarPerfilesSinPlan()` apaga un dealer con Premium `activa` de `fin` pasado.
    - `cambiarEstadoAnuncio(id, org, 'activo')` sobre un `vencido` devuelve `{ changes: 0, vencido: true }` y no lo toca; `'vendido'`/`'retirado'` sobre un vencido siguen permitidos.
    - La migración crea `recordatorios`, las tres columnas de `suscripciones` (con `renovacion_automatica = 0` en las filas existentes) y el índice `ux_pagos_renovacion_pendiente`.
  </behavior>
  <action>
    Migración nueva AL FINAL de `MIGRACIONES`, detrás de `'2026-09-borradores'`, llamada `'2026-09-renovacion'`, con un comentario en el estilo del archivo (qué falló antes y por qué: auditoría §1.9, suscripciones que nunca vencían; §1.15, un aviso por columna no sirve para tres avisos por ciclo). Sentencias, por D-07 y D-11:
    (a) `CREATE TABLE IF NOT EXISTS recordatorios (id TEXT PRIMARY KEY, anuncio_id TEXT NOT NULL REFERENCES anuncios(id) ON DELETE CASCADE, tipo TEXT NOT NULL CHECK (tipo IN ('7d','3d','1d')), vence TEXT NOT NULL, enviado TEXT, resultado TEXT, UNIQUE (anuncio_id, tipo, vence))`;
    (b) `ALTER TABLE suscripciones ADD COLUMN renovacion_automatica INTEGER NOT NULL DEFAULT 0`, `... renovacion_aceptada TEXT`, `... renovacion_texto TEXT`;
    (c) `CREATE UNIQUE INDEX IF NOT EXISTS ux_pagos_renovacion_pendiente ON pagos (suscripcion_id) WHERE suscripcion_id IS NOT NULL AND estado = 'pendiente' AND json_extract(intencion, '$.tipo') = 'renovacion'` (D-05; comprobado que SQLite de Node lo acepta y frena el segundo);
    (d) `CREATE INDEX IF NOT EXISTS ix_suscripciones_fin ON suscripciones (fin) WHERE estado = 'activa' AND fin IS NOT NULL`.
    Añade también la tabla `recordatorios` y las tres columnas a `db/schema.sql` (la tabla con IF NOT EXISTS; los ÍNDICES solo en la migración, por el mismo motivo que la 05.2: abrir() ejecuta schema.sql antes de migrar()). Nunca reescribas una migración anterior. Comenta en la migración que la rama de CardNet (06-02, `2026-10-cardnet`) añade las mismas columnas de renovación y que `migrar()` tolera «duplicate column», así que al fusionarla sus ALTER sobre estas columnas no romperán, pero conviene quitarlos.

    D-06: en `suscripcionesDe` añade `AND (s.fin IS NULL OR s.fin > ?)` con `ahora()`; en `apagarPerfilesSinPlan` la misma condición dentro del NOT EXISTS. Nueva `vencerSuscripciones()` → `UPDATE suscripciones SET estado = 'vencida' WHERE estado = 'activa' AND fin IS NOT NULL AND fin < ?`, devuelve `{ vencidas: changes }`. Comenta la consecuencia buscada: `ampliarMembresia`, `moverAnuncioDeSuscripcion` y la guarda de ampliación huérfana de la consola dejan de aceptar una membresía pasada (404/409), y eso es correcto: no se amplía lo vencido.

    `refrescarAnunciosDe(idSusc)`: restringe el UPDATE a `estado IN ('activo','pausado','vencido')` (el `vence` de un vendido es historial). Hazla aceptar un `d` opcional o sigue usando `abrir()` (misma conexión, vale dentro del SAVEPOINT).

    `cambiarEstadoAnuncio`: si `actual.estado === 'vencido'` y el estado pedido ocupa (`activo`/`pausado`), devuelve `{ changes: 0, vencido: true }` sin tocar nada: un vencido solo vuelve por la renovación pagada (D-03). Comentario con el porqué.

    Exporta `vencerSuscripciones`. Crea `tools/probar-renovacion.js` con el patrón de `probar-publicacion.js` (variables de entorno ANTES del require de db.js, carpeta `.tmp/prueba-renovacion` limpia, `MERCA_TRANSFERENCIA*` borradas, `ok()` con contador y `process.exit(mal ? 1 : 0)`), sección «1 · vencer de verdad» con los casos de <behavior>, preparando fechas con `UPDATE` directo por una conexión aparte. Script `"renovacion:probar": "node tools/probar-renovacion.js"` en package.json y paso de CI «Pruebas de renovación» con `run: npm run renovacion:probar` justo después de «Pruebas de publicación del particular» (el .yml es LF: edítalo con la herramienta de edición).

    RED primero (commit `test(05.3-01): …`), luego GREEN (`feat(05.3-01): …`). Commits en español con rutas explícitas.
  </action>
  <verify>
    <automated>npm run renovacion:probar && npm run publicacion:probar && npm run transferencia:probar && npm run pagos:probar && npm run dealer:probar && npm run seguridad:probar</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'2026-09-renovacion'" tools/db.js` = 1 y es la última entrada de `MIGRACIONES` (no hay otra `['2026-` después).
    - `git diff` de tools/db.js no modifica ninguna línea de migraciones anteriores.
    - `grep -c "s.fin IS NULL OR s.fin >" tools/db.js` ≥ 2.
    - `grep -c "ux_pagos_renovacion_pendiente" tools/db.js` = 1 y `grep -c "ux_pagos_renovacion_pendiente" db/schema.sql` = 0.
    - `grep -c "CREATE TABLE IF NOT EXISTS recordatorios" db/schema.sql` = 1.
    - `grep -c "renovacion:probar" package.json` = 1 y `grep -c "renovacion:probar" .github/workflows/desplegar.yml` = 1.
    - `npm run renovacion:probar` termina con 0 fallos y las demás pruebas del verify siguen en verde.
  </acceptance_criteria>
  <done>Las suscripciones vencen de verdad, la migración está al final y el arnés nuevo corre en CI.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarea 2: la renovación dentro de aprobarPago y su importe cero</name>
  <files>tools/db.js, tools/pagos.js, tools/probar-renovacion.js</files>
  <read_first>
    - tools/db.js líneas 2860-3200 (anotarPago, otorgarCompra, activarBorrador, publicarBorradorSinCosto, registrarCobro, aprobarPago, pagosPendientesDe)
    - tools/db.js líneas 3679-3715 (anunciosDeOrganizacion)
    - tools/pagos.js líneas 36-160 (lineaDeCupos, emitir, confirmarPago)
    - tools/probar-publicacion.js secciones 3-4 (cómo se prueba confirmarPago, la factura B02 y el NCF que avanza)
  </read_first>
  <behavior>
    - Activa con fin = hoy+5: confirmar una renovación de 30 días deja `fin = sumarDias(30, finAnterior)`, el anuncio con el mismo id, sus fotos y `vence = fin nuevo`.
    - Vencida (estado 'vencida', fin = hace 3 días) con su anuncio `vencido`: tras confirmar, suscripción `activa`, fin ≈ ahora+30 (tolerancia de un minuto), anuncio `activo` con el MISMO id, `aviso_vencido` NULL; el total de anuncios de la organización no cambia.
    - Confirmar dos veces → `yaEstaba`, el fin no se mueve y el NCF avanza solo una vez; dos pagos aprobados distintos → dos periodos.
    - Un segundo `registrarCobro` de renovación para la misma suscripción (con o sin idAnuncio) → 409 «ya tiene una renovación en espera».
    - Rechazo → nada cambia y no hay factura. `renovarSinCosto` → renueva sin factura, pago 'sin-costo'.
    - Suscripción de 2 cupos con 3 anuncios vencidos: se reactivan 2 (el de la intención primero) y el tercero sigue vencido; la aprobación NO lanza por capacidad.
    - Premium de un dealer aprobado, vencida y con la página apagada: renovarla vuelve a encender `perfil_publico`.
    - El comprobante cuadra y su periodo empieza en el fin anterior (o hoy si ya venció).
  </behavior>
  <action>
    En tools/db.js, función interna `aplicarRenovacion(d, { idOrg, idSusc, idAnuncio, dias, t })` (va dentro de la transacción de quien llama, como `otorgarCompra`), per D-01/D-03:
    1. Lee la suscripción con su plan (`SELECT s.*, p.destacado, p.perfil_publico, ... FROM suscripciones s JOIN planes p ...` por id y organizacion_id). Inexistente o ajena → 404 «Esa membresía no existe». `estado` fuera de ('activa','vencida') → 409. `fin` NULL → 409 «Esa membresía no vence: no hay nada que renovar».
    2. `desde = max(fin, t)` comparando ISO; `hasta = sumarDias(dias === 60 ? 60 : 30, desde)`.
    3. `UPDATE suscripciones SET fin = ?, estado = 'activa' WHERE id = ? AND organizacion_id = ?`. NO toques `precio_pactado` (comentario: qué precio pactar en una renovación lo decide la 05.4 con el hallazgo 2 de la auditoría).
    4. `refrescarAnunciosDe(idSusc)`.
    5. Reactiva vencidos hasta la capacidad libre (`anuncios_incluidos − COUNT(activo, pausado)`; NULL = sin límite): primero `idAnuncio` si es de esa suscripción y está `vencido`, luego el resto por `actualizado DESC`. Cada uno con `UPDATE anuncios SET estado = 'activo', aviso_vencido = NULL, actualizado = ? WHERE id = ? AND estado = 'vencido'` (resetear `aviso_vencido` hace que el aviso de corte del ciclo siguiente vuelva a salir). Nunca INSERT en anuncios. Nunca lances por falta de capacidad: el pago ya entró; lo que no quepa sigue vencido.
    6. `encenderPerfilSiProcede(d, idOrg, plan, t)`.
    7. Devuelve `{ idSusc, periodo: { inicio: desde, fin: hasta }, reactivados: [ids] }`.

    En `aprobarPago`, rama `else if (intencion.tipo === 'renovacion')`: exige `intencion.idSusc` y que `pago.suscripcion_id` sea el mismo (si no, 500 «El pago no apunta a la membresía que renueva»); llama a `aplicarRenovacion(d, { idOrg: pago.organizacion_id, idSusc: intencion.idSusc, idAnuncio: intencion.idAnuncio || null, dias: intencion.dias, t })`; guarda el `periodo` para devolverlo. Si el anuncio de la intención se vendió o movió entre tanto, la suscripción se renueva igual (es lo que se pagó). El retorno final pasa a `{ pago, membresia, yaEstaba: false, periodo }` (periodo solo en renovación; `null` en las demás y en `yaEstaba`).

    `registrarCobro`: al traducir el `UNIQUE constraint failed`, si `intencion && intencion.tipo === 'renovacion'` → 409 «Esa membresía ya tiene una renovación en espera»; el caso de `idAnuncio` sigue como está.

    Nuevas y exportadas: `renovarSinCosto({ idOrg, idSusc, idAnuncio, dias, cobro })` copiando la forma de `publicarBorradorSinCosto` (soloCero → BEGIN → aplicarRenovacion → anotarPago con idSusc e idAnuncio → COMMIT, ROLLBACK ante fallo; devuelve `{ membresia: suscripcion(idSusc, idOrg), anuncio: idAnuncio ? anuncio(idAnuncio) : null, periodo }`); `pagoPendienteDeRenovacion(idSusc)` (pendiente con `json_extract(intencion,'$.tipo') = 'renovacion'`); `suscripcionesRenovablesDe(idOrg)` (estado IN ('activa','vencida'), `fin IS NOT NULL`, `anuncios_incluidos IS NOT NULL`, con `s.*` (así salen `dias_ciclo` y `renovacion_automatica`), `plan_nombre`, `plan_activo`, `destacado`, `ocupados`, `renovacion_pendiente` (`COALESCE(referencia, id)` del pago pendiente de renovación, o NULL) y `precio_vigente` pasando el plan por `conPrecioVigente`; incluye las de fin pasado. El plan 05.3-02 lo usa tal cual en `GET /api/membresias` y NO toca db.js); `suscripcionRenovable(idSusc, idOrg)` (la de esa lista o null); `guardarRenovacionAutomatica({ idSusc, idOrg, activar, texto })` (activar: `renovacion_automatica = 1, renovacion_aceptada = ahora(), renovacion_texto = texto`; desactivar: `renovacion_automatica = 0`, conserva fecha y texto como historial; devuelve si cambió una fila) per D-11.

    `anunciosDeOrganizacion`: añade `suscripcion_cupo`, `suscripcion_fin`, `suscripcion_plan` (nombre del plan de la suscripción) y `renovacion_pendiente` (`COALESCE(referencia, id)` del pago pendiente de renovación de `a.suscripcion_id`); excluye las renovaciones de la subconsulta de `pago_pendiente` (`json_extract(p.intencion,'$.tipo') IS NOT 'renovacion'`) y deja `pendiente_pago: a.estado === 'borrador' && !!a.pago_pendiente` (que un anuncio activo con su renovación en espera no se lea como «pendiente de pago»).

    tools/pagos.js `confirmarPago`: toma también `periodo` de lo que devuelve `aprobarPago` y llama a `emitir(pago, intencion, periodo && membresia ? { ...membresia, inicio: periodo.inicio, fin: periodo.fin } : membresia)`, con un comentario: la línea del comprobante de una renovación dice el periodo PAGADO, no la vida entera de la membresía. `emitir`, `lineaDeCupos` y `facturas` no cambian (la intención de renovación lleva `cupo`). Reintento con `yaEstaba`: cae al periodo de la membresía; anótalo en el comentario.

    Arnés: sección «2 · renovar en la base» con los casos de <behavior>, cobros armados con `precios.precioRenovacion(...)` + referencia, intenciones `{ tipo: 'renovacion', idSusc, idAnuncio, idPlan, cupo, dias, concepto, cliente, correoCliente }`. RED → GREEN.
  </action>
  <verify>
    <automated>npm run renovacion:probar && npm run publicacion:probar && npm run pagos:probar && npm run transferencia:probar && npm run facturas:probar && npm run bitacora:probar</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "intencion.tipo === 'renovacion'" tools/db.js` ≥ 1 y `grep -c "function aplicarRenovacion" tools/db.js` = 1.
    - `grep -c "INSERT INTO anuncios" tools/db.js` no cambia respecto a antes del plan.
    - `grep -c "periodo" tools/pagos.js` ≥ 2 y `git diff --stat tools/facturas.js` vacío.
    - `renovarSinCosto`, `pagoPendienteDeRenovacion`, `suscripcionesRenovablesDe`, `suscripcionRenovable`, `guardarRenovacionAutomatica` aparecen en `module.exports` de tools/db.js.
    - `npm run renovacion:probar` 0 fallos (secciones 1-2) y el resto del verify en verde.
  </acceptance_criteria>
  <done>La renovación existe en la base por la única transición, suma al final, reactiva el mismo anuncio y su comprobante cuadra.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarea 3: registro de recordatorios en la base</name>
  <files>tools/db.js, tools/probar-renovacion.js</files>
  <read_first>
    - tools/db.js líneas 3999-4045 (anunciosPorVencer, anunciosVencidosSinAvisar, marcarAviso: consulta del destinatario)
    - .planning/phases/05.3-renovaci-n-manual-vencimientos-y-alertas-de-7-3-y-1-d-a/05.3-CONTEXT.md D-07 a D-10
  </read_first>
  <behavior>
    - Con `vence` a 6,5 días → tipo '7d'; a 2,5 días → '3d'; a 20 horas → '1d'; a 9 días → no sale; un `vendido`, `pausado` o `vencido` → no sale.
    - `reservarRecordatorio` dos veces con el mismo anuncio + tipo + vence → la segunda devuelve null; tras `anotarRecordatorio(id, 'fallido')` se puede reservar otra vez (y solo una).
    - Tras cambiar `vence` (renovación), el mismo tipo vuelve a salir para el ciclo nuevo; la fila vieja se queda.
    - Si el anuncio dejó de estar `activo` o su `vence` ya no es el del ciclo, `reservarRecordatorio` devuelve null.
  </behavior>
  <action>
    Per D-07, en tools/db.js, junto a la cola de avisos:
    `TIPOS_RECORDATORIO` y la regla por días que faltan redondeados hacia arriba (`Math.ceil((vence − ahora) / 86400000)`): 4-7 → '7d', 2-3 → '3d', 1 → '1d'. Comentario: la tarea corre una vez al día a las 05:00 (auditoría §1.14), así que cada ventana se pisa al menos una vez; no se rellenan avisos atrasados (si faltó una pasada, sale el del tramo actual, no los tres juntos).
    `recordatoriosPendientes(momento = ahora())`: anuncios `activo` con `vence > momento` y `vence <= sumarDias(7, momento)`, con el destinatario como en `anunciosPorVencer` (usuario del anuncio o propietario), `marca_nombre` si la tiene `conNombres`, `plan_nombre` de su suscripción; calcula el tipo en JS y descarta los que ya tienen fila `(anuncio_id, tipo, vence)` con `resultado` distinto de 'fallido'. No hay preferencia de «no quiero avisos» en la base (D-10): no se inventa.
    `reservarRecordatorio({ idAnuncio, tipo, vence })`: dentro de BEGIN/COMMIT comprueba `estado = 'activo' AND vence = ?` del anuncio; luego `INSERT OR IGNORE` con `resultado = 'enviando'` y `enviado = ahora()`; si no insertó, `UPDATE … SET resultado = 'enviando', enviado = ? WHERE anuncio_id = ? AND tipo = ? AND vence = ? AND resultado = 'fallido'`; devuelve el id de la fila si reservó, si no null. Comentario: reservar antes de enviar es lo que impide dos correos si dos pasadas coinciden; un proceso que muera entre reservar y enviar deja 'enviando' y no reenvía (preferible a duplicar).
    `anotarRecordatorio(idRecordatorio, resultado)`: `UPDATE recordatorios SET resultado = ?, enviado = ?`.
    Exporta las tres. `anunciosPorVencer` NO se toca aquí (lo retira el plan 03 junto con su única llamada).
    Arnés: sección «3 · registro de recordatorios» con los casos de <behavior>. RED → GREEN.
  </action>
  <verify>
    <automated>npm run renovacion:probar</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "function recordatoriosPendientes\|const recordatoriosPendientes" tools/db.js` = 1; `reservarRecordatorio` y `anotarRecordatorio` en `module.exports`.
    - `grep -c "INSERT OR IGNORE INTO recordatorios" tools/db.js` = 1.
    - `npm run renovacion:probar` 0 fallos (secciones 1-3).
  </acceptance_criteria>
  <done>La base sabe qué recordatorio toca a cada anuncio y garantiza uno por anuncio, tipo y ciclo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| procesador/consola → aprobarPago | la confirmación de un pago extiende vigencia; debe ocurrir una vez |
| tarea diaria → base | dos pasadas simultáneas o repetidas |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.3-01 | Tampering | aprobarPago rama renovacion | mitigate | solo desde el SAVEPOINT existente; `yaEstaba` si el pago no está pendiente; UPDATE final con `estado = 'pendiente'` |
| T-05.3-02 | Elevation | renovar suscripción ajena | mitigate | `aplicarRenovacion` filtra por `organizacion_id` del pago; `pago.suscripcion_id` debe coincidir con la intención |
| T-05.3-03 | Repudiation | doble renovación pendiente | mitigate | índice único parcial `ux_pagos_renovacion_pendiente` + 409 legible |
| T-05.3-04 | Tampering | comprobante de renovación | mitigate | el cobro sale de `precios.desglose` (registrarCobro ya lo exige); facturas.js intacto; prueba de cuadre |
| T-05.3-05 | Denial | reactivar más anuncios que la capacidad | mitigate | reactivación limitada a la capacidad libre; prueba con 3 vencidos y 2 cupos |
| T-05.3-06 | Repudiation | recordatorio duplicado | mitigate | `UNIQUE(anuncio_id, tipo, vence)` + reserva antes de enviar |
</threat_model>

<verification>
`npm run renovacion:probar`, `publicacion:probar`, `pagos:probar`, `transferencia:probar`, `facturas:probar`, `seguridad:probar`, `dealer:probar`, `bitacora:probar`, `metricas:probar`, `catalogo:probar`, `contactos:probar`: todo en verde. `2026-09-renovacion` es la última migración.
</verification>

<success_criteria>
Suscripciones que vencen, renovación por la única transición sumando al final sin duplicar anuncios, un solo pendiente por suscripción, comprobante que cuadra y registro idempotente de recordatorios, todo probado en `renovacion:probar`.
</success_criteria>

<output>
Create `.planning/phases/05.3-renovaci-n-manual-vencimientos-y-alertas-de-7-3-y-1-d-a/05.3-01-SUMMARY.md` when done
</output>

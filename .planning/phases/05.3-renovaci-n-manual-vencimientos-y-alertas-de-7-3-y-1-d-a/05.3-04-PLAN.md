---
phase: 05.3-renovaci-n-manual-vencimientos-y-alertas-de-7-3-y-1-d-a
plan: 04
type: execute
wave: 3
depends_on: [05.3-02]
files_modified:
  - assets/panel.js
  - panel.html
  - styles.css
  - tools/auditar-flujos.js
autonomous: true
requirements: [MOD-09, MOD-12]
comprobante_ncf: false  # solo pantallas; el cobro y el comprobante los decide el servidor (05.3-01/02)

must_haves:
  truths:
    - "Cada anuncio activo o pausado del panel enseña «Vence el [fecha]» y, a 7, 3 y 1 día, un aviso visual («Vence en 7 días», «Vence en 3 días», «Vence mañana») calculado al pintar."
    - "Un anuncio de una suscripción de un cupo enseña «Renovar ahora» (activo o pausado) o «Renovar anuncio» (vencido); uno de un plan de varios cupos enseña «Renovar plan»; con una renovación en espera enseña su referencia en vez del botón."
    - "Renovar abre una sección con el equipo o el plan, la fecha actual, la fecha tras renovar, el precio final «ITBIS incluido» y el método de pago; nunca el 3 % ni la palabra «cupo» para el particular."
    - "Pagar la renovación manda solo método, datos fiscales y (si se enseña) la casilla; 201 recarga el panel con la fecha nueva, 202 enseña los datos de la transferencia en «Pagos en espera»."
    - "Con la renovación automática apagada (sin CardNet) no aparece ninguna casilla ni interruptor; encendida, sale sin marcar con el texto del servidor y se activa o desactiva desde la tarjeta del plan."
    - "El enlace de los avisos `panel.html?renovar=<id>` abre directamente la renovación de ese anuncio, si es suyo."
  artifacts:
    - path: "assets/panel.js"
      provides: "textoVigencia con fecha y aviso 7/3/1, botones de renovar, abrirRenovacion/pagarRenovacion, RENOVABLES, RENOVACION_AUTO, ?renovar="
      contains: "abrirRenovacion"
    - path: "panel.html"
      provides: "sección #panelRenovar oculta con método, comprobante y casilla"
      contains: "panelRenovar"
    - path: "tools/auditar-flujos.js"
      provides: "comprobación en el navegador de «Vence el», «Renovar ahora», precio ITBIS incluido y sin casilla apagada (solo en CI)"
  key_links:
    - from: "assets/panel.js pagarRenovacion"
      to: "POST /api/anuncios/:id/renovar | POST /api/membresias/:id/renovar"
      via: "api() envuelto en try/catch propio"
      pattern: "/renovar"
    - from: "assets/panel.js"
      to: "GET /api/membresias renovables, metodosPago, renovacionAutomatica"
      via: "carga de membresías existente"
      pattern: "renovables"
    - from: "assets/panel.js interruptor de la tarjeta"
      to: "PUT /api/membresias/:id/renovacion-automatica"
      via: "solo si RENOVACION_AUTO.disponible"
      pattern: "renovacion-automatica"
---

<objective>
Lo que ve el anunciante: cuándo vence cada anuncio, el aviso a 7, 3 y 1 día, «Renovar» antes y después de
vencer con su pago dentro del panel, y la casilla de renovación automática construida pero invisible
mientras CardNet esté apagado.

**Toca la ruta del comprobante/NCF: NO** (solo pantallas; el importe, la aprobación y el comprobante
los decide el servidor).

Purpose: MOD-09 (renovar antes y después de vencer desde el panel) y MOD-12 (casilla opt-in, nunca marcada,
desactivable, apagada hasta CardNet); cierra el criterio 4 de la fase (panel con «Vence el…» y aviso visual).
Output: panel.js y panel.html con la renovación, estilos mínimos y la comprobación de navegador en
`tools/auditar-flujos.js` (corre en CI).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.3-renovaci-n-manual-vencimientos-y-alertas-de-7-3-y-1-d-a/05.3-CONTEXT.md
@.planning/phases/05.3-renovaci-n-manual-vencimientos-y-alertas-de-7-3-y-1-d-a/05.3-02-SUMMARY.md
@.planning/phases/05.2-publicar-este-equipo-borrador-pago-y-activaci-n-del-particul/05.2-04-SUMMARY.md
@CLAUDE.md

<interfaces>
API (05.3-01/02):
- `GET /api/mis-anuncios` → cada anuncio trae `suscripcion_id`, `suscripcion_cupo`, `suscripcion_fin`, `suscripcion_plan`,
  `renovacion_pendiente` (referencia o null), `vence`, `estado`.
- `GET /api/membresias` → `membresias` (solo vigentes), `pagosPendientes`, `transferencia?`, `metodosPago: string[]`,
  `renovacionAutomatica: { disponible: false } | { disponible: true, texto }`,
  `renovables: [{ id, plan_id, plan_nombre, estado, fin, vencida, cupo, ocupados, dias, renovacion_automatica, renovacion_pendiente, precio: { total, subtotal, itbis, … } | null }]`.
- `POST /api/anuncios/:id/renovar` y `POST /api/membresias/:id/renovar` con `{ metodo, conRnc?, razonSocial?, rnc?, direccionFiscal?, renovacionAutomatica? }`
  → 201 `{ anuncio, membresia, cobro, comprobante, pago }` | 202 `{ pago, cobro, aviso, transferencia? }` | 402/409/400 `{ error, idSusc? }`.
- `PUT /api/membresias/:id/renovacion-automatica` `{ activar }` → 200 | 403 | 404 | 409.

assets/panel.js (hoy): `ESTADOS`, `diasHasta(iso)`, `textoVigencia(a)` (~29, margen de 5 días), `MEMBRESIAS`, `EXENTA`,
`PAGOS_PENDIENTES`, `TRANSFERENCIA`, `AVISO_TRANSFERENCIA` (~48-60, se rellenan desde /api/membresias ~85),
`esParticular()` (~69), `pagosEnEsperaHTML()`, `tarjetaPlanParticular(m)` (~258), `pintarPlanParticular` (~282),
`pintarPlan()` (~327), `filaAnuncio(a)` (~535), `pintarTabla()`, `avisoPlan(mensaje, error, enlace)` (~990), `nombreDe(a)`, `esc`, `miles`.
assets/sesion.js: `api(url, { metodo, cuerpo })` LANZA si la respuesta no es ok (`err.codigo`, `err.message`);
`faltanLegales('pagar')` y `montarAvisoLegal('pagar')`.
assets/planes.js `datosFiscales()` (~570): el molde del comprobante fiscal (radio «tipoComprobante» + razón, RNC, dirección).
styles.css (CRLF): `.vigencia`, `.vigencia--pronto`, `.vigencia--fin` (~2780), `.membresia__vigencia` (~3333).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarea 1: «Vence el…», el aviso 7/3/1 y los botones de renovar</name>
  <files>assets/panel.js, styles.css</files>
  <read_first>
    - assets/panel.js líneas 1-130 (estados, vigencia, variables y carga de membresías), 190-340 (pagosEnEsperaHTML, tarjetas y pintarPlan del particular y del dealer), 535-600 (filaAnuncio, pintarTabla)
    - styles.css líneas 2775-2790 y 3325-3340
    - assets/app.js: busca el formateador de fechas que ya usa el sitio (`grep -n "toLocaleDateString\|function fecha" assets/app.js assets/panel.js`) y úsalo; no escribas otro
  </read_first>
  <action>
    Per D-13 y D-04, en assets/panel.js:
    - Variables nuevas junto a `PAGOS_PENDIENTES`, con su comentario: `RENOVABLES = []` (de `r.renovables`), `METODOS_PAGO = []` (de `r.metodosPago`) y `RENOVACION_AUTO = { disponible: false }` (de `r.renovacionAutomatica`), rellenadas en la misma carga de `/api/membresias`.
    - `textoVigencia(a)`: para `activo`/`pausado` con `vence`, «Vence el [fecha]» y debajo el aviso calculado con `diasHasta`: `dias <= 1` → «Vence mañana» (o «Vence hoy» si ya es el día) con clase `vigencia--urgente`; `dias <= 3` → «Vence en 3 días»/«Vence en 2 días» y `dias <= 7` → «Vence en N días», ambos con `vigencia--pronto`. Sustituye el comentario de los cinco días: ahora las ventanas son las de los avisos por correo (7, 3 y 1, D-07). `vencido` → «Venció el [fecha]» con `vigencia--fin`. Lo demás igual.
    - `accionRenovar(a)` (devuelve HTML para la columna de acciones): `a.renovacion_pendiente` → `<span class="celda-nota">Renovación en espera · ref. XXX</span>`; sin `suscripcion_id` o sin `suscripcion_fin` → nada; `suscripcion_cupo === 1` → botón `data-renovar="<id>"` «Renovar ahora» (activo/pausado, `btn-tabla`) o «Renovar anuncio» (vencido, `btn-tabla btn-tabla--fuerte`); cupo mayor → botón `data-renovar-plan="<suscripcion_id>"` «Renovar plan». Solo para `activo`, `pausado` y `vencido`. Todo con `esc()`. Llámalo desde `filaAnuncio` al principio de la celda de acciones.
    - Tarjetas de plan: en `tarjetaPlanParticular` y en la del dealer (la que pinta `pintarPlan` para capacidad), «Vence el [fecha]» en vez de «Quedan N días» cuando hay `fin`, y un botón `data-renovar-plan="<id>"` «Renovar» si la membresía está en `RENOVABLES` sin `renovacion_pendiente` (con ella, «Renovación en espera · ref. XXX»). Debajo de la lista de membresías vigentes, un bloque «Planes vencidos» con las de `RENOVABLES` que tengan `vencida: true`: nombre del plan, «Venció el [fecha]» y «Renovar». El particular no ve «cupo» en ninguno de estos textos (usa «publicación»/«equipos»); los textos del dealer los pule la 05.4, aquí solo «Renovar plan».
    - Los botones llevan solo sus atributos `data-renovar` / `data-renovar-plan`; los manejadores de clic y `abrirRenovacion` se escriben en la tarea 2, que va en el mismo PR.
    styles.css (CRLF, edítalo con la herramienta de edición): `.vigencia--urgente` (color de peligro, peso 800, fondo suave) junto a las otras `.vigencia`, `.vigencia__aviso` (bloque, 11.5px) y `.celda-nota` si no existe algo equivalente (búscalo antes con grep). Nada de colores nuevos: variables existentes.
  </action>
  <verify>
    <automated>node --check assets/panel.js && npm run check:encoding && grep -c "Vence el" assets/panel.js && grep -c "data-renovar" assets/panel.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Vence mañana" assets/panel.js` ≥ 1, `grep -c "Vence el" assets/panel.js` ≥ 2, `grep -c "Renovar anuncio" assets/panel.js` ≥ 1, `grep -c "Renovar ahora" assets/panel.js` ≥ 1, `grep -c "Renovar plan" assets/panel.js` ≥ 1.
    - `grep -c "vigencia--urgente" styles.css` ≥ 1 y `git diff --stat styles.css` muestra solo las líneas añadidas (no un diff de miles de líneas: el archivo sigue en CRLF).
    - `grep -c "dias <= 5" assets/panel.js` = 0.
    - `node --check assets/panel.js` y `npm run check:encoding` sin errores.
  </acceptance_criteria>
  <done>La tabla y las tarjetas enseñan la fecha de vencimiento, el aviso a 7/3/1 días y el botón de renovar que toca a cada anuncio o plan.</done>
</task>

<task type="auto">
  <name>Tarea 2: la sección de renovar, el pago y la casilla de renovación automática</name>
  <files>panel.html, assets/panel.js, styles.css</files>
  <read_first>
    - panel.html líneas 80-115 (#panelPlan y la tabla) y 255-274 (orden de los scripts)
    - planes.html líneas 170-200 (bloque del comprobante fiscal: ids, textos y clases que se reutilizan)
    - assets/planes.js líneas 36-60 y 560-620 (METODOS_PAGO, datosFiscales, montarComprobante) y cómo nombra los métodos al comprador
    - assets/sesion.js líneas 1-135 (api(), faltanLegales, montarAvisoLegal)
    - assets/panel.js: la función que carga /api/membresias y /api/mis-anuncios y vuelve a pintar (para recargar tras renovar), y avisoPlan (~990)
  </read_first>
  <action>
    panel.html: una `<section class="panel" id="panelRenovar" aria-labelledby="t-renovar" hidden>` justo después de `#panelPlan`, con: `<h2 class="panel__titulo" id="t-renovar" tabindex="-1">Renovar</h2>`, `<div id="renovarResumen"></div>`, `<fieldset id="renovarMetodos">` (lo rellena el JS con `METODOS_PAGO`, con los mismos rótulos que planes.js), el bloque del comprobante copiado de planes.html con ids propios (`ren-razon`, `ren-rnc`, `ren-direccion`, radio `name="tipoComprobanteRen"`, «Consumidor final» marcado por defecto), `<div id="renovarAutomatica" hidden></div>`, `<p class="acceso__aviso" id="avisoRenovar" role="alert" hidden></p>` y los botones «Pagar y renovar» (`#btnPagarRenovacion`, `btn btn--ambar`) y «Cancelar» (`#btnCancelarRenovacion`, `btn btn--linea`). Sin scripts en línea (CSP).

    assets/panel.js, per D-02, D-03, D-12 y D-13:
    - `abrirRenovacion({ idAnuncio, idSusc })`: resuelve el anuncio (de `ANUNCIOS`, por id exacto) y su `r = RENOVABLES.find(...)` por `suscripcion_id` o `idSusc`; sin `r` → `avisoPlan('Este anuncio no tiene un plan que se pueda renovar.')`; `r.renovacion_pendiente` → aviso con la referencia; `!r.precio` → «El plan de esta publicación ya no se ofrece. Escríbanos por correo o por el asistente del sitio.» (nunca un teléfono). Si `faltanLegales('pagar').length` → `montarAvisoLegal('pagar')` y un aviso «Antes de pagar hace falta aceptar las condiciones nuevas (arriba).». Pinta `#renovarResumen` con `esc()`: el equipo (`nombreDe(a)`) o «Plan [nombre] · capacidad para N equipos» (dealer), «Hoy vence el [fecha]» o «Venció el [fecha]», «Tras renovar: hasta el [fecha]» calculado como `max(fin, hoy) + r.dias` (solo informativo; comentario: la fecha real la pone el servidor, D-03) y «RD$ [total] · ITBIS incluido» con `miles(r.precio.total)` (con total 0: «Sin costo durante la promoción»). Nunca base, ajuste, «3 %» ni «cupo». Guarda el objetivo en una variable del módulo, enseña la sección, `scrollIntoView` y foco en el título.
    - Casilla (D-12): solo si `RENOVACION_AUTO.disponible`, `#renovarAutomatica` recibe un checkbox SIN `checked` con `RENOVACION_AUTO.texto` escapado como etiqueta y se muestra; si no, queda `hidden` y vacío.
    - `pagarRenovacion()`: construye el cuerpo solo con `metodo` (el radio marcado o el primero de `METODOS_PAGO`), los datos fiscales (misma lógica que `datosFiscales()` de planes.js: nada si es consumidor final) y `renovacionAutomatica: true` solo si la casilla existe y está marcada. NUNCA importe, plan, cupo ni días. `POST` a `/api/anuncios/<id>/renovar` o `/api/membresias/<id>/renovar` con `encodeURIComponent`. Envoltorio propio con `try/catch` porque `api()` lanza (regla de CLAUDE.md): el error se enseña en `#avisoRenovar` tal cual; un 409 que diga «Debe aceptar» → `montarAvisoLegal('pagar')`; un 409 con «renueve el plan completo» → ofrece «Renovar plan» con el id de membresía del anuncio. Botón deshabilitado y con `btn--ocupado` mientras espera. Con 201: oculta la sección, recarga anuncios y membresías y `avisoPlan('Listo: [equipo] sigue publicado hasta el [fecha nueva].', false)` (o «vuelve a estar publicado hasta…» si estaba vencido), con la fecha de `respuesta.membresia.fin`. Con 202: oculta la sección, recarga membresías (así «Pagos en espera» enseña la cuenta y la referencia) y enseña `respuesta.aviso`.
    - Interruptor en la tarjeta del plan: solo si `RENOVACION_AUTO.disponible`, cada membresía de `RENOVABLES` lleva «Renovación automática» con un checkbox que refleja `renovacion_automatica`; al cambiar, `PUT /api/membresias/<id>/renovacion-automatica` con `{ activar }` en su propio try/catch, y si falla se revierte el checkbox y se enseña el error. Apagada: no se pinta nada (ni desactivado ni «próximamente»).
    - `?renovar=<id>`: tras la primera carga, si `new URLSearchParams(location.search).get('renovar')` coincide EXACTAMENTE con el id de un anuncio de `ANUNCIOS`, `abrirRenovacion({ idAnuncio })`; si no coincide, no hace nada (el valor sale de la URL y no se pinta nunca).
    - Manejadores de clic: en la delegación que ya existe en la tabla (`#filasAnuncios`) y en `#panelPlan`, `[data-renovar]` → `abrirRenovacion({ idAnuncio })` y `[data-renovar-plan]` → `abrirRenovacion({ idSusc })`; «Cancelar» oculta la sección y limpia el aviso.
    styles.css (CRLF, herramienta de edición): lo mínimo para `#panelRenovar` si las clases existentes (`.panel`, el bloque de comprobante de planes, `.acciones`) no bastan; búscalas antes y reutilízalas.
  </action>
  <verify>
    <automated>node --check assets/panel.js && npm run check:encoding && grep -c "panelRenovar" panel.html</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "id=\"panelRenovar\"" panel.html` = 1 y `grep -c "<script>" panel.html` no aumenta (sin scripts en línea).
    - `grep -c "/renovar" assets/panel.js` ≥ 2 y `grep -c "renovacion-automatica" assets/panel.js` ≥ 1.
    - `grep -n "checked" assets/panel.js` no pone `checked` por defecto en la casilla del pago (solo refleja `renovacion_automatica` en el interruptor de la tarjeta).
    - `grep -c "RENOVACION_AUTO.disponible" assets/panel.js` ≥ 2.
    - En las líneas añadidas a assets/panel.js no hay «3 %», «ajuste» ni «cupo» en textos visibles del particular (`git diff assets/panel.js | grep '^+' | grep -i "cupo\|ajuste\|3 %"` solo devuelve comentarios o ramas del dealer).
    - `node --check assets/panel.js` y `npm run check:encoding` sin errores (los enlaces, `npm run check`, los comprueba el CI con el sitio arrancado).
  </acceptance_criteria>
  <done>Desde el panel se renueva un anuncio o un plan, con su pago, sin rehacer nada; la casilla existe y no aparece mientras CardNet esté apagado.</done>
</task>

<task type="auto">
  <name>Tarea 3: la comprobación del navegador (corre en CI)</name>
  <files>tools/auditar-flujos.js</files>
  <read_first>
    - tools/auditar-flujos.js entero, sobre todo el flujo del particular (~110-230): cómo abre sesión, si deja un anuncio publicado (tras la 05.2-05 lo hace por el asistente nuevo) y cómo anota fallos con `anota(quien, tipo, texto)`
    - .github/workflows/desplegar.yml trabajo `navegador` (sin `MERCA_CARDNET`: la renovación automática está apagada)
  </read_first>
  <action>
    En el flujo del particular de tools/auditar-flujos.js, después del punto en que tiene un anuncio publicado y está en `panel.html` (si el flujo no deja ninguno publicado, publícalo dentro de la página con `page.evaluate` y `fetch` por las rutas del borrador: `POST /api/borradores` con el Estándar, `PUT` con los campos mínimos y `POST /api/borradores/:id/pago`, que a precio cero publica al instante; recarga el panel): comprueba y anota con `anota('particular', 'flujo', …)` si falla:
    1. La fila del anuncio contiene «Vence el» y un botón «Renovar ahora».
    2. Pulsar «Renovar ahora» enseña `#panelRenovar` con «ITBIS incluido» o «Sin costo durante la promoción», y el texto de la sección no contiene «cupo» ni «3 %».
    3. `#renovarAutomatica` está oculto y vacío (CI no enciende `MERCA_CARDNET`: D-12).
    4. `panel.html?renovar=<id del anuncio>` abre la sección directamente; `panel.html?renovar=<script>` no abre nada ni lanza errores en la consola.
    5. «Cancelar» la oculta. No se paga nada en la auditoría.
    Imprime una línea de resumen como las demás del archivo. Comentario: Chrome no arranca en la nube de Victor; esto corre en el trabajo `navegador` del CI.
  </action>
  <verify>
    <automated>node --check tools/auditar-flujos.js && grep -c "panelRenovar" tools/auditar-flujos.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "panelRenovar" tools/auditar-flujos.js` ≥ 2 y `grep -c "renovarAutomatica" tools/auditar-flujos.js` ≥ 1.
    - `grep -c "?renovar=" tools/auditar-flujos.js` ≥ 2.
    - `node --check tools/auditar-flujos.js` sin errores. (La ejecución real la hace el CI en el trabajo `navegador`; si falla allí, se corrige antes de fusionar.)
  </acceptance_criteria>
  <done>El CI comprueba en un navegador real que el particular ve el vencimiento, abre la renovación con el precio final y no ve la casilla apagada.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL → panel | `?renovar=` lo puede fabricar cualquiera |
| API → HTML del panel | nombres de equipos, planes y referencias pintados con innerHTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.3-17 | Tampering | `?renovar=` | mitigate | solo se usa si coincide exactamente con un id de `ANUNCIOS`; nunca se pinta; auditoría con `<script>` |
| T-05.3-18 | Tampering | cuerpo del pago de renovar | mitigate | el panel no manda importe, plan, cupo ni días; el servidor los ignora de todos modos (05.3-02) |
| T-05.3-19 | Information disclosure / XSS | resumen, tarjetas, referencias | mitigate | todo por `esc()`; sin scripts en línea |
| T-05.3-20 | Spoofing | casilla de renovación automática | mitigate | sin `checked` por defecto, invisible apagada, texto del servidor |
</threat_model>

<verification>
`node --check assets/panel.js`, `node --check tools/auditar-flujos.js`, `npm run check:encoding`; `npm run renovacion:probar` sigue en verde. Las auditorías del navegador (`npm run auditar`) y los enlaces (`npm run check`) corren en el CI del PR, con el sitio arrancado.
</verification>

<success_criteria>
El panel enseña «Vence el…» y el aviso a 7, 3 y 1 día, deja renovar antes y después de vencer con el precio final ITBIS incluido y sin rehacer el anuncio, y la casilla de renovación automática existe pero no aparece hasta que CardNet se encienda.
</success_criteria>

<output>
Create `.planning/phases/05.3-renovaci-n-manual-vencimientos-y-alertas-de-7-3-y-1-d-a/05.3-04-SUMMARY.md` when done
</output>

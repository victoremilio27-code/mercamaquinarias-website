---
phase: 05.3-renovaci-n-manual-vencimientos-y-alertas-de-7-3-y-1-d-a
plan: 05
type: execute
wave: 4
depends_on: [05.3-02, 05.3-03, 05.3-04]
files_modified:
  - legal.html
  - assets/legales.js
  - tools/probar-renovacion.js
autonomous: true
requirements: [MOD-15, MOD-09, MOD-10, MOD-11, MOD-12]
comprobante_ncf: false  # condiciones y batería final; no toca aprobarPago, pagos.js ni facturas.js

must_haves:
  truths:
    - "Las condiciones de contratación dicen que la renovación mantiene el mismo anuncio, se cobra al precio vigente y suma el período nuevo al final del actual (o desde el pago si ya venció), que hay avisos a 7, 3 y 1 día y que la renovación automática solo aplica si el cliente la activa y se desactiva desde el panel."
    - "La versión de `contratacion` sube de 2.1 a 2.2 en `assets/legales.js` y en `legal.html` con la misma fecha, así que quien aceptó la 2.1 tiene que aceptar la 2.2 antes de pagar una renovación (mecanismo existente de `PARA_PAGAR`)."
    - "La batería completa de pruebas sin navegador está en verde con la fase entera, y la lista para Victor queda en el SUMMARY."
  artifacts:
    - path: "legal.html"
      provides: "sección 4 de contratación con 4.4-4.6 y `v2.2`"
      contains: "v2.2"
    - path: "assets/legales.js"
      provides: "contratacion version '2.2' con su vigenteDesde y el porqué"
      contains: "'2.2'"
    - path: "tools/probar-renovacion.js"
      provides: "sección 6: condiciones nuevas antes de renovar"
  key_links:
    - from: "assets/legales.js DOCUMENTOS contratacion.version"
      to: "tools/api.js exigirAceptacion(PARA_PAGAR) en renovarAnuncio/renovarMembresia"
      via: "legales.faltanPorAceptar"
      pattern: "version: '2.2'"
---

<objective>
Cerrar la fase: las condiciones de contratación dicen lo que ahora hace el sitio al renovar, con su versión
subida por el mecanismo que ya existe, y la batería completa demuestra que nada se rompió. Lo que solo
Victor puede comprobar o decidir queda en una lista en el SUMMARY.

**Toca la ruta del comprobante/NCF: NO.**

Purpose: MOD-15 (si cambian las condiciones de renovación, su versión sube) y la verificación final de
MOD-09 a MOD-12.
Output: `legal.html` y `assets/legales.js` con la 2.2, sección 6 del arnés de renovación, batería en verde
y lista para Victor.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.3-renovaci-n-manual-vencimientos-y-alertas-de-7-3-y-1-d-a/05.3-CONTEXT.md
@.planning/phases/05.3-renovaci-n-manual-vencimientos-y-alertas-de-7-3-y-1-d-a/05.3-02-SUMMARY.md
@.planning/phases/05.3-renovaci-n-manual-vencimientos-y-alertas-de-7-3-y-1-d-a/05.3-03-SUMMARY.md
@.planning/phases/05.3-renovaci-n-manual-vencimientos-y-alertas-de-7-3-y-1-d-a/05.3-04-SUMMARY.md
@CLAUDE.md

<interfaces>
assets/legales.js: `DOCUMENTOS` con `{ id: 'contratacion', nombre, version: '2.1', vigenteDesde: '2026-09-26', obligatorio: false }`
(el comentario encima explica por qué se subió a 2.1: el ejemplo a seguir), `PARA_PAGAR = ['terminos', 'privacidad', 'contratacion']`,
`faltanPorAceptar(aceptado, ids)`, `versionDe(id)`. Módulo compartido navegador/node: su final
`if (typeof module !== 'undefined' && module.exports)` no se toca.
legal.html (CRLF): sección `#contratacion` (~293) con `<p class="doc-version">v2.1 · vigente desde 2026-09-26</p>` y
«4. Vigencia, ampliación y renovación» (~312-315): 4.1 «La vigencia se computa desde la aprobación del pago…», 4.2 ampliación,
4.3 «La renovación no es automática salvo que el cliente la active expresamente…».
tools/api.js: `exigirAceptacion(res, idUsuario, ids)` → 409 `{ error: 'Debe aceptar … antes de continuar', faltan }`;
`POST /api/legales/aceptar`. db: `registrarAceptacion({ usuarioId, documento, version, ip, userAgent })`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarea 1: la cláusula de renovación y la versión 2.2 de las condiciones</name>
  <files>legal.html, assets/legales.js, tools/probar-renovacion.js</files>
  <read_first>
    - legal.html líneas 290-330 (sección de contratación completa)
    - assets/legales.js entero (≈120 líneas)
    - tools/probar-renovacion.js: helpers de sesión y la sección 4 (renovar por la API)
    - `grep -rn "doc-version\|versionDe\|'2\.1'" tools/ assets/` para saber si alguna prueba o página fija la versión
  </read_first>
  <behavior>
    - Una cuenta que aceptó `contratacion` 2.1 (anotada con `registrarAceptacion`) pide `POST /api/anuncios/:id/renovar` → 409 con `faltan` que incluye 'contratacion' y ningún pago anotado.
    - Tras aceptar por `POST /api/legales/aceptar` la 2.2 → la misma petición renueva (201 o 202 según el método).
    - `legales.versionDe('contratacion') === '2.2'` y `legal.html` enseña `v2.2` en la sección de contratación.
  </behavior>
  <action>
    Per D-14 y MOD-15. Hace falta cambiar el texto: la 4.1 dice que la vigencia se computa desde la aprobación del pago, y renovar antes de vencer suma el período al final del actual (D-03); dejarla así contradice lo que el sitio hace.

    RED: sección «6 · condiciones nuevas antes de renovar» en tools/probar-renovacion.js con los casos de <behavior> (la versión vieja se anota como '2.1' literal a propósito, con un comentario). Commit `test(05.3-05): …`.

    GREEN, legal.html (CRLF: herramienta de edición, nunca un heredoc ni un reemplazo con `\n`), dentro de «4. Vigencia, ampliación y renovación» de `#contratacion`:
    - 4.1: añade al final «, salvo en la renovación, que se rige por 4.4».
    - 4.3 se queda como está.
    - 4.4 nueva: «La <b>renovación manual</b> se puede hacer antes o después del vencimiento, desde el panel del cliente. Mantiene el mismo anuncio, con sus fotografías, su historial y sus estadísticas; se cobra al precio vigente del plan en el momento de renovar, y el nuevo período se suma al final del período en curso o, si ya venció, empieza al aprobarse el pago. Una renovación pendiente de pago no extiende la vigencia.»
    - 4.5 nueva: «La Empresa avisa por correo electrónico siete (7) días, tres (3) días y un (1) día antes del vencimiento de cada anuncio. Los avisos son informativos: no renovar a tiempo hace que el anuncio deje de publicarse al vencer, sin perder sus datos.»
    - 4.6 nueva: «La renovación automática, cuando esté disponible, solo se aplica si el cliente la activa expresamente aceptando el texto de autorización que se le muestra. Se puede desactivar en cualquier momento desde el panel, con efecto para los períodos siguientes.»
    - La línea `doc-version` de `#contratacion` pasa a `v2.2 · vigente desde AAAA-MM-DD` con la fecha del día en que se ejecuta el plan (`date +%F`). No cambies las líneas `doc-version` de los otros documentos ni el resto de secciones (el lenguaje de «cupos» de 1.1-1.2 no es de esta fase).
    assets/legales.js: `contratacion` → `version: '2.2'` y `vigenteDesde` con esa misma fecha; sustituye el comentario de la 2.1 por uno que conserve su historia en una línea y explique la 2.2 (la 2.1 decía que la vigencia empieza al aprobarse el pago; renovar suma al final del período, hay avisos 7/3/1 y la casilla de renovación automática; subir la versión hace que se vuelva a aceptar antes de pagar, por `PARA_PAGAR`). No toques el final compartido del módulo.
    Si el grep del read_first encuentra una prueba o página que fija '2.1' para contratación, actualízala en el mismo commit y dilo en el SUMMARY. Commit `feat(05.3-05): …`.
  </action>
  <verify>
    <automated>npm run renovacion:probar && npm run seguridad:probar && npm run publicacion:probar && npm run transferencia:probar && npm run check:encoding && node -e "const l=require('./assets/legales.js');process.exit(l.versionDe('contratacion')==='2.2'?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "version: '2.2'" assets/legales.js` = 1 y `grep -c "v2.2 · vigente desde" legal.html` = 1.
    - `grep -c "4.4. La <b>renovación manual</b>" legal.html` = 1, `grep -c "^\s*<p>4.5\." legal.html` ≥ 1 y `grep -c "4.6\. La renovación automática" legal.html` = 1.
    - `git diff --stat legal.html` muestra un puñado de líneas (el archivo sigue en CRLF, sin diff masivo).
    - `grep -ci "tel:\|teléfono" ` en las líneas añadidas a legal.html: 0 (`git diff legal.html | grep '^+' | grep -ci "tel:\|teléfono"`).
    - `npm run renovacion:probar` 0 fallos (secciones 1-6) y el resto del verify en verde.
  </acceptance_criteria>
  <done>Las condiciones dicen cómo funciona la renovación y su versión subió a 2.2, así que se vuelven a aceptar antes de pagar.</done>
</task>

<task type="auto">
  <name>Tarea 2: batería completa y lista para Victor</name>
  <files>tools/probar-renovacion.js</files>
  <read_first>
    - package.json (scripts) y .github/workflows/desplegar.yml (trabajo `pruebas`: la lista que debe quedar en verde)
    - .planning/phases/05.3-renovaci-n-manual-vencimientos-y-alertas-de-7-3-y-1-d-a/05.3-0[1234]-SUMMARY.md (desviaciones y pendientes)
  </read_first>
  <action>
    Corre la batería completa sin navegador, una por una para saber cuál falla: `check:encoding`, `taxonomia`, `facturas:letras`, `precios:probar`, `seguridad:probar`, `dealer:probar`, `bitacora:probar`, `contactos:probar`, `catalogo:probar`, `facturas:probar`, `pagos:probar`, `metricas:probar`, `transferencia:probar`, `publicacion:probar`, `renovacion:probar`, `recordatorios:probar`, `chat:probar`, y `MERCA_DB=.tmp/tareas-seco.db MERCA_CORREO=archivo node tools/tareas.js --seco`. Si algo falla por esta fase, corrígelo en el archivo de la fase que lo causa (anótalo como desviación en el SUMMARY con el porqué); nunca silencies ni saltes una prueba. `tools/probar-renovacion.js` solo se toca si una comprobación de esta fase estaba mal escrita, con el porqué en el SUMMARY. Las auditorías del navegador (`npm run auditar`, `npm run check`) corren en el CI del PR: en la nube Chrome no arranca.

    En el SUMMARY de este plan (no en otro archivo), la **lista para Victor**, corta y agrupada:
    1. Qué probar en vivo tras el despliegue: renovar un anuncio de prueba antes de vencer (la fecha nueva = fin anterior + días) y uno vencido (vuelve a salir en el catálogo con las mismas fotos); una renovación por transferencia marcada en la consola; que llegue el correo de 7 días de un anuncio con `vence` cercano; que no aparezca ninguna casilla de renovación automática.
    2. Decisiones tomadas por defecto que puede cambiar (reversibles): precio de renovación = el vigente del plan y período sumado al final (P8, D-02/D-03); se renueva el mismo ciclo contratado (30 o 60 días) sin elegir otro; correos de aviso en tuteo con los asuntos de D-09; los avisos se retiran del aviso único de 5 días (la columna vieja se conserva); la casilla solo sale con CardNet encendido.
    3. Qué hereda la fase 6: la migración del 06-02 (`2026-10-cardnet`) debe quitar sus `ALTER TABLE suscripciones ADD COLUMN renovacion_*`, que ya existen desde `2026-09-renovacion` (`migrar()` tolera la columna repetida, pero conviene no duplicarlas); `renovacionAutomaticaDisponible()` de api.js se sustituye por `cardnet.ACTIVO`; el cobro recurrente (06-07) lee `renovacion_automatica`, `renovacion_aceptada` y `renovacion_texto`.
    4. Qué hereda la 05.4: los textos del dealer en «Renovar plan» y qué `precio_pactado` guarda una renovación (hallazgo 2 de la auditoría).
    5. Que las condiciones subieron a la 2.2: todo usuario las vuelve a aceptar antes de su próximo pago.
    Termina con «Listo para revisión si quieres».
  </action>
  <verify>
    <automated>npm run check:encoding && npm run taxonomia && npm run facturas:letras && npm run precios:probar && npm run seguridad:probar && npm run dealer:probar && npm run bitacora:probar && npm run contactos:probar && npm run catalogo:probar && npm run facturas:probar && npm run pagos:probar && npm run metricas:probar && npm run transferencia:probar && npm run publicacion:probar && npm run renovacion:probar && npm run recordatorios:probar && npm run chat:probar</automated>
  </verify>
  <acceptance_criteria>
    - Todas las órdenes del verify terminan con 0.
    - `MERCA_DB=.tmp/tareas-seco.db MERCA_CORREO=archivo node tools/tareas.js --seco` termina con 0.
    - El SUMMARY del plan contiene la lista para Victor con los cinco apartados y ninguna prueba silenciada o saltada.
  </acceptance_criteria>
  <done>La fase entera pasa la batería sin navegador y Victor tiene la lista de lo que solo él puede comprobar o decidir.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente → condiciones aceptadas | lo aceptado debe poder reconstruirse por versión |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.3-21 | Repudiation | condiciones de renovación | mitigate | versión 2.2 nueva con su fecha; `PARA_PAGAR` exige aceptarla antes de pagar una renovación; prueba en la sección 6 |
| T-05.3-22 | Tampering | batería | mitigate | ninguna prueba se silencia ni se salta; los fallos se corrigen en su origen y se anotan |
</threat_model>

<verification>
La batería completa sin navegador en verde, `tools/tareas.js --seco` sobre una base desechable sin fallos, `contratacion` en 2.2 en los dos sitios. Las auditorías del navegador y los enlaces pasan en el CI del PR antes de fusionar.
</verification>

<success_criteria>
Las condiciones de contratación explican la renovación con su versión subida, toda la fase está probada y Victor sabe qué comprobar en vivo, qué se decidió por defecto y qué heredan la 05.4 y la fase 6.
</success_criteria>

<output>
Create `.planning/phases/05.3-renovaci-n-manual-vencimientos-y-alertas-de-7-3-y-1-d-a/05.3-05-SUMMARY.md` when done
</output>

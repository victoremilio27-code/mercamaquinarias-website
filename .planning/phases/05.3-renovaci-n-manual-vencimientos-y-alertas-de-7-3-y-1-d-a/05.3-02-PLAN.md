---
phase: 05.3-renovaci-n-manual-vencimientos-y-alertas-de-7-3-y-1-d-a
plan: 02
type: execute
wave: 2
depends_on: [05.3-01]
files_modified:
  - tools/api.js
  - tools/probar-renovacion.js
autonomous: true
requirements: [MOD-09, MOD-12]
comprobante_ncf: true   # pide cobros que acaban en confirmarPago y toca la consola que marca transferencias recibidas (emite comprobante): ejecutar con Opus

must_haves:
  truths:
    - "El dueño renueva un anuncio de una suscripción de un cupo con `POST /api/anuncios/:id/renovar`, antes y después de vencer; el anuncio es el mismo (mismo id, fotos, historial) y el importe sale del servidor con `precios.precioRenovacion` al precio vigente del plan."
    - "Una suscripción de varios cupos se renueva entera con `POST /api/membresias/:id/renovar`; pedir «renovar anuncio» sobre ella responde 409 con el id de la membresía para renovar el plan (D-04)."
    - "Pedir otra renovación con una en espera devuelve la misma (202, misma referencia), sin crear otro pago ni volver a avisar a facturación (D-05)."
    - "Una renovación por transferencia se marca recibida en la consola y renueva por `confirmarPago`; si la membresía ya no se puede renovar, 409 «Anule el pago» sin escribir nada."
    - "`PUT /api/membresias/:id/renovacion-automatica` solo lo usa el propietario; con `MERCA_CARDNET` apagado activar responde 409 y no guarda nada; desactivar siempre funciona; el texto aceptado es el del servidor, nunca el que manda el navegador (D-11, D-12)."
    - "`GET /api/membresias` entrega `renovables` (incluidas las vencidas) con su precio final sin base ni ajuste, `metodosPago` y `renovacionAutomatica: { disponible, texto? }`."
  artifacts:
    - path: "tools/api.js"
      provides: "renovarAnuncio, renovarMembresia, pedirRenovacion, cambiarRenovacionAutomatica, RENOVACION_HUERFANA, TEXTO_RENOVACION_AUTOMATICA, renovacionAutomaticaDisponible; misPlanes ampliado"
      contains: "tipo: 'renovacion'"
    - path: "tools/probar-renovacion.js"
      provides: "secciones 4 (rutas de renovar) y 5 (consola, renovación automática y seguridad)"
  key_links:
    - from: "tools/api.js pedirRenovacion"
      to: "db.registrarCobro → pagos.cobrar → pagos.confirmarPago → db.aprobarPago rama 'renovacion'"
      via: "intención { tipo: 'renovacion', idSusc, idAnuncio, cupo, dias }"
      pattern: "tipo: 'renovacion'"
    - from: "tools/api.js marcarTransferenciaRecibida"
      to: "RENOVACION_HUERFANA"
      via: "comprobación previa con db.suscripcionRenovable y traducción del 404/409 en el catch"
      pattern: "RENOVACION_HUERFANA"
    - from: "tools/api.js cambiarRenovacionAutomatica"
      to: "db.guardarRenovacionAutomatica"
      via: "solo con renovacionAutomaticaDisponible() para activar"
      pattern: "guardarRenovacionAutomatica"
---

<objective>
Las rutas de la renovación manual y de la casilla de renovación automática, sobre lo que dejó el plan
05.3-01 en la base. Renovar es pedir un cobro nuevo con intención `renovacion` que solo aprueba
`pagos.confirmarPago` (la única transición), con el mismo camino de importe cero, tarjeta y transferencia
que `pagarBorrador`.

**Toca la ruta del comprobante/NCF: SÍ** (pide cobros que emiten comprobante al confirmarse y toca
`marcarTransferenciaRecibida`). `tools/facturas.js`, `tools/pagos.js` y `tools/db.js` NO se tocan en
este plan: si falta algo de la base, se para y se dice (lo hizo el 05.3-01).

Purpose: MOD-09 (renovar antes y después de vencer, mismo anuncio, transacción nueva al precio vigente) y
MOD-12 (renovación automática opt-in, desactivable, apagada hasta CardNet).
Output: tres rutas nuevas, `GET /api/membresias` ampliado, el caso de renovación en la consola de
transferencias y las secciones 4-5 de `tools/probar-renovacion.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.3-renovaci-n-manual-vencimientos-y-alertas-de-7-3-y-1-d-a/05.3-CONTEXT.md
@.planning/phases/05.3-renovaci-n-manual-vencimientos-y-alertas-de-7-3-y-1-d-a/05.3-01-SUMMARY.md
@.planning/phases/05.2-publicar-este-equipo-borrador-pago-y-activaci-n-del-particul/05.2-03-SUMMARY.md
@CLAUDE.md

<interfaces>
De tools/db.js tras el 05.3-01 (usar, no reescribir):
- `suscripcionRenovable(idSusc, idOrg)` → fila de `suscripcionesRenovablesDe` o null: `s.*` (dias_ciclo,
  renovacion_automatica, fin, estado, anuncios_incluidos, plan_id), plan_nombre, plan_activo, destacado,
  ocupados, renovacion_pendiente, precio_vigente. Incluye las de fin pasado; excluye fin NULL y sin límite.
- `suscripcionesRenovablesDe(idOrg)` → lista con la misma forma.
- `pagoPendienteDeRenovacion(idSusc)` → pago pendiente de renovación o null.
- `renovarSinCosto({ idOrg, idSusc, idAnuncio, dias, cobro })` → `{ membresia, anuncio, periodo }` (solo total 0).
- `registrarCobro({ idOrg, idSusc, idAnuncio, cobro, intencion })` → 409 «Esa membresía ya tiene una
  renovación en espera» si choca con `ux_pagos_renovacion_pendiente`.
- `guardarRenovacionAutomatica({ idSusc, idOrg, activar, texto })` → true si cambió una fila.
- `anuncio(id)`, `pagoPorReferencia(ref)`, `permitir(clave, n, minutos)`.
- `cambiarEstadoAnuncio` ya niega reactivar un `vencido` (solo vuelve por la renovación pagada).

De tools/api.js (existentes, líneas aproximadas):
- `clienteDeCompra(c, ctx)` (~2570), `comprarMembresia` (~2588, `exigirAceptacion(res, id, legales.PARA_PAGAR)`),
  `ampliarMembresia` (~2681), `misPlanes` (~2435), `precioUnitario(plan)` (~2402), `referenciaCobro()` (~2411),
  `cobroPublico` (~2505, quita base y ajuste), `avisarTransferenciaPedida(ctx, { referencia, total, concepto })` (~2537),
  `puedeAdministrar(ctx)` (~1738), `ctx.organizacion.rol` ('propietario' | 'administrador' | …).
- `responderPagoEnEspera`, `excesoDelPlan`, `pagarBorrador` y los textos `*_PUBLICACION` (~3375-3540): el molde.
- `AMPLIACION_HUERFANA`, `PUBLICACION_HUERFANA`, `pagoDeTransferencia`, `falloInterno`,
  `marcarTransferenciaRecibida` (~1500-1600).
- Tabla de rutas (~3990-4075): `['POST', /^\/api\/membresias\/([\w-]+)\/ampliar$/, ampliarMembresia]`,
  las de `/api/anuncios/:id/...` antes de la genérica `['PATCH', /^\/api\/anuncios\/([\w-]+)$/, cambiarEstado]`.
- `pagos.procesadorDeCobro(metodo)`, `pagos.cobrar(pago)`, `pagos.metodosDeCobro()`, `pagos.nombreDeEquipo(a)`.
- `assets/precios.js`: `precioRenovacion({ precioUnitario, cupo, dias })`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarea 1: renovar un anuncio y renovar un plan</name>
  <files>tools/api.js, tools/probar-renovacion.js</files>
  <read_first>
    - tools/api.js líneas 2400-2470 (precioUnitario, referenciaCobro, misPlanes), 2500-2600 (cobroPublico, avisarTransferenciaPedida, clienteDeCompra, comprarMembresia), 2681-2770 (ampliarMembresia), 3370-3540 (textos de la publicación, responderPagoEnEspera, pagarBorrador), 3990-4075 (tabla de rutas)
    - tools/probar-renovacion.js entero (lo dejó el 05.3-01: helpers y secciones 1-3)
    - tools/probar-publicacion.js: `pedir`, `cuentaCon` y las secciones 9-10 (cómo se prueba pagarBorrador por HTTP falso, precio manipulado y pendiente repetido)
  </read_first>
  <behavior>
    - Anuncio activo de una suscripción de un cupo con precio > 0 (Destacado): `POST /api/anuncios/:id/renovar` con `{ total: 1, cupo: 5, dias: 7, precio: 1 }` → 201, `cobro.total` = `precios.precioRenovacion({ precioUnitario: vigente, cupo: 1, dias: dias_ciclo }).total`, comprobante presente y cuadrado, `membresia.fin` = fin anterior + días, mismo id de anuncio.
    - Anuncio `vencido` con su suscripción `vencida` → 201 y el anuncio vuelve a `activo` con el mismo id; el número de anuncios de la organización no cambia.
    - Estándar en promoción (importe cero) → 201 sin comprobante, pago 'sin-costo', fin extendido.
    - Con procesador de transferencia: la primera petición → 202 con `transferencia` y un aviso a facturación; la segunda → 202 con la MISMA referencia, sin pago nuevo y sin segundo aviso.
    - Rechazo del procesador → 402, la suscripción no cambia.
    - Suscripción de 2 cupos: renovar el anuncio → 409 con `idSusc`; `POST /api/membresias/:idSusc/renovar` → 201 y los dos anuncios extendidos.
    - Anuncio ajeno o inexistente → 404 con el mismo texto; borrador → 409; vendido → 409; anuncio sin suscripción (modelo viejo) → 409; membresía sin fecha de fin (cuenta interna) → 404 en la ruta de membresía.
    - Sin aceptar las condiciones de pago → 409 con `faltan` y nada anotado.
    - `GET /api/membresias` trae `renovables` (una vencida incluida) con `precio.total` y sin `base` ni `ajuste`, `metodosPago` y `renovacionAutomatica.disponible === false`.
  </behavior>
  <action>
    En tools/api.js, junto a `pagarBorrador`, per D-01, D-02, D-04 y D-05:

    Textos propios sin la palabra «cupo»: `NO_APROBADO_RENOVACION` («El pago no fue aprobado. No se le cobró nada y su anuncio sigue como estaba.»), `EN_PROCESO_RENOVACION` («Su pago está en proceso. La renovación se aplica cuando se confirme.»), `EN_ESPERA_RENOVACION` («Transfiera el importe con la referencia indicada. La renovación se aplica cuando confirmemos el ingreso.»). `responderRenovacionEnEspera(res, pago, membresia)` con la misma forma que `responderPagoEnEspera` (datos de la cuenta si es transferencia, `SIN_DATOS_TRANSFERENCIA` si se apagó) pero con estos textos.

    `async function pedirRenovacion(req, res, ctx, { s, idAnuncio })` — el núcleo compartido, en este orden: `leerCuerpo`; `s.renovacion_pendiente` o `db.pagoPendienteDeRenovacion(s.id)` → `responderRenovacionEnEspera` (202, sin crear nada); `!s.plan_activo` → 409 «El plan de esta publicación ya no se ofrece, así que no se puede renovar tal cual. Escríbanos por correo o por el asistente del sitio.» (nunca un teléfono); `dias = s.dias_ciclo === 60 ? 60 : 30` (se renueva el mismo ciclo que se contrató; `dias` del cuerpo NO se lee, comentario T-05.3-07); `cobro = { ...precios.precioRenovacion({ precioUnitario: s.precio_vigente, cupo: s.anuncios_incluidos, dias }), referencia: referenciaCobro() }` (D-02: lo que mande el navegador como importe, cupo, plan o días no se mira); `clienteDeCompra(c, ctx)` → 400; importe cero → `db.renovarSinCosto(...)` y 201 `{ anuncio, membresia, cobro: cobroPublico(cobro), comprobante: null, pago: pagoPublico(db.pagoPorReferencia(cobro.referencia)) }`; `pagos.procesadorDeCobro(c.metodo)` → 400; concepto: con anuncio `Renovación ${s.plan_nombre} · ${pagos.nombreDeEquipo(db.anuncio(idAnuncio))} · ${dias} días`, sin anuncio `Renovación ${s.plan_nombre} · ${cupo} publicaciones · ${dias} días`; `db.registrarCobro({ idOrg, idSusc: s.id, idAnuncio, cobro, intencion: { tipo: 'renovacion', idSusc: s.id, idAnuncio, idPlan: s.plan_id, cupo: s.anuncios_incluidos, dias, concepto, cliente, correoCliente: ctx.usuario.correo } })`, y en el catch un 409 con `db.pagoPendienteDeRenovacion(s.id)` → responder con el que ganó la carrera; `pagos.cobrar(pago)` → 402 `NO_APROBADO_RENOVACION` / 202 transferencia (con `avisarTransferenciaPedida`) / 202 en proceso / 201 `{ anuncio: idAnuncio ? db.anuncio(idAnuncio) : null, membresia: r.membresia, cobro: cobroPublico(cobro), comprobante: comprobantePublico(r.comprobante), pago: pagoPublico(r.pago) }`. Si `renovacionAutomaticaDisponible()` (tarea 2) y `c.renovacionAutomatica === true`, antes de cobrar llama a `db.guardarRenovacionAutomatica({ idSusc: s.id, idOrg, activar: true, texto: TEXTO_RENOVACION_AUTOMATICA })` (la casilla del pago, D-12; apagada no hace nada).

    `renovarAnuncio = conSesion(async (req, res, ctx, idAnuncio) => …)`: `exigirAceptacion(res, ctx.usuario.id, legales.PARA_PAGAR)`; tope `db.permitir('pedir-pago:' + usuario, 20, 60)` compartido con `pagarBorrador` → 429; `a = db.anuncio(idAnuncio)` ajeno o inexistente → 404 «Ese anuncio no es suyo o no existe»; `borrador` → 409 «Un borrador se publica, no se renueva.»; `vendido`/`retirado` → 409 «Este equipo ya no está publicado: no hay nada que renovar. Puede publicar otro equipo.»; sin `suscripcion_id` → 409 «Este anuncio es del sistema anterior y no tiene un plan que renovar. Duplíquelo para publicarlo de nuevo.»; `s = db.suscripcionRenovable(a.suscripcion_id, org.id)` null → 409 «Este anuncio no tiene un plan que se pueda renovar.»; `s.anuncios_incluidos !== 1` → 409 «Este anuncio es parte de un plan con capacidad para N equipos: renueve el plan completo.» con `{ idSusc: s.id }` (D-04); después `pedirRenovacion(…, { s, idAnuncio })`.

    `renovarMembresia = conSesion(async (req, res, ctx, idSusc) => …)`: mismas condiciones y tope; `s = db.suscripcionRenovable(idSusc, org.id)` null → 404 «Esa membresía no es suya, no existe o no se renueva»; `pedirRenovacion(…, { s, idAnuncio: null })`.

    Rutas: `['POST', /^\/api\/anuncios\/([\w-]+)\/renovar$/, renovarAnuncio]` junto a las demás subrutas de `/api/anuncios/:id/…` (antes de la genérica, con un comentario) y `['POST', /^\/api\/membresias\/([\w-]+)\/renovar$/, renovarMembresia]` junto a `ampliar`.

    `misPlanes`: añade `metodosPago: pagos.metodosDeCobro()`, `renovacionAutomatica` (tarea 2 le da contenido; aquí `{ disponible: false }` si todavía no existe la función) y `renovables: db.suscripcionesRenovablesDe(org.id).map(...)` con `{ id, plan_id, plan_nombre, estado, fin, vencida: fin <= ahora, cupo: anuncios_incluidos, ocupados, dias, renovacion_automatica: !!s.renovacion_automatica, renovacion_pendiente, precio: plan_activo ? cobroPublico(precios.precioRenovacion(...)) : null }`. Comentario: `membresias` ya no trae las vencidas (05.3-01) y el panel necesita verlas para ofrecer «Renovar».

    Arnés: sección «4 · renovar por la API» en tools/probar-renovacion.js con los casos de <behavior> (copia `pedir`/`cuentaCon` de probar-publicacion.js si el 05.3-01 no los trajo; el procesador se sustituye como allí). RED (`test(05.3-02): …`) → GREEN (`feat(05.3-02): …`).
  </action>
  <verify>
    <automated>npm run renovacion:probar && npm run publicacion:probar && npm run transferencia:probar && npm run pagos:probar && npm run seguridad:probar</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "renovar\$/, renovarAnuncio\]" tools/api.js` = 1 y `grep -c "renovar\$/, renovarMembresia\]" tools/api.js` = 1.
    - La línea de `renovarAnuncio` en la tabla está antes que `['PATCH', /^\/api\/anuncios\/([\w-]+)$/, cambiarEstado]` (`grep -n` de las dos).
    - `grep -c "tipo: 'renovacion'" tools/api.js` ≥ 1 y `grep -c "precioRenovacion" tools/api.js` ≥ 2.
    - `grep -v '^\s*//' tools/api.js | grep -c "c\.dias\|c\.total\|c\.cupo" ` no aumenta respecto a antes del plan (el cuerpo no decide el importe).
    - `git diff --stat tools/db.js tools/pagos.js tools/facturas.js` vacío.
    - `npm run renovacion:probar` 0 fallos (secciones 1-4) y el resto del verify en verde.
  </acceptance_criteria>
  <done>El dueño renueva un anuncio o un plan por la API, al precio vigente calculado en el servidor, con un solo pago en espera por membresía.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarea 2: la consola, la casilla de renovación automática y la seguridad</name>
  <files>tools/api.js, tools/probar-renovacion.js</files>
  <read_first>
    - tools/api.js líneas 1500-1600 (AMPLIACION_HUERFANA, PUBLICACION_HUERFANA, marcarTransferenciaRecibida) y 1735-1740 (puedeAdministrar)
    - tools/probar-transferencia.js: cómo enciende la transferencia con `MERCA_TRANSFERENCIA*`, cómo abre la sesión de administradora y llama a `/api/admin/pagos/:id/recibido`
    - .planning/phases/05.3-renovaci-n-manual-vencimientos-y-alertas-de-7-3-y-1-d-a/05.3-CONTEXT.md D-11 y D-12
  </read_first>
  <behavior>
    - Renovación por transferencia pedida → la administradora la marca recibida → la suscripción extiende su fin, el anuncio sigue siendo el mismo, hay un comprobante que cuadra y una fila `pago.transferencia_recibida`; marcarla otra vez → `yaEstaba`, sin segundo periodo ni NCF nuevo.
    - Si entre pedirla y marcarla la membresía se canceló → 409 `RENOVACION_HUERFANA`, sin fila de bitácora, sin comprobante y con la suscripción intacta.
    - `PUT /api/membresias/:id/renovacion-automatica` con `{ activar: true, texto: 'otro' }` y `MERCA_CARDNET` sin definir → 409 y `renovacion_automatica` sigue 0.
    - Con `MERCA_CARDNET=lab`: activar → 200, `renovacion_automatica = 1`, `renovacion_aceptada` con fecha y `renovacion_texto` igual a `TEXTO_RENOVACION_AUTOMATICA` (no a 'otro'); desactivar → 200, `renovacion_automatica = 0` y fecha y texto conservados; `GET /api/membresias` trae `renovacionAutomatica.disponible === true` y el texto.
    - Con `MERCA_CARDNET=apagado` → se comporta como sin definir.
    - Un miembro que no es propietario → 403; membresía ajena → 404; sin sesión → 401.
    - Una suscripción recién comprada tiene `renovacion_automatica = 0` (nunca marcada por defecto).
    - Renovar con `renovacionAutomatica: true` en el cuerpo y CardNet apagado → la renovación sale bien y la casilla sigue en 0.
  </behavior>
  <action>
    Consola (tools/api.js, junto a las otras huérfanas): `RENOVACION_HUERFANA = 'La membresía que renovaba este pago ya no existe o ya no se puede renovar. No se renovó nada ni se emitió comprobante. Anule el pago y devuelva la transferencia al cliente.'` con un comentario en el estilo de las dos anteriores. En `marcarTransferenciaRecibida`: `esRenovacion = intencion.tipo === 'renovacion'`; con el pago `pendiente` y `esRenovacion`, si `!(intencion.idSusc && db.suscripcionRenovable(intencion.idSusc, pago.organizacion_id))` → 409 `RENOVACION_HUERFANA` antes de escribir; en el `catch`, `esRenovacion && (e.codigo === 404 || e.codigo === 409) && !db.suscripcionRenovable(intencion.idSusc, pago.organizacion_id)` → el mismo 409 (el SAVEPOINT de `aprobarPago` ya deshizo todo). Un 409 con la membresía todavía renovable es otra carrera y se dice tal cual, como con la publicación.

    Renovación automática, per D-11 y D-12:
    - `renovacionAutomaticaDisponible()` lee `process.env.MERCA_CARDNET` en cada llamada (las pruebas lo cambian en caliente): disponible solo si tiene valor y no es 'apagado' (valores de `research/cardnet.md`: apagado | lab | produccion). Comentario: la fase 6 la sustituye por `cardnet.ACTIVO`; hasta entonces ofrecer algo que no se va a cobrar engaña.
    - `TEXTO_RENOVACION_AUTOMATICA` = exactamente «Al activar esta opción, autorizas la renovación de este anuncio al finalizar su período con el método de pago autorizado, según las condiciones y el precio vigente de renovación.» (sección 21 del modelo comercial). Se guarda ESE texto, nunca uno del cuerpo (T-05.3-10).
    - `cambiarRenovacionAutomatica = conSesion(async (req, res, ctx, idSusc) => …)`: `ctx.organizacion.rol !== 'propietario'` → 403 «Solo el propietario de la cuenta puede cambiar la renovación automática» (autoriza cobros futuros); `activar = c.activar === true`; `db.suscripcionRenovable(idSusc, org.id)` null → 404; activar sin `renovacionAutomaticaDisponible()` → 409 «La renovación automática todavía no está disponible. Puede renovar a mano cuando quiera.»; activar exige además `exigirAceptacion(res, usuario, legales.PARA_PAGAR)`; `db.guardarRenovacionAutomatica({ idSusc, idOrg: org.id, activar, texto: TEXTO_RENOVACION_AUTOMATICA })`; responde 200 `{ renovacionAutomatica: activar, membresia: db.suscripcionRenovable(idSusc, org.id) }`. Desactivar funciona siempre.
    - Ruta `['PUT', /^\/api\/membresias\/([\w-]+)\/renovacion-automatica$/, cambiarRenovacionAutomatica]` junto a `renovar`.
    - `misPlanes`: `renovacionAutomatica: renovacionAutomaticaDisponible() ? { disponible: true, texto: TEXTO_RENOVACION_AUTOMATICA } : { disponible: false }` (apagada, el panel no recibe ni el texto).

    Arnés: sección «5 · consola, renovación automática y seguridad» con los casos de <behavior>. Al principio del arnés borra `MERCA_CARDNET` del entorno (junto a `MERCA_TRANSFERENCIA*`, antes del require) para que un .env local no decida el resultado. RED → GREEN.
  </action>
  <verify>
    <automated>npm run renovacion:probar && npm run transferencia:probar && npm run bitacora:probar && npm run seguridad:probar && npm run publicacion:probar</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "RENOVACION_HUERFANA" tools/api.js` ≥ 3.
    - `grep -c "renovacion-automatica\$/, cambiarRenovacionAutomatica\]" tools/api.js` = 1.
    - `grep -c "autorizas la renovación de este anuncio al finalizar su período" tools/api.js` = 1.
    - `grep -c "MERCA_CARDNET" tools/probar-renovacion.js` ≥ 2.
    - `npm run renovacion:probar` 0 fallos (secciones 1-5) y el resto del verify en verde.
  </acceptance_criteria>
  <done>La renovación por transferencia pasa por la consola sin huérfanas, y la casilla de renovación automática existe, se guarda con su texto y se desactiva, apagada mientras CardNet lo esté.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → rutas de renovar | cuerpo no fiable: importe, cupo, plan, días, texto del consentimiento |
| personal → consola de transferencias | marca recibida una renovación que pudo quedar huérfana |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.3-07 | Tampering | pedirRenovacion | mitigate | importe con `precios.precioRenovacion` del precio vigente, el cupo y el ciclo de la base; el cuerpo solo aporta método y datos fiscales; prueba de precio manipulado |
| T-05.3-08 | Elevation | renovar anuncio/membresía ajena | mitigate | `db.anuncio` con comprobación de `organizacion_id` y `suscripcionRenovable(id, org.id)` de la sesión; 404 con el mismo texto para ajeno e inexistente |
| T-05.3-09 | Repudiation | dos pagos de renovación en espera | mitigate | devolver el pendiente existente; carrera frenada por `ux_pagos_renovacion_pendiente` y respondida con el ganador |
| T-05.3-10 | Spoofing | consentimiento de renovación automática | mitigate | texto fijo del servidor, fecha del servidor, solo el propietario, exige condiciones de pago; apagada sin CardNet |
| T-05.3-11 | Tampering | consola: renovación huérfana | mitigate | 409 antes de escribir y traducción en el catch; el SAVEPOINT deshace todo |
| T-05.3-12 | Denial | pedir renovaciones en bucle | mitigate | tope compartido `pedir-pago:<usuario>` 20/h |
</threat_model>

<verification>
`npm run renovacion:probar` (secciones 1-5), `publicacion:probar`, `transferencia:probar`, `pagos:probar`, `bitacora:probar`, `seguridad:probar`, `facturas:probar`: todo en verde. `tools/db.js`, `tools/pagos.js` y `tools/facturas.js` sin cambios en este plan.
</verification>

<success_criteria>
Renovar un anuncio o un plan por la API crea una transacción nueva al precio vigente que solo aplica `confirmarPago`, con un único pendiente por membresía, camino de transferencia en la consola y la casilla de renovación automática guardada, desactivable y apagada hasta CardNet.
</success_criteria>

<output>
Create `.planning/phases/05.3-renovaci-n-manual-vencimientos-y-alertas-de-7-3-y-1-d-a/05.3-02-SUMMARY.md` when done
</output>

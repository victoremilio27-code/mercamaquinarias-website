---
phase: 05-cobro-por-transferencia-bancaria
plan: 04
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - admin.html
  - assets/admin.js
autonomous: true
requirements: [ADMIN-06]

must_haves:
  truths:
    - "En admin.html hay una sección «Pagos por transferencia» que lista los pendientes con referencia, empresa, concepto, importe, fecha y correo del comprador."
    - "«Marcar recibido» pide confirmación con el importe y la referencia, acepta la referencia del banco como nota, y al terminar muestra el NCF emitido; el pago sale de la lista."
    - "Una ampliación cuya membresía ya no existe se ve marcada, sin botón de recibido, y solo ofrece «Anular» con motivo obligatorio (D-07)."
    - "Tras marcar o anular, la bitácora de la consola (fase 4) muestra la acción con su rótulo sin recargar la página."
    - "El filtro Pendientes / Recibidos / Anulados permite consultar lo ya resuelto con su comprobante."
  artifacts:
    - path: "admin.html"
      provides: "sección t-pagos"
      contains: "t-pagos"
    - path: "assets/admin.js"
      provides: "montarPagos, cargarPagos, pagoHTML, marcarRecibido, anularPago"
      contains: "/admin/pagos"
  key_links:
    - from: "assets/admin.js marcarRecibido"
      to: "POST /api/admin/pagos/:id/recibido"
      via: "api() con try/catch"
      pattern: "/recibido"
    - from: "assets/admin.js"
      to: "cargarBitacora()"
      via: "recarga tras cada escritura"
      pattern: "cargarBitacora\\("
---

<objective>
La pantalla del personal para ADMIN-06: ver las transferencias en espera,
marcarlas recibidas o anularlas, con la bitácora de la fase 4 al lado.

Purpose: criterios 3 y 4 de la fase en la consola, sin abrir una terminal.
Output: sección nueva en `admin.html` y su lógica en `assets/admin.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/05-cobro-por-transferencia-bancaria/05-01-PLAN.md
@.planning/phases/05-cobro-por-transferencia-bancaria/05-02-SUMMARY.md
@.planning/phases/04-bandeja-de-solicitudes-y-bit-cora-de-la-consola/04-03-SUMMARY.md
@.planning/phases/04-bandeja-de-solicitudes-y-bit-cora-de-la-consola/04-04-SUMMARY.md

Decisiones D-05 a D-08 en `05-01-PLAN.md`.

<interfaces>
`admin.html` (tras la fase 4): secciones `<section class="panel" aria-labelledby="t-…">`
en este orden: `t-cola`, `t-bandeja`, `t-facturas`, `t-legales`, `t-bitacora`,
`t-flota`, `t-heroe`, `t-pub`. La nueva va entre `t-bandeja` y `t-facturas`.

`assets/admin.js` (tras la fase 4) — patrones a copiar, no a reinventar:
- Bandeja (04-03): `avisarBandeja`, `bandejaHTML(s)`, `pintarFiltrosServicio`,
  `cargarBandeja`, `marcarBandeja`, `pedirNota(fila)`, `elegirFiltro`, `montarBandeja`.
- `pedirMotivo(fila, alConfirmar)` (~165): textarea `.sol__motivo` con botones
  `.sol__acciones`.
- Bitácora (04-04): `cargarBitacora()` (~1340), `montarBitacora()` (~1349).
- `montarAdmin()` (~859) monta las secciones; `api()` lanza si no es ok
  (envolver cada escritura en try/catch). `esc()` de `assets/app.js`.

API (05-02): `GET /api/admin/pagos?estado=pendiente|aprobado|rechazado` →
`{ estado, pagos: [{ id, referencia, organizacion_nombre, concepto, tipo, total, creado, correoCliente, membresiaViva?, factura? }] }`;
`POST /api/admin/pagos/:id/recibido { motivo? }` → `{ pago, membresia, comprobante, yaEstaba, aviso? }`, 409 con `error`;
`POST /api/admin/pagos/:id/anular { motivo }` → `{ pago }`, 400/409 con `error`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarea 1: sección «Pagos por transferencia» y su listado</name>
  <files>admin.html, assets/admin.js</files>
  <action>
**`admin.html`**: sección `<section class="panel" aria-labelledby="t-pagos">`
entre `t-bandeja` y `t-facturas`, con la misma estructura que la bandeja:
título «Pagos por transferencia», una línea de ayuda («Marque un pago como
recibido solo cuando el dinero esté en la cuenta. En ese momento el cliente
recibe sus cupos y su comprobante fiscal, y queda anotado en la bitácora.»),
filtros Pendientes / Recibidos / Anulados, una zona de avisos con
`role="status"` y la lista. Reutilizar las clases de la bandeja; no tocar
`styles.css` (lo toca 05-03 en la misma ola).

**`assets/admin.js`**: `avisarPagos`, `pagoHTML(p)`, `cargarPagos()`,
`montarPagos()` copiando la forma de la bandeja, llamado desde `montarAdmin()`
junto a `montarBandeja()`. `pagoHTML`: referencia destacada, empresa,
concepto, importe en `RD$`, fecha, correo del comprador (texto, sin
teléfono); en pendientes, botones «Marcar recibido» y «Anular»; si
`membresiaViva === false`, aviso «La membresía que ampliaba este pago ya no
existe: anúlelo y devuelva la transferencia» y SIN botón de recibido (per
D-07); en recibidos, el NCF de `factura`; en anulados, solo el estado. Todo
por `esc()`. Lista vacía: «No hay transferencias pendientes.»
  </action>
  <verify>
    <automated>node --check assets/admin.js &amp;&amp; npm run check:encoding &amp;&amp; node -e "const f=require('fs');const h=f.readFileSync('admin.html','utf8');const a=h.indexOf('t-bandeja'),p=h.indexOf('t-pagos'),c=h.indexOf('t-facturas');if(!(a<p&amp;&amp;p<c))throw new Error('seccion fuera de sitio');const s=f.readFileSync('assets/admin.js','utf8');if(!s.includes('/admin/pagos')||!s.includes('montarPagos'))throw new Error('falta la logica');console.log('ok')"</automated>
  </verify>
  <done>La consola lista las transferencias por estado, marca las ampliaciones huérfanas y no ofrece recibido en ellas.</done>
</task>

<task type="auto">
  <name>Tarea 2: marcar recibido y anular, con la bitácora al día</name>
  <files>assets/admin.js</files>
  <action>
1. «Marcar recibido»: abre en la fila (patrón `pedirNota`/`pedirMotivo`, sin
   `prompt()`) una confirmación «Confirmar que recibió {importe} con la
   referencia {ref}» y un campo opcional «Referencia del banco» que viaja como
   `motivo`. Botón deshabilitado mientras dura la petición (un doble clic no
   rompe nada en el servidor, pero no se provoca). En éxito:
   `avisarPagos('Recibido. Se otorgaron los cupos y se emitió el comprobante {ncf}.', true)`;
   si viene `aviso` (emisión fallida), mostrar ese aviso en vez del éxito;
   recargar `cargarPagos()` y `cargarBitacora()` (si existe) y, si la sección
   de facturas está montada, su recarga.
2. «Anular»: `pedirMotivo` con texto propio («Por qué se anula. Se le envía al
   cliente tal cual.»); motivo vacío o de menos de 5 caracteres no se envía.
   En éxito, aviso «Anulado. No se otorgó nada ni se emitió comprobante.» y las
   mismas recargas.
3. Errores (`try/catch` alrededor de cada `api()`): mostrar `e.message` en la
   zona de avisos; un 409 recarga la lista, porque la fila ya cambió.
  </action>
  <verify>
    <automated>node --check assets/admin.js &amp;&amp; node -e "const s=require('fs').readFileSync('assets/admin.js','utf8');for(const t of ['/recibido','/anular','cargarBitacora(']){if(!s.includes(t))throw new Error('falta '+t)}if(/prompt\(/.test(s.slice(s.indexOf('montarPagos'))))throw new Error('prompt en pagos');console.log('ok')"</automated>
  </verify>
  <done>El personal marca o anula desde la consola, ve el NCF emitido y la acción aparece en la bitácora sin recargar la página.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| base → DOM de la consola | Nombre de empresa, concepto y correo vienen de lo que teclearon los clientes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-14 | Tampering (XSS) | pagoHTML | mitigate | Todo campo pasa por `esc()`; ids en URL con `encodeURIComponent` |
| T-05-15 | Elevation of privilege | sección de pagos | accept | La pantalla no protege nada: la autorización está en el servidor (`conAdmin`, 404) y se prueba en 05-02 |
</threat_model>

<verification>
- `node --check assets/admin.js`, `check:encoding`.
- La verificación en navegador, en los dos temas, en 05-05.
</verification>

<success_criteria>
ADMIN-06 en pantalla: marcar recibido otorga cupos y comprobante en ese momento y queda en la bitácora visible.
</success_criteria>

<output>
Crear `.planning/phases/05-cobro-por-transferencia-bancaria/05-04-SUMMARY.md`.
</output>

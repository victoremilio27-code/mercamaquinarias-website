---
phase: 05-cobro-por-transferencia-bancaria
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - assets/planes.js
  - assets/panel.js
  - styles.css
autonomous: true
requirements: [PAGO-09]

must_haves:
  truths:
    - "En planes.html, con la transferencia encendida, el pedido muestra «Forma de pago: transferencia bancaria» y, tras contratar, un bloque con banco, titular, RNC, tipo y número de cuenta, importe y referencia, que no desaparece solo."
    - "Con la transferencia apagada, planes.html se ve y se comporta exactamente como antes de la fase."
    - "Una respuesta 202 nunca se presenta como compra hecha: ni «Listo. Contrató…», ni redirección al borrador, ni cupos pintados que no existen (pendiente anotado por la fase 3)."
    - "En el panel, el comprador ve «Pagos en espera de confirmación» con la referencia, el importe y los datos, y sin cupos nuevos todavía."
    - "Ningún texto nuevo contiene un número de teléfono ni WhatsApp: el soporte es el correo de facturación y el asistente."
  artifacts:
    - path: "assets/planes.js"
      provides: "método de pago en el pedido, bloque de datos de transferencia, 202 en compra y ampliación"
      contains: "pagosPendientes"
    - path: "assets/panel.js"
      provides: "lista de pagos en espera y 202 en ampliación"
      contains: "pagosPendientes"
  key_links:
    - from: "assets/planes.js contratar()"
      to: "POST /api/membresias → 202"
      via: "r.pago.estado === 'pendiente'"
      pattern: "'pendiente'"
    - from: "assets/panel.js"
      to: "GET /api/membresias pagosPendientes"
      via: "api('/membresias')"
      pattern: "pagosPendientes"
---

<objective>
Lo que ve el comprador: elegir transferencia, recibir los datos y la
referencia, y encontrar en su cuenta el pago en espera sin cupos todavía.
Cierra además el pendiente de la fase 3: el navegador todavía trataba un 202
como compra hecha.

Purpose: criterios 1 y 2 de la fase (PAGO-09) en pantalla.
Output: `assets/planes.js`, `assets/panel.js` y un bloque pequeño en `styles.css`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/05-cobro-por-transferencia-bancaria/05-01-PLAN.md
@.planning/phases/05-cobro-por-transferencia-bancaria/05-02-SUMMARY.md
@.planning/phases/02-tema-claro-y-oscuro-coherentes/02-06-SUMMARY.md

Decisiones D-01 a D-14 en `05-01-PLAN.md`. Forma exacta de las respuestas en
`05-02-SUMMARY.md`. Si el SUMMARY de la fase 2 tiene otro número, usar el
último de esa fase: lo que importa son los tokens de color y el comprobador.

<interfaces>
`assets/sesion.js`: `api(ruta, { metodo, cuerpo, silencioso })` devuelve el
JSON en cualquier 2xx (un 202 incluido) y LANZA si no es `ok` (con `codigo`);
con `silencioso` devuelve null en vez de lanzar. `esc()` en `assets/app.js`.

`assets/planes.js` (líneas al planificar, fase 4):
- `contratar()` (~247): `api('/membresias', { metodo: 'POST', cuerpo })`, luego
  si `destino()` redirige, si no recarga `MIS_CUPOS`, `pintarMisCupos()` y
  `avisar('Listo. Contrató …', true)`.
- `pintarPedido()` (~185-219): texto de `#btnContratar` y la inserción dinámica
  de `.pedido__legal` después del botón (patrón a copiar para el bloque nuevo).
- Ampliar (~355-372): `api('/membresias/:id/ampliar', …)` y `avisar('… pasó a N cupos.', true)`.
- Arranque (~383): `api('/planes', { silencioso: true })` y `api('/membresias', { silencioso: true })`.

`assets/panel.js`: `tarjetaMembresia(m)` (~69), `pintarPlan()` (~145, pinta
`<ul class="membresias">`), carga en `montarPanel()` (~653-704,
`MEMBRESIAS = datos.membresias`), ampliación (~795-843).

Respuestas (05-02): 202 → `{ membresia, cobro: { referencia, total, … }, comprobante: null, pago: { id, estado: 'pendiente' }, aviso, transferencia? }`;
`GET /api/membresias` → `{ membresias, exenta, pagosPendientes: [{ id, referencia, total, creado, procesador, concepto, tipo }], transferencia? }`;
`GET /api/planes` → `{ …, metodosPago: ['transferencia'] | ['demo'] }`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarea 1: planes.js ofrece la transferencia y deja de dar por hecha una compra pendiente</name>
  <files>assets/planes.js, styles.css</files>
  <action>
1. Guardar `metodosPago` de la respuesta de `/planes` en el arranque. Si
   incluye `transferencia`: en `pintarPedido()` insertar (mismo patrón
   dinámico que `.pedido__legal`, sin tocar planes.html) una línea
   «Forma de pago: transferencia bancaria. Le damos los datos y la referencia
   al confirmar; los cupos y el comprobante fiscal llegan cuando recibamos el
   ingreso.» y el botón pasa a «Pedir datos para transferir {total}». Mandar
   `metodo: 'transferencia'` en el cuerpo. Sin transferencia: nada cambia.
2. `contratar()`: si `r.pago && r.pago.estado === 'pendiente'`, NO redirigir a
   `destino()`, NO decir «Listo. Contrató», y pintar `pintarTransferencia(r)`:
   un bloque `.transferencia` (role="status") después del pedido con banco,
   titular, RNC, tipo y número de cuenta, importe con `pesos(r.cobro.total)`,
   la referencia en grande con un botón «Copiar» (`navigator.clipboard`, con
   caída silenciosa si no existe), y el texto: ponga la referencia en el
   concepto; envíe el comprobante de la transferencia a {correo} (enlace
   `mailto:`); los días de su membresía empiezan a contar cuando confirmemos;
   esto no es un comprobante fiscal. Si hay `destino()`, un enlace «Volver a mi
   borrador» (el borrador se publicará cuando lleguen los cupos). Si la 202 no
   trae `transferencia` (otro procesador en proceso), mostrar solo `r.aviso`.
   Todo texto del servidor pasa por `esc()`.
3. Ampliar en planes.js: con 202, `avisar(r.aviso)` sin decir que el cupo
   cambió, y pintar el mismo bloque si viene `transferencia`.
4. Al arrancar, si `/membresias` trae `pagosPendientes`, recordar arriba del
   pedido «Tiene N pagos en espera de confirmación» con enlace a panel.html.
5. `styles.css`: un bloque `.transferencia` pequeño con los tokens de color del
   sistema de temas de la fase 2 (nada de colores sueltos), legible en claro y
   oscuro. `styles.css` es CRLF: editar con la herramienta de edición, nunca
   restaurar saltos de línea.
  </action>
  <verify>
    <automated>node --check assets/planes.js &amp;&amp; npm run check:encoding &amp;&amp; npm run check:contraste &amp;&amp; node -e "const s=require('fs').readFileSync('assets/planes.js','utf8');for(const t of ['pendiente','transferencia','pagosPendientes']){if(!s.includes(t))throw new Error('falta '+t)}if(/whatsapp|tel:|8[024]9[-) ]?\d{3}/i.test(s))throw new Error('telefono en planes.js');console.log('ok')"</automated>
  </verify>
  <done>Con transferencia encendida el comprador ve cómo pagar y la referencia; con ella apagada la página no cambia; un 202 nunca se anuncia como compra hecha.</done>
</task>

<task type="auto">
  <name>Tarea 2: el panel muestra los pagos en espera</name>
  <files>assets/panel.js</files>
  <action>
1. En la carga del panel (`montarPanel` y la recarga de `/membresias`),
   guardar `pagosPendientes` y `transferencia`.
2. En `pintarPlan()`, antes de `<ul class="membresias">`, si hay pendientes:
   título «Pagos en espera de confirmación» y, por pago: concepto, importe,
   fecha de pedido y referencia; si es por transferencia y hay datos, los datos
   de la cuenta con el mismo formato y la misma clase `.transferencia` de la
   tarea 1 (reutilizar la clase, no crear otra). Si es por transferencia y NO
   hay datos (se apagó después), «Escríbanos a {correo de facturación} para
   completar este pago» sin inventar cuenta. Texto: «Sus cupos aparecerán aquí
   cuando confirmemos el ingreso; le enviaremos el comprobante fiscal por
   correo.» Todo por `esc()`.
3. Ampliación desde el panel (~795): con 202, `avisar(r.aviso)`, recargar para
   que el pendiente aparezca en la lista, y no decir que el cupo cambió.
4. Ningún teléfono ni WhatsApp en textos nuevos.
  </action>
  <verify>
    <automated>node --check assets/panel.js &amp;&amp; npm run check:encoding &amp;&amp; node -e "const s=require('fs').readFileSync('assets/panel.js','utf8');if(!s.includes('pagosPendientes')||!s.includes('Pagos en espera'))throw new Error('falta la lista');console.log('ok')"</automated>
  </verify>
  <done>El comprador ve en su panel el pago en espera con su referencia y los datos, sin cupos nuevos, y una ampliación pendiente no se anuncia como hecha.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| servidor → DOM | Nombre de banco, titular, concepto y referencia se pintan como HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-12 | Tampering (XSS) | pintarTransferencia, lista de pendientes | mitigate | Todo valor del servidor pasa por `esc()`; el `mailto:` se construye con `encodeURIComponent` |
| T-05-13 | Spoofing | datos de cuenta mostrados | mitigate | Solo se pintan los que manda el servidor; el navegador no tiene ningún valor por defecto |
</threat_model>

<verification>
- `node --check` de los dos archivos, `check:encoding`, `check:contraste`.
- La verificación en navegador, en los dos temas, se hace en 05-05.
</verification>

<success_criteria>
Criterios 1 y 2 de la fase visibles para el comprador; el 202 de la fase 3 queda tratado en compra y ampliación.
</success_criteria>

<output>
Crear `.planning/phases/05-cobro-por-transferencia-bancaria/05-03-SUMMARY.md`.
</output>

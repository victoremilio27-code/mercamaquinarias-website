---
phase: 05-cobro-por-transferencia-bancaria
plan: 05
type: execute
wave: 4
depends_on: ["05-02", "05-03", "05-04"]
files_modified:
  - tools/probar-transferencia.js
autonomous: false
requirements: [PAGO-09, ADMIN-06]

must_haves:
  truths:
    - "Sin ningún procesador de pasarela y con la transferencia encendida, una organización nueva compra cupos por transferencia, no puede publicar mientras está pendiente, el personal marca recibido, y entonces publica un anuncio que aparece en el catálogo público."
    - "En ese recorrido se emite exactamente un comprobante con NCF, en el momento del marcado y no antes, y la bitácora tiene exactamente una fila de la organización."
    - "El procesador demo no se llama ni una vez en todo el recorrido."
    - "Victor ha visto el flujo en el navegador, en tema claro y oscuro, sin ningún teléfono en pantalla."
  artifacts:
    - path: "tools/probar-transferencia.js"
      provides: "Sección de extremo a extremo del criterio 5"
      contains: "extremo a extremo"
  key_links:
    - from: "tools/probar-transferencia.js"
      to: "POST /api/anuncios"
      via: "pedir() tras el marcado"
      pattern: "/api/anuncios"
---

<objective>
Probar el criterio 5 de principio a fin —comprar cupos por transferencia,
marcar recibido, publicar un anuncio— con CardNet inexistente, pasar la
batería entera y que Victor vea el flujo en el navegador.

Purpose: la fase existe para que el 14 de octubre se pueda cobrar y publicar
sin nadie externo; esto es lo que lo demuestra.
Output: sección final de `tools/probar-transferencia.js` y la verificación humana.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/05-cobro-por-transferencia-bancaria/05-01-PLAN.md
@.planning/phases/05-cobro-por-transferencia-bancaria/05-02-SUMMARY.md
@.planning/phases/05-cobro-por-transferencia-bancaria/05-03-SUMMARY.md
@.planning/phases/05-cobro-por-transferencia-bancaria/05-04-SUMMARY.md

<interfaces>
Publicar: `POST /api/anuncios` con sesión. Sin membresía → 402 «Todavía no
tiene cupos. Contrate un plan para publicar este equipo.»; sin cupo libre → 409.
Cuerpo válido y subida de fotos por `POST /api/fotos`: copiar el caso
«bienPublicado» de `tools/probar-seguridad.js` (~340-420: fotos subidas antes,
categoría `camiones`, `cam-volteo`, etc.). Límite `publicar:<idUsuario>` de 20
por hora: una sola publicación aquí.
Catálogo público: la ruta GET de listado de anuncios de `tools/api.js` (buscar
la que usa `index.html`/`buscar.html`) sin sesión.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarea 1: el criterio 5 de extremo a extremo en el arnés</name>
  <files>tools/probar-transferencia.js</files>
  <action>
Sección final «Extremo a extremo: comprar por transferencia, marcar recibido,
publicar», con una organización NUEVA (no reutilizar las de secciones previas,
para que «una fila de bitácora» y «un comprobante» se cuenten limpios):

1. Encender la transferencia; comprobar `pagos.metodosDeCobro()` es
   `['transferencia']` y que `pagos.PROCESADORES` no tiene ninguna entrada de
   pasarela aparte de `demo` y `transferencia` (si la fase 6 ya añadió la suya,
   comprobar que está apagada). Sustituir `PROCESADORES.demo` por un espía que
   cuenta llamadas; restaurarlo en `finally`.
2. Crear cuenta, aceptar legales, `GET /api/planes` (trae `transferencia`),
   `POST /api/membresias` sin `metodo` → 202 con datos y referencia.
3. `POST /api/anuncios` → 402; `GET /api/membresias` → `pagosPendientes` con
   esa referencia y ninguna membresía; facturas de la organización: 0.
4. Admin: `GET /api/admin/pagos` incluye la referencia;
   `POST /api/admin/pagos/:id/recibido` con `cf-connecting-ip` fija → 200 con
   `comprobante.ncf`.
5. Ahora: una membresía con los cupos comprados; una factura del pago con NCF
   B02 (o B01 si se pidió RNC); `bitacora_admin` con exactamente una fila de
   esa organización, acción `pago.transferencia_recibida`, IP la de la cabecera;
   `pagosPendientes` vacío; correo del comprobante en `.tmp/correos`.
6. `POST /api/anuncios` → éxito; el anuncio aparece en el listado público sin
   sesión.
7. Espía de `demo`: 0 llamadas.

Al final, el resumen del arnés tiene que incluir la cuenta de comprobaciones.
  </action>
  <verify>
    <automated>npm run transferencia:probar</automated>
  </verify>
  <done>El recorrido completo pasa en el arnés y bloquea la fusión desde el paso de CI de 05-01.</done>
</task>

<task type="auto">
  <name>Tarea 2: batería completa y arranque local para la verificación</name>
  <files>(ninguno: solo ejecuta pruebas y levanta el servidor local)</files>
  <action>
Correr, en este orden, y anotar el resultado de cada uno en el SUMMARY:
`check:encoding`, `taxonomia`, `facturas:letras`, `seguridad:probar`,
`dealer:probar`, `bitacora:probar`, `facturas:probar`, `pagos:probar`,
`transferencia:probar`, `chat:probar`, `check:contraste`. Si alguno falla por
algo ajeno a la fase, encontrar la causa antes de tocar nada y anotarla.
Después preparar la verificación humana: una base desechable
(`MERCA_DB=.tmp/verificacion-05.db`), semilla de demostración, una cuenta
admin con `node tools/admin.js`, las cinco `MERCA_TRANSFERENCIA_*` con valores
claramente de prueba («BANCO DE PRUEBA», cuenta «000-000000-0») solo en el
entorno de ese proceso —nunca en `.env` ni en un archivo del repo—, y el
servidor levantado con `npm start` (o el script que use el repo). Además, con
la base de verificación, `npm run auditar` (auditorías con navegador).
  </action>
  <verify>
    <automated>npm run check:encoding &amp;&amp; npm run seguridad:probar &amp;&amp; npm run dealer:probar &amp;&amp; npm run bitacora:probar &amp;&amp; npm run facturas:probar &amp;&amp; npm run pagos:probar &amp;&amp; npm run transferencia:probar &amp;&amp; npm run check:contraste</automated>
  </verify>
  <done>Batería en verde y servidor local con la transferencia encendida con datos de prueba, listo para Victor.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Tarea 3: Victor recorre el cobro por transferencia en los dos temas</name>
  <files>(ninguno)</files>
  <action>Parar y pedir a Victor que siga los pasos de how-to-verify sobre el servidor local de la tarea 2. No continuar sin su respuesta.</action>
  <verify>Respuesta de Victor en el punto de control</verify>
  <done>Victor escribe «aprobado», o los fallos que describa quedan anotados y corregidos antes de cerrar la fase.</done>
  <what-built>Cobro por transferencia completo: opción en planes, datos y referencia, pago en espera en el panel, sección de pagos en la consola con marcado y anulación, bitácora.</what-built>
  <how-to-verify>
    1. Con una cuenta de anunciante sin cupos, abrir http://localhost:8080/planes.html, elegir Destacado y 1 cupo: el pedido dice «transferencia bancaria». Contratar: aparece el bloque con los datos DE PRUEBA, el importe y la referencia; «Copiar» copia la referencia.
    2. Ir al panel: «Pagos en espera de confirmación» con esa referencia, y ningún cupo nuevo. Intentar publicar: el sitio dice que no hay cupos.
    3. Con la cuenta admin, abrir admin.html: la sección «Pagos por transferencia» lista el pago. Marcar recibido con «Ref. banco 123»: sale el NCF, el pago desaparece de Pendientes y aparece en Recibidos; la bitácora muestra «Transferencia marcada como recibida» con su nombre y la hora.
    4. De vuelta en el panel del anunciante: los cupos ya están, el comprobante aparece en sus facturas, y puede publicar un equipo.
    5. Repetir 1-3 con otro pedido y «Anular» con un motivo: queda en Anulados sin NCF.
    6. Todo lo anterior en tema claro y en tema oscuro: legible, sin cajas que desaparezcan.
    7. En ninguna de esas pantallas ni correos (`.tmp/correos`) hay un número de teléfono ni WhatsApp.
    8. Parar el servidor, quitar las variables y arrancarlo otra vez: planes.html no menciona la transferencia y se comporta como antes de la fase.
  </how-to-verify>
  <resume-signal>Escriba «aprobado» o describa lo que falla</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| entorno de verificación → repositorio | Valores de prueba que no deben acabar commiteados |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-16 | Information disclosure | datos de verificación | mitigate | Solo en el entorno del proceso; `git status` limpio de `.env` y de `.tmp/` antes de cerrar |
| T-05-17 | Tampering | recorrido completo | mitigate | El arnés E2E corre en CI en cada fusión: una regresión en cualquier eslabón bloquea |
</threat_model>

<verification>
- `npm run transferencia:probar` incluye la sección de extremo a extremo y sale 0.
- La batería completa en verde, anotada en el SUMMARY.
- Aprobación de Victor en el punto de control.
</verification>

<success_criteria>
Los cinco criterios de la fase 5 demostrados: 1-2 en pantalla, 3-4 en consola y bitácora, 5 de extremo a extremo en el arnés y a mano.
</success_criteria>

<output>
Crear `.planning/phases/05-cobro-por-transferencia-bancaria/05-05-SUMMARY.md` con la batería, el resultado de la verificación y lo que Victor tiene que poner en `/etc/mercamaquinarias.env` (las cinco variables de D-01).
</output>

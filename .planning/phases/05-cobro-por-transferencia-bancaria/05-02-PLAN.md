---
phase: 05-cobro-por-transferencia-bancaria
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - tools/api.js
  - tools/correo.js
  - tools/probar-transferencia.js
  - tools/auditar-permisos.js
autonomous: true
requirements: [PAGO-09, ADMIN-06]

must_haves:
  truths:
    - "Con la transferencia encendida, `POST /api/membresias` con importe responde 202 con el pago `pendiente`, la referencia, el total y los datos de la cuenta; no hay membresía nueva, ni factura, ni NCF consumido."
    - "El comprador recibe por correo los datos para transferir, que dicen que no son un comprobante fiscal, y el buzón de facturación recibe el aviso de la transferencia en espera."
    - "`GET /api/membresias` devuelve `pagosPendientes` de SU organización; `GET /api/planes` dice qué métodos hay sin revelar la cuenta."
    - "`POST /api/admin/pagos/:id/recibido` pasa por `conAdminEnNombreDe('pago.transferencia_recibida')` y `pagos.confirmarPago`: en esa respuesta ya están los cupos y el comprobante con NCF, y la bitácora tiene la fila."
    - "Una ampliación cuya membresía ya no existe responde 409 al marcarla y solo se puede anular; anular deja `rechazado`, sin NCF, con fila de bitácora y correo al comprador."
    - "A quien no es administrador, las tres rutas de `/api/admin/pagos` le responden 404."
  artifacts:
    - path: "tools/api.js"
      provides: "compra/ampliación con procesadorDeCobro, listarPagosAdmin, marcarTransferenciaRecibida, anularTransferencia"
      contains: "conAdminEnNombreDe('pago.transferencia_recibida'"
    - path: "tools/correo.js"
      provides: "enviarDatosTransferencia, enviarTransferenciaAnulada"
      exports: ["enviarDatosTransferencia", "enviarTransferenciaAnulada"]
  key_links:
    - from: "tools/api.js marcarTransferenciaRecibida"
      to: "pagos.confirmarPago"
      via: "envolver → ctx.enNombreDe"
      pattern: "confirmarPago\\([^)]*envolver"
    - from: "tools/api.js comprarMembresia"
      to: "pagos.procesadorDeCobro"
      via: "procesador del cobro elegido por el servidor"
      pattern: "procesadorDeCobro\\("
    - from: "tools/probar-bitacora.js guarda sobre RUTAS"
      to: "las dos rutas POST nuevas"
      via: "propiedad bitacora del manejador"
      pattern: "bitacora"
---

<objective>
Las rutas: el comprador pide pagar por transferencia y recibe los datos y la
referencia; el personal lista, marca como recibida o anula desde
`/api/admin/pagos`, siempre por la bitácora y siempre con la transición única.

Purpose: criterios 1 a 4 de la fase del lado del servidor (PAGO-09, ADMIN-06),
y la decisión sobre la ampliación cuya membresía desapareció (pendiente
anotado por la fase 3).
Output: cambios en `tools/api.js` y `tools/correo.js`, secciones nuevas en
`tools/probar-transferencia.js`, tres comprobaciones en `auditar-permisos.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/05-cobro-por-transferencia-bancaria/05-01-PLAN.md
@.planning/phases/05-cobro-por-transferencia-bancaria/05-01-SUMMARY.md
@.planning/phases/03-el-pago-deja-de-darse-por-cobrado/03-03-SUMMARY.md
@.planning/phases/04-bandeja-de-solicitudes-y-bit-cora-de-la-consola/04-02-SUMMARY.md

Decisiones D-01 a D-14 en `05-01-PLAN.md` (`<decisiones_de_la_fase>`).

<interfaces>
De 05-01 (ver su SUMMARY para las firmas finales):
- `transferencia.datosTransferencia()` → `{ banco, titular, rnc, tipoCuenta, cuenta, moneda }` | null; `faltantes()`.
- `pagos.metodosDeCobro()`, `pagos.procesadorDeCobro(pedido)` (lanza `codigo: 400`).
- `pagos.confirmarPago(idPago, { envolver })` → `{ pago, membresia, comprobante, yaEstaba }`.
- `pagos.rechazarPago(idPago, { motivo })` → `{ pago, cambiado, motivo }`.
- `db.pagosPendientesDe(idOrg)`, `db.pagosParaConsola({ estado })`, acciones
  `pago.transferencia_recibida` y `pago.transferencia_anulada` en `db.ACCIONES_BITACORA`.

De `tools/api.js` (fases 3 y 4, en la rama):
- `comprarMembresia` / `ampliarMembresia` (`conSesion`): construyen
  `cobro = { ...precios..., referencia: referenciaCobro(), procesador: 'demo' }`
  (literal `'demo'` en las dos), `db.registrarCobro(...)`, `await pagos.cobrar(pago)`;
  pendiente → `responder(res, 202, { membresia, cobro, comprobante: null, pago: pagoPublico(r.pago), aviso: EN_PROCESO })`.
- Ayudantes: `pagoPublico`, `comprobantePublico`, `referenciaCobro()` (`TE-AAAA-XXXXXX`),
  `esExenta`, `SIN_COSTO`, `texto(v, max)`, `leerCuerpo`, `responder`, `fallo(res, codigo, mensaje, extra?)`.
- `misPlanes` (`GET /api/membresias`) responde `{ membresias, exenta }`.
- `conAdmin` (404, no 403) y `conAdminEnNombreDe(accion, manejador)` (real, commit 5a8876f):
  inyecta `ctx.enNombreDe(idOrganizacion, { objetoTipo, objetoId, motivo }, escribir)`
  que fija admin, acción e IP (`origen(req)`); lanza al cargar si la acción no está en el catálogo;
  marca `envuelto.bitacora = accion`.
- `ESCRITURAS_ADMIN_PROPIAS` (Set, justo antes de `const RUTAS = [`) y la guarda de
  `tools/probar-bitacora.js`: toda ruta no-GET cuyo patrón contenga `api\/admin`
  tiene `bitacora` o está en el Set, nunca las dos.
- `RUTAS`: `['GET', /^\/api\/admin\/facturas$/, listarFacturas]` etc.; lo específico antes de lo genérico.
- `module.exports = { manejar, ITBIS, RUTAS, ESCRITURAS_ADMIN_PROPIAS }`.

De `tools/correo.js`: `envoltura({ titulo, saludo, parrafos, extra, nota, accion, ... })`,
`enviar({ para, asunto, texto, html, responderA })`, `avisarInternamente({ buzon, asunto, texto, html, copia })`,
`BUZONES`, `EMPRESA`, `SITIO`. Modelo a copiar: `enviarResolucionDealer`.

`tools/auditar-permisos.js`: comprobaciones del estilo
`const rBit = await pedir('/admin/bitacora', { cookie: cibao })` esperando 404.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarea 1: el comprador pide pagar por transferencia</name>
  <files>tools/api.js, tools/correo.js, tools/probar-transferencia.js</files>
  <behavior>
    - Encendida: `POST /api/membresias { plan: 'destacado', cupo: 1, dias: 30 }` → 202; `pago.estado = 'pendiente'`; `transferencia` con los seis campos y `correo`; `cobro.referencia` = `pagos.referencia` de la fila; `procesador = 'transferencia'` en la base; membresías, facturas y B02 sin cambio; `pagos.PROCESADORES.demo` sustituido por un espía que nunca se llama.
    - Encendida: `{ metodo: 'demo' }` → 400 «Ese método de pago no está disponible.» y ninguna fila nueva en `pagos`.
    - Apagada: la compra sigue 201 con `demo` como en la fase 3, y `{ metodo: 'transferencia' }` → 400.
    - Ampliación encendida → 202 con los mismos datos; la membresía no cambia de cupo.
    - Importe cero (Estándar en promoción) → 201 al instante, sin transferencia, como hoy.
    - En `.tmp/correos` aparece un correo al comprador con la referencia, el importe y «no es un comprobante fiscal», y un aviso interno al buzón de facturación; ninguno contiene un número de teléfono ni «WhatsApp».
    - `GET /api/membresias` de otra organización no ve el pendiente ajeno; la propia lo ve con `transferencia`.
    - `GET /api/planes` trae `metodosPago` y no contiene la cuenta.
  </behavior>
  <action>
Arnés primero. El arnés necesita el `pedir()` de `probar-pagos.js` (req/res
falsos), una cuenta compradora con los documentos legales aceptados (copiar el
bloque de la sección 17 de `probar-pagos.js`) y otra cuenta ajena.

**`tools/api.js`**
1. En `comprarMembresia` y `ampliarMembresia`, el `procesador: 'demo'` literal
   pasa a `procesador: pagos.procesadorDeCobro(c.metodo)` (per D-03), dentro
   de un try/catch que responda `fallo(res, e.codigo || 400, e.message)`
   ANTES de `registrarCobro`: un método no disponible no deja pago. La rama
   exenta (`procesador: 'interna'`) no cambia.
2. Respuesta 202: además de lo que ya lleva, `transferencia` (D-10) cuando
   `pago.procesador === 'transferencia'` y un `aviso` propio: «Transfiera el
   importe con la referencia indicada. Los cupos y el comprobante fiscal
   llegan cuando confirmemos el ingreso.» (sin plazo prometido).
   `datosDeCuenta()` = `datosTransferencia()` + `correo: correo.BUZONES.facturacion`.
3. Tras el 202 por transferencia, sin `await`: `correo.enviarDatosTransferencia(...)`
   al `ctx.usuario.correo` y `correo.avisarInternamente({ buzon: 'facturacion', ... })`
   con referencia, organización, concepto e importe, cada uno con `.catch` que registre.
4. `misPlanes`: añadir `pagosPendientes: db.pagosPendientesDe(org.id)` y, si
   alguno es por transferencia y está encendida, `transferencia: datosDeCuenta()`.
   Si el pendiente existe pero la transferencia se apagó después, se lista sin
   datos y con el texto de que escriba a facturación (no se inventa cuenta).
5. La ruta de `GET /api/planes`: añadir `metodosPago: pagos.metodosDeCobro()`.
6. Al arrancar el módulo, si hay alguna `MERCA_TRANSFERENCIA_*` definida pero
   `faltantes()` no está vacío, un único `console.warn` con los NOMBRES que
   faltan (nunca valores): así un error de tecleo en el VPS no apaga el cobro
   en silencio.

**`tools/correo.js`**: `enviarDatosTransferencia({ para, nombre, referencia, total, concepto, datos })`
y `enviarTransferenciaAnulada({ para, nombre, referencia, motivo })` con
`envoltura`, al estilo de `enviarResolucionDealer`, `responderA: BUZONES.facturacion`.
El primero: datos de la cuenta en tabla, importe con `RD$`, la referencia
destacada y la instrucción de ponerla en el concepto; que envíe el comprobante
de la transferencia respondiendo a este correo o a facturación; que NO es un
comprobante fiscal y que el comprobante con NCF llega al confirmar; que la
membresía empieza a contar desde la confirmación (D-09). Sin teléfono. Exportar
las dos.
  </action>
  <verify>
    <automated>npm run transferencia:probar &amp;&amp; npm run pagos:probar &amp;&amp; npm run seguridad:probar</automated>
  </verify>
  <done>Los comportamientos de arriba pasan en el arnés; `pagos:probar` y `seguridad:probar` siguen en verde con la transferencia apagada, porque sus pruebas no definen las variables.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarea 2: la consola marca la transferencia como recibida o la anula</name>
  <files>tools/api.js, tools/probar-transferencia.js, tools/auditar-permisos.js</files>
  <behavior>
    - `GET /api/admin/pagos` (admin) → `{ pagos, estado }` con los pendientes por transferencia; `?estado=aprobado` trae `factura.ncf`.
    - `POST /api/admin/pagos/:id/recibido { motivo: 'Ref. banco 123' }` → 200 `{ pago, membresia, comprobante: { numero, tipo, ncf }, yaEstaba: false }`; en la base: pago aprobado, membresía con los cupos, una factura, B02 +1, fila `pago.transferencia_recibida` con la organización del pago, la IP de `CF-Connecting-IP`, `antes.estado = 'pendiente'`, `despues.estado = 'aprobado'` y el motivo.
    - Repetir → 200, `yaEstaba: true`, el mismo comprobante, B02 igual, una segunda fila.
    - Pago de otro procesador (`demo` pendiente) → 409 sin fila; id inexistente → 404 sin fila; pago rechazado → 409.
    - Ampliación pendiente cuya membresía se borró: listado con `membresiaViva: false`; recibido → 409 con el texto de D-07, pago pendiente, sin factura, sin fila.
    - `POST /api/admin/pagos/:id/anular` sin motivo → 400; con motivo → 200, pago `rechazado`, sin factura, fila `pago.transferencia_anulada`, correo al comprador; anular uno aprobado → 409.
    - Sin ser admin, las tres rutas → 404 (en `auditar-permisos.js`).
  </behavior>
  <action>
**`tools/api.js`**, junto a los manejadores de facturas de admin:

1. `listarPagosAdmin = conAdmin(...)`: `estado` de la consulta (lista cerrada
   de D-04/05-01), responde `{ estado, pagos: db.pagosParaConsola({ estado }) }`.
2. `marcarTransferenciaRecibida = conAdminEnNombreDe('pago.transferencia_recibida', async (req, res, ctx, idPago) => ...)`:
   lee `motivo = texto(c.motivo, 300)` (opcional: la referencia del banco).
   Antes de escribir: `db.pagoPorId`; 404 «Ese pago no existe»; procesador
   distinto de `transferencia` → 409 per D-06; estado `rechazado`/`devuelto`
   → 409; ampliación con membresía no viva → 409 con el texto de D-07. Luego
   `pagos.confirmarPago(idPago, { envolver: (aprobar) => ctx.enNombreDe(pago.organizacion_id, { objetoTipo: 'pago', objetoId: idPago, motivo }, () => { const r = aprobar(); return { antes: { estado: pago.estado }, despues: { estado: r.pago.estado, referencia: pago.referencia, total: pago.total, idSusc: r.membresia && r.membresia.id, yaEstaba: r.yaEstaba }, resultado: r }; }) })`.
   Nunca llamar a `db.aprobarPago` desde api.js (la prueba de la fase 3 lo
   vigila). try/catch: `e.codigo === 404` que venga de la ampliación → el 409
   de D-07; otro → `fallo(res, e.codigo || 500, e.message)`. Responde
   `{ pago: pagoPublico, membresia, comprobante: comprobantePublico(...), yaEstaba }`.
   Si `comprobante` es null con importe (emisión fallida), responder igual 200
   con `aviso` de que el pago quedó aprobado y el comprobante sale en «pendientes»
   de Facturas; repetir «recibido» lo emite (recuperación de la fase 3).
3. `anularTransferencia = conAdminEnNombreDe('pago.transferencia_anulada', ...)`:
   motivo obligatorio (≥ 5 caracteres, 400 si no); 404/409 como arriba
   (solo `pendiente` y solo `transferencia`); dentro de `ctx.enNombreDe(...)`
   llama `pagos.rechazarPago(idPago, { motivo })` y, si `cambiado` es false,
   lanza 409 para que no quede fila. Después, sin esperar,
   `correo.enviarTransferenciaAnulada(...)` al `correoCliente` de la intención.
   Per D-08.
4. `RUTAS`, antes de cualquier patrón más genérico de `/api/admin/`:
   `['GET', /^\/api\/admin\/pagos$/, listarPagosAdmin]`,
   `['POST', /^\/api\/admin\/pagos\/([\w-]+)\/recibido$/, marcarTransferenciaRecibida]`,
   `['POST', /^\/api\/admin\/pagos\/([\w-]+)\/anular$/, anularTransferencia]`.
   No se añaden a `ESCRITURAS_ADMIN_PROPIAS`: son escrituras en nombre de otro.

**`tools/auditar-permisos.js`**: tres comprobaciones con la cookie del no
administrador, esperando 404, en el estilo de las de la bitácora.

**Arnés**: cuenta admin con `db.marcarAdmin(correo, true)`; las comprobaciones
de la bitácora leen `bitacora_admin` directamente. Incluir que la guarda de
`probar-bitacora.js` sigue en verde con las dos rutas nuevas.
  </action>
  <verify>
    <automated>npm run transferencia:probar &amp;&amp; npm run bitacora:probar &amp;&amp; npm run pagos:probar &amp;&amp; npm run seguridad:probar &amp;&amp; npm run facturas:probar</automated>
  </verify>
  <done>Marcar recibido otorga cupos y emite el comprobante en la misma respuesta y deja la fila; la ampliación huérfana solo se puede anular; toda la batería de servidor sigue en verde. `auditar-permisos.js` queda con las tres comprobaciones (corre con el servidor en el CI).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → POST /api/membresias | El cliente puede mandar cualquier `metodo` |
| consola → /api/admin/pagos | Una persona otorga cupos y consume NCF en nombre de otra organización |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | Elevation of privilege | comprarMembresia / ampliarMembresia | mitigate | `procesadorDeCobro(c.metodo)` valida contra la lista del servidor; `demo` fuera con la transferencia encendida; probado con espía |
| T-05-07 | Elevation of privilege | /api/admin/pagos/* | mitigate | `conAdmin`/`conAdminEnNombreDe` (404 a no admin), comprobado en `auditar-permisos.js` |
| T-05-08 | Repudiation | marcado y anulación | mitigate | `conAdminEnNombreDe` fija admin, acción e IP; la fila cae en la misma transacción que la escritura |
| T-05-09 | Information disclosure | datos bancarios, pagos pendientes | mitigate | Solo en respuestas con sesión y filtradas por `ctx.organizacion.id`; `/api/planes` expone ids de método, no la cuenta |
| T-05-10 | Tampering | doble clic / dos administradores | mitigate | `aprobarPago` idempotente con UPDATE condicionado; la segunda pulsación devuelve el mismo comprobante |
| T-05-11 | Spoofing | marcar recibido sin que el dinero esté en el banco | accept | Es un juicio humano por diseño (PAGO-09); queda en la bitácora con motivo (referencia del banco) y se corrige con nota de crédito B04 |
</threat_model>

<verification>
- `npm run transferencia:probar`, `bitacora:probar`, `pagos:probar`, `seguridad:probar`, `facturas:probar`, `dealer:probar`, `chat:probar` en verde.
- `grep -n "db.aprobarPago" tools/api.js` no devuelve nada.
- `grep -n "procesador: 'demo'" tools/api.js` no devuelve nada.
</verification>

<success_criteria>
Criterios 1 a 4 de la fase cumplidos del lado del servidor y probados en el arnés; la ampliación huérfana tiene una salida definida que no emite nada.
</success_criteria>

<output>
Crear `.planning/phases/05-cobro-por-transferencia-bancaria/05-02-SUMMARY.md` con la forma exacta de las respuestas (202 de compra y ampliación, `GET /api/membresias`, las tres rutas de admin) para 05-03 y 05-04.
</output>

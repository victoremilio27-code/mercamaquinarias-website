---
phase: 01-barrera-de-pruebas-en-la-fusi-n
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - CLAUDE.md
autonomous: false
requirements: [CI-01, CI-02]

must_haves:
  truths:
    - "`CLAUDE.md` está en la raíz con las reglas fiscales, de dependencias y de despliegue, y ya no afirma que no hay barrera de pruebas."
    - "`CLAUDE.md` dice qué comprobaciones bloquean la fusión y qué hacer cuando una se pone en rojo."
    - "Fusionar a `main` con todo en verde despliega a producción igual que hoy, sin ningún paso manual nuevo."
    - "El sitio en producción responde después de ese despliegue."
  artifacts:
    - path: "CLAUDE.md"
      provides: "Reglas del repositorio, con la sección de despliegue al día"
      contains: "Barrera de pruebas"
  key_links:
    - from: "CLAUDE.md"
      to: ".github/workflows/desplegar.yml"
      via: "sección de despliegue que nombra las dos comprobaciones obligatorias"
      pattern: "pruebas|navegador"
---

<objective>
Dejar `CLAUDE.md` diciendo la verdad sobre el despliegue —hoy afirma que no hay
barrera de pruebas, y a partir del plan `01-01` eso es falso— y comprobar con
una fusión de verdad que el camino a producción sigue siendo el de siempre.

Purpose: cierra CI-02 verificándolo (no reescribiéndolo) y cierra CI-01
demostrando el criterio que queda: que en verde se despliega igual que hoy.
Output: `CLAUDE.md` al día y un despliegue a producción observado de punta a
punta detrás de las pruebas.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.github/workflows/desplegar.yml
@.planning/phases/01-barrera-de-pruebas-en-la-fusi-n/01-01-SUMMARY.md
@.planning/phases/01-barrera-de-pruebas-en-la-fusi-n/01-02-SUMMARY.md
</context>

<nota_de_alcance>
La restricción de la fase es no tocar código de la aplicación, y no se toca:
el único archivo que se modifica aquí es `CLAUDE.md`, que es documentación y es
además el objeto de CI-02. Se edita porque su sección de despliegue quedó
**falsa** al poner la barrera, y una regla escrita que ya no es cierta es peor
que no tenerla: la siguiente persona que la lea tomará una decisión con datos
viejos.

CI-02 se da por **satisfecho** desde el commit `bb97a3d` del 2026-09-25. Este
plan lo **verifica**, no lo reescribe: la tarea 1 comprueba que las tres
familias de reglas que pide el criterio 4 de la fase están presentes, y solo
corrige la parte que la Fase 1 acaba de dejar obsoleta.
</nota_de_alcance>

<tasks>

<task type="auto">
  <name>Tarea 1: poner al día la sección de despliegue de CLAUDE.md y verificar CI-02</name>
  <files>CLAUDE.md</files>
  <action>
Primero verificar, después editar lo mínimo.

**Verificar CI-02** (criterio 4 de la fase): comprobar que `CLAUDE.md` está en
la raíz y que contiene las tres familias de reglas que el criterio nombra, y
anotar en el SUMMARY la línea o el encabezado donde vive cada una:
- **Fiscales**: la sección «Fiscal (DGII) — irreversible» con el comprobante
  que no se borra ni se reescribe, las secuencias de NCF con tope y
  vencimiento, y el ITBIS al 18 %.
- **Dependencias**: «Cero dependencias en tiempo de ejecución» y que la única
  dependencia de desarrollo es puppeteer.
- **Despliegue**: la sección «Despliegue — cuidado».
Si alguna faltara, decirlo y parar: sería un requisito dado por hecho que no lo
está.

**Editar la sección «Despliegue — cuidado»**, que es la única parte falsa.
Cambios, sin reescribir el resto del archivo y conservando su estilo —viñetas
cortas, negrita en lo que cuesta caro, y comentarios que explican qué falló
antes y por qué el código es así—:

- Sustituir la frase «No hay barrera de pruebas en CI todavía» por la
  descripción de lo que ahora hay: fusionar a `main` sigue desplegando
  automáticamente, pero el despliegue va **detrás** de las pruebas, y el job de
  despliegue de `.github/workflows/desplegar.yml` lleva `needs` de los dos jobs
  de pruebas, así que con una en rojo no arranca.
- Nombrar las dos comprobaciones obligatorias de `main`, **`pruebas` y
  `navegador`**, y decir que son un ajuste de la protección de rama en GitHub:
  quien clone el repositorio no las hereda del código.
- Decir qué hacer cuando una se pone en rojo: leer el paso que falló en la
  pestaña Actions del propio PR —ahí está la salida del script— y, si el que
  falló fue el job `navegador`, que el registro del servidor queda como
  artefacto `registro-del-servidor` de esa pasada. Nada de entrar al VPS a
  mirar.
- Dejar escrito lo que la barrera **no** cubre, que es lo que de verdad ahorra
  un malentendido: `tools/auditar-publico.js` y `tools/auditar-flujos.js`
  corren en CI e imprimen sus hallazgos, pero **salen 0 pase lo que pase**, así
  que sus hallazgos no suspenden la fusión. Hay que leerlos. Y
  `npm run correo:probar` no está en CI a propósito porque manda correo de
  verdad y exige una dirección de destino.
- Mantener intactas las dos reglas que ya estaban y siguen valiendo: rama y
  Pull Request siempre, nunca empujón directo a `main`; y nunca reescribir
  historial ni usar `--force` porque hay otra persona en el repositorio.

En la sección «Pruebas», añadir una línea al pie que diga qué corre la barrera
y dónde está escrito (`.github/workflows/desplegar.yml`), sin duplicar la lista
de scripts que ya está ahí arriba.

Respetar los saltos de línea del archivo: el repositorio es CRLF
(`core.autocrlf=true`). No convertir el archivo entero a LF al guardarlo.
  </action>
  <verify>
    <automated>test -f CLAUDE.md &amp;&amp; node -e "const s=require('fs').readFileSync('CLAUDE.md','utf8'); const fallos=[]; if(s.includes('No hay barrera de pruebas en CI todav')) fallos.push('sigue diciendo que no hay barrera'); for (const t of ['Fiscal (DGII)','Cero dependencias en tiempo de ejecuci','Despliegue','pruebas','navegador','registro-del-servidor','correo:probar','auditar-publico','nunca push directo','B04']) if(!s.includes(t)) fallos.push('falta: '+t); if(fallos.length){console.error(fallos.join(' | '));process.exit(1);} console.log('CLAUDE.md al dia y CI-02 verificado')"</automated>
  </verify>
  <done>`CLAUDE.md` ya no afirma que no hay barrera, nombra `pruebas` y `navegador`, dice dónde leer un fallo y qué no cubre la barrera, y conserva las reglas fiscales, de dependencias y de no reescribir historial. El SUMMARY anota dónde vive cada familia de reglas de CI-02.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Tarea 2: fusionar en verde y ver el despliegue pasar por la barrera</name>
  <what-built>
  <action>
Claude no ejecuta nada en esta tarea. Presenta las instrucciones de abajo,
espera la respuesta de Victor, y copia al SUMMARY el tiempo total de la
pasada sobre main y el resultado de cada job.
  </action>
Claude abrió el Pull Request con la actualización de `CLAUDE.md`. Ese PR es, a
propósito, la prueba del último criterio que falta: una fusión normal, en
verde, con la protección de rama ya puesta. Las dos comprobaciones `pruebas` y
`navegador` están en verde en el PR.
  </what-built>
  <how-to-verify>
1. Abrir el Pull Request. Comprobar que al pie se ven **`pruebas`** y
   **`navegador`** en verde y que el botón de fusionar está habilitado.
2. Pulsar **Merge pull request**. No hace falta nada más: no hay ningún paso
   manual nuevo respecto a antes de esta fase.
3. Ir a la pestaña **Actions** y abrir la pasada que arranca sobre `main`.
   Comprobar el orden, que es lo que demuestra que la barrera está delante:
   - `pruebas` y `navegador` corren primero, en paralelo, y terminan en verde.
   - **Después** arranca `desplegar` y termina en verde.
4. Abrir https://mercamaquinarias.com y comprobar que el sitio responde y se ve
   normal. Si no respondiera, el script del servidor ya habrá revertido solo a
   la versión anterior: en ese caso el sitio sigue en pie con lo de antes, y lo
   que hay que mirar es el registro del job `desplegar`.
5. Mirar cuánto tardó la pasada completa de punta a punta. Es lo que van a
   tardar los despliegues a partir de ahora: unos minutos más que antes,
   porque antes no se probaba nada.
  </how-to-verify>
  <resume-signal>Escribe "aprobado" si el despliegue pasó detrás de las pruebas y el sitio responde, o pega el fallo que veas</resume-signal>
</task>

</tasks>

<threat_model>
## Fronteras de confianza

| Frontera | Descripción |
|---|---|
| Runner de Actions → droplet de producción | La fusión de este PR provoca un despliegue real; es el único paso de la fase que toca producción. |

## Registro STRIDE

| ID | Categoría | Componente | Disposición | Mitigación |
|---|---|---|---|---|
| T-01-11 | Denial of service | sitio en producción | transfer | El PR solo cambia `CLAUDE.md`, que no se sirve ni se ejecuta. Y el riesgo residual ya está cubierto por el script del servidor, que comprueba salud y revierte solo. |
| T-01-12 | Information disclosure | `CLAUDE.md` en el repositorio | accept | Lo que se añade son nombres de jobs y rutas de archivos del propio repositorio. Ninguna clave, ningún dominio interno, ningún teléfono. |
| T-01-13 | Repudiation | quién cambió las reglas del repositorio | mitigate | La edición va por Pull Request con la barrera activa, así que queda con autor, fecha y revisión posible, en vez de aparecer en un empujón directo. |
</threat_model>

<verification>
1. `CLAUDE.md` existe en la raíz y pasa la comprobación automática de la tarea 1.
2. La pasada sobre `main` muestra `pruebas` y `navegador` en verde **antes** de `desplegar`.
3. `desplegar` termina en verde y https://mercamaquinarias.com responde.
4. No hubo ningún paso manual además de pulsar el botón de fusionar.
</verification>

<success_criteria>
- `CLAUDE.md` está en la raíz con las reglas fiscales, de dependencias y de despliegue, y la de despliegue es cierta (criterio 4 de la fase).
- Fusionar a `main` con todo en verde despliega igual que hoy, sin paso manual nuevo (criterio 2 de la fase).
- El despliegue ocurrió después de las pruebas y no podría haber ocurrido sin ellas.
</success_criteria>

<output>
Crear `.planning/phases/01-barrera-de-pruebas-en-la-fusi-n/01-03-SUMMARY.md` al terminar,
con la duración de la pasada completa sobre `main` y la confirmación de que el
sitio responde. Anotar también, para la fase siguiente, la recomendación
pendiente: dar código de salida a `tools/auditar-publico.js` y
`tools/auditar-flujos.js` una vez medido su recuento limpio de hallazgos.
</output>

---
phase: 09-contactos-verificados-y-se-ales-de-estafa
plan: 03
type: execute
wave: 3
depends_on: ["09-02"]
files_modified:
  - assets/contactos.js
  - panel.html
  - assets/panel.js
  - publicar.html
  - assets/publicar.js
  - styles.css
autonomous: true
requirements: [CONF-03]
user_setup: []

must_haves:
  truths:
    - "El panel tiene una sección «Teléfonos de contacto» con cada número de la organización, si está verificado (y por qué vía) o no, y en cuántos anuncios va."
    - "Desde esa sección el vendedor pide el código por correo, lo escribe y el número queda verificado sin recargar la página."
    - "El botón «por SMS» solo aparece cuando la API dice que el SMS está encendido."
    - "En el paso 4 de publicar, con sesión, cada número válido dice si está verificado y ofrece verificarlo ahí; sin verificar se avisa que no se mostrará."
    - "Sin sesión el paso 4 explica que los números se verifican al entrar y no bloquea."
  artifacts:
    - path: "assets/contactos.js"
      provides: "VerificarContacto: estado de un número y formulario del código, compartido por panel y publicar"
    - path: "assets/panel.js"
      provides: "montarContactos"
    - path: "assets/publicar.js"
      provides: "estado de verificación por fila en el paso 4"
---

<objective>
La interfaz del vendedor para verificar sus números.
Output: `assets/contactos.js` (nuevo, cargado tras `app.js` en `panel.html` y `publicar.html`), sección del panel,
estado por fila en el paso 4, y los pocos estilos que falten con los tokens de tema existentes.
</objective>

<context>
@CLAUDE.md
@.planning/phases/09-contactos-verificados-y-se-ales-de-estafa/09-CONTEXT.md
@.planning/phases/09-contactos-verificados-y-se-ales-de-estafa/09-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Tarea 1: módulo assets/contactos.js</name>
  <files>assets/contactos.js</files>
  <action>
  Módulo de navegador (no compartido con node). `VerificarContacto.estadoHTML(contacto, sms)`, y
  `VerificarContacto.montar(contenedor, { alVerificar })` con delegación de eventos sobre `[data-verificar]`
  (pide el código), `[data-confirmar]` (form del código). Usa `api()` en su propio `try/catch` (lanza en no-ok),
  `esc`, `icono`. Mensajes de error en `role="alert"`.
  </action>
  <verify>`node --check assets/contactos.js`; auditoría de flujos sin errores de consola.</verify>
  <done>El módulo existe y no rompe páginas que no lo usan.</done>
</task>

<task type="auto">
  <name>Tarea 2: panel y paso 4</name>
  <files>panel.html, assets/panel.js, publicar.html, assets/publicar.js, styles.css</files>
  <action>
  - `panel.html`: `<section class="panel" id="panelContactos" hidden>` antes de sucursales; `<script src="assets/contactos.js">` tras `app.js`.
  - `panel.js`: `montarContactos()` pide `GET /api/contactos`, pinta y remonta al verificar.
  - `publicar.html`: script tras `app.js`; nota bajo «Teléfonos».
  - `publicar.js`: con sesión, carga los contactos una vez y pinta bajo cada fila válida su estado; se refresca al escribir.
  - `styles.css`: solo lo imprescindible, con variables de tema (sin colores sueltos) para que el contraste siga en 0.
  </action>
  <verify>`npm run auditar` (flujos y contraste) contra servidor propio; `npm run check`.</verify>
  <done>El vendedor puede verificar desde el panel y desde publicar.</done>
</task>

</tasks>

<output>
`.planning/phases/09-contactos-verificados-y-se-ales-de-estafa/09-03-SUMMARY.md`
</output>

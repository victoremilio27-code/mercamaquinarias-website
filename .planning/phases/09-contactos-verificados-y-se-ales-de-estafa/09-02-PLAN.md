---
phase: 09-contactos-verificados-y-se-ales-de-estafa
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - tools/api.js
  - tools/seed.js
  - tools/probar-contactos.js
  - tools/auditar-permisos.js
autonomous: true
requirements: [CONF-03]
user_setup: []

must_haves:
  truths:
    - "`GET /api/anuncios/:id` sin sesión o con la sesión de otra organización no devuelve ningún teléfono sin verificar."
    - "El dueño recibe todos sus teléfonos, cada uno con `verificado` y `via`."
    - "`POST /api/contactos/codigo` con `via:'correo'` manda el código al correo de la cuenta; con `via:'sms'` y SMS apagado responde 400 sin enviar nada."
    - "`POST /api/contactos/confirmar` verifica el número y desde ese momento aparece en la ficha pública de todos los anuncios de la organización que lo llevan."
    - "Con `MERCA_SMS=archivo` las mismas dos rutas verifican por SMS sin ningún otro cambio."
    - "`GET /api/contactos` lista los números de la organización y dice si el SMS está encendido."
    - "Ninguna ruta llama a `marcarContactoVerificado`."
    - "La semilla deja verificados los números de la demostración."
  artifacts:
    - path: "tools/api.js"
      provides: "listarContactos, pedirCodigoContacto, confirmarContacto, filtro en verAnuncio"
      contains: "api\\/contactos"
    - path: "tools/seed.js"
      provides: "marcarContactoVerificado para los anuncios sembrados"
    - path: "tools/probar-contactos.js"
      provides: "Bloque «La API»"
  key_links:
    - from: "tools/api.js verAnuncio"
      to: "telefono.verificado"
      via: "filtro para quien no es el dueño"
      pattern: "verificado"
---

<objective>
Rutas de verificación y filtro público en la ficha.
Output: tres rutas nuevas en `RUTAS`, `verAnuncio` filtrado, semilla verificada, bloque de API en la prueba
y las rutas nuevas en `auditar-permisos.js` (sin sesión → 401).
</objective>

<context>
@CLAUDE.md
@.planning/phases/09-contactos-verificados-y-se-ales-de-estafa/09-CONTEXT.md
@.planning/phases/09-contactos-verificados-y-se-ales-de-estafa/09-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Tarea 1: rutas y filtro en tools/api.js</name>
  <files>tools/api.js</files>
  <action>
  - `listarContactos` (conSesion): `{ contactos: db.contactosDe(org), sms: correo.smsActivo() }`. Sin organización → 403.
  - `pedirCodigoContacto` (conSesion): número de 10 dígitos o 400; vía `correo` o `sms`; SMS apagado → 400
    «La verificación por SMS todavía no está disponible. Verifique el número por correo.»; límites D-09 con
    `db.permitir` (429); ya verificado → 200 `{ yaVerificado:true }`; correo → `enviarCodigoContacto` al
    `ctx.usuario.correo`; SMS → `enviarSms` al número. Responde `{ enviado, via, destino }` con el destino enmascarado.
  - `confirmarContacto` (conSesion): límite por IP; mensajes como `verificar`; responde `{ contacto }`.
  - `verAnuncio`: si no es el dueño, `telefonos` = solo verificados, con `numero, tipo, nota, via`.
  - Rutas `GET /api/contactos`, `POST /api/contactos/codigo`, `POST /api/contactos/confirmar`.
  </action>
  <verify>Bloque «La API» de `tools/probar-contactos.js`; `npm run seguridad:probar` y `dealer:probar` sin cambios.</verify>
  <done>Criterios 1, 2 y 3 demostrados por la prueba.</done>
</task>

<task type="auto">
  <name>Tarea 2: semilla y permisos</name>
  <files>tools/seed.js, tools/auditar-permisos.js</files>
  <action>
  - En la semilla, tras crear cada anuncio, `db.marcarContactoVerificado(org.id, numero, 'correo', null)`.
  - En `auditar-permisos.js`, las tres rutas sin sesión responden 401.
  </action>
  <verify>`npm run db:demo` sobre una base temporal y `auditar` contra un servidor propio.</verify>
  <done>La ficha de la demo sigue teniendo `tel:`.</done>
</task>

</tasks>

<output>
`.planning/phases/09-contactos-verificados-y-se-ales-de-estafa/09-02-SUMMARY.md`
</output>

---
phase: 10-alcance-y-m-tricas-del-vendedor
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - assets/app.js
  - equipo.html
  - guardados.html
  - styles.css
  - tools/check-links.js
  - tools/auditar-publico.js
  - tools/check-contraste.js
autonomous: true
requirements: [MET-01, MET-02]
user_setup: []

must_haves:
  truths:
    - "En la ficha, «Guardar» guarda el anuncio en este navegador, cambia a «Guardado» con `aria-pressed=\"true\"` y anota un evento `favorito`; pulsarlo otra vez lo quita sin anotar nada."
    - "`guardados.html` lista los equipos guardados con su tarjeta de catálogo, deja quitarlos uno a uno, avisa de los que ya no están publicados y, sin ninguno, ofrece ir al catálogo."
    - "Con al menos un guardado, la navegación principal de cualquier página muestra «Guardados (N)» enlazando a `guardados.html`."
    - "«Compartir» usa la hoja nativa si existe y, si no, abre un menú con WhatsApp (`https://wa.me/?text=` con nombre, precio y la URL de la ficha) y «Copiar enlace»; completar cualquiera anota un evento `compartir`."
    - "`guardados.html` pasa `check-links`, `auditar-publico` y `check-contraste` en los dos temas."
  artifacts:
    - path: "assets/app.js"
      provides: "GUARDADOS (leer, esta, alternar, quitar), montarEnlaceGuardados, montarGuardados, acciones de la ficha"
      contains: "mercamaquinarias:guardados"
    - path: "guardados.html"
      provides: "Página de equipos guardados"
      contains: "listaGuardados"
  key_links:
    - from: "assets/app.js (montarGuardados)"
      to: "GET /api/anuncios?ids="
      via: "api()"
      pattern: "ids="
    - from: "assets/app.js (montarDetalle)"
      to: "anotar(e.id, 'favorito') y anotar(e.id, 'compartir')"
      via: "clic en #btnGuardar y menú de compartir"
      pattern: "'compartir'"
---

<objective>
Las dos acciones del comprador en la ficha —guardar y compartir— y el sitio donde
vuelve a encontrar lo guardado.

Purpose: MET-01 y MET-02. Son los emisores que faltaban de `favorito` y `compartir`.
Output: acciones en `montarDetalle`, módulo `GUARDADOS`, `guardados.html`, estilos
mínimos con fichas del tema y la página nueva dada de alta en las tres auditorías.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/10-alcance-y-m-tricas-del-vendedor/10-CONTEXT.md
@.planning/phases/10-alcance-y-m-tricas-del-vendedor/10-UI-SPEC.md
@.planning/phases/10-alcance-y-m-tricas-del-vendedor/10-01-SUMMARY.md

<interfaces>
- `GET /api/anuncios?ids=a,b,c` → `{ anuncios, total, ... }`, solo activos (10-01).
- `assets/app.js`: `SPRITE` (añadir `i-compartir`), `icono()`, `esc()`, `$`, `$$`,
  `anuncioDeApi(a)`, `avisoHTML(e)`, `nombreEquipo(e)`, `precioTexto(e)`, `anotar()`,
  `montarDetalle()`, `montarNav()` y el arranque por `DOMContentLoaded` al final.
- `api()` de `assets/sesion.js` LANZA si la respuesta no es ok: envolver en try/catch.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarea 1: Guardar y compartir en la ficha</name>
  <files>assets/app.js, styles.css</files>
  <read_first>
    - assets/app.js (SPRITE, montarDetalle, contactosHTML, anotar, montarNav, arranque final)
    - assets/sesion.js (api)
    - styles.css (bloques .detalle__*, .btn, .btn--linea, .btn--chico, tokens del tema)
  </read_first>
  <action>
    Añadir al `SPRITE` el símbolo `i-compartir` (tres círculos unidos por dos líneas, trazo
    como los demás).
    Módulo `GUARDADOS` en `assets/app.js` con clave `mercamaquinarias:guardados`: `leer()`
    (lista de `{ id, guardado }`, valida la forma, `[]` si falla), `esta(id)`,
    `alternar(id)` → true si quedó guardado (añade al principio, tope 100), `quitar(ids)`,
    `cuantos()`. Todo acceso a `localStorage` en try/catch con copia en memoria si no hay
    almacenamiento. Comentario: por qué en el navegador y no en la cuenta (D-01).
    `montarEnlaceGuardados()`: si `cuantos() > 0` inserta o actualiza en `#navMenu`, antes
    de `.cab__nav-cta`, `<a href="guardados.html" data-guardados>Guardados (N)</a>`; si es
    0 lo quita. Se llama al arrancar y tras cada cambio.
    En `montarDetalle`, tras `condicionesHTML(e)`, la fila `.detalle__acciones` del
    UI-SPEC: `#btnGuardar`, `#btnCompartir`, `#enlaceGuardados` y `#menuCompartir` oculto
    con el enlace de WhatsApp (`https://wa.me/?text=` + `encodeURIComponent` de
    `<nombre> · <precio> en MercaMaquinarias: <url>`, `target="_blank"`, `rel="noopener"`)
    y `#btnCopiarEnlace`, más `<p role="status" class="detalle__estado">`. URL de la ficha:
    `${location.origin}/equipo.html?id=${encodeURIComponent(e.id)}`.
    Comportamiento: Guardar → `GUARDADOS.alternar(e.id)`; si quedó guardado,
    `anotar(e.id, 'favorito')`; actualiza texto, `aria-pressed`, clase `is-activo`, el
    enlace «Ver guardados (N)» y la navegación. Compartir → si existe `navigator.share`,
    `navigator.share({ title, text, url })` y `anotar(e.id, 'compartir')` al resolverse
    (un rechazo, incluido `AbortError`, no anota); si no, alterna `#menuCompartir` y
    `aria-expanded`. Clic en WhatsApp → `anotar(e.id, 'compartir')`. Copiar →
    `navigator.clipboard.writeText(url)` con respaldo de `document.execCommand('copy')`
    sobre un input temporal; si copia, anota y muestra «Enlace copiado» 2 s.
    `styles.css`: `.detalle__acciones` (flex, gap, wrap, margen), `.detalle__compartir`
    (fila de dos acciones), `#btnGuardar.is-activo .ico` relleno `currentColor`; solo
    tokens existentes (`var(--...)`), nada de colores literales.
  </action>
  <verify>
    <automated>node -e "require('vm'); new (require('vm').Script)(require('fs').readFileSync('assets/app.js','utf8'))" && npm run seguridad:probar</automated>
  </verify>
  <acceptance_criteria>
    - `assets/app.js` contiene `mercamaquinarias:guardados`, `id="i-compartir"`, `anotar(e.id, 'favorito')` y `anotar(e.id, 'compartir')`.
    - `assets/app.js` contiene `wa.me/?text=` y `navigator.share`.
    - Todo `localStorage` nuevo de `assets/app.js` está dentro de `try`.
    - `styles.css` no gana ningún color hexadecimal nuevo en los bloques añadidos.
  </acceptance_criteria>
  <done>La ficha emite `favorito` y `compartir` desde botones reales.</done>
</task>

<task type="auto">
  <name>Tarea 2: La página guardados.html y su alta en las auditorías</name>
  <files>guardados.html, assets/app.js, styles.css, tools/check-links.js, tools/auditar-publico.js, tools/check-contraste.js</files>
  <read_first>
    - equipos.html (cabecera, pie, orden de scripts)
    - assets/app.js (montarResultados como referencia de rejilla, arranque final)
    - tools/check-links.js, tools/auditar-publico.js, tools/check-contraste.js (listas de páginas)
    - tools/serve.js (si hay lista de páginas o PRIVADAS que afecte a una página nueva)
  </read_first>
  <action>
    Crear `guardados.html` copiando cabecera, metadatos OG genéricos, pie y el orden exacto
    de `<script>` de `equipos.html`; `<title>Equipos guardados · MercaMaquinarias</title>`,
    `<meta name="robots" content="noindex">` (es una lista personal). Contenido del
    UI-SPEC: título, texto, `#guardadosRetirados` oculto, `ul.rejilla#listaGuardados`,
    `#guardadosVacio` oculto.
    `montarGuardados()` en `assets/app.js` (sale si no hay `#listaGuardados`): lee ids; sin
    ids muestra el vacío; con ids pide `/anuncios?ids=<ids unidos por coma>&porPagina=60`
    en try/catch (si falla, mensaje «No se pudo cargar la lista. Vuelva a intentarlo.»),
    pinta en el orden de guardado cada `anuncioDeApi(a)` como `<li class="guardado">` con
    `avisoHTML(e)` sin su `<li>` exterior o envuelto de forma que el botón «Quitar» quede
    FUERA del `<a>` de la tarjeta; los ids que no volvieron cuentan como retirados y se
    ofrecen quitar. Quitar repinta y actualiza la navegación.
    Añadir `guardados.html` a `PAGINAS` de `tools/check-links.js` (con su contenedor
    `#listaGuardados` si esa tabla existe para las demás) y a las listas de
    `tools/auditar-publico.js` y `tools/check-contraste.js` con el rótulo «Guardados».
    Estilos mínimos para `.guardado` (columna con la tarjeta y el botón debajo) con tokens.
  </action>
  <verify>
    <automated>npm run seguridad:probar && npm run metricas:probar</automated>
  </verify>
  <acceptance_criteria>
    - Existe `guardados.html` con `id="listaGuardados"` y el mismo orden de scripts que `equipos.html`.
    - `tools/check-links.js`, `tools/auditar-publico.js` y `tools/check-contraste.js` contienen `guardados.html`.
    - `assets/app.js` contiene `montarGuardados` y la llamada con `ids=`.
    - Con el servidor de demo, `npm run check` y `node tools/check-contraste.js` salen 0 (se corre en el plan 10-04 junto a la auditoría completa).
  </acceptance_criteria>
  <done>El comprador vuelve a encontrar lo que guardó, y la página nueva está bajo las mismas barreras que las demás.</done>
</task>

</tasks>

<threat_model>
| Id | Amenaza | Mitigación |
|----|---------|------------|
| T-10-04 | XSS por datos del anuncio o por un id manipulado en `localStorage` | Todo va por `esc()`/`avisoHTML`; los ids se validan con `^[\w-]{1,64}$` al leer y el servidor los vuelve a validar |
| T-10-06 | Enlace de compartir que abre otra ventana con acceso a la original | `rel="noopener"` en el enlace de WhatsApp |
</threat_model>

<verification>
Arnés en verde; el recorrido de navegador del plan 10-04 cubre el comportamiento.
</verification>

<success_criteria>
Criterios 1 (lado comprador) y 2 del ROADMAP alcanzables desde la ficha.
</success_criteria>

<output>
Crear `.planning/phases/10-alcance-y-m-tricas-del-vendedor/10-02-SUMMARY.md`.
</output>

---
phase: 10-alcance-y-m-tricas-del-vendedor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/db.js
  - tools/api.js
  - tools/meta.js
  - tools/probar-metricas.js
  - package.json
  - .github/workflows/desplegar.yml
autonomous: true
requirements: [MET-01, MET-02, MET-03, MET-04]
user_setup: []

must_haves:
  truths:
    - "Un evento `whatsapp` o `telefono` que `anotarEvento` devuelve como 'contado' deja una fila en `contactos_anuncio` con anuncio, organización, canal, día y hora, sin huella del visitante; uno 'repetido' no deja ninguna."
    - "`GET /api/mis-contactos` devuelve a la organización con sesión sus contactos más recientes (anuncio, nombre del equipo, canal, fecha y hora), filtrables por `?anuncio=` y `?canal=`; sin sesión responde 401 y nunca devuelve contactos de otra organización."
    - "`GET /api/anuncios?ids=a,b` devuelve solo los anuncios activos de esa lista, descarta ids mal formados y no admite más de 60."
    - "`resumenOrganizacion` devuelve `favoritos` y `compartidos` de los últimos 30 días, y un evento `favorito` o `compartir` contado los sube."
    - "`meta.para('/equipo.html', ?id=)` usa como `og:image` la miniatura `/fotos/…` de la primera foto y `aplicar` quita `og:image:width` y `og:image:height` cuando la imagen no es la de marca."
    - "`GET /api/mis-anuncios/:id/copia` devuelve al dueño el borrador sin número de serie, con fotos (url y miniatura), videos, teléfonos y sucursal; a otra organización le responde 404."
    - "Borrar un anuncio no borra del disco una foto o un video que otro anuncio sigue usando, y `rutasEnUso()` incluye el logotipo, el banner y la galería de la página del dealer."
    - "`npm run metricas:probar` pasa y corre en el job `pruebas` del CI."
  artifacts:
    - path: "tools/db.js"
      provides: "Migración 2026-09-contactos-anuncio al final de MIGRACIONES, registrarContacto, contactosDeOrganizacion, copiaDeAnuncio, fotoParaCompartir, filtro ids, compartidos en el resumen, rutasEnUso completo, borrarAnuncio que respeta rutas en uso"
      contains: "contactos_anuncio"
    - path: "tools/api.js"
      provides: "misContactos, copiarAnuncio, ids en catalogo, registro del contacto en evento"
      contains: "mis-contactos"
    - path: "tools/probar-metricas.js"
      provides: "Arnés de la fase"
      contains: "contactos_anuncio"
  key_links:
    - from: "tools/api.js (evento)"
      to: "db.registrarContacto"
      via: "resultado === 'contado' y tipo telefono/whatsapp"
      pattern: "registrarContacto"
    - from: "tools/api.js RUTAS"
      to: "copiarAnuncio"
      via: "GET /api/mis-anuncios/:id/copia antes de rutas genéricas"
      pattern: "mis-anuncios"
---

<objective>
Todo lo del servidor que necesitan los cuatro requisitos, con su arnés de pruebas, para
que los planes de interfaz solo pinten.

Purpose: los eventos crudos se purgan a 90 días y el dealer pregunta al renovar; la
vista previa necesita la miniatura; duplicar necesita una copia fiel y que borrar el
original no deje la copia sin fotos.
Output: migración nueva, cinco funciones de base, tres rutas, `meta.js` ajustado,
`tools/probar-metricas.js` y su paso de CI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/10-alcance-y-m-tricas-del-vendedor/10-CONTEXT.md
@.planning/phases/10-alcance-y-m-tricas-del-vendedor/10-RESEARCH.md

<interfaces>
De `tools/db.js`:
- `MIGRACIONES` (array de `[nombre, [sql, ...]]`, cierra antes de la línea ~911). La
  última hoy es `'2026-09-bitacora-admin'`.
- `anotarEvento(idAnuncio, tipo, visitante)` → `'invalido' | 'contado' | 'repetido'`.
- `resumenOrganizacion(idOrg, dias = 30)` → `{ totales: { vistas, telefono, whatsapp, favoritos }, serie, porEstado }`.
- `filtrosCatalogo(f)` / `buscarAnuncios(f)` con parámetros con nombre.
- `borrarAnuncio(idAnuncio, idOrg)` → `null | { fotos: [], videos: [] }`.
- `rutasEnUso()` → `Set` de rutas.
- `anuncio(id)` → fila con `fotos` (solo url), `videos`, `telefonos`.
- `hoy()`, `ahora()`, `abrir()`, `conNombres(a)`.

De `tools/api.js`:
- `conSesion(fn)` → 401 sin sesión; `ctx.organizacion.id`.
- `evento` (POST /api/eventos), `catalogo(req, res, ctx, consulta)`, `eliminarAnuncio`.
- `RUTAS`: `['GET', /^\/api\/mis-anuncios$/, misAnuncios]` y siguientes.
- Arnés de referencia: `pedir()` en `tools/probar-seguridad.js`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarea 1: Contactos atribuibles (tabla, registro, lista y ruta)</name>
  <files>tools/db.js, tools/api.js, tools/probar-metricas.js, package.json, .github/workflows/desplegar.yml</files>
  <read_first>
    - tools/db.js (MIGRACIONES completo, anotarEvento, resumenOrganizacion, purgar, borrarAnuncio, module.exports)
    - tools/api.js (evento, misAnuncios, RUTAS, conSesion)
    - tools/probar-seguridad.js (cabecera y pedir(), bloque de eventos)
    - .github/workflows/desplegar.yml (job pruebas)
  </read_first>
  <action>
    En `tools/db.js`, al FINAL de `MIGRACIONES`, añadir `['2026-09-contactos-anuncio', [...]]`
    con: `CREATE TABLE IF NOT EXISTS contactos_anuncio (id INTEGER PRIMARY KEY AUTOINCREMENT,
    anuncio_id TEXT NOT NULL REFERENCES anuncios(id) ON DELETE CASCADE, organizacion_id TEXT
    NOT NULL, canal TEXT NOT NULL CHECK (canal IN ('whatsapp','telefono')), dia TEXT NOT NULL,
    creado TEXT NOT NULL)`; índices `ix_contactos_anuncio_org (organizacion_id, creado)` y
    `ix_contactos_anuncio_anuncio (anuncio_id)`; y un `INSERT INTO contactos_anuncio
    (anuncio_id, organizacion_id, canal, dia, creado) SELECT e.anuncio_id, a.organizacion_id,
    e.tipo, e.dia, MIN(e.creado) FROM eventos e JOIN anuncios a ON a.id = e.anuncio_id WHERE
    e.tipo IN ('whatsapp','telefono') GROUP BY e.anuncio_id, e.tipo, e.dia, COALESCE(e.visitante,
    'sin-' || e.id)` que rellena lo que aún guarda `eventos`. Comentario con el porqué (purga
    a 90 días, sin huella a propósito).
    Nueva `registrarContacto(idAnuncio, canal)` que inserta con la organización del anuncio y
    `hoy()`/`ahora()`; no lanza (try/catch con `console.error`, como `anotarVisita`).
    Llamarla dentro de `anotarEvento` cuando devuelve 'contado' y `tipo` es `whatsapp` o
    `telefono` (así el arnés que llama a la base también la ejerce).
    Nueva `contactosDeOrganizacion(idOrg, { anuncio, canal, limite = 100 })` → filas
    `{ id, anuncio_id, canal, dia, creado, marca, modelo, anio, estado }` ordenadas por
    `creado DESC`, `limite` acotado a 1..200, filtros opcionales parametrizados.
    Añadir `contactos_anuncio` a la lista de tablas que borra `borrarAnuncio`.
    Exportar ambas.
    En `tools/api.js`: `misContactos = conSesion((req, res, ctx, consulta) => ...)` que valida
    `canal` contra `['whatsapp','telefono']`, `anuncio` con `^[\w-]+$` y `limite`, y responde
    `{ contactos }`. Ruta `['GET', /^\/api\/mis-contactos$/, misContactos]` junto a
    `mis-anuncios`.
    Crear `tools/probar-metricas.js` con la cabecera y el arnés de `probar-seguridad.js`
    (BANCO `.tmp/prueba-metricas`, `MERCA_DB` antes del require, `comprobar`, `pedir` con
    `cf-connecting-ip`), dos organizaciones con un anuncio cada una, y el bloque «Contactos
    atribuibles»: whatsapp contado → 1 fila; repetido del mismo visitante el mismo día → sigue
    1; otro visitante → 2; `telefono` → canal telefono; `vista` no crea fila; la fila no tiene
    columna de visitante; `GET /api/mis-contactos` con sesión devuelve las suyas con marca y
    hora, `?canal=whatsapp` filtra, la otra organización ve 0 de estas; sin sesión 401. Al
    final `process.exit(mal ? 1 : 0)` y resumen de contadores.
    `package.json`: script `"metricas:probar": "node tools/probar-metricas.js"`.
    `.github/workflows/desplegar.yml` (LF): paso `npm run metricas:probar` en el job
    `pruebas`, tras `pagos:probar`, con el mismo formato de `name:` que los demás.
  </action>
  <verify>
    <automated>npm run metricas:probar && npm run seguridad:probar</automated>
  </verify>
  <acceptance_criteria>
    - `tools/db.js` contiene `'2026-09-contactos-anuncio'` como última entrada de `MIGRACIONES`.
    - `grep -c "visitante" ` sobre el CREATE de `contactos_anuncio` es 0.
    - `npm run metricas:probar` sale 0 con el bloque «Contactos atribuibles» en verde.
    - `npm run seguridad:probar` sigue en verde (la deduplicación no cambió).
    - `.github/workflows/desplegar.yml` contiene `npm run metricas:probar`.
  </acceptance_criteria>
  <done>Cada contacto contado queda para siempre, sin identificar al visitante, y el dueño lo lee por la API.</done>
</task>

<task type="auto">
  <name>Tarea 2: Guardados por ids, compartidos en el resumen y vista previa con miniatura</name>
  <files>tools/db.js, tools/api.js, tools/meta.js, tools/probar-metricas.js</files>
  <read_first>
    - tools/db.js (filtrosCatalogo, buscarAnuncios, resumenOrganizacion, anunciosDeOrganizacion)
    - tools/api.js (catalogo)
    - tools/meta.js completo
    - tools/probar-seguridad.js (bloque 6b de metadatos)
  </read_first>
  <action>
    `filtrosCatalogo`: si `f.ids` es un array no vacío, añadir `a.id IN (:id0, :id1, ...)` con
    parámetros con nombre (nunca interpolar el valor). `catalogo` en `tools/api.js` lee
    `ids`, parte por comas, se queda con los que casan `^[\w-]{1,64}$`, sin repetidos, máximo
    60, y lo pasa como `ids`; si viene `ids` y queda vacío tras validar, responde
    `{ anuncios: [], total: 0, pagina: 1, paginas: 1, porPagina }` sin consultar. Con `ids`
    el `porPagina` es 60.
    `resumenOrganizacion`: añadir `COALESCE(SUM(m.compartidos), 0) AS compartidos` a
    `totales`. `anunciosDeOrganizacion`: añadir `compartidos` igual que `favoritos`.
    Nueva `fotoParaCompartir(idAnuncio)` en `tools/db.js` → `COALESCE(miniatura, url)` de la
    primera foto por `orden`, o null. En `tools/meta.js`, `delAnuncio` usa esa ruta si empieza
    por `/fotos/` y si no, `imagenDe(a.fotos)`; el JSON-LD sigue con la foto completa.
    `aplicar`: cuando `meta.imagen !== IMAGEN_MARCA`, quitar las etiquetas
    `<meta property="og:image:width" ...>` y `og:image:height` (regex sobre esas dos
    propiedades); con la imagen de marca, se quedan. Comentario con el porqué (WhatsApp y
    el peso de la foto de 1.600 px; dimensiones que mienten).
    En `tools/probar-metricas.js`, bloque «Guardados y compartir»: `?ids=` con uno activo,
    uno pausado y uno inventado devuelve solo el activo; `?ids=';DROP` devuelve 0 y 200;
    61 ids se recortan a 60 (comprobar con `porPagina` o con la longitud aceptada); un
    `favorito` contado sube `resumenOrganizacion(...).totales.favoritos` a 1 y un `compartir`
    sube `compartidos` a 1; un anuncio con fotos `/fotos/AAAA-MM/x.jpg` y miniatura
    `/fotos/AAAA-MM/x-m.jpg` da `og:image` con la miniatura, título con `RD$` y el HTML
    aplicado a `equipo.html` no contiene `og:image:width`. Para crear anuncios con fotos
    `/fotos/` en la base basta `db.crearAnuncio` (no valida el disco).
  </action>
  <verify>
    <automated>npm run metricas:probar && npm run seguridad:probar</automated>
  </verify>
  <acceptance_criteria>
    - `tools/db.js` contiene `AS compartidos` dentro de `resumenOrganizacion`.
    - `tools/meta.js` contiene `og:image:width` en un patrón de borrado.
    - `npm run metricas:probar` sale 0 con el bloque «Guardados y compartir» en verde.
    - `npm run seguridad:probar` sigue en verde, incluidas sus comprobaciones de metadatos.
  </acceptance_criteria>
  <done>La página de guardados tiene de dónde leer, el panel sabe cuántas veces se compartió y la tarjeta de WhatsApp lleva una imagen que sí se pinta.</done>
</task>

<task type="auto">
  <name>Tarea 3: Copia de un anuncio y borrado que respeta archivos compartidos</name>
  <files>tools/db.js, tools/api.js, tools/probar-metricas.js</files>
  <read_first>
    - tools/db.js (anuncio, borrarAnuncio, rutasEnUso, crearAnuncio, organizacion_galeria)
    - tools/api.js (eliminarAnuncio, publicar para ver los nombres de campo que espera, RUTAS)
    - assets/publicar.js (estadoInicial, anuncioParaApi, forma de fotos y videos)
  </read_first>
  <action>
    `rutasEnUso()`: añadir `SELECT logo, banner FROM organizaciones` y `SELECT url FROM
    organizacion_galeria`, con comentario (la tarea diaria de huérfanos los habría borrado
    tras 48 h).
    `borrarAnuncio`: tras el COMMIT, calcular `rutasEnUso()` y devolver solo las rutas que ya
    no estén en uso (fotos y videos). Comentario: una copia reutiliza las fotos por ruta.
    Nueva `copiaDeAnuncio(idAnuncio, idOrg)` → null si el anuncio no es de esa organización;
    si lo es, la fila de `anuncios` más `fotos: [{ url, miniatura }]` por `orden`, `videos:
    [{ url, poster, duracion }]` y `telefonos: [{ numero, tipo, nota }]`. Exportarla.
    En `tools/api.js`, `copiarAnuncio = conSesion((req, res, ctx, idAnuncio) => ...)`: 404
    «Ese anuncio no es suyo o no existe» si null; si no, responde `{ copia }` con la forma de
    `estadoInicial()` de `assets/publicar.js`: `equipo` { categoria, subcategoria, marca,
    modelo, anio (texto), condicion, uso (texto de dígitos o ''), unidad, serie: '',
    potencia, peso, provincia, ciudad (de municipio), implementos, descripcion, motorMarca,
    motorModelo, transmisionMarca, transmisionModelo } con '' en lugar de null; `precio`
    { modalidad, monto, moneda, minimo, itbisIncluido, permuta, financiamiento } con
    booleanos; `contacto` { sucursal, telefonos } (al menos una fila vacía si no hay);
    `fotos` y `videos` con un `id` `f-copia-<n>` / `v-copia-<n>` y `nombre` 'Foto <n>' /
    'Video <n>'; y `origen: { id, nombre: '<anio> <marca> <modelo>' }`.
    Ruta `['GET', /^\/api\/mis-anuncios\/([\w-]+)\/copia$/, copiarAnuncio]` antes de
    `['GET', /^\/api\/anuncios\/([\w-]+)$/, verAnuncio]`.
    En `tools/probar-metricas.js`, bloque «Duplicar»: el dueño recibe 200 con `serie === ''`,
    marca, modelo, 3 fotos con miniatura, teléfonos y `precio.minimo`; otra organización
    recibe 404; sin sesión 401. Bloque «Archivos compartidos»: crear el original con fotos
    reales escritas en la carpeta de fotos del banco (`MERCA_FOTOS` apuntando a
    `.tmp/prueba-metricas/fotos`, fijado antes del require) usando `fotos.guardar` o
    escribiendo archivos y sus rutas; crear la copia con las mismas rutas; `DELETE
    /api/anuncios/<original>` → las fotos siguen en disco; borrar la copia → ya no.
    `rutasEnUso()` contiene el logo y una foto de galería de una organización tras
    `db.anadirAGaleria` y un UPDATE del logo.
  </action>
  <verify>
    <automated>npm run metricas:probar && npm run seguridad:probar && npm run dealer:probar</automated>
  </verify>
  <acceptance_criteria>
    - `tools/api.js` contiene `mis-anuncios\/([\w-]+)\/copia` en `RUTAS` antes de la ruta de `verAnuncio`.
    - `tools/db.js` `rutasEnUso` contiene `organizacion_galeria` y `logo, banner`.
    - `npm run metricas:probar` sale 0 con los bloques «Duplicar» y «Archivos compartidos».
    - `npm run dealer:probar` y `npm run seguridad:probar` siguen en verde.
  </acceptance_criteria>
  <done>El asistente de publicar puede precargar una copia fiel y borrar el original no rompe la copia ni la página del dealer.</done>
</task>

</tasks>

<threat_model>
| Id | Amenaza | Mitigación |
|----|---------|------------|
| T-10-01 | Un anunciante lee los contactos de otro (IDOR) o identifica a un visitante | `contactosDeOrganizacion` filtra siempre por `ctx.organizacion.id`; la tabla no guarda huella ni IP |
| T-10-02 | Inyección SQL por `?ids=` | Validación `^[\w-]{1,64}$`, tope 60, parámetros con nombre |
| T-10-03 | Leer el borrador de un anuncio ajeno (incluido el precio mínimo privado) por `/copia` | `copiaDeAnuncio` exige `organizacion_id`; 404 idéntico para ajeno e inexistente |
| T-10-05 | Inflar contactos para aparentar tráfico | Hereda la deduplicación y el tope de `POST /api/eventos`; solo se registra lo 'contado' |
</threat_model>

<verification>
`npm run metricas:probar`, `npm run seguridad:probar`, `npm run dealer:probar`,
`npm run bitacora:probar` en verde.
</verification>

<success_criteria>
Las cuatro capacidades tienen su pieza de servidor probada; ninguna prueba previa cambió.
</success_criteria>

<output>
Crear `.planning/phases/10-alcance-y-m-tricas-del-vendedor/10-01-SUMMARY.md`.
</output>

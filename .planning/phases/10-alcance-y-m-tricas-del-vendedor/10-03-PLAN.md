---
phase: 10-alcance-y-m-tricas-del-vendedor
plan: 03
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - assets/panel.js
  - panel.html
  - assets/publicar.js
  - styles.css
autonomous: true
requirements: [MET-01, MET-03, MET-04]
user_setup: []

must_haves:
  truths:
    - "La tarjeta «Guardados» del panel muestra los favoritos de 30 días y, debajo, cuántas veces se compartieron sus anuncios."
    - "El panel tiene una sección «Contactos recibidos» que lista cada contacto con fecha y hora de Santo Domingo, el equipo enlazado a su ficha y el canal, filtrable por equipo y por canal."
    - "Cada fila de la tabla de anuncios tiene «Duplicar», que lleva a `publicar.html?duplicar=<id>`."
    - "`publicar.html?duplicar=<id>` precarga el asistente con todos los datos del anuncio salvo el número de serie, avisa de que es una copia, pregunta antes de reemplazar un borrador a medias y quita el parámetro de la URL."
  artifacts:
    - path: "assets/panel.js"
      provides: "pintarMetricas con compartidos, filaAnuncio con Duplicar, montarContactos"
      contains: "mis-contactos"
    - path: "assets/publicar.js"
      provides: "cargarCopia en montarPublicador"
      contains: "duplicar"
  key_links:
    - from: "assets/panel.js (montarContactos)"
      to: "GET /api/mis-contactos"
      via: "api() en try/catch"
      pattern: "mis-contactos"
    - from: "assets/publicar.js (montarPublicador)"
      to: "GET /api/mis-anuncios/:id/copia"
      via: "params().get('duplicar')"
      pattern: "/copia"
---

<objective>
El lado del anunciante: atribuir cada contacto a un anuncio y a una hora, y duplicar un
anuncio para cambiar solo lo que difiere.

Purpose: MET-03 y MET-04, y cerrar MET-01 en el panel.
Output: tarjeta con compartidos, sección de contactos, botón «Duplicar» y la carga de la
copia en el asistente de publicar.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/10-alcance-y-m-tricas-del-vendedor/10-CONTEXT.md
@.planning/phases/10-alcance-y-m-tricas-del-vendedor/10-UI-SPEC.md
@.planning/phases/10-alcance-y-m-tricas-del-vendedor/10-01-SUMMARY.md

<interfaces>
- `GET /api/mis-contactos?anuncio=&canal=&limite=` → `{ contactos: [{ id, anuncio_id, canal, dia, creado, marca, modelo, anio, estado }] }`.
- `GET /api/mis-anuncios/:id/copia` → `{ copia: { equipo, precio, contacto, fotos, videos, origen } }`.
- `GET /api/mis-anuncios` ya trae `resumen.totales.compartidos` y `anuncios[].compartidos`.
- `assets/panel.js`: `pintarMetricas`, `tarjetaMetrica`, `filaAnuncio`, `pintarTabla`,
  `montarPanel` (listeners en `#filasAnuncios`), `montarFacturas` (patrón de sección que
  carga sin bloquear).
- `assets/publicar.js`: `estadoInicial`, `leerBorrador`, `guardarBorrador`,
  `montarPublicador` (rama `guardado`), `#avisoBorrador`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarea 1: Contactos recibidos y compartidos en el panel</name>
  <files>assets/panel.js, panel.html, styles.css</files>
  <read_first>
    - assets/panel.js (pintarMetricas, montarPanel, montarFacturas)
    - panel.html (sección de anuncios y de comprobantes, para copiar estructura y clases)
    - styles.css (tablas del panel, .pastilla, .filtro-panel)
  </read_first>
  <action>
    `pintarMetricas`: la tarjeta «Guardados» lleva como detalle «Compradores que lo
    guardaron · N compartido(s)» con `t.compartidos` (singular/plural).
    `panel.html`: sección `section.panel#panelContactos` justo después de la de anuncios,
    con título «Contactos <em>recibidos</em>», texto de ayuda del UI-SPEC, `select#filtroContactoAnuncio`
    («Todos los equipos» + un `option` por anuncio), `select#filtroContactoCanal` (Todos /
    WhatsApp / Llamadas), tabla con `thead` Fecha y hora · Equipo · Canal y
    `tbody#filasContactos`, `p#contactosVacio` oculto. Etiquetas `<label>` visibles o
    `visualmente-oculto` para los selects.
    `montarContactos()` en `assets/panel.js`, llamada desde `montarPanel` sin `await` como
    `montarFacturas`: rellena el select de equipos con `ANUNCIOS`, pide
    `/mis-contactos?limite=100[&anuncio=][&canal=]` en try/catch (error → mensaje en
    `#contactosVacio`), pinta filas con `new Date(c.creado).toLocaleString('es-DO', {
    timeZone: 'America/Santo_Domingo', dateStyle: 'medium', timeStyle: 'short' })`, el equipo
    `esc('<anio> <marca> <modelo>')` enlazado a `equipo.html?id=` y una pastilla «WhatsApp»
    (`pastilla--verde`) o «Llamada»; vuelve a pedir al cambiar cualquiera de los dos selects.
  </action>
  <verify>
    <automated>node -e "new (require('vm').Script)(require('fs').readFileSync('assets/panel.js','utf8'))" && npm run metricas:probar</automated>
  </verify>
  <acceptance_criteria>
    - `panel.html` contiene `id="panelContactos"`, `id="filasContactos"`, `id="filtroContactoAnuncio"` y `id="filtroContactoCanal"`.
    - `assets/panel.js` contiene `/mis-contactos`, `America/Santo_Domingo` y `compartido`.
    - La llamada a `api('/mis-contactos` está dentro de un `try`.
  </acceptance_criteria>
  <done>El dealer ve qué anuncio trajo cada contacto y cuándo.</done>
</task>

<task type="auto">
  <name>Tarea 2: Duplicar desde el panel y precargar el asistente</name>
  <files>assets/panel.js, assets/publicar.js, publicar.html</files>
  <read_first>
    - assets/panel.js (filaAnuncio, listeners de #filasAnuncios en montarPanel)
    - assets/publicar.js (estadoInicial, leerBorrador, guardarBorrador, montarPublicador, volcarEstadoAlFormulario, pintarFotos, pintarVideos, pintarTelefonos)
    - publicar.html (#avisoBorrador)
  </read_first>
  <action>
    `filaAnuncio`: botón `<button type="button" class="btn-tabla" data-duplicar="<id>"
    aria-label="Duplicar <equipo>">Duplicar</button>` antes de «Eliminar», en todos los
    estados. En `montarPanel`, el listener de clic de `#filasAnuncios` manda a
    `publicar.html?duplicar=<encodeURIComponent(id)>`.
    `assets/publicar.js`: `cargarCopia(idAnuncio)` que, con sesión, pide
    `/mis-anuncios/<id>/copia` en try/catch y devuelve el estado fusionado como
    `leerBorrador` (bloques `equipo`, `precio`, `contacto` sobre `estadoInicial()`; fotos y
    videos de la copia; `paso: 0`) o null. En `montarPublicador`, antes de `leerBorrador()`:
    si `params().get('duplicar')` y hay sesión, carga la copia; si existe un borrador con
    `equipo.marca` o `equipo.modelo` y el usuario no confirma con `window.confirm('Tiene un
    anuncio a medio escribir. ¿Reemplazarlo por la copia de <origen.nombre>?')`, se ignora
    la copia. Si se usa, `estado = copia`, `guardarBorrador()`, y se sigue por la misma rama
    que un borrador recuperado (volcar al formulario, subcategoría, etc.). Después,
    `history.replaceState(null, '', 'publicar.html')` para que recargar no vuelva a copiar.
    El texto de `#avisoBorrador span` pasa a «Copia de <nombre>. Cambie lo que sea distinto
    —número de serie, horas, precio y fotos— y publíquelo.» (con `textContent`). Si la copia
    falla, se sigue como hoy y se muestra el error en el aviso. Comprobar que las fotos,
    videos y teléfonos de la copia se pintan en sus pasos (las funciones de pintar leen
    `estado`); si alguna se pinta solo al montar, llamarla tras asignar.
  </action>
  <verify>
    <automated>node -e "for (const f of ['assets/panel.js','assets/publicar.js']) new (require('vm').Script)(require('fs').readFileSync(f,'utf8'))" && npm run metricas:probar</automated>
  </verify>
  <acceptance_criteria>
    - `assets/panel.js` contiene `data-duplicar` y `publicar.html?duplicar=`.
    - `assets/publicar.js` contiene `/copia`, `history.replaceState` y `window.confirm`.
    - La llamada a `api(` de la copia está dentro de un `try`.
  </acceptance_criteria>
  <done>Duplicar abre el asistente lleno, sin serie, y publicar pasa por el cupo como siempre.</done>
</task>

</tasks>

<threat_model>
| Id | Amenaza | Mitigación |
|----|---------|------------|
| T-10-04 | XSS por nombre de equipo en la tabla de contactos o en el aviso de copia | `esc()` en HTML y `textContent` en el aviso |
| T-10-07 | Duplicar como vía para publicar sin cupo | La copia solo precarga el navegador; `POST /api/anuncios` sigue exigiendo membresía |
</threat_model>

<verification>
Arnés en verde; recorrido de navegador del plan 10-04.
</verification>

<success_criteria>
Criterios 1 (lado anunciante), 3 y 4 del ROADMAP visibles en el panel.
</success_criteria>

<output>
Crear `.planning/phases/10-alcance-y-m-tricas-del-vendedor/10-03-SUMMARY.md`.
</output>

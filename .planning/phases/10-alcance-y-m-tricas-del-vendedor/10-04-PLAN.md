---
phase: 10-alcance-y-m-tricas-del-vendedor
plan: 04
type: execute
wave: 3
depends_on: ["10-02", "10-03"]
files_modified:
  - tools/auditar-metricas.js
  - package.json
autonomous: true
requirements: [MET-01, MET-02, MET-03, MET-04]
user_setup: []

must_haves:
  truths:
    - "`node tools/auditar-metricas.js` recorre, contra el servidor de demo, guardar en la ficha → encontrarlo en `guardados.html` → quitarlo; compartir por el menú de respaldo con la URL de la ficha; y, como anunciante de demostración, ver el contacto de WhatsApp listado en el panel y duplicar un anuncio hasta ver el asistente lleno sin número de serie."
    - "`npm run auditar` incluye `tools/auditar-metricas.js` y sale 0."
    - "La auditoría sale 1 con cada hallazgo impreso, como las demás."
  artifacts:
    - path: "tools/auditar-metricas.js"
      provides: "Recorrido de navegador de la fase"
      contains: "guardados.html"
  key_links:
    - from: "package.json (auditar)"
      to: "tools/auditar-metricas.js"
      via: "cadena &&"
      pattern: "auditar-metricas"
---

<objective>
Demostrar con un navegador los cuatro criterios de éxito, y dejarlo dentro de la barrera
del CI para que no se rompan en silencio.

Purpose: los planes 10-02 y 10-03 son interfaz; sin un recorrido real nadie sabría si
el botón hace lo que dice.
Output: `tools/auditar-metricas.js` y su sitio en `npm run auditar`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/10-alcance-y-m-tricas-del-vendedor/10-02-SUMMARY.md
@.planning/phases/10-alcance-y-m-tricas-del-vendedor/10-03-SUMMARY.md

<interfaces>
- `tools/auditar-flujos.js`: patrón de `vigilar`, `anota`, `ok`, `esperar`, entrada con
  código del buzón `.tmp/correos`, `process.exit(fallos.length ? 1 : 0)`,
  `limpiarLimitador()` que escribe directo en la base con la misma `MERCA_DB`.
- `tools/seed.js`: cuentas de demostración y su clave (buscar cómo entran las auditorías
  que usan un anunciante de demo, o crear uno por `tools/admin.js`/la base igual que
  `crearAdministrador`).
- CI: `npm run db:demo`, servidor en 8080, `npm run auditar`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarea 1: Recorrido de navegador de la fase y su sitio en `npm run auditar`</name>
  <files>tools/auditar-metricas.js, package.json</files>
  <read_first>
    - tools/auditar-flujos.js completo
    - tools/seed.js (anunciantes de demo, claves, cómo quedan las cuentas)
    - tools/admin.js (si ofrece crear o dar clave a una cuenta)
    - package.json (script auditar)
  </read_first>
  <action>
    Crear `tools/auditar-metricas.js` con el estilo de `auditar-flujos.js` (cabecera que
    explica qué criterio del ROADMAP prueba cada bloque). Pasos:
    1. Visitante: abrir `/equipos.html`, tomar el primer `a[href^="equipo.html?id="]`, ir a
       la ficha; comprobar `#btnGuardar` y `#btnCompartir`; pulsar guardar →
       `aria-pressed="true"` y aparece `[data-guardados]` en la navegación; ir a
       `/guardados.html` → la tarjeta de ese id está en `#listaGuardados`; pulsar «Quitar»
       → la lista queda vacía y se ve `#guardadosVacio`.
    2. Compartir: en la ficha, borrar `navigator.share` antes de cargar
       (`evaluateOnNewDocument`), pulsar `#btnCompartir` → `#menuCompartir` visible, su
       enlace de WhatsApp empieza por `https://wa.me/?text=` y su texto decodificado
       contiene `equipo.html?id=<id>`. Pedir por HTTP `/equipo.html?id=<id>` y comprobar
       que el HTML servido lleva un `og:title` distinto de «Equipo» y con el precio.
    3. Anunciante: entrar con una cuenta dueña de un anuncio activo (la de demo si
       `seed.js` deja una clave conocida; si no, crear un particular por el sitio como
       `auditar-flujos.js` o escribir la cuenta directo en la base con la misma
       `MERCA_DB`, explicando por qué). Antes, como visitante, pulsar WhatsApp en la ficha
       de uno de sus anuncios (o `POST /api/eventos` con `tipo: 'whatsapp'`). En
       `/panel.html` comprobar que `#filasContactos` tiene una fila con el nombre del
       equipo y «WhatsApp». Pulsar `[data-duplicar]` de ese anuncio → la URL queda en
       `publicar.html` sin `duplicar`, `#e-modelo` tiene el modelo del original y
       `#e-serie` está vacío, y `#avisoBorrador` visible con «Copia de». Descartar el
       borrador al terminar.
    Cada comprobación usa `ok()`/`anota()` y la salida es `process.exit(fallos.length ? 1 : 0)`.
    `package.json`: añadir `node tools/auditar-metricas.js` a la cadena `auditar` antes de
    `check-contraste.js`, y el script `"auditar:metricas": "node tools/auditar-metricas.js"`.
    Levantar el servidor de demo localmente como en el CI (`npm run db:demo` con una
    `MERCA_DB` en `.tmp/`, `node tools/serve.js --port 8080` en segundo plano,
    `node tools/esperar-servidor.js`) y correr `npm run auditar` y `npm run check`; si algo
    de 10-02 o 10-03 falla, se arregla allí con commit propio, nunca aflojando la auditoría.
  </action>
  <verify>
    <automated>npm run auditar && npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` `auditar` contiene `tools/auditar-metricas.js`.
    - `node tools/auditar-metricas.js` contra el servidor de demo imprime 0 hallazgos y sale 0.
    - `npm run auditar` y `npm run check` salen 0.
  </acceptance_criteria>
  <done>Los cuatro criterios quedan demostrados en un navegador y protegidos por el CI.</done>
</task>

</tasks>

<threat_model>
| Id | Amenaza | Mitigación |
|----|---------|------------|
| — | La auditoría escribe en una base de producción | Solo corre contra `127.0.0.1:8080` con la base de demo, igual que las otras |
</threat_model>

<verification>
`npm run auditar` y `npm run check` en verde con el servidor de demo.
</verification>

<success_criteria>
Los cuatro criterios del ROADMAP pasan en un navegador; queda pendiente solo lo visual y
la vista previa real de WhatsApp (ver 10-VALIDATION.md, «Solo manual»).
</success_criteria>

<output>
Crear `.planning/phases/10-alcance-y-m-tricas-del-vendedor/10-04-SUMMARY.md`.
</output>

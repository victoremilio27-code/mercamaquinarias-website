---
phase: 02-tema-claro-y-oscuro-coherentes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - styles.css
autonomous: true
requirements: [UI-01, UI-02]
user_setup: []

must_haves:
  truths:
    - "Los dos bloques de tema declaran exactamente el mismo juego de tokens: ninguno existe solo en oscuro."
    - "El tema claro tiene un token de borde de control que alcanza 3:1 contra la tarjeta, el fondo y la superficie suave."
    - "El tema oscuro tiene ese mismo token y también alcanza 3:1 contra sus tres superficies."
    - "`--texto-claro` y `--ambar-texto` están definidos en el bloque claro con valores legibles sobre superficie clara."
    - "Ningún `var()` de la hoja lleva un color de reserva en línea: si falta una definición, se rompe a la vista."
  artifacts:
    - path: "styles.css"
      provides: "Bloque claro con los tokens semánticos y de control que solo tenía el oscuro"
      contains: "--app-borde-control: #848889"
    - path: "styles.css"
      provides: "Bloque oscuro con el borde de control medido"
      contains: "--app-borde-control: #727677"
  key_links:
    - from: "styles.css bloque :root[data-theme=\"light\"]"
      to: "styles.css bloque :root[data-theme=\"dark\"]"
      via: "mismo juego de nombres de token en los dos"
      pattern: "--sup-calida"
    - from: "styles.css"
      to: "declaraciones de token"
      via: "uso sin color de reserva"
      pattern: "var\\(--[a-z0-9-]+\\)"
---

<objective>
Completar la capa de tokens del sistema de temas: que el bloque claro y el
bloque oscuro de `styles.css` declaren el mismo juego de nombres, con un token
de borde para los controles que alcanza 3:1 en los dos temas, y quitar los
colores de reserva en línea que hoy tapan las definiciones que faltan.

Purpose: el tema funciona redefiniendo tokens en `styles.css:4099` (claro) y
`:4121` (oscuro), así que las 3.500 líneas de arriba cambian solas. Todo lo que
se sale de ese mecanismo se queda congelado. Este plan arregla el mecanismo; los
planes 02-04 y 02-05 traen al redil lo que se había salido. Sin esto primero,
ninguno de los dos tiene tokens a los que apuntar.
Output: `styles.css` con los dos bloques de tema simétricos y sin colores de
reserva en línea.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/phases/02-tema-claro-y-oscuro-coherentes/02-01-PLAN.md

El plan aprobado y medido por Victor, del que sale todo lo de esta fase:
@C:\Users\Victor\.claude\plans\majestic-dancing-firefly.md

La hoja de estilos. Las líneas que importan aquí son `4098-4120` (bloque claro)
y `4121-4150` (bloque oscuro); no hace falta leer las 4.600.
@styles.css
</context>

<decisiones_de_esta_fase>

## D-01 · Los valores están medidos y aprobados: no se recalculan

Vienen del plan aprobado `majestic-dancing-firefly.md`, que midió la hoja
entera antes de escribirse. **No se vuelven a discutir ni a «afinar» durante la
ejecución.**

Contraste del borde de control contra tarjeta / fondo / superficie suave:

| tema | valor | card | bg | soft | mínimo WCAG 1.4.11 |
|---|---|---|---|---|---|
| claro | `#848889` | **3.58** | **3.25** | **3.08** | 3.0 |
| oscuro | `#727677` | **4.27** | **3.80** | **3.30** | 3.0 |

Confirmado con Victor: **se suben los dos temas a 3:1**, no solo el claro. El
oscuro hoy está en 1.66 y también incumple; que se «lea mejor» no lo hace
correcto, y el objetivo de la fase es que los dos se comporten idéntico.

## D-02 · Dos valores aprobados se quedan cortos sobre las superficies que no son la tarjeta blanca — medido al planificar

El plan aprobado fijó `--ambar-texto: #9A6800` y `--texto-claro: #6E7376`
midiéndolos **contra tarjeta blanca**, donde dan 4.82 y 4.80. Medido ahora
contra las otras dos superficies claras:

| token | sobre `--app-card` `#FFFFFF` | sobre `--app-bg` `#F4F4F1` | sobre `--app-soft` `#EEEEEA` |
|---|---|---|---|
| `--ambar-texto: #9A6800` | 4.82 | **4.38** | **4.15** |
| `--texto-claro: #6E7376` | 4.80 | **4.35** | **4.12** |

Es decir: los dos cumplen el 4.5:1 sobre tarjeta y **no lo cumplen** sobre el
hueso del fondo ni sobre la superficie suave, que es donde viven los chips, los
filtros y buena parte de los metadatos.

Decisión, para no cambiar por mi cuenta un valor que Victor aprobó: **se
escriben los valores aprobados tal cual** en esta tarea. Es el comprobador del
plan 02-03 —no yo— quien dice si hace falta bajarlos, y si lo pide, el plan
02-06 usa estos sustitutos **ya medidos** y lo anota en el SUMMARY:

| token | sustituto | card / bg / soft |
|---|---|---|
| `--ambar-texto` | `#8F6100` | 5.42 / 4.91 / 4.66 |
| `--texto-claro` | `#686D70` | 5.24 / 4.75 / 4.50 |

No se toca `--app-accent-texto`, que es el token existente y del que
`--ambar-texto` es alias: se mueven los dos juntos o ninguno.

## D-03 · `--texto-ok` en claro se unifica en el más oscuro de los tres que hoy conviven

Hoy no existe: solo vive como color de reserva, y con **tres valores distintos**
(`#14532D`, `#1B5638`, `#2C4A3C`) según la línea que se mire. Se declara uno
solo, `#14532D` (9.11 sobre tarjeta, 8.58 sobre `--sup-ok`), que es el de más
contraste de los tres. Unificar es justamente el punto: tres verdes para el
mismo papel es el síntoma de que el token nunca se declaró.

## D-04 · Los colores de reserva en línea se van, los 62

`var(--sup-calida, #FFFDF7)` pasa a `var(--sup-calida)`. Hay **62** en la hoja,
medidos al planificar. Mientras estén, una definición que falte se disimula sola
y el fallo aparece meses después en el tema que nadie miró. Además hay dos
copiados mal (`styles.css:2835` y `:2856`: el de `--gris-acero` lleva el valor
de `--texto-claro` y al revés), que es exactamente el tipo de error que esta
costumbre produce y esconde.

</decisiones_de_esta_fase>

<contrato_de_tokens>

Estado final de los dos bloques. Lo que ya está escrito hoy **se conserva**;
esto es lo que se añade o se corrige, y tiene que quedar el **mismo juego de
nombres en los dos**.

### Bloque claro — `:root, :root[data-theme="light"]` (`styles.css:4098`)

Añadir (hoy ninguno de estos existe en el bloque claro):

```
--app-borde-control: #848889;   /* 3.58 card · 3.25 bg · 3.08 soft */
--texto-claro:  #6E7376;
--ambar-texto:  #9A6800;        /* alias de --app-accent-texto */
--sup-calida:   #FFFDF7;
--sup-error:    #FDF3F3;
--sup-ok:       #F2FAF6;
--texto-error:  #7A1B15;        /* 10.53 sobre tarjeta, 9.67 sobre --sup-error */
--texto-ok:     #14532D;        /* 9.11 sobre tarjeta, 8.58 sobre --sup-ok */
--texto-peligro: #B3261E;       /* 6.54 sobre tarjeta, 6.00 sobre --sup-error */
--borde-error:  #E8C4C4;        /* 1.47 sobre --sup-error: el valor de hoy, que en claro esta bien */
--borde-ok:     #B9D7C8;
--sobre-oscuro:   #FFFFFF;
--sobre-oscuro-2: #D5DEE5;
```

### Bloque oscuro — `:root[data-theme="dark"]` (`styles.css:4121`)

Añadir (los cinco semánticos `--sup-calida`, `--sup-error`, `--texto-error`,
`--sup-ok`, `--texto-ok` **ya están** y no se tocan):

```
--app-borde-control: #727677;   /* 4.27 card · 3.80 bg · 3.30 soft */
--ambar-texto:   #F2A900;       /* igual que --app-accent-texto en oscuro */
--texto-peligro: #F3A29C;       /* el rojo de texto del oscuro; #B3261E da 2.66 aquí */
--borde-error:   #5A2A26;       /* 1.49 sobre --sup-error, a la par del claro. El #E8C4C4 de hoy da 10.87 aqui: un contorno chillon */
--borde-ok:      #1A4530;       /* 1.56 sobre --sup-ok, a la par del claro */
--sobre-oscuro:   #FFFFFF;
--sobre-oscuro-2: #D5DEE5;
```

`--sobre-oscuro` y `--sobre-oscuro-2` valen **lo mismo en los dos temas** a
propósito: son para texto sobre superficies que son oscuras en los dos temas
—el mosaico, la banda del vídeo, el sello de la foto, y desde el plan 02-05
también la cabecera del perfil del dealer—. Que aparezcan en los dos bloques con
el mismo valor es lo que deja escrito que **es deliberado** y no un olvido.

Cada bloque nuevo lleva un comentario corto en español, en el estilo del
proyecto: qué papel tiene el token y, donde aplique, el número medido. Nada de
comentar lo obvio.

</contrato_de_tokens>

<tasks>

<task type="auto">
  <name>Tarea 1: el mismo juego de tokens en los dos bloques de tema</name>
  <files>styles.css</files>
  <action>
Editar los dos bloques de tema de `styles.css` —el claro empieza en la línea
4098 (`:root, :root[data-theme="light"]`) y el oscuro en la 4121
(`:root[data-theme="dark"]`)— añadiendo exactamente las declaraciones del bloque
`<contrato_de_tokens>` de este plan, con sus valores y sus comentarios.

Reglas que no se pueden desviar:

- **No se borra ni se cambia ninguna declaración que ya esté.** Esta tarea solo
  añade. `--app-line` se queda como está: sigue sirviendo para los filetes
  decorativos entre contenido, que a 1.30:1 están bien porque no delimitan nada
  pulsable. El borde de control es un token **nuevo y aparte**, no un cambio de
  valor del existente.
- **Los valores van tal cual están escritos en el contrato.** Están medidos y
  aprobados por Victor (D-01). Si al ejecutar parece que alguno debería ser
  otro, se anota en el SUMMARY y se sigue: quien decide es el comprobador del
  plan 02-03, no la ejecución de esta tarea.
- **Simetría obligatoria:** al terminar, todo nombre de token que aparezca en un
  bloque tiene que aparecer en el otro. Es el punto entero de la tarea: los
  cinco semánticos que hoy viven **solo en el bloque oscuro** son la razón por la
  que el claro se cae sin que nadie lo note.
- **`styles.css` es LF** aunque el resto del repositorio sea CRLF. No conviertas
  los saltos de línea al guardar y no busques con `\r\n` dentro de este archivo.

Todavía **no se aplica** ningún token a ningún selector: eso es de los planes
02-04 y 02-05. Aquí solo se declara la paleta a la que ellos van a apuntar.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('styles.css','utf8');const bloque=(re)=>{const i=s.search(re);if(i&lt;0)throw new Error('no encuentro el bloque '+re);return s.slice(i,s.indexOf('}',i));};const claro=bloque(/:root\[data-theme=&quot;light&quot;\]/);const oscuro=bloque(/:root\[data-theme=&quot;dark&quot;\]/);const nom=(b)=>new Set((b.match(/--[a-z0-9-]+(?=\s*:)/g)||[]));const c=nom(claro),o=nom(oscuro);const soloClaro=[...c].filter(x=>!o.has(x));const soloOscuro=[...o].filter(x=>!c.has(x));if(soloClaro.length||soloOscuro.length){console.error('bloques asimetricos. solo claro: '+soloClaro.join(',')+' | solo oscuro: '+soloOscuro.join(','));process.exit(1);}if(!/--app-borde-control:\s*#848889/.test(claro)){console.error('falta el borde de control medido del claro');process.exit(1);}if(!/--app-borde-control:\s*#727677/.test(oscuro)){console.error('falta el borde de control medido del oscuro');process.exit(1);}console.log('los dos bloques declaran los mismos '+c.size+' tokens')"</automated>
  </verify>
  <done>Los dos bloques de tema declaran exactamente el mismo conjunto de nombres de token. `--app-borde-control` vale `#848889` en claro y `#727677` en oscuro. Ninguna declaración previa se borró ni cambió de valor. `styles.css` sigue siendo LF.</done>
</task>

<task type="auto">
  <name>Tarea 2: fuera los 62 colores de reserva en línea</name>
  <files>styles.css</files>
  <action>
Convertir todos los `var(--token, #hex)` de `styles.css` en `var(--token)`. Al
planificar se contaron **62**, repartidos así:

| forma | veces |
|---|---|
| `var(--texto-2, #33475A)` | 22 |
| `var(--sup-calida, #FFFDF7)` | 17 |
| `var(--texto-error, #7A1B15)` | 4 |
| `var(--texto-claro, #60717D)` | 4 |
| `var(--sup-error, #FDF3F3)` | 4 |
| `var(--gris-acero, #60717D)` | 3 |
| `var(--gris-acero, #33475A)` | 2 |
| `var(--verde, #1E7B4D)`, `var(--texto-ok, #2C4A3C)`, `var(--texto-ok, #1B5638)`, `var(--texto-ok, #14532D)`, `var(--sup-ok, #F2FAF6)`, `var(--hueso, #F7F5EF)` | 1 cada uno |

Antes de quitar cada reserva, **comprobar que el token existe en los dos
bloques**. Tras la tarea 1 todos deberían existir; si alguno no está —el caso a
vigilar es `--texto-2`, `--verde` y `--hueso`, que viven en el `:root` general
de la línea 6 y puede que solo uno de los temas los redefina— **no se quita esa
reserva**: se declara primero el token que falte en el bloque donde falte,
siguiendo el mismo criterio de simetría de la tarea 1, y se anota en el SUMMARY.
Quitar una reserva de un token inexistente pinta el elemento de negro, que es
peor que el problema que se está arreglando.

**Los dos copiados mal se arreglan aquí:** `styles.css:2835` y `:2856` llevan
cruzados los valores de `--gris-acero` y `--texto-claro`. Al quitar la reserva
el error se va solo, pero hay que **mirar qué token queda** en cada uno y
confirmar que es el que la regla quería, no el que el copiado sugiere. Anotar en
el SUMMARY qué se encontró: es la prueba de por qué esta costumbre se elimina.

Al terminar, `styles.css` no puede contener **ninguna** aparición de la forma
`var(--algo, #hex)`. Los colores literales sueltos —los que no están dentro de
un `var()`— **no se tocan en este plan**: son el trabajo del plan 02-05 y
quitarlos aquí pisaría ese trabajo.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('styles.css','utf8');const r=s.match(/var\(\s*--[a-z0-9-]+\s*,[^)]*\)/g)||[];if(r.length){console.error('quedan '+r.length+' reservas en linea: '+r.slice(0,8).join(' | '));process.exit(1);}const usados=new Set((s.match(/var\(\s*(--[a-z0-9-]+)\s*\)/g)||[]).map(x=>x.replace(/var\(\s*|\s*\)/g,'')));const declarados=new Set((s.match(/--[a-z0-9-]+(?=\s*:)/g)||[]));const huerfanos=[...usados].filter(t=>!declarados.has(t));if(huerfanos.length){console.error('tokens usados y nunca declarados: '+huerfanos.join(', '));process.exit(1);}console.log('cero reservas en linea y cero tokens huerfanos')"</automated>
  </verify>
  <done>`styles.css` no contiene ninguna forma `var(--token, #hex)`. Todo token usado con `var()` está declarado en alguna parte de la hoja. Los dos cruces de `:2835` y `:2856` quedan resueltos y anotados en el SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Fronteras de confianza

| Frontera | Descripción |
|---|---|
| navegador del visitante → `styles.css` | La hoja se sirve tal cual a todo el mundo. No lleva datos ni lógica; el daño posible es visual, no de acceso. |
| repositorio → producción | Fusionar a `main` despliega. Un token mal escrito llega a producción sin que nadie lo pare, salvo por la barrera de la Fase 1. |

## Registro STRIDE

| ID | Categoría | Componente | Disposición | Mitigación |
|---|---|---|---|---|
| T-02-01 | Denial of service | legibilidad del sitio | mitigate | Quitar los colores de reserva puede pintar de negro un texto si su token no existe. La comprobación de la tarea 2 falla si queda algún token usado y no declarado, y lo hace antes de que el cambio salga de la rama. |
| T-02-02 | Tampering | `styles.css` | accept | El archivo es LF en un repositorio CRLF: una edición descuidada cambia las 4.600 líneas y hace el diff ilegible para la otra persona. Se acepta con la regla explícita de no convertir saltos de línea, y se ve en el diff del PR antes de fusionar. |
| T-02-SC | Tampering | cadena de suministro de npm | mitigate | **Este plan no instala nada.** Cero dependencias nuevas es regla dura del proyecto, así que no hay tabla de legitimidad de paquetes que auditar. |
</threat_model>

<verification>
1. Los dos bloques de tema declaran el mismo conjunto de nombres de token (comprobación automática de la tarea 1).
2. `--app-borde-control` vale `#848889` en claro y `#727677` en oscuro.
3. `styles.css` no contiene ninguna forma `var(--token, #hex)`.
4. Ningún token usado con `var()` queda sin declarar.
5. `git diff --stat styles.css` muestra decenas de líneas cambiadas, **no** 4.600: si salen 4.600, los saltos de línea se convirtieron a CRLF y hay que rehacerlo.
6. Abrir `equipos.html` en los dos temas y comprobar que nada se pintó de negro ni desapareció: esta tarea no debería cambiar **nada** de lo que se ve, porque todavía no aplica ningún token.
</verification>

<success_criteria>
- El bloque claro y el oscuro son simétricos: ningún token existe solo en uno.
- El token de borde de control existe en los dos temas con los valores medidos y aprobados.
- `--texto-claro` y `--ambar-texto` están declarados en el bloque claro.
- Cero colores de reserva en línea en toda la hoja.
- La apariencia del sitio no cambia todavía: este plan solo prepara la paleta.
- Cero dependencias nuevas.
</success_criteria>

<output>
Crear `.planning/phases/02-tema-claro-y-oscuro-coherentes/02-01-SUMMARY.md` al
terminar, incluyendo: la lista de tokens añadidos a cada bloque, qué se encontró
en los dos cruces de `:2835` y `:2856`, y cualquier token que hubiera que
declarar de más para poder quitar su reserva.
</output>

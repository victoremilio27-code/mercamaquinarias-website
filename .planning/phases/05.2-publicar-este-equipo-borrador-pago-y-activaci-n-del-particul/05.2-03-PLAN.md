---
phase: 05.2-publicar-este-equipo-borrador-pago-y-activaci-n-del-particul
plan: 03
type: execute
wave: 3
depends_on: ["05.2-02"]
files_modified:
  - tools/api.js
  - tools/pagos.js
  - tools/probar-publicacion.js
autonomous: true
requirements: [MOD-05, MOD-08]

must_haves:
  truths:
    - "`POST /api/borradores/:id/pago` valida que el borrador esté completo, calcula el importe en el servidor con `precios.precioCompra({ precioUnitario: vigente del plan elegido, cupo: 1, dias })` y registra un cobro con `intencion.tipo = 'publicacion'` y `pagos.anuncio_id`; cualquier importe que mande el navegador se ignora."
    - "Con el procesador aprobando: 201, el anuncio queda `activo` por `pagos.confirmarPago` (una suscripción de un cupo) y sale un comprobante que cuadra; con importe cero (Estándar en promoción): 201 al instante, sin comprobante, por `db.publicarBorradorSinCosto`."
    - "Con el procesador en pendiente (transferencia): 202 con la referencia y los datos de la cuenta; el anuncio sigue `borrador` con `pendiente_pago`, y pedir otra vez devuelve el MISMO pago sin crear otro."
    - "Con el procesador rechazando: 402, el anuncio sigue `borrador`, sin suscripción ni NCF, y se puede volver a pedir el pago."
    - "Marcar recibida en la consola una transferencia de publicación activa el anuncio por `confirmarPago` dentro de la bitácora; si el borrador ya no existe o ya no es borrador, 409 con un texto que manda anular, y nada queda otorgado."
    - "Al activarse una publicación (por cualquier camino que pase por `confirmarPago`) sale una sola vez el correo «ya está publicado»."
    - "El arnés prueba la sección 39: precio manipulado, activar sin pago, doble pedido de pago, doble aviso de pago, publicar dos anuncios con un pago, pagar, activar, editar, marcar vendido, historial, publicar otro con la capacidad liberada y reactivar un vendido sin capacidad (PR #33)."
  artifacts:
    - path: "tools/api.js"
      provides: "pagarBorrador y su ruta; clienteDeCompra compartido con comprarMembresia; textos de publicación; PUBLICACION_HUERFANA en la consola"
      contains: "pagarBorrador"
    - path: "tools/pagos.js"
      provides: "aviso de anuncio publicado dentro de confirmarPago para intenciones 'publicacion'"
      contains: "publicacion"
    - path: "tools/probar-publicacion.js"
      provides: "secciones 9-12"
  key_links:
    - from: "tools/api.js pagarBorrador"
      to: "db.registrarCobro({ idAnuncio }) → pagos.cobrar → pagos.confirmarPago → db.aprobarPago (rama publicacion)"
      via: "la única transición pendiente → aprobado"
      pattern: "pagos\\.cobrar\\("
    - from: "tools/api.js marcarTransferenciaRecibida"
      to: "pagos.confirmarPago con envolver = ctx.enNombreDe"
      via: "bitácora de la fase 4"
      pattern: "PUBLICACION_HUERFANA"
---

<objective>
El cobro de la publicación del particular: pedir el pago de un borrador, activarlo solo cuando el pago
se confirma (al instante, por transferencia desde la consola o a importe cero) y demostrar en el
arnés todas las pruebas de seguridad y de ciclo de vida de la sección 39 que tocan esta fase.

Purpose: MOD-05 (solo `pagos.confirmarPago` activa) y MOD-08 (ni activar sin pago, ni cobrar dos
veces, ni aceptar dos veces un aviso, ni dos anuncios con un pago, ni superar la capacidad).
Output: ruta `POST /api/borradores/:id/pago`, aviso de publicación en `tools/pagos.js`, guarda de la
consola y secciones 9-12 del arnés.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05.2-publicar-este-equipo-borrador-pago-y-activaci-n-del-particul/05.2-CONTEXT.md
@.planning/phases/05.2-publicar-este-equipo-borrador-pago-y-activaci-n-del-particul/05.2-01-SUMMARY.md
@.planning/phases/05.2-publicar-este-equipo-borrador-pago-y-activaci-n-del-particul/05.2-02-SUMMARY.md
@CLAUDE.md

<interfaces>
Del plan 05.2-01 (tools/db.js): `registrarCobro({ idOrg, idSusc, idAnuncio, cobro, intencion })` (409 'Ese anuncio ya tiene un pago en espera' si ya hay un pendiente), `aprobarPago` con rama `publicacion` (404 si el borrador no existe, 409 si ya no es borrador), `publicarBorradorSinCosto({ idAnuncio, idOrg, idPlan, dias, cobro })` → `{ membresia, anuncio }`, `pagoPendienteDeAnuncio(idAnuncio)`, `borradorDe(idAnuncio, idOrg)`.

Del plan 05.2-02 (tools/api.js): `validarCamposAnuncio(c, plan, { completo })`, `cuerpoDeBorrador(b)`, `borradorPublico(b)`, `soloParticular(ctx, res)`, rutas `/api/borradores` (POST, GET, PUT). Helpers del arnés: `pedir`, `cuentaCon`, `subirFoto`, `siguienteB02`.

De tools/api.js (hoy):
- `comprarMembresia`: aceptación `PARA_PAGAR`; compone `cobro`; procesador con `pagos.procesadorDeCobro(c.metodo)` (400 antes de anotar nada si no vale); datos fiscales `conRnc` / `rnc` (`rncValido`) / `razonSocial` / `direccionFiscal` validados ANTES de cobrar; cero → otorga al instante; con importe → `db.registrarCobro` + `await pagos.cobrar(pago)`; `rechazado` → 402 `NO_APROBADO`; pendiente + transferencia → 202 con `aviso: EN_ESPERA_TRANSFERENCIA`, `transferencia: datosDeCuenta()` y `avisarTransferenciaPedida(ctx, { referencia, total, concepto })`; pendiente sin transferencia → 202 `EN_PROCESO`; aprobado → 201 con `comprobantePublico(r.comprobante)`.
- `pagoPublico(pago)`, `comprobantePublico(c)`, `cobroPublico(cobro)` (05.1), `referenciaCobro()`, `intencionDePago(pago)`, `sinEsperar(que, envio)`, `datosDeCuenta()`, `SIN_DATOS_TRANSFERENCIA`.
- `marcarTransferenciaRecibida` (conAdminEnNombreDe 'pago.transferencia_recibida'): `pagoDeTransferencia` → 409 si rechazado/devuelto → pre-comprobación de la ampliación huérfana (`AMPLIACION_HUERFANA`) → `pagos.confirmarPago(idPago, { envolver: ... ctx.enNombreDe(...) })` → `catch` que traduce el 404 de la ampliación en 409.
- `cambiarEstado` y `db.cambiarEstadoAnuncio`: la guarda de capacidad al reactivar (`r.sinCupo` → 409) es del PR #33 (commit `524b2a3`, rama `claude/seguridad-reactivar-vendido`).

De tools/pagos.js: `confirmarPago(idPago, { envolver })` → `{ pago, membresia, comprobante, yaEstaba }`; `emitir(pago, intencion, membresia)`; `leerIntencion(pago)`; `PROCESADORES` (se consulta en el momento: la prueba puede sustituir `demo`); `cobrar(pago)`.

De tools/correo.js: `enviarAnuncioPublicado({ para, nombre, equipo, idAnuncio, vence, plan })` (no lanza; devuelve promesa con Brevo u objeto con el transporte de archivo). `correo.js` no requiere `pagos.js`: requerirlo desde pagos no crea un ciclo.

De tools/probar-transferencia.js (para copiar, no se modifica): `encender()`/`apagar()` de las variables `MERCA_TRANSFERENCIA_*` (se leen en cada llamada), admin con `db.crearCuenta` + `db.marcarAdmin(correo, true)`, `comoAdmin` con `cf-connecting-ip`, `recibido(idPago)` = `POST /api/admin/pagos/:id/recibido`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarea 1: pedir el pago de un borrador y activarlo solo con el pago confirmado</name>
  <files>tools/probar-publicacion.js, tools/api.js, tools/pagos.js</files>
  <read_first>
    - tools/api.js: `comprarMembresia` entero, `NO_APROBADO`/`EN_PROCESO`/`EN_ESPERA_TRANSFERENCIA`, `pagoPublico`, `comprobantePublico`, `cobroPublico`, `avisarTransferenciaPedida`, `datosDeCuenta`, y lo que dejó el plan 02 (`borradorPublico`, `cuerpoDeBorrador`, `soloParticular`, rutas del borrador)
    - tools/pagos.js entero
    - tools/correo.js: `enviarAnuncioPublicado` y cómo el transporte de archivo deja los correos en la bandeja (para buscarlos en la prueba)
    - tools/probar-publicacion.js tal como lo dejó el plan 02
    - .planning/phases/05.2-publicar-este-equipo-borrador-pago-y-activaci-n-del-particul/05.2-CONTEXT.md (D-09 a D-13)
  </read_first>
  <behavior>
    - Sección 9 (incompleto): `POST /api/borradores/:id/pago` sobre un borrador sin fotos → 400 con el motivo de la validación y ningún pago en `pagos` con ese `anuncio_id`; con 25 fotos guardadas bajo Premium y el plan cambiado a Estándar (8) → 409 con 'admite 8 fotografías'; con el plan elegido retirado a mano (`activo = 0`) → 409.
    - Sección 9 (aprobado, precio manipulado): borrador completo Destacado/30, cuerpo `{ total: 1, subtotal: 1, precio: 1, base: 1, cupo: 5, plan: 'estandar', dias: 60 }` → 201; el pago tiene `total === precios.precioCompra({ precioUnitario: <vigente destacado>, cupo: 1, dias: 30 }).total`, `anuncio_id` = el borrador, intención `publicacion` con `cupo: 1`; `datos.cobro` sin `base`/`ajuste`; `datos.anuncio.estado === 'activo'`; una suscripción nueva de 1 cupo Destacado 30 días; factura B02 con `subtotal + itbis === total`; en la bandeja, exactamente un correo nuevo «ya está publicado» para ese equipo.
    - Sección 9 (doble aviso): `pagos.confirmarPago(idPago)` otra vez → `yaEstaba: true`; sigue una suscripción, una factura y un solo correo «ya está publicado».
    - Sección 10 (pendiente): con `pagos.PROCESADORES.demo` devolviendo `pendiente` (restaurado en `finally`): → 202, `datos.pago.estado === 'pendiente'`, anuncio `borrador`; `GET /api/mis-anuncios` lo marca `pendiente_pago: true`; un segundo `POST .../pago` → 202 con el MISMO `datos.pago.id` y una sola fila pendiente; `PUT /api/borradores/:id` → 409; luego `pagos.confirmarPago(id)` → anuncio `activo`.
    - Sección 10 (rechazado): con `demo` devolviendo `rechazado`: → 402 con 'sigue guardado como borrador'; anuncio `borrador`, `suscripcion_id` NULL, B02 y facturas sin cambio; restaurado `demo`, un nuevo `POST .../pago` → 201 y el anuncio se activa.
    - Sección 10 (importe cero): borrador Estándar/30 con la promoción vigente → 201, `datos.comprobante === null`, pago `aprobado` de total 0 con procesador `'sin-costo'` y `anuncio_id`, anuncio `activo`, sin factura.
    - Sección 10 (ajeno y no borrador): `POST .../pago` con sesión de otra organización → 404; sobre un anuncio ya `activo` → 404; sin aceptar las condiciones de contratación → 409.
  </behavior>
  <action>
    RED: añadir las secciones 9 y 10 de `<behavior>` a `tools/probar-publicacion.js` (buscar los correos en la bandeja por el nombre del equipo con el sello de la pasada, como hace probar-transferencia con las referencias). Correr y ver en rojo.

    GREEN en `tools/api.js` (per D-09, D-11, D-12, D-13):
    - Extraer de `comprarMembresia` `clienteDeCompra(c, ctx)` → `{ cliente } | { error }` con la validación fiscal de hoy (mismos textos y orden); `comprarMembresia` la usa y responde 400 con el error igual que antes.
    - Textos nuevos con comentario de por qué no reutilizan los de la compra (hablan de cupos): `NO_APROBADO_PUBLICACION = 'El pago no fue aprobado. No se le cobró nada, su anuncio sigue guardado como borrador y no se emitió comprobante.'`, `EN_PROCESO_PUBLICACION = 'Su pago está en proceso. Su anuncio se publica cuando se confirme.'`, `EN_ESPERA_PUBLICACION = 'Transfiera el importe con la referencia indicada. Su anuncio se publica y el comprobante fiscal llega cuando confirmemos el ingreso.'`.
    - `pagarBorrador = conSesion(async (req, res, ctx, idAnuncio) => ...)`, en este orden: `exigirAceptacion(res, ctx.usuario.id, [...legales.PARA_PUBLICAR, ...legales.PARA_PAGAR])` sin duplicados; `soloParticular`; `db.permitir('pedir-pago:' + usuario, 20, 60)` → 429; leer el cuerpo; `b = db.borradorDe(idAnuncio, org.id)` → 404 'Ese borrador no es suyo o no existe'; si `b.pagoPendiente`, responder 202 con ese pago (`pago: pagoPublico`, `cobro: { total, referencia }`, `aviso` según su procesador y `transferencia: datosDeCuenta()` si es transferencia, o `avisoTransferencia: SIN_DATOS_TRANSFERENCIA` si se apagó) SIN crear nada (D-12); plan elegido activo o 409 'Ese plan ya no se ofrece. Elija otro para publicar este equipo.'; fotos o videos por encima del tope del plan → 409 'Este plan admite N fotografías y su borrador tiene M. Quite X o elija otro plan.' (igual con videos); `validarCamposAnuncio(cuerpoDeBorrador(b), plan, { completo: true })` → 400 con su error; `cobro = { ...precios.precioCompra({ precioUnitario: precioUnitario(plan), cupo: 1, dias: b.dias_elegidos }), referencia: referenciaCobro() }` — del cuerpo solo se leen `metodo` y los datos fiscales, nunca importes, plan, cupo ni días (comentario); `clienteDeCompra` → 400; si `!(cobro.total > 0)`: `db.publicarBorradorSinCosto(...)`, correo `enviarAnuncioPublicado` con `sinEsperar`, 201 `{ anuncio, membresia, cobro: cobroPublico(cobro), comprobante: null, pago: pagoPublico(db.pagoPorReferencia(cobro.referencia)) }`; con importe: `cobro.procesador = pagos.procesadorDeCobro(c.metodo)` (400 si no vale, antes de anotar); `db.registrarCobro({ idOrg, idAnuncio, cobro, intencion: { tipo: 'publicacion', idAnuncio, idPlan: plan.id, cupo: 1, dias, concepto: 'Publicación ' + plan.nombre + ' · ' + equipo + ' · ' + dias + ' días', cliente, correoCliente: ctx.usuario.correo } })` y, si lanza 409 (carrera con otro pedido), responder como el caso de pago pendiente existente; `r = await pagos.cobrar(pago)`; `rechazado` → 402 `NO_APROBADO_PUBLICACION` con `pago`; pendiente + transferencia → 202 con `EN_ESPERA_PUBLICACION`, datos de la cuenta y `avisarTransferenciaPedida`; pendiente sin transferencia → 202 `EN_PROCESO_PUBLICACION`; aprobado → 201 `{ anuncio: db.anuncio(idAnuncio), membresia: r.membresia, cobro: cobroPublico(cobro), comprobante: comprobantePublico(r.comprobante), pago: pagoPublico(r.pago) }`. El `equipo` del concepto es `anio + nombre de marca + modelo`.
    - Ruta `['POST', /^\/api\/borradores\/([\w-]+)\/pago$/, pagarBorrador]` junto a las del borrador (antes de cualquier patrón más genérico).

    GREEN en `tools/pagos.js`: requerir `./correo`; tras `emitir` en `confirmarPago`, si `!yaEstaba`, el pago está `aprobado` y `leerIntencion(pago).tipo === 'publicacion'`, mandar `correo.enviarAnuncioPublicado` con el anuncio (`db.anuncio(intencion.idAnuncio)`), `para: intencion.correoCliente`, el nombre de `intencion.cliente.razonSocial`, el plan (`db.planPorId(intencion.idPlan).nombre`) y `vence`, envuelto como `sinEsperar` de api.js (try + `Promise.resolve(...).catch`), sin que un fallo afecte al pago. Comentario: va aquí y no en la ruta porque la transferencia se confirma desde la consola y CardNet lo hará desde su aviso; los tres pasan por esta función, y `yaEstaba` impide un segundo correo.
    Editar con la herramienta de edición (CRLF).
  </action>
  <verify>
    <automated>npm run publicacion:probar &amp;&amp; npm run pagos:probar &amp;&amp; npm run transferencia:probar &amp;&amp; npm run facturas:probar &amp;&amp; npm run seguridad:probar</automated>
  </verify>
  <acceptance_criteria>
    - Los cinco scripts de `<verify>` salen con código 0.
    - `grep -c "pagarBorrador\]" tools/api.js` = 1 (la ruta está en `RUTAS`) y `grep -c "borradores.*pago\$/, pagarBorrador" tools/api.js` = 1.
    - `grep -c "tipo: 'publicacion'" tools/api.js` = 1.
    - `grep -c "enviarAnuncioPublicado" tools/pagos.js` = 1.
    - `grep -c "function clienteDeCompra\|const clienteDeCompra" tools/api.js` = 1.
    - La salida de `npm run publicacion:probar` contiene «9.» y «10.» y ninguna línea «FALLA».
  </acceptance_criteria>
  <done>El particular paga la publicación de su borrador con el importe que calcula el servidor; el anuncio se activa solo al confirmarse el pago, una sola vez, con su comprobante y su correo; pendiente, rechazado e importe cero se comportan como dice D-11 a D-13.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarea 2: la transferencia desde la consola y la batería de seguridad y ciclo de vida de la sección 39</name>
  <files>tools/probar-publicacion.js, tools/api.js</files>
  <read_first>
    - tools/api.js: `marcarTransferenciaRecibida`, `AMPLIACION_HUERFANA`, `pagoDeTransferencia`, `falloInterno`, `cambiarEstado` y `publicar`
    - tools/db.js: `cambiarEstadoAnuncio` (si contiene `sinCupo`, el PR #33 ya está en la rama)
    - tools/probar-transferencia.js: `encender`/`apagar`, la creación del admin y `comoAdmin`/`recibido` (buscar `IP_ADMIN`)
    - tools/probar-publicacion.js tal como lo dejó la tarea 1
  </read_first>
  <behavior>
    - Sección 11 (transferencia, D-13): con la transferencia encendida (`encender()` de la prueba de transferencia, apagada al final): `POST .../pago` → 202 con `transferencia.cuenta` y `pago.estado === 'pendiente'`, anuncio `borrador`; la administradora hace `POST /api/admin/pagos/:id/recibido` → 200, anuncio `activo` con una suscripción de 1 cupo, factura B02 que cuadra y UNA fila nueva en `bitacora_admin` con acción `pago.transferencia_recibida`; repetir el marcado → 200 con `yaEstaba: true` y nada nuevo.
    - Sección 11 (huérfana): otro borrador con su transferencia pendiente cuyo anuncio se pasa a mano a `retirado` → marcar recibido → 409 con 'Anule el pago', pago sigue `pendiente`, sin suscripción nueva ni fila de bitácora. Anularlo (`POST /api/admin/pagos/:id/anular` con motivo) → 200 y el anuncio no se toca.
    - Sección 12 (seguridad): «publicar dos anuncios con un pago»: tras activar el anuncio A de un particular sin otras membresías, `POST /api/anuncios` con `membresia` = la suscripción de A y un anuncio completo → 409 y no se crea anuncio; `POST /api/borradores/<A>/pago` → 404 (ya no es borrador); el pago de A confirmado otra vez → `yaEstaba`.
    - Sección 12 (ciclo del particular): «editar» tras activar: `PATCH /api/anuncios/<A>/disponibilidad { disponibilidad: 'bajo-pedido' }` → 200; «marcar vendido»: `PATCH /api/anuncios/<A> { estado: 'vendido' }` → 200 con `capacidadLibre: true`; «historial»: `GET /api/mis-anuncios` sigue devolviendo A como `vendido` con su foto y su plan; «publicar otro»: `POST /api/anuncios` con otro equipo completo (sin `membresia`) → 201 en la capacidad que dejó A; un tercero → 402/409 (la suscripción era de un cupo); «reutilización simultánea»: `PATCH /api/anuncios/<A> { estado: 'activo' }` → 409 y A sigue `vendido`.
  </behavior>
  <action>
    Paso 0, antes de escribir nada: comprobar si el PR #33 está en la rama con `grep -c "sinCupo" tools/db.js`. Si da 0: si `git branch -r --contains 524b2a3` incluye `origin/main`, fusionar `origin/main` en la rama (sin reescribir historial, sin --force); si main todavía no lo tiene, `git cherry-pick 524b2a3` (mismo parche que el PR; al fusionar main después no choca) y anotarlo en el SUMMARY. Nunca reimplementar esa guarda a mano en esta fase: es del PR #33 y dos versiones distintas acabarían en conflicto.

    RED: añadir las secciones 11 y 12 de `<behavior>` a `tools/probar-publicacion.js`, copiando `encender`/`apagar` (con los mismos datos falsos `PRUEBA`) y la creación de la administradora de probar-transferencia.js. Correr y ver en rojo lo que falte.

    GREEN en `tools/api.js` (per D-13):
    - Constante `PUBLICACION_HUERFANA = 'El borrador de esta publicación ya no existe o ya se publicó. No se publicó nada ni se emitió comprobante. Anule el pago y devuelva la transferencia al cliente.'` junto a `AMPLIACION_HUERFANA`, con comentario.
    - En `marcarTransferenciaRecibida`: `esPublicacion = intencion.tipo === 'publicacion'`; con el pago `pendiente` y `esPublicacion`, si `db.anuncio(intencion.idAnuncio)` no existe, es de otra organización o su `estado !== 'borrador'` → 409 `PUBLICACION_HUERFANA` antes de escribir (sin fila de bitácora). En el `catch`, un 404 o 409 con `esPublicacion` → 409 `PUBLICACION_HUERFANA` (la carrera: el SAVEPOINT ya deshizo suscripción y activación, y la bitácora no queda). Nada más cambia en la consola.
    Si alguna comprobación de la sección 12 falla por algo que no sea de esta fase (p. ej. la guarda del PR #33), no silenciarla: arreglar la causa dentro de esta fase o, si es del PR #33, volver al paso 0.
    Editar con la herramienta de edición (CRLF).
  </action>
  <verify>
    <automated>npm run publicacion:probar &amp;&amp; npm run transferencia:probar &amp;&amp; npm run bitacora:probar &amp;&amp; npm run seguridad:probar &amp;&amp; npm run pagos:probar &amp;&amp; npm run facturas:probar</automated>
  </verify>
  <acceptance_criteria>
    - Los seis scripts de `<verify>` salen con código 0.
    - `grep -c "sinCupo" tools/db.js` ≥ 1 (el PR #33 está en la rama).
    - `grep -c "PUBLICACION_HUERFANA" tools/api.js` ≥ 3 (definición y dos usos).
    - La salida de `npm run publicacion:probar` contiene «11.» y «12.» y ninguna línea «FALLA».
  </acceptance_criteria>
  <done>La transferencia de una publicación se activa desde la consola por la misma transición y con bitácora; un pago cuyo borrador ya no existe no otorga nada; el arnés cubre de punta a punta la seguridad y el ciclo del particular de la sección 39.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → POST /api/borradores/:id/pago | El cuerpo puede traer cualquier importe, plan o cupo |
| procesador / consola → confirmarPago | El aviso de pago puede repetirse |
| consola → pago de otra organización | Escritura en nombre de otro |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.2-12 | Tampering | pagarBorrador | mitigate | Importe de `precios.precioCompra` con el plan y los días GUARDADOS en el borrador; del cuerpo solo `metodo` y datos fiscales; `registrarCobro` exige el desglose de la 05.1; sección 9 con `total: 1`, `cupo: 5`, `plan` y `dias` falsos |
| T-05.2-13 | Spoofing | activación sin pago | mitigate | Solo `aprobarPago` (vía `confirmarPago`) y `publicarBorradorSinCosto` (`soloCero`) llaman a `activarBorrador`; secciones 9-10 |
| T-05.2-14 | Repudiation | doble pedido / doble aviso | mitigate | Pendiente existente devuelto sin crear otro (índice único + 409 capturado); `yaEstaba` en la confirmación; un solo correo y un solo NCF; secciones 9-11 |
| T-05.2-15 | Elevation of privilege | dos anuncios con un pago / reactivar vendido | mitigate | Suscripción de 1 cupo; 409 al publicar en ella ocupada; PR #33 al reactivar; sección 12 |
| T-05.2-16 | Tampering (fiscal) | rechazo y huérfana | mitigate | Rechazo sin NCF; huérfana 409 antes de escribir y ROLLBACK del SAVEPOINT; secciones 10-11 |
| T-05.2-17 | Repudiation | consola | mitigate | `marcarTransferenciaRecibida` sigue dentro de `conAdminEnNombreDe` (bitácora con IP de CF-Connecting-IP); sección 11 cuenta la fila |
| T-05.2-SC | Tampering | instalación de paquetes | accept | No se instala nada |
</threat_model>

<verification>
- `npm run publicacion:probar`, `pagos:probar`, `transferencia:probar`, `facturas:probar`, `seguridad:probar`, `bitacora:probar` en verde.
</verification>

<success_criteria>
- MOD-05: pedir el pago → confirmarPago → activo; ningún otro camino.
- MOD-08: el arnés prueba precio manipulado, activar sin pago, doble pago, doble aviso, dos anuncios con un pago y reactivar sin capacidad.
</success_criteria>

<output>
Create `.planning/phases/05.2-publicar-este-equipo-borrador-pago-y-activaci-n-del-particul/05.2-03-SUMMARY.md` when done
</output>

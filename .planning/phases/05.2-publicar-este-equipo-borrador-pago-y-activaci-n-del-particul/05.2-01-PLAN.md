---
phase: 05.2-publicar-este-equipo-borrador-pago-y-activaci-n-del-particul
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/db.js
  - db/schema.sql
  - tools/probar-publicacion.js
  - package.json
  - .github/workflows/desplegar.yml
autonomous: true
requirements: [MOD-05, MOD-06, MOD-08]

must_haves:
  truths:
    - "Tras abrir la base, `anuncios` tiene `plan_elegido` y `dias_elegidos`, `pagos` tiene `anuncio_id`, y existen los índices `ix_pagos_anuncio` y `ux_pagos_anuncio_pendiente` (único, parcial sobre `estado = 'pendiente'`); la migración `2026-09-borradores` es la ÚLTIMA de `MIGRACIONES` y va detrás de las dos de la 05.1."
    - "`db.crearBorrador` deja un anuncio `borrador` sin membresía (`suscripcion_id` NULL), con el plan y los días elegidos, que no aparece en `db.buscarAnuncios` ni en `db.anunciosPublicos`."
    - "`db.guardarBorrador` guarda por partes solo en un borrador propio y sin pago pendiente; en uno ajeno, en uno activo o en uno con pago pendiente no cambia nada."
    - "Confirmar el pago de una intención `publicacion` (por `pagos.confirmarPago`) crea UNA suscripción de un cupo del plan elegido y pasa el anuncio a `activo` con `suscripcion_id`, `vence`, `destacado_hasta` y `publicado`, todo en el mismo SAVEPOINT; repetirlo no crea otra suscripción ni toca el anuncio."
    - "Un segundo pago `pendiente` para el mismo anuncio es imposible (índice único); aprobar un pago cuyo anuncio ya no es borrador lanza 409 y no deja suscripción."
    - "Un pago rechazado deja el anuncio en `borrador`, sin suscripción y sin comprobante."
    - "`db.publicarBorradorSinCosto` activa al instante con un pago aprobado de importe cero (`sin-costo`, sin NCF) y `soloCero` sigue impidiendo colar un importe por ahí."
    - "`db.anunciosDeOrganizacion` devuelve `plan_elegido`, `dias_elegidos`, `plan_elegido_nombre`, `pago_pendiente` (referencia) y `pendiente_pago` (booleano derivado, D-04)."
  artifacts:
    - path: "tools/db.js"
      provides: "migración '2026-09-borradores' al final; ANIO_SIN_DEFINIR, crearBorrador, guardarBorrador, borradorDe, contarBorradores, pagoPendienteDeAnuncio, publicarBorradorSinCosto, activarBorrador (interna), rama 'publicacion' de aprobarPago, idAnuncio en registrarCobro/anotarPago"
      contains: "'2026-09-borradores'"
    - path: "db/schema.sql"
      provides: "columnas plan_elegido, dias_elegidos en anuncios y anuncio_id en pagos (sin índices: los índices van solo en la migración)"
      contains: "plan_elegido"
    - path: "tools/probar-publicacion.js"
      provides: "arnés nuevo de la fase, secciones 1-4 (base de datos)"
      contains: "process.exitCode"
    - path: "package.json"
      provides: "script publicacion:probar"
      contains: "\"publicacion:probar\": \"node tools/probar-publicacion.js\""
    - path: ".github/workflows/desplegar.yml"
      provides: "paso de CI en el trabajo pruebas"
      contains: "npm run publicacion:probar"
  key_links:
    - from: "tools/db.js aprobarPago (tipo publicacion)"
      to: "otorgarCompra + activarBorrador"
      via: "mismo SAVEPOINT aprobar_pago"
      pattern: "intencion\\.tipo === 'publicacion'"
    - from: "tools/db.js registrarCobro"
      to: "pagos.anuncio_id"
      via: "INSERT con idAnuncio; el índice único parcial frena un segundo pendiente"
      pattern: "anuncio_id"
---

<objective>
La base de datos del modelo «publicar este equipo»: el anuncio nace como borrador en el servidor,
el pago sabe a qué anuncio pertenece, y la única transición de dinero (`db.aprobarPago`, llamada solo
desde `pagos.confirmarPago`) aprende a activar ese borrador con una suscripción de un cupo.

Purpose: MOD-05 (borrador → pago → activación, solo por confirmarPago), la parte de base de MOD-06
(el borrador no sale en listados públicos) y de MOD-08 (ni dos pendientes, ni dos aprobados, ni
activar sin pago). Todo lo demás de la fase se construye encima de estas funciones.
Output: una migración al final de `MIGRACIONES`, funciones nuevas en `tools/db.js`, el arnés nuevo
`tools/probar-publicacion.js` con su script y su paso de CI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.2-publicar-este-equipo-borrador-pago-y-activaci-n-del-particul/05.2-CONTEXT.md
@.planning/research/auditoria-modelo-comercial.md
@CLAUDE.md

<precondicion>
La fase 05.1 está terminada en esta rama: `tools/db.js` ya tiene las migraciones
`'2026-09-pagos-desglose'` y `'2026-09-precios-base'`, `registrarCobro` exige un cobro salido de
`precios.desglose` (con `base`, `ajuste`) y `aprobarPago` usa `precioPactado: pago.base ?? pago.subtotal`.
Si falta algo de eso, parar y decirlo: esta fase no se ejecuta sobre la 05.1 a medias.
</precondicion>

<interfaces>
De tools/db.js (buscar por nombre; los números de línea cambian con la 05.1):
- `MIGRACIONES`: array de `[nombre, [sentencias]]`; tras la 05.1 las dos últimas son `'2026-09-pagos-desglose'` y `'2026-09-precios-base'`. `migrar()` corre cada una en transacción y tolera «duplicate column».
- `abrir()` ejecuta `db/schema.sql` ENTERO y DESPUÉS `migrar()`. Consecuencia: un `CREATE INDEX` sobre una columna nueva NO puede ir en schema.sql (en la base de producción la columna aún no existe cuando corre schema.sql y el arranque fallaría). Los índices nuevos van solo en la migración.
- `anuncios` (schema.sql): `categoria`, `marca`, `modelo` son `TEXT NOT NULL` y `anio INTEGER NOT NULL`; los disparadores `tr_anuncios_valores_ins/upd` abortan con `anio < 1900` o `> año actual + 1`. El CHECK de `estado` ya admite `'borrador'`.
- `ESTADOS_QUE_OCUPAN = "('activo', 'pausado')"` — un borrador no ocupa capacidad.
- `crearAnuncio(datos)`: INSERT con BEGIN/COMMIT, fotos en `anuncio_fotos (id, anuncio_id, url, miniatura, orden, creada)`, videos en `anuncio_videos (id, anuncio_id, url, poster, duracion, orden, creada)`, teléfonos en `anuncio_contactos (id, anuncio_id, numero, tipo, nota, orden)`. Nombres de campos de `datos`: idOrg, idSucursal, idUsuario, idSuscripcion, categoria, subcategoria, marca, modelo, anio, condicion, usoValor, usoUnidad, serie, potencia, peso, implementos, descripcion, provincia, municipio, precio, moneda, modalidadPrecio, precioMinimo, itbisIncluido, permuta, financiamiento, video, disponibilidad, motorMarca, motorModelo, transmisionMarca, transmisionModelo, destacadoHasta, vence, fotos, videos, telefonos.
- `copiaDeAnuncio(idAnuncio, idOrg)`: `SELECT *` + fotos {url, miniatura} + videos {url, poster, duracion} + telefonos {numero, tipo, nota}, pasado por `conNombres`.
- `otorgarCompra(d, { idOrg, plan, cupo, dias, precioPactado, t })` → idSusc; va dentro de la transacción de quien llama.
- `anotarPago(d, { idOrg, idSusc, cobro, t })`: solo importe cero (`soloCero`); INSERT 'aprobado', procesador 'sin-costo' si total 0 (la 05.1 añadió base/ajuste/ajuste_tasa/itbis_tasa).
- `comprarCupos({ idOrg, idPlan, cupo, dias, cobro })`: patrón BEGIN → otorgarCompra → anotarPago → COMMIT.
- `registrarCobro({ idOrg, idSusc = null, cobro, intencion })` → fila del pago pendiente (`pagoPorId`).
- `aprobarPago(idPago)`: SAVEPOINT `aprobar_pago`; si el pago no está `pendiente` devuelve `{ pago, membresia, yaEstaba: true }`; ramas `compra` y `ampliacion`; luego `UPDATE pagos SET estado = 'aprobado' ... WHERE estado = 'pendiente'` con 409 si no cambia una fila; devuelve `{ pago, membresia, yaEstaba: false }`.
- `rechazarPago(idPago)`, `pagoPorId`, `planPorId(id)` (con `precio_vigente`, `destacado`, `fotos_maximas`, `videos_maximos`, `activo`), `suscripcion(idSusc, idOrg)` (con `fin`, `destacado`, `libres`), `sucursalPrincipal(idOrg)`, `anunciosDeOrganizacion(idOrg)` (la consulta del panel, mapeada con `conNombres`), `buscarAnuncios(f)`, `anunciosPublicos(f)`, `borrarAnuncio(idAnuncio, idOrg)`, `id()`, `ahora()`, `sumarDias(n)`.

De tools/pagos.js (no se modifica en este plan): `confirmarPago(idPago, { envolver })` llama a `db.aprobarPago` y después emite con `emitir(pago, intencion, membresia)`, que usa `intencion.cupo` (para una publicación, 1) y `pago.subtotal`. `cobrar(pago)` consulta `PROCESADORES[pago.procesador]` en el momento. `rechazarPago(idPago)`.

De assets/precios.js (tras la 05.1): `precioCompra({ precioUnitario, cupo, dias })` y `desglose(base)` → `{ base, ajusteTasa, ajuste, subtotal, itbisTasa, itbis, total, ... }`.

Patrón de arnés a copiar (tools/probar-pagos.js): variables de entorno ANTES de `require('./db')` (MERCA_DB, MERCA_FACTURAS, MERCA_FOTOS, MERCA_VIDEOS, MERCA_CORREO='archivo', MERCA_SECRETO), carpeta `.tmp/<nombre>` borrada y recreada, `ok(condicion, texto)` con contadores, `lanza(fn)`, `consulta`/`ejecuta` con una `DatabaseSync` aparte, carga de una secuencia B02 de prueba con `db.cargarSecuencia` y `siguienteB02()`, cuerpo async en un IIFE y `process.exitCode = fallos ? 1 : 0` al final.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarea 1: migración, borrador en la base y el arnés nuevo con su paso de CI</name>
  <files>tools/probar-publicacion.js, package.json, .github/workflows/desplegar.yml, tools/db.js, db/schema.sql</files>
  <read_first>
    - tools/db.js: cabecera de `MIGRACIONES` y sus últimas entradas (buscar `'2026-09-precios-base'`), `migrar`, `abrir`, `crearAnuncio`, `anuncio`, `anunciosDeOrganizacion`, `borrarAnuncio`, `copiaDeAnuncio`, `module.exports`
    - db/schema.sql: `CREATE TABLE IF NOT EXISTS anuncios` y los disparadores `tr_anuncios_valores_*`, `CREATE TABLE IF NOT EXISTS pagos`
    - tools/probar-pagos.js líneas 1-130 (cabecera, entorno, helpers) y la zona de `siguienteB02` / `cargarSecuencia`
    - package.json y .github/workflows/desplegar.yml (trabajo `pruebas`; el archivo va en LF según .gitattributes)
    - .planning/phases/05.2-publicar-este-equipo-borrador-pago-y-activaci-n-del-particul/05.2-CONTEXT.md (D-03, D-04, D-05, D-06)
  </read_first>
  <behavior>
    - Sección 1: `PRAGMA table_info(anuncios)` tiene `plan_elegido` y `dias_elegidos`; `PRAGMA table_info(pagos)` tiene `anuncio_id`; `sqlite_master` tiene `ix_pagos_anuncio` y `ux_pagos_anuncio_pendiente` (este último con `UNIQUE` en su `sql`); la tabla `migraciones` tiene `2026-09-borradores`.
    - Sección 2: `db.crearBorrador({ idOrg, idUsuario, idPlan: 'destacado', dias: 60 })` devuelve un id; la fila tiene `estado = 'borrador'`, `suscripcion_id` NULL, `plan_elegido = 'destacado'`, `dias_elegidos = 60`, `anio = db.ANIO_SIN_DEFINIR`, `categoria = ''`, `publicado` NULL, `sucursal_id` = la principal; `dias` distinto de 60 se guarda como 30.
    - Sección 2: `db.guardarBorrador(id, idOrg, { modelo: '320D', anio: 2018, fotos: [{url, miniatura}×2], telefonos: [...] })` devuelve `{ ok: true }` y deja esos valores y dos filas en `anuncio_fotos`; una segunda llamada con `fotos` de una sola foto deja UNA fila (reemplaza); una llamada sin `fotos` no toca las fotos; `{ modelo: null }` deja `modelo = ''` y `{ anio: null }` deja el centinela (nunca viola el NOT NULL ni el disparador).
    - Sección 2: `db.guardarBorrador` con otra organización devuelve `{ ok: false, motivo: 'no-existe' }` y no cambia nada; sobre un anuncio `activo` también `'no-existe'`.
    - Sección 2: `db.borradorDe(id, idOrg)` devuelve el borrador con `fotos`, `videos`, `telefonos`, `plan_elegido`, `dias_elegidos` y `pagoPendiente: null`; con otra organización o sobre un anuncio activo, `null`. `db.contarBorradores(idOrg)` cuenta solo los borradores de esa organización.
    - Sección 2: el borrador no aparece en `db.buscarAnuncios({})` ni en `db.anunciosPublicos({ porPagina: 5000 })`; sí en `db.anunciosDeOrganizacion(idOrg)` con `plan_elegido`, `dias_elegidos`, `plan_elegido_nombre` ('Destacado'), `pago_pendiente` null y `pendiente_pago === false`.
  </behavior>
  <action>
    RED primero. Crear `tools/probar-publicacion.js` con la cabecera de comentario al estilo del proyecto (qué falló antes: el borrador vivía solo en el navegador y el anuncio se creaba ocupando un cupo ya pagado; el modelo comercial pide que el anuncio exista como borrador en el servidor y se active solo con el pago confirmado), base desechable en `.tmp/prueba-publicacion`, entorno ANTES de cualquier `require('./db')` (MERCA_DB, MERCA_FACTURAS, MERCA_FOTOS, MERCA_VIDEOS dentro de esa carpeta, MERCA_CORREO='archivo', MERCA_SECRETO de prueba, y borrar toda variable `MERCA_TRANSFERENCIA*` heredada como hace probar-transferencia.js), helpers `ok`, `lanza`, `consulta`, `ejecuta`, `prepararOrganizacion(idOrg, nombre, tipo)`, `referencia(etiqueta)` con sello por pasada, la secuencia B02 de prueba (`db.cargarSecuencia`, con `hasta` holgado, p. ej. 200) y `siguienteB02()` copiados de probar-pagos.js, un IIFE async y `process.exitCode = fallos ? 1 : 0` con el resumen «N comprobaciones, M fallos». Escribir las secciones 1 y 2 de `<behavior>` con encabezados numerados en consola. Añadir `"publicacion:probar": "node tools/probar-publicacion.js"` a `package.json` (junto a `transferencia:probar`) y, en `.github/workflows/desplegar.yml`, un paso `- name: Pruebas de publicación del particular` con `run: npm run publicacion:probar` justo después del paso de `transferencia:probar` del trabajo `pruebas`, con la misma sangría; editar ese YAML con la herramienta de edición conservando LF. Correr `npm run publicacion:probar` y verlo en rojo.

    GREEN en `tools/db.js` (per D-03, D-04, D-05, D-06):
    - Añadir AL FINAL de `MIGRACIONES`, detrás de `'2026-09-precios-base'` y sin tocar ninguna anterior, `['2026-09-borradores', [...]]` con: `ALTER TABLE anuncios ADD COLUMN plan_elegido TEXT`, `ALTER TABLE anuncios ADD COLUMN dias_elegidos INTEGER`, `ALTER TABLE pagos ADD COLUMN anuncio_id TEXT`, `CREATE INDEX IF NOT EXISTS ix_pagos_anuncio ON pagos (anuncio_id) WHERE anuncio_id IS NOT NULL`, `CREATE UNIQUE INDEX IF NOT EXISTS ux_pagos_anuncio_pendiente ON pagos (anuncio_id) WHERE anuncio_id IS NOT NULL AND estado = 'pendiente'` y `CREATE INDEX IF NOT EXISTS ix_anuncios_borrador ON anuncios (creado) WHERE estado = 'borrador'`. Comentario encima con el porqué: «pendiente de pago» se deriva (borrador + pago pendiente con ese `anuncio_id`) en vez de ampliar el CHECK, que obligaría a reconstruir `anuncios` con fotos, contactos y métricas colgando (auditoría §1.9); `anuncio_id` SIN clave foránea a propósito, para que borrar un anuncio no borre ni deje en NULL el rastro de un pago (el historial fiscal no se toca); el índice único parcial es la garantía en la base de «un solo pago pendiente por anuncio» (D-12) y deja libres las renovaciones de la 05.3, que tendrán varios aprobados por anuncio; y por qué los índices NO van en schema.sql (corre antes que las migraciones).
    - En `db/schema.sql`, añadir `plan_elegido TEXT` y `dias_elegidos INTEGER` a `CREATE TABLE IF NOT EXISTS anuncios` (tras `vence`) y `anuncio_id TEXT` a `CREATE TABLE IF NOT EXISTS pagos` (tras las columnas del desglose de la 05.1), cada una con un comentario de una línea. Ningún índice nuevo en schema.sql.
    - Constante exportada `ANIO_SIN_DEFINIR = 1900`, con comentario: `anio` es NOT NULL y el disparador exige ≥ 1900, pero un borrador nace antes de que se sepa el año; 1900 pasa el disparador y la API nunca lo acepta (exige ≥ 1970), así que no puede confundirse con un año real ni pasar la validación al pedir el pago.
    - `crearBorrador({ idOrg, idUsuario, idSucursal = null, idPlan, dias })`: INSERT en `anuncios` con `estado = 'borrador'`, `categoria = ''`, `marca = ''`, `modelo = ''`, `anio = ANIO_SIN_DEFINIR`, `plan_elegido = idPlan`, `dias_elegidos = dias === 60 ? 60 : 30`, `sucursal_id = idSucursal || (sucursalPrincipal(idOrg) || {}).id || null`, `suscripcion_id` NULL, `publicado` y `vence` NULL, `creado = actualizado = ahora()`. Devuelve el id.
    - `guardarBorrador(idAnuncio, idOrg, datos)`: en una transacción (BEGIN/COMMIT/ROLLBACK como `crearAnuncio`), comprobar que existe `WHERE id = ? AND organizacion_id = ? AND estado = 'borrador'` (si no, `{ ok: false, motivo: 'no-existe' }`) y que no hay un pago `pendiente` con ese `anuncio_id` (si lo hay, `{ ok: false, motivo: 'pago-pendiente' }`). Actualizar SOLO las claves de `datos` que no sean `undefined`, con la misma correspondencia de nombres que `crearAnuncio` más `idSucursal → sucursal_id`, `planElegido → plan_elegido`, `diasElegidos → dias_elegidos`; `categoria`, `marca` y `modelo` nulos se guardan como `''` y `anio` nulo como `ANIO_SIN_DEFINIR`; booleanos a 0/1; `disponibilidad` por `disponibilidadValida`. Si `datos.fotos`/`datos.videos`/`datos.telefonos` son arrays, borrar las filas hijas de ese anuncio y volver a insertarlas con el mismo formato que `crearAnuncio`. Siempre `actualizado = ahora()`. Devuelve `{ ok: true }`. Construir el UPDATE con una lista blanca fija de columnas (nunca nombres de columna que vengan de fuera).
    - `pagoPendienteDeAnuncio(idAnuncio)`: la fila de `pagos` con ese `anuncio_id` y `estado = 'pendiente'`, o null.
    - `borradorDe(idAnuncio, idOrg)`: `copiaDeAnuncio` + comprobar `estado === 'borrador'` (si no, null) + `pagoPendiente: pagoPendienteDeAnuncio(idAnuncio)`.
    - `contarBorradores(idOrg)`: COUNT de `estado = 'borrador'` de esa organización.
    - `anunciosDeOrganizacion`: añadir al SELECT `a.plan_elegido, a.dias_elegidos, a.creado`, `(SELECT nombre FROM planes WHERE id = a.plan_elegido) AS plan_elegido_nombre` y `(SELECT p.referencia FROM pagos p WHERE p.anuncio_id = a.id AND p.estado = 'pendiente' LIMIT 1) AS pago_pendiente`; en el `.map` añadir `pendiente_pago: !!a.pago_pendiente` además de `conNombres`.
    - Exportar `ANIO_SIN_DEFINIR, crearBorrador, guardarBorrador, borradorDe, contarBorradores, pagoPendienteDeAnuncio` en `module.exports`.
    Editar con la herramienta de edición (CRLF); nada de heredocs.
  </action>
  <verify>
    <automated>npm run publicacion:probar &amp;&amp; npm run pagos:probar &amp;&amp; npm run seguridad:probar</automated>
  </verify>
  <acceptance_criteria>
    - `npm run publicacion:probar`, `npm run pagos:probar` y `npm run seguridad:probar` salen con código 0.
    - `node -e "const s=require('fs').readFileSync('tools/db.js','utf8');const a=s.indexOf(\"'2026-09-precios-base'\"),b=s.indexOf(\"'2026-09-borradores'\");if(!(a>0&&b>a&&s.lastIndexOf(\"['2026-\")===s.lastIndexOf(\"['2026-09-borradores'\")))process.exit(1)"` sale con código 0 (va detrás de la 05.1 y es la última).
    - `grep -c "ux_pagos_anuncio_pendiente" db/schema.sql` = 0 y `grep -c "ux_pagos_anuncio_pendiente" tools/db.js` ≥ 1.
    - `grep -c "\"publicacion:probar\": \"node tools/probar-publicacion.js\"" package.json` = 1 y `grep -c "npm run publicacion:probar" .github/workflows/desplegar.yml` = 1.
    - `git diff --unified=0 tools/db.js` no muestra líneas eliminadas dentro de migraciones anteriores.
    - La salida de `npm run publicacion:probar` contiene «1.» y «2.» y ninguna línea «FALLA».
  </acceptance_criteria>
  <done>La migración está al final, el borrador existe en la base con su plan y sus días, se guarda por partes solo por su dueño y sin pago pendiente, no sale en listados públicos, y el panel recibe `pendiente_pago` derivado; el arnés corre en CI.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarea 2: el pago conoce su anuncio y la aprobación activa el borrador, con importe cero incluido</name>
  <files>tools/probar-publicacion.js, tools/db.js</files>
  <read_first>
    - tools/db.js: `soloCero`, `anotarPago`, `comprarCupos`, `otorgarCompra`, `registrarCobro`, `aprobarPago`, `rechazarPago`, `suscripcion`, `moverAnuncioDeSuscripcion` (cómo calcula `vence` y `destacado_hasta`) y lo que dejó la tarea 1
    - tools/pagos.js entero (234 líneas: `emitir`, `confirmarPago`, `cobrar`, `PROCESADORES`)
    - tools/probar-publicacion.js tal como lo dejó la tarea 1
    - .planning/phases/05.2-publicar-este-equipo-borrador-pago-y-activaci-n-del-particul/05.2-CONTEXT.md (D-09 a D-12)
  </read_first>
  <behavior>
    - Sección 3 (aprobación): con un borrador Destacado/30 completo en la base y `pago = db.registrarCobro({ idOrg, idAnuncio, cobro: {...precios.precioCompra({ precioUnitario: 3200, cupo: 1, dias: 30 }), referencia, procesador: 'demo'}, intencion: { tipo: 'publicacion', idAnuncio, idPlan: 'destacado', cupo: 1, dias: 30, concepto, cliente, correoCliente } })`, la fila tiene `anuncio_id = idAnuncio` y `estado = 'pendiente'`; `db.pagoPendienteDeAnuncio(idAnuncio).id === pago.id`; `anunciosDeOrganizacion` marca ese anuncio con `pendiente_pago === true`; `db.guardarBorrador` sobre él devuelve `{ ok: false, motivo: 'pago-pendiente' }`.
    - Sección 3: un segundo `registrarCobro` pendiente con el mismo `idAnuncio` lanza con `codigo === 409` y no deja fila nueva.
    - Sección 3: `pagos.confirmarPago(pago.id)` deja el pago `aprobado`; hay exactamente UNA suscripción nueva de la organización con `plan_id = 'destacado'`, `anuncios_incluidos = 1`, `dias_ciclo = 30`, `precio_pactado = 3200`; `pagos.suscripcion_id` apunta a ella; el anuncio queda `activo` con esa `suscripcion_id`, `vence = suscripcion.fin`, `destacado_hasta = suscripcion.fin`, `publicado` no nulo; hay una factura B02 del pago con `subtotal + itbis === total === pago.total` y `siguienteB02()` avanzó 1.
    - Sección 3: repetir `pagos.confirmarPago(pago.id)` devuelve `yaEstaba: true`, no crea suscripción ni factura, y el anuncio sigue igual (mismo `publicado`).
    - Sección 4 (lo que no debe pasar): un pago `publicacion` pendiente de un segundo borrador cuyo anuncio se pasa a mano (SQL) a `retirado` antes de aprobar: `lanza(() => db.aprobarPago(id))` da `codigo === 409`, el pago sigue `pendiente` y no hay suscripción nueva; con el anuncio borrado a mano, `codigo === 404`.
    - Sección 4: un pago `publicacion` rechazado (`pagos.rechazarPago`) deja el anuncio en `borrador`, `suscripcion_id` NULL, sin factura, B02 igual; después se puede registrar otro pendiente para ese mismo anuncio.
    - Sección 4: `db.publicarBorradorSinCosto({ idAnuncio, idOrg, idPlan: 'estandar', dias: 30, cobro: { ...precios.desglose(0), referencia } })` deja el anuncio `activo` con una suscripción Estándar de 1 cupo, un pago `aprobado` de total 0, procesador `'sin-costo'` y `anuncio_id = idAnuncio`, sin factura; con un cobro de total > 0 lanza (`soloCero`) y no deja nada.
    - Sección 4: `publicarBorradorSinCosto` sobre un anuncio que ya no es borrador lanza 409 y hace ROLLBACK (ni suscripción ni pago nuevos).
  </behavior>
  <action>
    RED: añadir a `tools/probar-publicacion.js` las secciones 3 y 4 de `<behavior>`, preparando los borradores completos con `db.crearBorrador` + `db.guardarBorrador` (categoría, subcategoría y marca válidas de assets/taxonomia.js, p. ej. las de la prueba de transferencia: `camiones`/`cam-volteo`/`peterbilt`, modelo, año 2019, precio, 3 fotos con rutas de texto y un teléfono), `require('./pagos')` y `require('../assets/precios.js')` después del entorno. Correr y ver en rojo.

    GREEN en `tools/db.js` (per D-05, D-09, D-10, D-11, D-12):
    - Función interna `activarBorrador(d, { idAnuncio, idOrg, idSusc, t })`: lee `suscripcion(idSusc, idOrg)` (si no existe, lanza 404) y hace `UPDATE anuncios SET estado = 'activo', suscripcion_id = ?, vence = ?, destacado_hasta = ?, publicado = ?, actualizado = ? WHERE id = ? AND organizacion_id = ? AND estado = 'borrador'` con `vence = s.fin` y `destacado_hasta = s.destacado ? s.fin : null` (el mismo criterio que `publicar` en la API); si `changes !== 1`, lanza `Object.assign(new Error('Ese anuncio ya no es un borrador: no se publica otra vez'), { codigo: 409 })`. Comentario: es el ÚNICO sitio que pasa un borrador a activo; lo llaman la aprobación de un pago y el importe cero, nunca una ruta.
    - `registrarCobro({ idOrg, idSusc = null, idAnuncio = null, cobro, intencion })`: añadir `anuncio_id` al INSERT; envolver el INSERT para que un error `UNIQUE constraint failed` (el índice `ux_pagos_anuncio_pendiente`) se relance como `codigo: 409` con el texto 'Ese anuncio ya tiene un pago en espera'.
    - `anotarPago(d, { idOrg, idSusc, idAnuncio = null, cobro, t })`: añadir `anuncio_id` al INSERT.
    - `aprobarPago`: nueva rama `else if (intencion.tipo === 'publicacion')` antes del `else` final: `plan = planPorId(intencion.idPlan)` (400 si no existe); leer `SELECT estado, organizacion_id FROM anuncios WHERE id = ?` con `intencion.idAnuncio`: si no existe o es de otra organización, lanza 404 'El borrador de este pago ya no existe'; si `estado !== 'borrador'`, lanza 409; `idSusc = otorgarCompra(d, { idOrg: pago.organizacion_id, plan, cupo: 1, dias: intencion.dias, precioPactado: pago.base ?? pago.subtotal, t })`; `UPDATE pagos SET suscripcion_id = ? WHERE id = ?`; `activarBorrador(d, { idAnuncio: intencion.idAnuncio, idOrg: pago.organizacion_id, idSusc, t })`. Todo dentro del SAVEPOINT existente, así un fallo deshace suscripción y activación. Comentario: cada publicación del particular es una suscripción de UN cupo nacida con su pago (cadena Plan → Suscripción → Anuncio de la auditoría §3); el `cupo` de la intención se ignora a propósito y siempre es 1, para que un pago nunca sostenga dos anuncios.
    - `publicarBorradorSinCosto({ idAnuncio, idOrg, idPlan, dias, cobro })`: `soloCero(cobro)`; plan por `planPorId` (400 si falta); BEGIN → `otorgarCompra` (cupo 1, `precioPactado: cobro.base ?? cobro.subtotal`) → `anotarPago(d, { idOrg, idSusc, idAnuncio, cobro, t })` → `activarBorrador` → COMMIT, ROLLBACK si algo lanza. Devuelve `{ membresia: suscripcion(idSusc, idOrg), anuncio: anuncio(idAnuncio) }`. Comentario: es el camino de la promoción del Estándar (D-11), igual que `comprarCupos` para el importe cero.
    - Exportar `publicarBorradorSinCosto`. `activarBorrador` NO se exporta.
    Editar con la herramienta de edición (CRLF).
  </action>
  <verify>
    <automated>npm run publicacion:probar &amp;&amp; npm run pagos:probar &amp;&amp; npm run transferencia:probar &amp;&amp; npm run facturas:probar</automated>
  </verify>
  <acceptance_criteria>
    - `npm run publicacion:probar`, `npm run pagos:probar`, `npm run transferencia:probar` y `npm run facturas:probar` salen con código 0.
    - `grep -c "intencion.tipo === 'publicacion'" tools/db.js` = 1.
    - `grep -c "function activarBorrador" tools/db.js` = 1 y `sed -n '/^module.exports = {/,/^};/p' tools/db.js | grep -c activarBorrador` = 0 (no se exporta).
    - `grep -c "UPDATE anuncios SET estado = 'activo'" tools/db.js` = 1 (un solo sitio activa un borrador).
    - La salida de `npm run publicacion:probar` contiene «3.» y «4.» y ninguna línea «FALLA».
  </acceptance_criteria>
  <done>Un pago de publicación queda atado a su anuncio, solo puede haber un pendiente por anuncio, la aprobación activa el borrador con una suscripción de un cupo en la misma transacción y es idempotente, un rechazo deja el borrador recuperable y el importe cero publica al instante sin NCF.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migración → base de producción | Se aplica sola al desplegar |
| rutas (planes 02-03) → funciones de base | Las rutas pasan ids que pueden venir manipulados |
| aviso de pago repetido → aprobarPago | Una confirmación puede llegar dos veces |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.2-01 | Tampering | migración '2026-09-borradores' | mitigate | Solo ADD COLUMN y CREATE INDEX IF NOT EXISTS, al final de MIGRACIONES, en transacción; ninguna migración anterior se toca; índices fuera de schema.sql porque corre antes que migrar() |
| T-05.2-02 | Elevation of privilege | guardarBorrador / borradorDe | mitigate | Toda consulta filtra por `organizacion_id` y `estado = 'borrador'`; columnas del UPDATE por lista blanca fija; sección 2 prueba otra organización |
| T-05.2-03 | Spoofing / Repudiation | aprobarPago tipo publicacion | mitigate | Idempotente por el estado del pago (yaEstaba); `activarBorrador` exige `estado = 'borrador'` y lanza 409 si no; todo en el SAVEPOINT; sección 3 repite la confirmación |
| T-05.2-04 | Tampering | dos pagos para un anuncio | mitigate | Índice único parcial `ux_pagos_anuncio_pendiente` + 409 en registrarCobro; `cupo` siempre 1 en la suscripción de una publicación; sección 3-4 |
| T-05.2-05 | Tampering (fiscal) | rechazo o importe cero | mitigate | Rechazo sin factura ni NCF (sección 4); importe cero por `soloCero` y procesador 'sin-costo', sin NCF |
| T-05.2-SC | Tampering | instalación de paquetes | accept | No se instala nada |
</threat_model>

<verification>
- `npm run publicacion:probar`, `pagos:probar`, `transferencia:probar`, `facturas:probar`, `seguridad:probar` en verde.
- `2026-09-borradores` es la última entrada de `MIGRACIONES`.
</verification>

<success_criteria>
- MOD-05 (base): el borrador existe en el servidor y solo la aprobación de un pago (o el importe cero) lo activa.
- MOD-06 (base): el borrador no sale en los listados públicos.
- MOD-08 (base): un pendiente por anuncio, ninguna segunda activación, una suscripción de un cupo por pago.
</success_criteria>

<output>
Create `.planning/phases/05.2-publicar-este-equipo-borrador-pago-y-activaci-n-del-particul/05.2-01-SUMMARY.md` when done
</output>

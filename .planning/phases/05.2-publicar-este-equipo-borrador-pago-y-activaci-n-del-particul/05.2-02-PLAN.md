---
phase: 05.2-publicar-este-equipo-borrador-pago-y-activaci-n-del-particul
plan: 02
type: execute
wave: 2
depends_on: ["05.2-01"]
files_modified:
  - tools/api.js
  - tools/probar-publicacion.js
  - tools/probar-transferencia.js
autonomous: true
requirements: [MOD-05, MOD-06, MOD-07, MOD-08]

must_haves:
  truths:
    - "Un particular con sesión crea un borrador con `POST /api/borradores { plan, dias }` (201) y lo guarda por partes con `PUT /api/borradores/:id`; `GET /api/borradores/:id` se lo devuelve con la forma del formulario de publicar.js y el precio final (`precio.total`), sin base ni ajuste."
    - "Guardar un borrador aplica las mismas validaciones de valor que publicar (taxonomía, año, precio, fotos y videos del propio sitio, teléfonos, tren motriz) salvo las que exigen estar completo; esas las devuelve como `completo: false` y `falta: '<motivo>'`."
    - "Un borrador responde 404 a quien no es su dueño en `GET /api/anuncios/:id`, `GET/PUT /api/borradores/:id`, `DELETE /api/anuncios/:id` y `PATCH`; el catálogo no lo lista."
    - "Ninguna ruta existente activa un borrador: `PATCH /api/anuncios/:id { estado }` y `PATCH /api/anuncios/:id/plan` sobre un borrador responden 409 y lo dejan en `borrador`."
    - "Un borrador con un pago pendiente no se edita (409) ni se elimina (409); sin pago se elimina y se borran sus archivos que nadie más usa."
    - "Crear borradores tiene tope: el mismo contador de 20 por hora que publicar (429) y como máximo 10 borradores abiertos por organización (409). Un dealer o una cuenta exenta recibe 409: siguen publicando como hoy."
    - "`POST /api/anuncios` sigue igual para el dealer y para el particular con capacidad libre (D-02); al particular sin capacidad le responde sin la palabra «cupo»."
    - "Marcar un anuncio vendido devuelve `capacidadLibre` (si a la organización le queda sitio en alguna membresía viva), para que el panel diga «con la capacidad disponible de tu plan» solo cuando es verdad."
  artifacts:
    - path: "tools/api.js"
      provides: "validarCamposAnuncio, formaDeFormulario, crearBorradorApi, verBorrador, guardarBorradorApi y sus rutas; guardas de borrador en verAnuncio, cambiarEstado, cambiarPlanDeAnuncio y eliminarAnuncio; textos del particular en publicar"
      contains: "/api\\/borradores"
    - path: "tools/probar-publicacion.js"
      provides: "secciones 5-8 por el enrutador con req/res fingidos"
  key_links:
    - from: "tools/api.js publicar"
      to: "validarCamposAnuncio(c, plan, { completo: true })"
      via: "la misma validación sirve a publicar, al guardado del borrador (completo: false) y al pedir el pago (plan 03)"
      pattern: "validarCamposAnuncio\\("
    - from: "tools/api.js verAnuncio"
      to: "estado 'borrador'"
      via: "404 si !esSuyo"
      pattern: "estado === 'borrador'"
---

<objective>
Las rutas del borrador del particular y las guardas que impiden que un borrador se vea o se active
por cualquier otra puerta. El cobro (pedir el pago) es el plan 03.

Purpose: MOD-05 (el borrador vive en el servidor desde que se elige el plan), MOD-06 (nadie más que
su dueño lo ve; el hallazgo 4 de la auditoría, `verAnuncio` sin filtro, se cierra antes de que
existan borradores de verdad), MOD-07 (textos sin «cupo» del servidor al particular y la señal
`capacidadLibre` tras vender) y la parte de rutas de MOD-08 (sin activación por la puerta de atrás,
sin creación ilimitada).
Output: rutas `/api/borradores`, una validación compartida extraída de `publicar`, guardas nuevas y
las secciones 5-8 del arnés.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05.2-publicar-este-equipo-borrador-pago-y-activaci-n-del-particul/05.2-CONTEXT.md
@.planning/phases/05.2-publicar-este-equipo-borrador-pago-y-activaci-n-del-particul/05.2-01-SUMMARY.md
@.planning/research/auditoria-modelo-comercial.md
@CLAUDE.md

<interfaces>
Del plan 05.2-01 (tools/db.js):
- `ANIO_SIN_DEFINIR` (1900), `crearBorrador({ idOrg, idUsuario, idSucursal, idPlan, dias })` → id.
- `guardarBorrador(idAnuncio, idOrg, datos)` → `{ ok: true } | { ok: false, motivo: 'no-existe' | 'pago-pendiente' }`; `datos` con los nombres de `crearAnuncio` + `idSucursal`, `planElegido`, `diasElegidos`; solo escribe las claves no `undefined`; `fotos`/`videos`/`telefonos` como arrays reemplazan las filas.
- `borradorDe(idAnuncio, idOrg)` → fila completa (`SELECT *`, `conNombres`) + `fotos` {url, miniatura} + `videos` + `telefonos` + `pagoPendiente` (fila o null), o null si no es un borrador de esa organización.
- `contarBorradores(idOrg)`, `pagoPendienteDeAnuncio(idAnuncio)`.
- `anunciosDeOrganizacion(idOrg)` ya trae `plan_elegido`, `dias_elegidos`, `plan_elegido_nombre`, `pago_pendiente`, `pendiente_pago`.

De tools/api.js (buscar por nombre):
- helpers: `responder(res, codigo, cuerpo)`, `fallo(res, codigo, mensaje, extra)`, `leerCuerpo(req)`, `texto(v, max)`, `entero(v)`, `conSesion`, `exigirAceptacion(res, idUsuario, ids)`, `esExenta(idUsuario)`, `precioUnitario(plan)`, `esRutaDeFoto(ruta)`, `videos.archivoDe`, `taxonomia` (`validarCadena`, `pideTrenMotriz`, `MOTORES`, `TRANSMISIONES`), `db.permitir(clave, tope, minutos)`, `correo.BUZONES.facturacion`, `legales.PARA_PUBLICAR`, `cobroPublico(cobro)` (de la 05.1: quita base, ajuste y ajusteTasa).
- `publicar` (POST /api/anuncios): aceptación → `db.permitir('publicar:' + usuario, 20, 60)` → membresía (exenta: `membresiaInterna`; si no, la pedida o `suscripcionConHueco`) → 402 'Sus cupos están ocupados…' / 'Todavía no tiene cupos. Contrate un plan para publicar este equipo.' → 409 '…no tiene cupos libres…' → plan → validación de taxonomía, modelo, año (1970..año+1), modalidad, precio > 0, tren motriz, fotos (rutas del sitio, recorte a `plan.fotos_maximas`, mínimo 3), videos (rutas del sitio, recorte a `plan.videos_maximos`), teléfonos (10 dígitos, máx. 5, mínimo 1) → sucursal propia o principal → `db.crearAnuncio({...})` → correo → 201.
- `copiarAnuncio` (GET /api/mis-anuncios/:id/copia): arma `{ equipo, precio, contacto, fotos, videos, origen }` con la forma de `estadoInicial()` de assets/publicar.js ('' en lugar de null; `serie: ''` a propósito en la copia).
- `verAnuncio(req, res, ctx, idAnuncio)`: `esSuyo = !!ctx && !!ctx.organizacion && a.organizacion_id === ctx.organizacion.id`; a quien no es dueño le quita `PRIVADOS_DEL_ANUNCIO` y los teléfonos sin verificar.
- `cambiarEstado` (PATCH /api/anuncios/:id): `permitidos = ['activo','pausado','vendido','retirado']`, `db.cambiarEstadoAnuncio(...)`; responde `{ ok: true, estado }`. (Si el PR #33 ya está en la rama, además devuelve 409 con `r.sinCupo`; no se toca esa parte.)
- `cambiarPlanDeAnuncio` (PATCH /api/anuncios/:id/plan), `eliminarAnuncio` (DELETE /api/anuncios/:id: `db.borrarAnuncio`, después borra archivos, responde `{ ok, membresias }`).
- `RUTAS`: array plano `[metodo, regexp, manejador]`; las específicas antes que las genéricas. `['POST', /^\/api\/anuncios$/, publicar]` está en el bloque de anuncios.

De tools/probar-transferencia.js: en la sección de punta a punta (buscar `antesDeTiempo`) compara el texto exacto 'Todavía no tiene cupos. Contrate un plan para publicar este equipo.' de un particular que publica con el pago pendiente.

Forma del cuerpo que manda assets/publicar.js (`anuncioParaApi`): `membresia, categoria, subcategoria, marca, modelo, anio, condicion, usoValor, usoUnidad, motorMarca, motorModelo, transmisionMarca, transmisionModelo, serie, potencia, peso, implementos, descripcion, provincia, municipio, disponibilidad, precio, moneda, modalidadPrecio, precioMinimo, itbisIncluido, permuta, financiamiento, fotos [{url, miniatura}], videos [{url, poster, duracion}], telefonos [{numero, tipo, nota}], sucursal`. El PUT del borrador recibe esa misma forma (sin `membresia`) más `plan` y `dias`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarea 1: validación compartida con publicar y las rutas del borrador</name>
  <files>tools/probar-publicacion.js, tools/api.js</files>
  <read_first>
    - tools/api.js: `publicar` entero, `copiarAnuncio`, `esRutaDeFoto`, `precioUnitario`, `esExenta`, `cobroPublico`, `exigirAceptacion`, los helpers `texto`/`entero`/`fallo`/`responder`, y el bloque de anuncios de `RUTAS`
    - tools/probar-seguridad.js líneas 380-460 (lo que ya se prueba de publicar: fotos en base64, URL ajena, rutas buenas) — debe seguir pasando igual
    - tools/probar-publicacion.js (secciones 1-4 del plan 01) y tools/probar-pagos.js (helper `pedir`, creación de cuenta, aceptación de legales y cookie `te_sesion`)
    - tools/probar-transferencia.js alrededor de `const foto = await pedir` (cómo sube una foto PNG de prueba por /api/fotos)
    - .planning/phases/05.2-publicar-este-equipo-borrador-pago-y-activaci-n-del-particul/05.2-CONTEXT.md (D-01, D-03, D-06, D-07)
  </read_first>
  <behavior>
    - Sección 5 (crear): particular con legales aceptados, `POST /api/borradores { plan: 'destacado', dias: 60 }` → 201, `datos.borrador.id`, `borrador.plan.id === 'destacado'`, `borrador.dias === 60`, `borrador.pendientePago === false`, `borrador.precio.total === precios.precioCompra({ precioUnitario: <precio_vigente del destacado>, cupo: 1, dias: 60 }).total`, `borrador.precio` sin claves `base`/`ajuste`/`subtotal`; en la base, `estado = 'borrador'` y `plan_elegido = 'destacado'`.
    - Sección 5: sin sesión → 401; plan inexistente o inactivo → 400; sin aceptar las condiciones de publicación → 409 con `faltan`; cuenta dealer → 409; cuenta exenta → 409; con cuerpo que trae campos (lo que manda «Duplicar»: `modelo`, `anio`, `fotos`) el borrador nace con ellos guardados.
    - Sección 6 (guardar): `PUT /api/borradores/:id` con `{ modelo: '320D', anio: 2018, precio: 4500000, fotos: [3 rutas subidas por /api/fotos], telefonos: [{ numero: '(809) 555-1234', tipo: 'ambos' }] }` → 200 y la base lo refleja; una foto `https://otro-sitio.example/f.jpg` o un `data:` se descarta; `anio: 1965` → 400 con 'Año entre 1970'; `precio: -5` → 400; `{ plan: 'estandar', dias: 30 }` cambia `plan_elegido` y `dias_elegidos`; 25 fotos con plan Destacado se recortan a `fotos_maximas`.
    - Sección 6: `GET /api/borradores/:id` → 200 con `borrador.datos.equipo.modelo === '320D'`, `borrador.datos.equipo.anio === '2018'` (texto, como el formulario), `datos.fotos` con `url` y `miniatura`, y `completo`/`falta` coherentes: sin categoría → `completo === false` y `falta` no vacío; con todo → `completo === true`, `falta === null`. Un borrador recién creado devuelve `datos.equipo.anio === ''` (el centinela no se enseña).
    - Sección 6: `copiarAnuncio` sigue devolviendo `serie: ''` (la copia no arrastra la serie) mientras `GET /api/borradores/:id` devuelve la serie guardada del propio borrador.
  </behavior>
  <action>
    RED: añadir a `tools/probar-publicacion.js` el helper `pedir` (copiado de probar-pagos.js), un `cuentaCon({ correo, tipo, exenta })` que crea la cuenta con `db.crearCuenta`, acepta todos los documentos de `legales.DOCUMENTOS` con `db.registrarAceptacion` y devuelve `{ idUsuario, org, cabeceras: { cookie: 'te_sesion=' + db.abrirSesion(idUsuario), 'cf-connecting-ip': '201.8.8.8' } }` (la exenta se marca con SQL `UPDATE organizaciones SET exenta_pago = 1`), un `subirFoto(cabeceras)` por `/api/fotos` con el PNG de 1×1 de probar-transferencia.js, y las secciones 5 y 6 de `<behavior>`. Correr y ver en rojo.

    GREEN en `tools/api.js` (per D-01, D-03, D-06, D-07):
    - Extraer de `publicar` una función `validarCamposAnuncio(c, plan, { completo = true } = {})` que devuelva `{ error }` o `{ datos }`, con `datos` = exactamente los campos que hoy recibe `db.crearAnuncio` desde el cuerpo (categoria … telefonos, sin idOrg/idSucursal/idUsuario/idSuscripcion/vence/destacadoHasta). Con `completo: true` hace LO MISMO que hoy, en el mismo orden y con los mismos textos de error (probar-seguridad, probar-transferencia y probar-pagina-dealer no pueden notar el cambio). Con `completo: false` solo valida y devuelve las claves presentes en `c` (`!== undefined`; fotos/videos/teléfonos solo si son arrays): mismos formatos y rangos (año 1970..año+1 si viene no vacío; precio entero > 0 si viene no vacío; tren motriz contra las listas; rutas de fotos y videos del sitio con recorte al tope del plan; teléfonos de 10 dígitos, máx. 5), sin los mínimos (3 fotos, un teléfono, modelo, año, precio) y validando la cadena categoría → subcategoría → marca con `taxonomia.validarCadena` solo cuando vienen las tres no vacías (si no, se guardan como texto recortado a 80). Comentario: una sola validación para publicar, guardar el borrador y pedir su pago; dos copias acabarían aceptando en el borrador algo que el pago rechaza. `publicar` pasa a llamarla con `completo: true` y conserva todo lo demás.
    - Extraer de `copiarAnuncio` `formaDeFormulario(a, { copia })` que devuelve `{ equipo, precio, contacto, fotos, videos }` con la forma de hoy, añadiendo `equipo.disponibilidad` (faltaba en la copia); con `copia: true` deja `serie: ''` y los ids `f-copia-N`/`v-copia-N`; con `copia: false` conserva la serie y usa `f-N`/`v-N`; `anio === db.ANIO_SIN_DEFINIR` sale como `''`. `copiarAnuncio` la usa con `copia: true` (respuesta idéntica salvo `disponibilidad`).
    - `soloParticular(ctx, res)`: 409 'Las cuentas de empresa publican con la capacidad de su plan desde el asistente.' si `ctx.organizacion.tipo !== 'particular'`; 409 'Su cuenta publica sin costo desde el asistente.' si `esExenta`. Devuelve true si respondió.
    - `borradorPublico(b)`: `{ id, estado: 'borrador', plan: { id, nombre, fotos_maximas, videos_maximos, destacado, activo }, dias, pendientePago: !!b.pagoPendiente, pago: b.pagoPendiente ? { referencia, total, procesador } : null, precio: { total }, completo, falta, datos: formaDeFormulario(b, { copia: false }) }`, donde `precio.total` sale de `precios.precioCompra({ precioUnitario: precioUnitario(plan), cupo: 1, dias }).total` (nunca base ni ajuste: MOD-02) y `completo`/`falta` de `validarCamposAnuncio(cuerpoDeBorrador(b), plan, { completo: true })`. `cuerpoDeBorrador(b)` convierte la fila a la forma del cuerpo de publicar (anio centinela → null, fotos `{url, miniatura}`, videos, teléfonos, sucursal).
    - `crearBorradorApi = conSesion(async ...)`: `exigirAceptacion(PARA_PUBLICAR)`; `soloParticular`; `db.permitir('publicar:' + ctx.usuario.id, 20, 60)` → 429 con el mismo texto de publicar (el tope de 20 por hora es compartido a propósito: crear borradores no abre una segunda puerta sin límite); `db.contarBorradores(org.id) >= BORRADORES_ABIERTOS` (constante 10, con comentario) → 409 'Tiene 10 borradores sin terminar. Termine o elimine alguno antes de empezar otro.'; plan por `db.planPorId(String(c.plan))` activo (400 'Seleccione un nivel válido'); `dias = Number(c.dias) === 60 ? 60 : 30`; `db.crearBorrador(...)`; si el cuerpo trae campos, `validarCamposAnuncio(c, plan, { completo: false })` → 400 con el error (y el borrador recién creado se borra para no dejar basura) o `db.guardarBorrador`; 201 `{ borrador: borradorPublico(db.borradorDe(id, org.id)) }`.
    - `verBorrador = conSesion(...)`: `db.borradorDe(id, org.id)` → 404 'Ese borrador no es suyo o no existe' (mismo texto para ajeno e inexistente) o 200 `{ borrador }`.
    - `guardarBorradorApi = conSesion(async ...)`: `db.permitir('borrador:' + usuario, 240, 60)` → 429; borrador propio o 404; si `pagoPendiente` → 409 'Este borrador tiene un pago en espera de confirmación. Podrá cambiar el anuncio cuando se publique.'; plan: el pedido si viene (activo, 400 si no) o el elegido; `dias` si viene; `validarCamposAnuncio(c, plan, { completo: false })` → 400; sucursal pedida solo si `db.sucursal(id, org.id)`; `db.guardarBorrador(..., { ...datos, planElegido, diasElegidos, idSucursal })` y si devuelve `motivo: 'pago-pendiente'` el mismo 409; 200 `{ borrador }`.
    - Rutas en `RUTAS`, en el bloque de anuncios y antes de `['GET', /^\/api\/anuncios\/([\w-]+)$/, verAnuncio]`: `['POST', /^\/api\/borradores$/, crearBorradorApi]`, `['GET', /^\/api\/borradores\/([\w-]+)$/, verBorrador]`, `['PUT', /^\/api\/borradores\/([\w-]+)$/, guardarBorradorApi]`, con un comentario de por qué el borrador tiene rutas propias (se guarda incompleto; `POST /api/anuncios` exige estar completo y ocupar capacidad).
    Editar con la herramienta de edición (CRLF).
  </action>
  <verify>
    <automated>npm run publicacion:probar &amp;&amp; npm run seguridad:probar &amp;&amp; npm run dealer:probar &amp;&amp; npm run transferencia:probar &amp;&amp; npm run metricas:probar &amp;&amp; npm run catalogo:probar &amp;&amp; npm run contactos:probar</automated>
  </verify>
  <acceptance_criteria>
    - Los siete scripts de `<verify>` salen con código 0.
    - `grep -c "validarCamposAnuncio(" tools/api.js` ≥ 3 (definición, publicar, borrador).
    - `grep -c "function formaDeFormulario\|const formaDeFormulario" tools/api.js` = 1.
    - `node -e "const s=require('fs').readFileSync('tools/api.js','utf8');const a=s.indexOf('/^\\\\/api\\\\/borradores$/'),b=s.indexOf('verAnuncio]');if(!(a>0&&b>a))process.exit(1)"` sale con código 0 (las rutas del borrador van antes que la ficha genérica).
    - La salida de `npm run publicacion:probar` contiene «5.» y «6.» y ninguna línea «FALLA».
  </acceptance_criteria>
  <done>El particular crea, guarda y lee su borrador por la API con la misma validación que publicar; el precio que ve es el final, calculado en el servidor.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarea 2: nadie más ve un borrador, ninguna puerta lo activa y los textos del particular sin «cupo»</name>
  <files>tools/probar-publicacion.js, tools/probar-transferencia.js, tools/api.js</files>
  <read_first>
    - tools/api.js: `verAnuncio`, `cambiarEstado`, `cambiarPlanDeAnuncio`, `eliminarAnuncio`, `publicar` (bloque de la membresía y sus 402/409), `catalogo`
    - tools/probar-transferencia.js alrededor de `antesDeTiempo` (el texto exacto que compara)
    - tools/probar-publicacion.js tal como quedó tras la tarea 1
    - .planning/phases/05.2-publicar-este-equipo-borrador-pago-y-activaci-n-del-particul/05.2-CONTEXT.md (D-02, D-12, D-14, D-15)
  </read_first>
  <behavior>
    - Sección 7 (visibilidad, MOD-06): con un borrador de la organización A: `GET /api/anuncios/:id` sin sesión → 404; con sesión de B → 404; con sesión de A → 200. `GET /api/borradores/:id`, `PUT` (cuerpo `{ modelo: 'X' }`), `DELETE /api/anuncios/:id`, `PATCH /api/anuncios/:id { estado: 'activo' }` y `PATCH /api/anuncios/:id/plan` con sesión de B → 404, y la fila de A no cambió (modelo, estado, actualizado). `GET /api/anuncios` (catálogo) no contiene el id. `GET /api/mis-anuncios` de A lo contiene con `estado: 'borrador'` y `pendiente_pago: false`; el de B no.
    - Sección 7 (sin puerta de atrás, MOD-08): con sesión de A, `PATCH /api/anuncios/:id` con `estado` `activo`, `pausado`, `vendido` y `retirado` → 409 cada uno y sigue `borrador`; `PATCH /api/anuncios/:id/plan { membresia: <una suya con hueco> }` → 409 y `suscripcion_id` sigue NULL.
    - Sección 7: con un pago pendiente registrado a mano para ese borrador (`db.registrarCobro` con `idAnuncio`), `PUT /api/borradores/:id` → 409 y `DELETE /api/anuncios/:id` → 409 con el correo de facturación en el texto; tras `db.rechazarPago` ese pago, `DELETE` → 200, la fila ya no existe y un archivo de foto que solo usaba ese borrador ya no está en disco.
    - Sección 8 (límites): con 10 borradores abiertos, el 11.º → 409; tras llenar el contador `publicar:<id>` con `db.permitir` hasta el tope, `POST /api/borradores` → 429 y `POST /api/anuncios` → 429 (el contador es el mismo).
    - Sección 8 (el camino de hoy, D-02): un particular SIN membresía hace `POST /api/anuncios` con un anuncio completo → 402 y el texto no casa con /cupo/i; un particular con una membresía viva con hueco publica → 201 (camino de hoy intacto); un dealer sin membresía recibe el 402 de siempre.
    - Sección 8 (vendido, MOD-07): marcar vendido un anuncio activo del particular cuya membresía de un cupo queda libre → 200 con `capacidadLibre === true`; si la organización no tiene ninguna membresía con hueco tras la venta, `capacidadLibre === false`.
  </behavior>
  <action>
    RED: añadir las secciones 7 y 8 de `<behavior>` a `tools/probar-publicacion.js` (las membresías con hueco se preparan con `db.comprarCupos` a importe cero `precios.desglose(0)` o con `db.aprobarPago` de un cobro demo, como hace el propio arnés de pagos). Correr y ver en rojo.

    GREEN en `tools/api.js` (per D-02, D-12, D-14, D-15):
    - `verAnuncio`: justo después de calcular `esSuyo`, si `a.estado === 'borrador' && !esSuyo` → `fallo(res, 404, 'Ese anuncio no existe')` (el mismo texto que un id inexistente, para no confirmar que existe). Comentario: hallazgo 4 de la auditoría §1.8 (D-14).
    - `cambiarEstado`: antes de llamar a `db.cambiarEstadoAnuncio`, leer `db.anuncio(idAnuncio)`; si es de la organización y `estado === 'borrador'` → 409 'Este anuncio todavía es un borrador: se publica al confirmarse su pago.' Esta guarda va aquí y no depende del PR #33: sin ella, un `PATCH { estado: 'activo' }` publicaría gratis un borrador. Tras un cambio a `vendido` con éxito, añadir a la respuesta `capacidadLibre: !!db.suscripcionConHueco(ctx.organizacion.id)`. No tocar lo que el PR #33 añada (`r.sinCupo`) si ya está en la rama.
    - `cambiarPlanDeAnuncio`: si el anuncio propio es `borrador` → 409 'Un borrador cambia de plan desde el asistente de publicación.'
    - `eliminarAnuncio`: antes de `db.borrarAnuncio`, si el anuncio propio es `borrador` y `db.pagoPendienteDeAnuncio(id)` existe → 409 'Este borrador tiene un pago en espera. Si ya no lo quiere, escríbanos a ' + `correo.BUZONES.facturacion` + ' con la referencia ' + referencia + ' para anularlo.' (el correo es uno de los dos canales de soporte permitidos; nunca un teléfono).
    - `publicar`: cuando la organización es `tipo === 'particular'` y no es exenta, los textos de 402 y 409 dejan de nombrar el cupo: 402 'Para publicar este equipo, elija cómo publicarlo y pague su publicación.' y 409 'Su plan no tiene capacidad libre para otro equipo. Elija cómo publicar este equipo y pague su publicación, o marque como vendido uno de los publicados.'; para el dealer, los textos de siempre (05.4 los cambia). Comentario con D-15. Nada más cambia en `publicar`.
    - En `tools/probar-transferencia.js`, sustituir SOLO el texto esperado en la comprobación de `antesDeTiempo` por el nuevo 402 del particular; comentario de una línea: la cuenta de la prueba es particular y la fase 05.2 le quitó la palabra «cupo».
    Editar con la herramienta de edición (CRLF).
  </action>
  <verify>
    <automated>npm run publicacion:probar &amp;&amp; npm run seguridad:probar &amp;&amp; npm run transferencia:probar &amp;&amp; npm run dealer:probar &amp;&amp; npm run bitacora:probar &amp;&amp; npm run pagos:probar</automated>
  </verify>
  <acceptance_criteria>
    - Los seis scripts de `<verify>` salen con código 0.
    - `grep -c "estado === 'borrador' && !esSuyo" tools/api.js` = 1.
    - `grep -c "capacidadLibre" tools/api.js` ≥ 1.
    - `grep -c "Para publicar este equipo, elija cómo publicarlo y pague su publicación." tools/api.js` = 1 y `grep -c "Para publicar este equipo, elija cómo publicarlo y pague su publicación." tools/probar-transferencia.js` = 1.
    - La salida de `npm run publicacion:probar` contiene «7.» y «8.» y ninguna línea «FALLA».
  </acceptance_criteria>
  <done>El borrador es invisible e intocable para cualquiera que no sea su dueño, ninguna ruta existente lo publica, no se crean sin límite, el particular deja de leer «cupo» en las respuestas del servidor y el panel sabe si tras vender le queda capacidad.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → /api/borradores | El cuerpo y el id del borrador llegan del cliente |
| visitante / otra organización → GET /api/anuncios/:id | Ruta pública; `ctx` puede ser null |
| navegador → PATCH /api/anuncios/:id | Vía para intentar activar sin pagar |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.2-06 | Information disclosure | verAnuncio | mitigate | 404 si `estado === 'borrador'` y no es el dueño (comprobando `!!ctx` antes de `ctx.organizacion`); sección 7 |
| T-05.2-07 | Elevation of privilege | GET/PUT /api/borradores, DELETE, PATCH | mitigate | La organización sale de la sesión; `borradorDe`/`guardarBorrador` filtran por ella; 404 igual para ajeno e inexistente; sección 7 |
| T-05.2-08 | Tampering | cambiarEstado / cambiarPlanDeAnuncio | mitigate | 409 sobre un borrador, independiente del PR #33; `activarBorrador` exige además `estado = 'borrador'`; sección 7 |
| T-05.2-09 | Denial of service | POST /api/borradores | mitigate | Contador compartido `publicar:<usuario>` 20/h y tope de 10 borradores abiertos; PUT con 240/h; sección 8 |
| T-05.2-10 | Tampering | validación del borrador | mitigate | `validarCamposAnuncio` única para publicar, borrador y pago; rutas de fotos y videos del propio sitio; recorte al tope del plan; sección 6 |
| T-05.2-11 | Information disclosure | precio del borrador | mitigate | Solo `precio.total`; nunca base ni ajuste (MOD-02); sección 5 |
| T-05.2-SC | Tampering | instalación de paquetes | accept | No se instala nada |
</threat_model>

<verification>
- `npm run publicacion:probar`, `seguridad:probar`, `transferencia:probar`, `dealer:probar`, `bitacora:probar`, `pagos:probar`, `metricas:probar`, `catalogo:probar`, `contactos:probar` en verde.
</verification>

<success_criteria>
- MOD-05: el borrador se crea y se guarda en el servidor desde que se elige el plan.
- MOD-06: solo su dueño lo ve o lo toca.
- MOD-07: el servidor no le dice «cupo» al particular y avisa de la capacidad libre tras vender.
- MOD-08: ninguna ruta existente activa un borrador; creación con tope.
</success_criteria>

<output>
Create `.planning/phases/05.2-publicar-este-equipo-borrador-pago-y-activaci-n-del-particul/05.2-02-SUMMARY.md` when done
</output>

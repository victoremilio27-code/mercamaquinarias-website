---
phase: 05.2-publicar-este-equipo-borrador-pago-y-activaci-n-del-particul
plan: 05
type: execute
wave: 4
depends_on: ["05.2-03", "05.2-04"]
files_modified:
  - publicar.html
  - assets/publicar.js
  - tools/auditar-flujos.js
  - tools/auditar-metricas.js
autonomous: true
requirements: [MOD-05, MOD-06, MOD-07]

must_haves:
  truths:
    - "Un particular sin capacidad libre (o un visitante sin sesión) que abre publicar.html ve primero el paso «Elige cómo publicar este equipo» con Estándar/Destacada/Premium, 30 o 60 días y el precio final «ITBIS incluido»; al elegir, el borrador se crea en el servidor (`POST /api/borradores`) y la URL pasa a `publicar.html?borrador=<id>`."
    - "El formulario del borrador se guarda en el servidor al cambiar de paso y tras cada cambio (con espera), y `localStorage` queda como copia de seguridad; la clave vieja de TuEquipoRD se sigue rescatando como hoy."
    - "El último paso es un resumen con el precio final del servidor, datos fiscales opcionales y un botón que pide el pago: 201 enseña «Anuncio publicado»; 202 enseña «Esperando confirmación del pago» con la referencia y los datos de la transferencia; 402 dice que el anuncio sigue guardado como borrador."
    - "`publicar.html?borrador=<id>` retoma un borrador desde el panel; si tiene un pago pendiente enseña el estado de espera y no deja editar."
    - "«Duplicar» de un particular sin capacidad pasa por el paso del plan y crea un borrador nuevo con la copia; sin atajo al anuncio publicado (D-16)."
    - "El dealer, la cuenta exenta y el particular con capacidad libre siguen el camino de hoy (`POST /api/anuncios`); al particular se le habla de «la capacidad disponible de su plan», nunca de «cupo»."
    - "`tools/auditar-flujos.js` y `tools/auditar-metricas.js` recorren el flujo nuevo sin silenciar nada: el particular nuevo ve el paso del plan sin «cupo» en `main`, el borrador se crea, el visitante recibe 404 al pedirlo, el panel lo ofrece con «Continuar», planes.html no dice «cupo» al particular, y duplicar deja el formulario lleno tras elegir plan."
  artifacts:
    - path: "publicar.html"
      provides: "sección data-paso=\"plan\", bloque fiscal del resumen, textos sin cupo"
      contains: "data-paso=\"plan\""
    - path: "assets/publicar.js"
      provides: "MODO publicación/capacidad, paso del plan, borrador en el servidor, resumen y pago, estado de espera, duplicar como borrador"
      contains: "/borradores"
    - path: "tools/auditar-flujos.js"
      provides: "recorrido del particular por el flujo nuevo, panel y planes"
      contains: "Elige cómo publicar este equipo"
    - path: "tools/auditar-metricas.js"
      provides: "duplicar con el paso del plan cuando lo hay"
      contains: "data-paso=\"plan\""
  key_links:
    - from: "assets/publicar.js paso plan"
      to: "POST /api/borradores"
      via: "crearBorradorEnServidor()"
      pattern: "/borradores'"
    - from: "assets/publicar.js último paso"
      to: "POST /api/borradores/:id/pago"
      via: "pagarPublicacion()"
      pattern: "/pago`"
---

<objective>
El asistente de publicación del particular en el orden nuevo: plan → borrador en el servidor →
formulario → resumen → pago → activación, sin «cupo», y las dos auditorías de navegador que recorren
ese asistente actualizadas para que CI siga en verde de verdad.

Purpose: MOD-05 (el orden y el borrador en el servidor desde que se elige plan), MOD-06 (retomar un
borrador desde el panel), MOD-07 (sin «cupo» en el flujo del particular).
Output: `publicar.html` y `assets/publicar.js` con el modo publicación; `tools/auditar-flujos.js` y
`tools/auditar-metricas.js` al día.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05.2-publicar-este-equipo-borrador-pago-y-activaci-n-del-particul/05.2-CONTEXT.md
@.planning/phases/05.2-publicar-este-equipo-borrador-pago-y-activaci-n-del-particul/05.2-02-SUMMARY.md
@.planning/phases/05.2-publicar-este-equipo-borrador-pago-y-activaci-n-del-particul/05.2-03-SUMMARY.md
@.planning/phases/05.2-publicar-este-equipo-borrador-pago-y-activaci-n-del-particul/05.2-04-SUMMARY.md
@CLAUDE.md

<interfaces>
API (planes 02-03):
- `POST /api/borradores { plan, dias, ...campos opcionales con la forma de anuncioParaApi() }` → 201 `{ borrador }`; 409 para dealer/exenta; 409 con 10 abiertos; 429; 409 `faltan` sin legales.
- `GET /api/borradores/:id` → `{ borrador: { id, estado, plan: { id, nombre, fotos_maximas, videos_maximos, destacado, activo }, dias, pendientePago, pago: { referencia, total, procesador } | null, precio: { total }, completo, falta, datos: { equipo, precio, contacto, fotos, videos } } }` (la forma de `datos` es la de `estadoInicial()` / `cargarCopia()`; `equipo.anio` llega '' si no hay año).
- `PUT /api/borradores/:id` (misma forma que `anuncioParaApi()` sin `membresia`, más `plan`, `dias`) → 200 `{ borrador }`; 409 si hay pago pendiente; 404 si no es suyo; 400 con el error de validación.
- `POST /api/borradores/:id/pago { metodo?, conRnc?, rnc?, razonSocial?, direccionFiscal? }` → 201 `{ anuncio, membresia, cobro: { total, referencia, ... sin base/ajuste }, comprobante, pago }` | 202 `{ pago: { id, estado: 'pendiente' }, cobro, aviso, transferencia? , avisoTransferencia? }` | 402 `{ error, pago }` | 400/409 `{ error }`.
- `DELETE /api/anuncios/:id` (borrador sin pago) → 200; con pago pendiente → 409.
- `GET /api/planes` → `{ planes: [{ id, nombre, precio_vigente, precio, fotos_maximas, videos_maximos, destacado, perfil_publico, activo }], duraciones, metodosPago }`.
- `GET /api/mis-anuncios/:id/copia` → `{ copia }` (lo usa `cargarCopia`).

De assets/publicar.js (buscar por nombre): `PASOS` (5 pasos fijos), `CLAVE_BORRADOR`, `CLAVE_BORRADOR_ANTERIOR` + `rescatarBorradorAnterior` (NO se toca), `estadoInicial`, `guardarBorrador`/`leerBorrador`/`borrarBorrador` (localStorage), `cargarCopia`, `MEMBRESIAS`, `EXENTA`, `conHueco`, `membresiaElegida`, `limiteFotos`, `limiteVideos`, `cargarMembresias`, `pintarSinCupo`, `pintarResumenPublicacion`, `pintarPasoFinal`, `montarPasoConfirmar` (redirige a planes.html sin membresías), `validarConfirmar`, `anuncioParaApi`, `pintarConfirmacion(respuesta)`, `publicar()` (POST /api/anuncios; sin sesión guarda y manda a `cuenta.html?destino=publicar.html&crear=1`), `pintarVistaPrevia`, `pintarResumenPedido`, `VALIDADORES`, `pintarPasos`, `irAPaso`, `leerPaso`, `volcarEstadoAlFormulario`, `montarPublicador` (carga sesión, aviso legal, borrador local, duplicar con `history.replaceState`, monta pasos, vuelca y dispara la cadena categoría → subcategoría → marca → modelo → tren motriz).
- Globales de otros scripts cargados antes: `api`, `cargarSesion`, `haySesion`, `SESION`, `montarAvisoLegal(para)` (una sola caja a la vez), `params`, `esc`, `pesos`, `icono`, `$`, `$$`, `nombreMarca`, `precioCompra` (assets/precios.js, tras la 05.1 devuelve el `total` final).
- assets/cuenta.js `destinoTrasEntrar` solo acepta `^[\w-]+\.html$`: el plan elegido por un visitante NO puede viajar en la URL de vuelta; viaja en la copia de `localStorage`.
- assets/app.js `montarPromo()` pone `hidden = false` a todo `[data-promo-aviso]` con fecha: un aviso que deba ocultarse por modo va dentro de un envoltorio que se oculta.

Auditorías: `tools/auditar-flujos.js` (particular nuevo: hoy espera que `/publicar.html` redirija a `planes.html` y que el cuerpo «explique qué es un cupo») y `tools/auditar-metricas.js` (con la cuenta sembrada `jperez@demo.mercamaquinarias.do`, particular con 2 cupos Destacado llenos: pulsa `[data-duplicar]`, espera `publicar.html` sin `?duplicar=`, lee `#e-modelo`, `#e-serie`, `#avisoBorrador` y pulsa `#btnDescartar` esperando navegación). `tools/check-links.js` exige hijos en `#pasosNav` y `#vistaPrevia` de publicar.html para el visitante.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarea 1: el paso del plan y el borrador en el servidor</name>
  <files>publicar.html, assets/publicar.js</files>
  <read_first>
    - publicar.html entero (444 líneas)
    - assets/publicar.js líneas 1-240 (configuración, estado, localStorage, cargarCopia) y 1290-2035 (capacidad, confirmación, publicación, pasos, arranque)
    - assets/planes.js `tarjetaNivel` y `tarjetaNivelPublicacion` (plan 04) para reutilizar marcado y clases
    - .planning/phases/05.2-publicar-este-equipo-borrador-pago-y-activaci-n-del-particul/05.2-CONTEXT.md (D-01, D-02, D-03, D-06, D-07, D-15)
  </read_first>
  <action>
    Per D-01, D-02, D-03, D-06, D-07 y D-15:
    - `publicar.html`: textos estáticos sin «cupo» (meta description y og:description: 'Publique su maquinaria en MercaMaquinarias: elija cómo publicarla, complete la ficha técnica y cargue las fotografías.'; `.pagina__sub`: 'Elija cómo publicar su equipo, complete la ficha y cargue las fotografías. El anuncio se publica en cuanto se confirma el pago, sin revisión previa ni intermediarios.'; el aviso de promoción pasa a 'La publicación Estándar no se cobra hasta …' con sus mismos atributos `data-promo-*`; comentarios HTML que hablaban de cupos, actualizados). Nueva `<section class="paso" data-paso="plan" aria-labelledby="t-p0" hidden>` ANTES de la del equipo, con `<h2 class="panel__titulo" id="t-p0">` 'Elige cómo publicar este equipo' (texto de D-15), un `<fieldset class="duracion" id="duracionPublicar">` 30/60 como el de planes.html, `<ul class="planes-op planes-op--pagina" id="planesPublicar"></ul>` y `<p class="paso__nota">` 'Paga solo esta publicación. Puede dejarla a medias: queda guardada como borrador en su cuenta.' Dentro de la sección `confirmar`, un bloque `#bloqueFiscalPublicar` (hidden) con la casilla «Necesito comprobante fiscal con RNC» y los campos `#pub-razon`, `#pub-rnc` (maxlength 9, inputmode numeric), `#pub-direccion` con las mismas etiquetas y ayudas que planes.html, y un `<p id="metodoPublicar" hidden>`. El `#bloqueSinCupo` se conserva para el dealer. Reutilizar clases existentes; si hiciera falta una regla nueva de CSS, parar y anotarlo en el SUMMARY en lugar de tocar `styles.css` a ciegas.
    - `assets/publicar.js`, modo: `let MODO = 'capacidad'` y `decidirModo()` tras `cargarSesion` y `cargarMembresias`: `'publicacion'` si no hay sesión, o si la organización es `particular`, no exenta y `conHueco()` está vacío, o si la URL trae `borrador`; en otro caso `'capacidad'` (dealer, exenta, particular con capacidad libre: el camino de hoy, D-02). `pasosDelModo()`: en publicación `[{ id: 'plan', nombre: 'Plan', detalle: 'Cómo publicar' }, ...PASOS sin 'confirmar', { id: 'confirmar', nombre: 'Pago', detalle: 'Resumen' }]`; en capacidad `PASOS` tal cual. `pintarPasos`, `irAPaso`, el texto «Paso N de M» y los validadores usan `pasosDelModo()`; `VALIDADORES.plan = validarPlan`.
    - Estado: `estadoInicial()` gana `idBorrador: null`, `planElegido: ''`, `diasElegidos: 30` (con comentario: el plan vuelve a vivir en el borrador, pero ahora es el del SERVIDOR; el comentario viejo «ni plan ni pago viven en el borrador» se reescribe explicando el cambio del modelo comercial). `leerBorrador` fusiona esas claves nuevas. `rescatarBorradorAnterior` no se toca.
    - Paso del plan: `cargarPlanes()` con `GET /api/planes` (`silencioso: true`) guarda `PLANES` y `METODOS_PAGO`; `pintarPlanesPublicar()` pinta una tarjeta por plan activo con el marcado y las clases de `tarjetaNivel` de planes.js, radio `name="planPublicar"`, nombre `Publicación ${n.nombre}`, precio `precioCompra({ precioUnitario: vigente, cupo: 1, dias }).total` con 'ITBIS incluido', sin el rasgo de página pública. `validarPlan(seccion)` exige un plan elegido (aviso 'Elija cómo quiere publicar este equipo.').
    - `limiteFotos()`/`limiteVideos()` en modo publicación usan los topes del plan elegido.
    - Crear y retomar: al pasar del paso del plan, `asegurarBorrador()`: sin sesión → `guardarBorrador()` (con el plan en la copia local) y `location.href = 'cuenta.html?destino=publicar.html&crear=1'`; con sesión y sin `idBorrador` → `POST /api/borradores` con `{ plan, dias, ...cuerpoParaBorrador() }` (así un duplicado o lo escrito sin sesión entra entero), guardar `idBorrador` y `history.replaceState(null, '', 'publicar.html?borrador=' + id)`; con `idBorrador` y otro plan o días → `PUT` con `{ plan, dias }`. Errores con `try/catch` (api() lanza): 409 de tope o de empresa y 429 → aviso en el paso con el mensaje del servidor; sin conexión → aviso y se queda en el paso.
    - `?plan=<id>&dias=<n>` (desde planes.html) en modo publicación: preselecciona ese plan; con sesión crea el borrador y salta al paso del equipo. Al volver de crear la cuenta con una copia local que tiene `planElegido` y no `idBorrador`, lo mismo.
    - `?borrador=<id>`: `GET /api/borradores/:id`; 404 → aviso 'Ese borrador ya no existe o no es suyo.' y paso del plan con lo que haya en local; si `pendientePago`, `pintarEspera(...)` de la tarea 2 y no se monta el formulario editable; si no, el `estado` sale de `borrador.datos` + plan y días del servidor (el servidor manda sobre la copia local), paso 1 (equipo), `#avisoBorrador` 'Retomando su borrador de Publicación <plan>.' y la misma cadena de `dispatchEvent` de hoy para rellenar los selects.
    - Guardado en el servidor: `cuerpoParaBorrador()` = `anuncioParaApi()` sin `membresia` más `plan` y `dias`; `guardarEnServidor()` hace `PUT` con ese cuerpo, en `irAPaso` sin espera y en el `change` del formulario con una espera de 1,5 s (un solo temporizador); nunca en `input` por tecla. 409 (pago pendiente) → `pintarEspera`; 404 → se olvida `idBorrador` y se avisa; otro error → aviso discreto 'No se pudo guardar en su cuenta; sigue guardado en este navegador.' sin bloquear. `guardarBorrador()` local se sigue llamando siempre (D-06: copia de seguridad).
    - `#btnDescartar` en modo publicación con `idBorrador`: `confirm('¿Descartar este borrador? Se borran sus datos y fotografías.')` → `DELETE /api/anuncios/:id` (409 con pago en espera → se enseña su mensaje y no se descarta) → `borrarBorrador()` → `location.href = 'publicar.html'`.
    - `montarPasoConfirmar`: la redirección a planes.html sin membresías solo en modo capacidad (en publicación ya no existe: el plan se elige aquí).
    - En modo capacidad para un particular (tiene capacidad libre), los textos de `pintarResumenPublicacion`, `pintarResumenPedido`, `pintarConfirmacion` y `validarConfirmar` dejan de decir «cupo»: 'Se publica con la capacidad disponible de su plan', 'Costo: ninguno · usa la capacidad que ya pagó', 'Capacidad libre que le queda'. Para el dealer, como hoy.
    Editar con la herramienta de edición (CRLF); sin backticks ni `${}` en ningún comando de shell.
  </action>
  <verify>
    <automated>node --check assets/publicar.js &amp;&amp; node -e "const h=require('fs').readFileSync('publicar.html','utf8'),s=require('fs').readFileSync('assets/publicar.js','utf8');const m=h.replace(/&lt;!--[\s\S]*?--&gt;/g,'');for(const [t,d] of [['data-paso=\"plan\"',h],['Elige cómo publicar este equipo',h],['/borradores',s],['CLAVE_BORRADOR_ANTERIOR',s],['guardarEnServidor',s]])if(!d.includes(t)){console.error('falta: '+t);process.exit(1)}"</automated>
  </verify>
  <acceptance_criteria>
    - `node --check assets/publicar.js` sale con código 0 y el `node -e` de `<verify>` también.
    - `grep -c "tuequipord:borrador" assets/publicar.js` = 1 (la clave vieja se sigue rescatando).
    - `grep -n -i "cupo" publicar.html` solo devuelve líneas del pie de página (enlace «Planes y precios», que cambia el plan 06) o ids/atributos del bloque del dealer (`bloqueSinCupo`, `textoSinCupo`, `accionesSinCupo`); ningún texto visible del cuerpo.
    - `grep -c "id=\"pasosNav\"" publicar.html` = 1 y `grep -c "id=\"vistaPrevia\"" publicar.html` = 1.
  </acceptance_criteria>
  <done>El particular sin capacidad elige plan primero, el borrador nace en el servidor y se guarda solo mientras rellena; se retoma desde `?borrador=`; el dealer y el particular con capacidad siguen como hoy sin leer «cupo» el particular.</done>
</task>

<task type="auto">
  <name>Tarea 2: resumen, pago, espera de la transferencia y duplicar como borrador</name>
  <files>publicar.html, assets/publicar.js</files>
  <read_first>
    - assets/publicar.js tal como lo dejó la tarea 1 (zonas de confirmación, publicación y arranque)
    - assets/planes.js `htmlTransferencia`, `pintarEspera`, `enlaceComprobante`, `botonCopiar`, `datosFiscales` (para copiar el patrón de la espera y de los datos fiscales, sin requerir planes.js)
    - .planning/phases/05.2-publicar-este-equipo-borrador-pago-y-activaci-n-del-particul/05.2-CONTEXT.md (D-09, D-11, D-12, D-13, D-16)
  </read_first>
  <action>
    Per D-09, D-11, D-12, D-13 y D-16, en `assets/publicar.js` (y el marcado mínimo que falte en `publicar.html`):
    - Último paso en modo publicación: al entrar, `guardarEnServidor()` y `GET /api/borradores/:id` para tener `precio.total`, `completo` y `falta` del servidor (el navegador no calcula lo que se cobra: solo lo enseña). `pintarResumenPublicacion` pinta 'Resumen de su publicación': equipo, `Publicación <plan>` (y 'sale destacado' si lo es), fotografías N de tope, vigencia '<días> días desde que se confirme el pago', y 'Total: RD$X — ITBIS incluido' o 'Sin costo' con total 0; si `falta`, se enseña y el botón queda deshabilitado. `#bloqueFiscalPublicar` visible solo con total > 0; `#metodoPublicar` con 'Forma de pago: transferencia bancaria. Le damos los datos y la referencia al confirmar; el anuncio se publica cuando recibamos el ingreso.' si `METODOS_PAGO` incluye transferencia y total > 0. Junto al botón, la nota legal de planes.js ('Al confirmar el pago acepta las Condiciones de contratación y pagos…'). Llamar a `montarAvisoLegal('pagar')` al entrar en este paso. `#btnPublicar` dice 'Publicar sin costo', 'Pedir datos para transferir RD$X' o 'Pagar RD$X y publicar'.
    - `pagarPublicacion()`: exige `#aceptaCondiciones`; `POST /api/borradores/:id/pago` con `metodo` solo si está en `METODOS_PAGO` y los datos fiscales si se marcó la casilla (nunca importes). En `try/catch`: 201 → `borrarBorrador()` y `pintarConfirmacion` en su variante de publicación (título 'Anuncio publicado', filas Referencia, 'Publicación <plan>', 'Pagado: RD$X — ITBIS incluido' o 'Sin costo', 'Vigente hasta …', Fotografías; sin la fila de cupos libres; botones Ver el anuncio / Ir a mi panel / Publicar otro equipo); 202 → `pintarEspera(r)`; 402 → aviso con el mensaje del servidor y el botón rehabilitado; 400/409 → aviso con el mensaje (si es de fotos de más, se ofrece volver al paso de fotos); sin conexión → aviso 'No hay conexión con el servidor. Su borrador sigue guardado.'.
    - `pintarEspera(r)`: oculta `#publicar`, pinta en `#publicado` 'Esperando confirmación del pago' (D-13) con la referencia y botón «Copiar», el total 'RD$X — ITBIS incluido', y si viene `transferencia`, banco, titular, RNC, tipo y número de cuenta con el `mailto:` a `transferencia.correo` para mandar el comprobante (patrón de `htmlTransferencia` de planes.js, con `esc()` y `encodeURIComponent`); si viene `avisoTransferencia`, ese texto. Cierra con 'Su anuncio se publica cuando confirmemos el ingreso. Puede cerrar esta página: lo encontrará en su panel.' y el enlace al panel. Lo usan el 202 y un `?borrador=` con `pendientePago` (con `borrador.pago`).
    - Duplicar en modo publicación: `?duplicar=` carga la copia como hoy y, en vez de volcarla directamente, arranca en el paso del plan con `#avisoBorrador` 'Copia de <equipo>. Elija cómo publicarla; después cambie lo que sea distinto —número de serie, horas, precio y fotos.'; al elegir plan, `asegurarBorrador()` crea el borrador NUEVO con los datos de la copia (sin serie) y se vuelca al formulario con la misma cadena de `dispatchEvent`. `history.replaceState` sigue quitando `?duplicar=`. Comentario: D-16, la copia es un borrador más y pasa por el pago.
    - Borrador con capacidad libre: si en modo publicación, con un `idBorrador`, `conHueco()` no está vacío (vendió algo entretanto), el resumen ofrece además 'Publicar con la capacidad disponible de su plan (sin pagar)': `POST /api/anuncios` con `anuncioParaApi()` y, si responde 201, `DELETE /api/anuncios/<idBorrador>` (las fotos compartidas no se pierden: `borrarAnuncio` solo borra las rutas que ya nadie usa) y `pintarConfirmacion`. Comentario: D-02, lo ya pagado se usa antes de cobrar otra publicación.
    Editar con la herramienta de edición (CRLF).
  </action>
  <verify>
    <automated>node --check assets/publicar.js &amp;&amp; node -e "const s=require('fs').readFileSync('assets/publicar.js','utf8');for(const t of ['pagarPublicacion','pintarEspera','Esperando confirmación del pago','ITBIS incluido','capacidad disponible de su plan'])if(!s.includes(t)){console.error('falta: '+t);process.exit(1)}"</automated>
  </verify>
  <acceptance_criteria>
    - `node --check assets/publicar.js` sale con código 0 y el `node -e` de `<verify>` también.
    - `grep -c "/pago\`" assets/publicar.js` ≥ 1 (la llamada al pago del borrador) y ninguna llamada manda `total`, `subtotal`, `base` ni `ajuste` en el cuerpo: `grep -n "total:" assets/publicar.js` no aparece dentro de un `cuerpo:` de `api(...)` (revisado a mano y anotado en el SUMMARY).
    - `grep -c "innerHTML" assets/publicar.js` no crece sin su `esc()`: cada `innerHTML` nuevo con datos del servidor pasa por `esc()` (revisado y anotado).
  </acceptance_criteria>
  <done>El particular revisa su publicación con el precio final del servidor, paga o pide los datos de la transferencia, ve el anuncio publicado o la espera del pago, y duplicar crea un borrador nuevo que pasa por el pago.</done>
</task>

<task type="auto">
  <name>Tarea 3: las auditorías de navegador recorren el flujo nuevo</name>
  <files>tools/auditar-flujos.js, tools/auditar-metricas.js</files>
  <read_first>
    - tools/auditar-flujos.js entero (326 líneas)
    - tools/auditar-metricas.js líneas 1-60 y 195-310
    - .github/workflows/desplegar.yml, trabajo `navegador` (cómo se siembra la base, se levanta el servidor y se corre `npm run auditar`)
    - .planning/STATE.md, decisión «Nube, Chrome como root» (envoltorio de Chrome en el scratchpad para correr puppeteer en la nube)
  </read_first>
  <action>
    Sin silenciar ninguna comprobación existente que siga teniendo sentido; las que describían el flujo viejo se sustituyen por su equivalente del nuevo, con comentario de por qué cambian (fase 05.2).
    - `tools/auditar-flujos.js`, bloque «Publicar un equipo» del particular recién registrado: sustituir la espera de redirección a `planes.html` por: `/publicar.html` se queda en `publicar.html`; `.paso[data-paso="plan"]` visible con 'Elige cómo publicar este equipo'; al menos tres `input[name="planPublicar"]` y el texto 'ITBIS incluido'; `main#contenido` sin /cupo/i (`anota('publicar', 'ux', …)` si aparece, con el fragmento); elegir el primer plan y pulsar `#btnSiguiente` → la URL lleva `?borrador=` y `fetch('/api/mis-anuncios')` devuelve un anuncio `estado: 'borrador'`; `fetch('/api/anuncios/<id>', { credentials: 'omit' })` → 404 (`anota('publicar', 'SEGURIDAD', …)` si no); pulsar `#btnSiguiente` con el paso del equipo vacío sigue frenando con aviso (la comprobación de hoy). Después, en el panel del particular: una fila con 'Borrador' y un enlace a `publicar.html?borrador=<id>` con el texto 'Continuar'; `#panelPlan` sin /cupo/i. Y en `planes.html` con esa sesión: `main#contenido` visible sin /cupo/i y al menos un enlace `a[href^="publicar.html?plan="]`.
    - `tools/auditar-metricas.js`, bloque de duplicar: tras llegar a `publicar.html`, si `.paso[data-paso="plan"]` está visible (particular sin capacidad libre, que es el caso de la cuenta sembrada), comprobar que `#avisoBorrador` empieza por 'Copia de', marcar el primer `input[name="planPublicar"]`, pulsar `#btnSiguiente`, esperar a que la URL lleve `?borrador=` y seguir con las comprobaciones de hoy (`#e-modelo` con el modelo del original, `#e-serie` vacío); si el paso del plan no está (cuenta con capacidad), el recorrido de hoy. La comprobación de que la URL no conserva `?duplicar=` se mantiene. Antes de pulsar `#btnDescartar`, registrar `p.once('dialog', (d) => d.accept())` (ahora descartar confirma porque borra el borrador del servidor) y seguir esperando la navegación.
    - Correr las auditorías en local si hay Chrome: con el envoltorio del scratchpad (`--no-sandbox --ignore-certificate-errors`) vía `PUPPETEER_EXECUTABLE_PATH`, una base desechable (`MERCA_DB` en el scratchpad), `npm run db:demo`, el servidor en 8080 y `node tools/auditar-flujos.js && node tools/auditar-metricas.js && node tools/check-links.js`, igual que el trabajo `navegador` de CI. Si no se puede (sin Chrome o el puerto ocupado por otra copia), dejarlo dicho en el SUMMARY: el trabajo `navegador` de CI es la barrera antes de fusionar y el plan 06 lo vuelve a intentar.
  </action>
  <verify>
    <automated>node --check tools/auditar-flujos.js &amp;&amp; node --check tools/auditar-metricas.js &amp;&amp; node -e "const a=require('fs').readFileSync('tools/auditar-flujos.js','utf8'),b=require('fs').readFileSync('tools/auditar-metricas.js','utf8');for(const [t,d] of [['Elige cómo publicar este equipo',a],['publicar.html?borrador=',a],['credentials',a],['data-paso=\"plan\"',b],['dialog',b]])if(!d.includes(t)){console.error('falta: '+t);process.exit(1)}"</automated>
  </verify>
  <acceptance_criteria>
    - `node --check` de las dos auditorías sale con código 0 y el `node -e` de `<verify>` también.
    - `grep -c "explica qué es un cupo" tools/auditar-flujos.js` = 0 (la comprobación del flujo viejo se sustituyó, no se comentó).
    - `grep -c "process.exit(fallos.length ? 1 : 0)" tools/auditar-flujos.js` = 1 (sigue saliendo con 1 ante cualquier hallazgo).
    - Si se corrieron en local: salida de las tres con 0 hallazgos, pegada en el SUMMARY; si no, el motivo en el SUMMARY.
  </acceptance_criteria>
  <done>Las auditorías de navegador comprueban el flujo nuevo del particular (plan primero, borrador en el servidor e invisible para otros, «Continuar» en el panel, sin «cupo») y duplicar como borrador, sin haber silenciado nada.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → /api/borradores y /pago | Todo lo que manda el asistente es manipulable |
| servidor → DOM | Datos del borrador y de la cuenta bancaria pintados con innerHTML |
| URL → publicar.js | `borrador`, `plan`, `dias`, `duplicar` vienen de la URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.2-21 | Tampering | cuerpo del pago | mitigate | El asistente no manda importes; aunque los mandara, el servidor los ignora (plan 03, sección 9) |
| T-05.2-22 | Tampering (XSS) | pintarEspera, resumen, tarjetas de plan | mitigate | `esc()` en todo dato; `encodeURIComponent` en URLs y en el `mailto:` |
| T-05.2-23 | Information disclosure | `?borrador=` ajeno | mitigate | El servidor responde 404 (plan 02); la pantalla solo dice que no existe o no es suyo |
| T-05.2-24 | Denial of service | guardado automático | mitigate | Un temporizador con espera de 1,5 s, nunca por tecla; el servidor limita el PUT a 240/h |
| T-05.2-SC | Tampering | instalación de paquetes | accept | No se instala nada |
</threat_model>

<verification>
- `node --check` de publicar.js y de las dos auditorías en verde.
- `npm run auditar` y `npm run check` (trabajo `navegador` de CI, o en local con el envoltorio de Chrome) sin hallazgos.
</verification>

<success_criteria>
- MOD-05: plan → borrador → formulario → resumen → pago → activación en el asistente.
- MOD-06: `?borrador=` retoma el borrador; el visitante no lo ve.
- MOD-07: el particular no lee «cupo» en publicar.html.
</success_criteria>

<output>
Create `.planning/phases/05.2-publicar-este-equipo-borrador-pago-y-activaci-n-del-particul/05.2-05-SUMMARY.md` when done
</output>

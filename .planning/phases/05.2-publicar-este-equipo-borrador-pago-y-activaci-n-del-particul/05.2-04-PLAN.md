---
phase: 05.2-publicar-este-equipo-borrador-pago-y-activaci-n-del-particul
plan: 04
type: execute
wave: 3
depends_on: ["05.2-02"]
files_modified:
  - assets/panel.js
  - assets/planes.js
  - planes.html
autonomous: true
requirements: [MOD-06, MOD-07]

must_haves:
  truths:
    - "En el panel, cada borrador del particular sale con «Borrador» y un enlace «Continuar» a `publicar.html?borrador=<id>`; si tiene un pago pendiente sale «Esperando confirmación del pago» con su referencia y sin «Continuar» ni «Eliminar»."
    - "Un borrador no ofrece Pausar, Marcar vendido, Duplicar, disponibilidad, motor ni selector de plan: no es un anuncio publicado."
    - "El panel del particular no dice «cupo» en ningún sitio: resume sus publicaciones, dice si le queda capacidad pagada y ofrece «Publicar un equipo»; no le ofrece «Añadir cupos»."
    - "Al marcar vendido, el particular lee «Este equipo fue vendido. Ahora puedes publicar otro equipo» y, solo si `capacidadLibre`, «con la capacidad disponible de tu plan»."
    - "`planes.html` para un visitante sin sesión o un particular presenta «Elige cómo publicar este equipo»: tarjetas Estándar/Destacada/Premium con el precio final «ITBIS incluido» por publicación y un botón a `publicar.html?plan=<id>&dias=<30|60>`, sin la compra de cupos ni la regla del quinto gratis a la vista; el dealer (y quien pulse «¿Vende como empresa?») ve la página de hoy."
    - "`#nivelesLista` y `#tablaPlanes` siguen llenándose para el visitante sin sesión (los exige `tools/check-links.js`)."
  artifacts:
    - path: "assets/panel.js"
      provides: "filas de borrador y de pago pendiente, filtro de borradores, plan del particular sin cupos, mensaje de vendido"
      contains: "Esperando confirmación del pago"
    - path: "assets/planes.js"
      provides: "modo publicación para visitante y particular; modo capacidad para dealer"
      contains: "publicar.html?plan="
    - path: "planes.html"
      provides: "bloques data-modo=\"capacidad\" y data-modo=\"publicacion\""
      contains: "data-modo=\"publicacion\""
  key_links:
    - from: "assets/panel.js filaAnuncio (borrador)"
      to: "publicar.html?borrador=<id>"
      via: "enlace «Continuar»"
      pattern: "publicar\\.html\\?borrador="
    - from: "assets/panel.js marcar vendido"
      to: "respuesta PATCH con capacidadLibre (plan 02)"
      via: "avisoPlan con el texto de D-15"
      pattern: "capacidadLibre"
    - from: "assets/planes.js tarjeta en modo publicación"
      to: "publicar.html?plan=<id>&dias=<n>"
      via: "enlace del botón"
      pattern: "publicar\\.html\\?plan="
---

<objective>
Lo que el particular ve fuera del asistente: su panel con los borradores recuperables y los pagos en
espera, sin la palabra «cupo», y `planes.html` convertido en «Elige cómo publicar este equipo» cuando
entra un particular o un visitante. El asistente de publicación es el plan 05.

Purpose: MOD-06 (el borrador abandonado se recupera desde el panel) y MOD-07 (sin «cupo» para el
particular; tras vender, «publicar otro equipo» con la capacidad que queda).
Output: `assets/panel.js`, `assets/planes.js` y `planes.html` con el modo del particular.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05.2-publicar-este-equipo-borrador-pago-y-activaci-n-del-particul/05.2-CONTEXT.md
@.planning/phases/05.2-publicar-este-equipo-borrador-pago-y-activaci-n-del-particul/05.2-02-SUMMARY.md
@.planning/codebase/CONVENTIONS.md
@CLAUDE.md

<interfaces>
Datos que ya llegan (planes 01-02):
- `GET /api/mis-anuncios` → `anuncios[]` de `db.anunciosDeOrganizacion`, cada uno con `estado` (incluido `'borrador'`), `plan_elegido`, `dias_elegidos`, `plan_elegido_nombre`, `pago_pendiente` (referencia o null), `pendiente_pago` (booleano), `creado`, además de lo de hoy (`marca_nombre`, `modelo`, `anio` — en un borrador sin año llega `1900`, `db.ANIO_SIN_DEFINIR`: no enseñarlo —, `foto`, `vistas`…); `membresias`, `resumen`, `exenta`.
- `GET /api/membresias` → `membresias` (con `anuncios_incluidos`, `ocupados`, `libres`, `fin`, `plan_nombre`), `pagosPendientes` (con `concepto`, `tipo` — `'publicacion'` para las publicaciones —, `referencia`, `total`, `procesador`), `transferencia`, `exenta`.
- `PATCH /api/anuncios/:id { estado: 'vendido' }` → `{ ok, estado, capacidadLibre }`. Sobre un borrador cualquier `estado` → 409.
- `DELETE /api/anuncios/:id` sobre un borrador con pago pendiente → 409 (con el correo de facturación en el texto).
- `SESION.organizacion.tipo` es `'particular'` o `'dealer'` (`sesionPublica` en tools/api.js).

De assets/panel.js (buscar por nombre): `ESTADOS` (ya tiene `borrador`), `textoVigencia`, `MEMBRESIAS`, `EXENTA`, `PAGOS_PENDIENTES`, `guardarPagos`, `cupoTexto`, `barraCupos`, `tarjetaMembresia`, `tarjetaPagoEnEspera`, `pagosEnEsperaHTML`, `pintarPlan`, `selectorPlan`, `botonDisponibilidad`, `filaAnuncio`, `pintarTabla`, `pintarFiltros` (`FILTRO` = todos/activos/inactivos), `montarPanel` con sus manejadores: `[data-plan-de]`, `[data-ampliar]`, `[data-duplicar]`, `[data-borrar]`, `[data-accion]` (confirm de vendido con «su cupo queda libre…»), `avisoPlan(mensaje, error)`, `refrescarCupos()`.
De assets/planes.js: `NIVELES_PLAN`, `MIS_CUPOS`, `EXENTA_PLAN`, `NIVEL_ELEGIDO`, `DIAS_PLAN`, `unitario(n)`, `tarjetaNivel(n)` (radio `name="nivel"`; la 05.1 ya enseña el precio final «ITBIS incluido»), `pintarTablaComparativa` (fila 'Cupo reutilizable al vender'), `pintarMisCupos`, `pintarTodo`, `pintarRegla`, `pintarPedido`, `montarPlanes`, `esDealer()` (de app.js/sesion.js), `destinoPropio()`, `precioCompra` global de assets/precios.js.
De planes.html: `header.pagina .pagina__sub` (habla de cupos), `#estadoCuenta`, sección de niveles con `#duracionPlan`, `[data-promo-aviso]` («los cupos del nivel Estándar…»), `#nivelesLista`, `#notaPremium`, sección «¿Cuántos equipos quiere publicar?» (`#cuantosCupos`, `#resumenPlan`, `#reglaCupos`, `#bloqueComprobante`, `#btnContratar`, `#btnVolverPublicar`), `#tablaPlanes`, sección FAQ «Cómo funcionan los cupos».

Reglas de CLAUDE.md que aplican: `api()` de assets/sesion.js lanza si la respuesta no es ok (cada llamada nueva con su try/catch); `SESION.organizacion` puede faltar (usar `(SESION.organizacion || {})`); todo `innerHTML` con datos pasa por `esc()`; sin compilación: scripts sueltos en el orden de siempre.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarea 1: el panel del particular recupera borradores, enseña el pago en espera y habla sin «cupo»</name>
  <files>assets/panel.js</files>
  <read_first>
    - assets/panel.js entero (1.265 líneas; con atención a las líneas 1-300 y 340-480, y a `montarPanel` 770-1141)
    - .planning/phases/05.2-publicar-este-equipo-borrador-pago-y-activaci-n-del-particul/05.2-CONTEXT.md (D-04, D-13, D-15, D-16)
  </read_first>
  <action>
    Per D-04, D-13, D-15 y D-16, en `assets/panel.js`, con una función `esParticular = () => (SESION.organizacion || {}).tipo === 'particular' && !EXENTA` usada para todo lo que cambia solo para el particular (el dealer ve exactamente lo de hoy; sus textos son de la 05.4):
    - `nombreDe(a)`: `anio` (salvo 1900), marca y modelo; si no queda nada, 'Borrador sin terminar'. Usarla en `filaAnuncio`, `selectorPlan`, `botonDisponibilidad`, eliminar y vendido, en lugar de los `${a.anio} ${a.marca} ${a.modelo}` sueltos.
    - `filaAnuncio(a)` con `a.estado === 'borrador'`: el nombre enlaza a `publicar.html?borrador=<id>` (no a la ficha, que no existe para el público); estado 'Esperando confirmación del pago' si `a.pendiente_pago`, si no 'Borrador'; en vez del selector de plan, el texto `Publicación ${a.plan_elegido_nombre} · ${a.dias_elegidos} días`; vigencia '—'; acciones: sin pago pendiente, un enlace `btn-tabla btn-tabla--fuerte` «Continuar» a `publicar.html?borrador=<id>` y «Eliminar»; con pago pendiente, solo el texto `Ref. ${a.pago_pendiente}` (sin Eliminar: el servidor respondería 409). Nada de Pausar, Marcar vendido, Duplicar, disponibilidad ni motor en un borrador. Todo lo pintado con `esc()`.
    - Filtros: añadir 'Borradores (n)' solo si hay alguno; 'Inactivos' deja de contar borradores; `pintarTabla` filtra en consecuencia. Texto vacío para el filtro de borradores: 'No tiene borradores sin terminar.'
    - `pintarPlan` para el particular: título `<em>Sus</em> publicaciones`; sin membresías: 'Cada equipo se publica con su propio plan: elija Estándar, Destacada o Premium al publicarlo y pague solo esa publicación.' + `pagosEnEsperaHTML()` + botón «Publicar un equipo» a `publicar.html`; con membresías: una línea con cuántas publicaciones activas tiene y, si alguna membresía tiene `libres > 0` o `null`, 'Tiene capacidad disponible en su plan: puede publicar otro equipo sin pagar.' con el botón «Publicar otro equipo»; la lista de membresías se pinta con una `tarjetaPlanParticular(m)` que dice nivel, vencimiento y 'Capacidad para N equipos, M disponible(s)' (sin barra de cupos ni «Añadir cupos»); se conservan «Página pública» y «Sello de verificación» y el botón final pasa a «Ver planes» (`planes.html`).
    - `pagosEnEsperaHTML` para el particular: 'Su anuncio se publica cuando confirmemos el ingreso; le enviaremos el comprobante fiscal por correo.' en lugar de 'Sus cupos aparecerán aquí…'. Para el dealer, igual que hoy.
    - `selectorPlan` para el particular: el motivo ' · sin cupos libres' pasa a ' · sin capacidad libre'.
    - Marcar vendido (`[data-accion]`) para el particular: el `confirm` dice '¿Marcar este equipo como vendido?\n\nEl anuncio deja de aparecer en el catálogo y queda en su historial con sus fotos, visitas y contactos.'; tras un 200, `avisoPlan('Este equipo fue vendido. Ahora puedes publicar otro equipo' + (r.capacidadLibre ? ' con la capacidad disponible de tu plan.' : '.'), false)` — el texto exacto de D-15 — y dejar a la vista, junto al aviso, un enlace «Publicar otro equipo» a `publicar.html`. Para el dealer, el `confirm` y el comportamiento de hoy. La llamada va con `silencioso: true` como hoy; si vuelve null, el botón se rehabilita.
    - Eliminar (`[data-borrar]`): para un borrador, '¿Eliminar este borrador?\n\nSe borran sus datos y fotografías, y no se puede deshacer.' y aviso 'Borrador eliminado.'; para un anuncio del particular, el mismo texto de hoy sin 'Su cupo queda libre…' (sustituido por 'Si estaba publicado, su plan vuelve a tener capacidad para otro equipo mientras dure.') y aviso '<nombre> se eliminó.'; si el servidor responde 409 (pago en espera), se enseña su mensaje tal cual.
    - Duplicar: sin cambios en el enlace (`publicar.html?duplicar=<id>`: el asistente decide si pasa por plan y pago, D-16); actualizar su comentario, que hoy dice «sigue pasando por el cupo»: para el particular sin capacidad la copia entra como borrador nuevo y pasa por el pago, sin atajo.
    Comentarios en el estilo del proyecto (qué falló antes y por qué el código es así). Editar con la herramienta de edición (CRLF).
  </action>
  <verify>
    <automated>node --check assets/panel.js &amp;&amp; node -e "const s=require('fs').readFileSync('assets/panel.js','utf8');for(const t of ['Esperando confirmación del pago','publicar.html?borrador=','Este equipo fue vendido. Ahora puedes publicar otro equipo','con la capacidad disponible de tu plan.','capacidadLibre','Borrador sin terminar'])if(!s.includes(t)){console.error('falta: '+t);process.exit(1)}"</automated>
  </verify>
  <acceptance_criteria>
    - `node --check assets/panel.js` sale con código 0 y el `node -e` de `<verify>` también (los seis textos están).
    - `grep -c "esParticular" assets/panel.js` ≥ 5.
    - `grep -c "Sus cupos aparecerán aquí" assets/panel.js` = 1 (el texto del dealer sigue) y `grep -c "Su anuncio se publica cuando confirmemos el ingreso" assets/panel.js` = 1.
    - La revisión en navegador (panel con un borrador, un pendiente y un vendido, en claro y oscuro, 390 px) queda en la lista del SUMMARY del plan 06; la comprobación automática del «Continuar» y de la ausencia de «cupo» la añade el plan 05 a `tools/auditar-flujos.js`.
  </acceptance_criteria>
  <done>El particular encuentra en su panel sus borradores con «Continuar», ve «Esperando confirmación del pago» en los que esperan una transferencia, no lee «cupo» y, al vender, se le ofrece publicar otro equipo con la frase de D-15.</done>
</task>

<task type="auto">
  <name>Tarea 2: planes.html como «Elige cómo publicar este equipo» para el particular y el visitante</name>
  <files>planes.html, assets/planes.js</files>
  <read_first>
    - planes.html líneas 55-259 (cuerpo principal y FAQ)
    - assets/planes.js entero (651 líneas)
    - assets/precios.js (tras la 05.1: `precioCompra`, `desglose`, `DURACIONES`)
    - tools/check-links.js líneas 20-70 (`#nivelesLista` y `#tablaPlanes` deben quedar con hijos para el visitante)
    - .planning/phases/05.2-publicar-este-equipo-borrador-pago-y-activaci-n-del-particul/05.2-CONTEXT.md (D-01, D-02, D-15)
  </read_first>
  <action>
    Per D-01, D-02 y D-15:
    - En `planes.html`, marcar con `data-modo="capacidad"` lo que habla de cupos y solo sirve para comprar capacidad: el `.pagina__sub` de hoy, el `[data-promo-aviso]` de hoy, la sección entera «¿Cuántos equipos quiere publicar?» (compra, regla, comprobante y botones) y la sección FAQ «Cómo funcionan los cupos». Añadir sus pares `data-modo="publicacion" hidden`: un `.pagina__sub` 'Cada equipo se publica con su propio plan. Elija el nivel, complete el anuncio y pague solo esa publicación: se activa en cuanto se confirma el pago.'; un aviso de promoción con la misma estructura y los mismos `data-promo-hasta`/`data-promo-quedan` que hoy, con 'la publicación Estándar no se cobra hasta el'. OJO: `montarPromo()` de assets/app.js recorre TODOS los `[data-promo-hasta]` y pone `hidden = false` en su `[data-promo-aviso]`, así que el `data-modo` NO puede ir en el propio aviso (montarPromo lo volvería a enseñar): cada aviso va dentro de un `<div data-modo="...">` envoltorio, y es el envoltorio el que se oculta. No se toca app.js; y un FAQ «Cómo funciona la publicación de un equipo» con: ¿Qué pasa cuando vendo el equipo? → 'Lo marca como vendido en su panel: el anuncio sale del catálogo y queda en su historial. Mientras dure el plan que pagó, puede publicar otro equipo en su lugar sin volver a pagar.'; ¿Puedo dejar el anuncio a medias? → 'Sí. Se guarda como borrador en su cuenta y lo retoma desde su panel cuando quiera; no se publica ni se cobra hasta que usted pague.'; ¿Cómo pago? → 'Con el método que se le indique al final del anuncio. Si es por transferencia, el anuncio se publica cuando confirmamos el ingreso.'; y el «¿Qué pasa al terminar el plazo?» de hoy tal cual. Ningún texto nuevo nombra el cupo ni el 3 %.
    - En `assets/planes.js`: `MODO_PLANES = 'publicacion' | 'capacidad'`, decidido en `montarPlanes` después de cargar sesión y membresías: `'capacidad'` si la cuenta es dealer, si es exenta o si la URL trae `modo=empresa`; `'publicacion'` en los demás casos (visitante sin sesión y particular). `aplicarModo()` muestra/oculta los `[data-modo]` y cambia el `h2#t-niveles` a 'Elige cómo publicar este equipo' (texto de D-15) en modo publicación, o 'Elija su nivel' en capacidad.
    - En modo publicación, `tarjetaNivelPublicacion(n)`: misma estructura y clases que `tarjetaNivel` (`plan-op`, cinta, nombre, precio, periodo, lista de rasgos) pero con nombre `Publicación ${n.nombre}`, precio = `precioCompra({ precioUnitario: unitario(n), cupo: 1, dias: DIAS_PLAN }).total` con 'ITBIS incluido' (como la 05.1), periodo `por publicación · ${DIAS_PLAN} días`, sin el rasgo de 'Página pública de su empresa' (solo sirve a un dealer aprobado) y, en lugar del radio, un enlace `btn btn--ambar` 'Publicar con este plan' a `publicar.html?plan=${encodeURIComponent(n.id)}&dias=${DIAS_PLAN}`. `#notaPremium` oculto en este modo. El cambio de duración repinta las tarjetas y los enlaces.
    - En modo publicación, `pintarTablaComparativa` cambia la fila 'Cupo reutilizable al vender' por 'Publicar otro equipo al vender, mientras dure el plan'; en capacidad, como hoy.
    - En modo publicación, `pintarMisCupos` para un particular con membresías: si le queda sitio, 'Tiene capacidad disponible en su plan: puede publicar otro equipo sin pagar.' con el atajo `#btnVolverPublicar` a `destinoPropio() || 'publicar.html'` (recuerda la nota pendiente de STATE: el `destino` sin validar se pasa por `destinoPropio()` aquí, ya que se toca); sin «Añadir cupos», sin «cupos libres» y sin ofrecerle comprar capacidad.
    - Un enlace discreto en modo publicación, al pie de la sección de niveles: '¿Vende como empresa? Vea la capacidad para su inventario' a `planes.html?modo=empresa`; en modo capacidad para un visitante sin sesión, el enlace inverso '¿Es un particular? Publique un solo equipo' a `planes.html`. Para el dealer con sesión no se enseña ninguno.
    - En modo capacidad, TODO se comporta como hoy (contratar, ampliar, regla, comprobante): no se toca esa lógica.
    Comentarios con el porqué (el particular compra la publicación de este equipo, no un cupo; el dealer sigue comprando capacidad hasta la 05.4). Editar con la herramienta de edición (CRLF).
  </action>
  <verify>
    <automated>node --check assets/planes.js &amp;&amp; node -e "const h=require('fs').readFileSync('planes.html','utf8'),s=require('fs').readFileSync('assets/planes.js','utf8');for(const [t,d] of [['data-modo=\"publicacion\"',h],['data-modo=\"capacidad\"',h],['Elige cómo publicar este equipo',s],['publicar.html?plan=',s],['modo=empresa',s+h]])if(!d.includes(t)){console.error('falta: '+t);process.exit(1)}"</automated>
  </verify>
  <acceptance_criteria>
    - `node --check assets/planes.js` sale con código 0 y el `node -e` de `<verify>` también.
    - `grep -c "id=\"nivelesLista\"" planes.html` = 1 y `grep -c "id=\"tablaPlanes\"" planes.html` = 1 (siguen existiendo).
    - Todo `grep -n -i "cupo" planes.html` fuera del pie de página cae dentro de un bloque `data-modo="capacidad"` o en un comentario HTML (comprobado leyendo cada coincidencia y anotado en el SUMMARY).
    - La comprobación en navegador (visitante y particular sin «cupo» en `main`, tarjetas enlazando a `publicar.html?plan=`) la añade el plan 05 a `tools/auditar-flujos.js`; la revisión visual en claro y oscuro va a la lista del plan 06.
  </acceptance_criteria>
  <done>El particular y el visitante ven en planes.html publicaciones por equipo con su precio final y un botón que empieza el borrador con ese plan; el dealer ve la página de hoy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| servidor → DOM | Datos del anuncio y del pago pintados con innerHTML |
| URL → planes.js | `destino` y `modo` vienen de la URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.2-18 | Tampering (XSS) | filaAnuncio de borrador, tarjetaPlanParticular, tarjetaNivelPublicacion | mitigate | Todo dato con `esc()`; ids en URL con `encodeURIComponent` |
| T-05.2-19 | Tampering | destino en planes.js | mitigate | El atajo usa `destinoPropio()` (solo `*.html` del sitio), cierra el pendiente de STATE en la parte que se toca |
| T-05.2-20 | Elevation of privilege | modo publicación/capacidad | accept | Es solo presentación: el servidor decide quién crea borradores (409 al dealer) y quién compra capacidad; `modo=empresa` no da nada que la API no permita ya |
| T-05.2-SC | Tampering | instalación de paquetes | accept | No se instala nada |
</threat_model>

<verification>
- `node --check assets/panel.js assets/planes.js` en verde; los `node -e` de cada tarea en verde.
- Las auditorías de navegador (`npm run check`, `check:contraste` dentro de `npm run auditar`) corren en el trabajo `navegador` de CI y en el plan 06.
</verification>

<success_criteria>
- MOD-06: el borrador abandonado se recupera desde el panel con «Continuar».
- MOD-07: el particular no lee «cupo» en panel ni en planes; tras vender se le ofrece publicar otro equipo con la capacidad que queda.
</success_criteria>

<output>
Create `.planning/phases/05.2-publicar-este-equipo-borrador-pago-y-activaci-n-del-particul/05.2-04-SUMMARY.md` when done
</output>

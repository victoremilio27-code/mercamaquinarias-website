---
phase: 05.1-precio-nico-ajuste-del-3-dentro-del-subtotal-e-itbis-incluid
verified: 2026-09-26T05:13:10Z
status: passed
score: 4/4 criterios de éxito verificados (MOD-01..MOD-04 satisfechos; D-01..D-11 respetadas)
overrides_applied: 0
head: 6a01a2f (rama claude/fase-05.1-precio-unico, 24 commits sobre origin/main)
human_verification:
  - test: "Revisión visual de planes.html en claro y oscuro: tarjetas con un solo precio final (Destacado RD$3,889, Premium RD$6,685, Estándar en promoción «Sin costo»/RD$0) y pie «por equipo · 30 días · ITBIS incluido»; resumen del pedido con una sola fila «Total · ITBIS incluido»."
    expected: "Un único importe por tarjeta y en el resumen; ninguna cifra de subtotal, ITBIS aparte, «cargo», «comisión» ni «3 %»."
    why_human: "Juicio visual y de maquetación; el CI (navegador) solo cubre lo estructural."
  - test: "Confirmación de «añadir cupo» en panel.html, planes.html y publicar.html, y línea «Base · Ajuste · ITBIS · Total» en la consola de pagos (admin), en los dos temas."
    expected: "El comprador ve un solo importe «(ITBIS incluido)»; solo la consola enseña base y ajuste; un pago antiguo sale como «sin ajuste»."
    why_human: "Diálogos confirm() y consola con sesión de personal: no se ven sin navegador con pantalla."
  - test: "Redacción de las Condiciones de contratación v2.1, cláusula 2.1 (legal.html:304)."
    expected: "Victor da por buena la frase «precios finales … incluyen el ITBIS … El comprobante detalla el subtotal gravado y el ITBIS». Todos volverán a aceptarla antes de su próximo pago."
    why_human: "Texto legal: decisión de Victor."
  - test: "Antes de fusionar: respaldo verificado de la base de producción (VACUUM INTO + integrity_check), porque el despliegue aplica 2026-09-pagos-desglose y 2026-09-precios-base."
    expected: "Respaldo del día comprobado antes del merge."
    why_human: "Operación sobre producción; regla de CLAUDE.md."
---

# Fase 05.1: Precio único (ajuste del 3 % dentro del subtotal, ITBIS incluido) — Verificación

**Objetivo de la fase:** que todo precio del sitio salga de una sola fórmula —base × 1,03 × 1,18—,
que el comprador vea siempre el precio final con «ITBIS incluido» y que el comprobante cuadre ante la
DGII con el 3 % dentro del subtotal gravado.
**Verificado:** 2026-09-26T05:13:10Z sobre `6a01a2f` (`git diff origin/main...HEAD`)
**Estado:** passed (con dos avisos no bloqueantes y cuatro puntos para revisión humana)
**Re-verificación:** No, verificación inicial.

## Cumplimiento del objetivo

### Verdades observables (criterios de éxito del ROADMAP)

| # | Verdad | Estado | Evidencia |
|---|--------|--------|-----------|
| 1 | `assets/precios.js` es la única fórmula; navegador y servidor dan el mismo importe para compra, ampliación y renovación; la tasa se cambia en un solo sitio | VERIFICADA | `AJUSTE = 0.03` solo en `assets/precios.js:54` (el grep de `0.03`/`1.03`/`1.18` en `assets/`, `tools/` y `*.html` solo encuentra además un factor tipográfico de `tools/pdf.js:101`, ajeno). `desglose` (`assets/precios.js:137-147`) es el único que aplica ajuste e ITBIS; `precioCompra`, `precioAmpliacion` y `precioRenovacion` pasan por él. Servidor: `tools/api.js:2556` y `:2668` componen el cobro con `precios.precioCompra/precioAmpliacion`, con el plan y la suscripción leídos de la base, nunca del cuerpo. Navegador: `assets/planes.js:140,211`, `assets/panel.js:915`, `assets/publicar.js:1343,1420` usan las mismas funciones y enseñan `.total`. `tools/chat.js:107-112` dejó de multiplicar a mano por (1 + ITBIS) y usa `desglose(...).total`. Final `module.exports` intacto con `AJUSTE` exportado (`assets/precios.js:202-209`). `npm run precios:probar`: 9/9 en verde en esta verificación (casos 1.800→2.188, 3.200→3.889, 5.500→6.685, 2.000→2.431, 0→0). |
| 2 | Tarjetas de planes y resumen de pago con un único precio final «ITBIS incluido»; ninguna pantalla del comprador enseña el 3 % ni un «cargo» | VERIFICADA (visual en revisión humana) | `assets/planes.js:163-164` (tarjeta: `pesos(total)` + «ITBIS incluido»), `:231-234` (resumen: se retiran las filas de subtotal e ITBIS; queda «Total · ITBIS incluido»); `assets/panel.js:927` («(ITBIS incluido)» en lugar de «subtotal + ITBIS»). Las respuestas de compra y ampliación pasan por `cobroPublico` (`tools/api.js:2481-2485`, 8 respuestas + `siguiente` en `:2428`). Barrido de «ajuste/comisión/3 %/más ITBIS» en `planes.html`, `panel.html`, `publicar.html`, `tools/correo.js`, `tools/chat.js`: solo comentarios y un texto de importación de equipos (`tools/chat.js:175`) ajeno a la tarifa. Correos al comprador: `tools/api.js:2533` y `tools/correo.js:1119` dicen «(ITBIS incluido)»; el comprobante conserva Subtotal/ITBIS/Total sin línea de ajuste (D-08; `tools/facturas.js` sin cambios). |
| 3 | Cada pago guarda base, tasa e importe del ajuste, subtotal gravado, tasa e importe de ITBIS y total; el comprobante cuadra y un rechazo no consume NCF | VERIFICADA | Migración `2026-09-pagos-desglose` añade `base`, `ajuste`, `ajuste_tasa`, `itbis_tasa` (`tools/db.js:1027-1032`; también en `db/schema.sql:557-562`, tolerado por el `duplicate column` de `migrar()`). Los dos únicos `INSERT INTO pagos` guardan el desglose: `anotarPago` (`tools/db.js:2836-2842`, importe cero) y `registrarCobro` (`tools/db.js:2935-2941`), que además rechaza un cobro que no cuadre `base + ajuste = subtotal` y `subtotal + ITBIS = total` (`:2926-2931`). Cuadre: `total = subtotal + itbis` por construcción en `desglose`; `emitirPorPago` factura `pago.subtotal/itbis/total` tal cual. Rechazo sin NCF: arnés `tools/probar-pagos.js` sección 12 y `tools/probar-transferencia.js` sección 33 (Premium 6.685 anulado sin NCF ni factura). CI verde en este head. |
| 4 | Estándar y Destacado a RD$1.800 y RD$3.200 con migración al final; lo vendido conserva su `precio_pactado` | VERIFICADA | Migración `2026-09-precios-base` (`tools/db.js:1043-1046`), última entrada de `MIGRACIONES`; ninguna migración anterior tocada (diff solo añade). Premium y retirados sin cambios; promoción (`precio_promocional`, `promo_hasta`) sin tocar (D-11). No hay `UPDATE suscripciones` en la migración: `precio_pactado` existente intacto. Las compras nuevas pactan la base, no el gravado (`tools/db.js:2874` y `:2996`, `base ?? subtotal`), lo que evita sumar el 3 % dos veces en una futura renovación al pactado. Un pago pendiente anterior a la migración (base NULL) se aprueba con `subtotal` como pactado, que en el modelo viejo era la base: correcto. |

**Puntuación:** 4/4 verdades verificadas.

### Decisiones del CONTEXT (D-01..D-11)

| Decisión | Estado | Evidencia |
|----------|--------|-----------|
| D-01 `desglose` único, `subtotal` = gravado | Cumple | `assets/precios.js:137-147` |
| D-02 `AJUSTE` junto a `ITBIS`, exportada | Cumple | `assets/precios.js:54`, `:204` |
| D-03 pesos enteros, tres redondeos como máximo | Cumple | `round(b·0,03)`, `round(subtotal·0,18)`, total = suma exacta |
| D-04 base 0 → todo 0, camino sin NCF | Cumple | `Math.max(0, …)`; `SIN_COSTO = precios.desglose(0)` (`tools/api.js:2385`) |
| D-05 columnas nuevas al final, NULL = pago viejo | Cumple | `tools/db.js:1027-1032`; consola lee NULL como «sin ajuste» (`assets/admin.js:819`) |
| D-06 todo camino guarda el desglose; el servidor recalcula | Cumple | únicos inserts en `tools/db.js:2836,2935`; cobro compuesto en servidor (`tools/api.js:2556,2668`); la sección 25 del arnés prueba que se ignoran `total/subtotal/precio/base/ajuste` del cuerpo |
| D-07 un solo precio final para el comprador | Cumple | ver verdad 2 |
| D-08 comprobante con Subtotal/ITBIS/Total, sin línea de ajuste | Cumple | `tools/facturas.js` sin diff; la línea de cupos lleva el ajuste dentro del precio unitario (`tools/pagos.js:42-46`) |
| D-09 la consola ve base, ajuste, ITBIS y total | Cumple | `tools/db.js:3068`, `assets/admin.js:819` |
| D-10 1.800 / 3.200 por migración | Cumple | `tools/db.js:1043-1046` |
| D-11 promoción del Estándar sigue | Cumple | la migración no toca las columnas de promoción |

### Artefactos

| Artefacto | Esperado | Estado |
|-----------|----------|--------|
| `assets/precios.js` | fórmula y constante | VERIFICADO, cableado en servidor, navegador y asistente |
| `tools/precios.prueba.js` + `precios:probar` + paso de CI | prueba node:test | VERIFICADO (9/9; paso «Formula del precio» en `.github/workflows/desplegar.yml:79-80`) |
| `tools/db.js` (2 migraciones, inserts, consola) | desglose persistido | VERIFICADO |
| `tools/api.js` (`cobroPublico`, `SIN_COSTO`) | respuestas del comprador sin base/ajuste | VERIFICADO |
| `assets/planes.js`, `assets/panel.js`, `tools/chat.js`, `legal.html`, `assets/legales.js` | precio final «ITBIS incluido» | VERIFICADO (legales sube a v2.1, `assets/legales.js:54-58`, y `legal.html:295` coincide) |
| `assets/admin.js` | base y ajuste en la consola | VERIFICADO |
| `tools/probar-pagos.js`, `tools/probar-transferencia.js`, `tools/prueba-chat.js` | arnés | VERIFICADO (CI verde en el head) |

### Enlaces clave

| De | A | Vía | Estado |
|----|---|-----|--------|
| `tools/api.js` compra/ampliación | `assets/precios.js` | `precios.precioCompra/precioAmpliacion` | CABLEADO |
| `tools/api.js` cobro | `tools/db.js` `registrarCobro`/`comprarCupos`/`ampliarCupos` | cobro completo (no el público) | CABLEADO |
| `tools/db.js` `aprobarPago` | `tools/facturas.js` `emitirPorPago` | `pago.subtotal/itbis/total` | CABLEADO, cuadra |
| `assets/planes.js` tarjeta/resumen | `assets/precios.js` | `precioCompra(...).total` | CABLEADO |
| `tools/chat.js` tarifas | `assets/precios.js` | `desglose(...).total` | CABLEADO |
| `tools/db.js` `pagosParaConsola` | `assets/admin.js` `pagoHTML` | columnas `base`, `ajuste` | CABLEADO |

### Comprobaciones de comportamiento

| Comportamiento | Comando | Resultado | Estado |
|----------------|---------|-----------|--------|
| Fórmula y casos de la auditoría §3 | `npm run -s precios:probar` | 9 pass, 0 fail | PASA |
| Resto de la batería (arnés y navegador) | CI del PR (`pruebas` + `navegador`) | verde en `6a01a2f` según el encargo | no se volvió a correr, a propósito |

### Ejecución de sondas

No aplica: la fase no declara sondas `scripts/*/tests/probe-*.sh`.

### Cobertura de requisitos

| Requisito | Planes | Estado | Evidencia |
|-----------|--------|--------|-----------|
| MOD-01 fórmula única, tasa en un sitio, servidor recalcula | 05.1-01, 05.1-02 | SATISFECHO | verdad 1, D-06 |
| MOD-02 el comprador solo ve el precio final; el ajuste solo en la consola | 05.1-02, 05.1-03 | SATISFECHO (ver aviso A2) | verdad 2 |
| MOD-03 cada pago guarda el desglose; el comprobante cuadra | 05.1-02, 05.1-04 | SATISFECHO (ver aviso A1) | verdad 3 |
| MOD-04 precios base 1.800/3.200, lo vendido conserva el pactado | 05.1-02 | SATISFECHO | verdad 4 |

No hay requisitos huérfanos: REQUIREMENTS.md asigna a la fase 05.1 solo MOD-01..MOD-04.

### Revisión de código del diff (cierre de fase)

Revisado lo que pedía el encargo: manipulación de precio, cuadre fiscal, fuga del ajuste al
comprador, migraciones y el final `module.exports` de los módulos compartidos.

- **Manipulación de precio:** sin hallazgos. El cobro lo compone el servidor con precios de la base;
  `registrarCobro` además rechaza un cobro incoherente.
- **Migraciones:** sin hallazgos. Solo se añaden dos entradas al final; `db/schema.sql` declara las
  columnas y `migrar()` tolera el `duplicate column` en bases nuevas; el seed se queda en 2000/3500 a
  propósito y la migración lo corrige también en una base nueva (`INSERT OR IGNORE` no lo revierte).
- **Final de `assets/precios.js`:** intacto.
- **Cuadre fiscal:** cuadra (subtotal + ITBIS = total). Una observación, en el aviso A1.
- **Fuga del ajuste:** ninguna pantalla, correo ni PDF lo enseña. Una observación sobre el JSON, en
  el aviso A2.

### Anti-patrones y avisos

| Archivo | Línea | Patrón | Severidad | Impacto |
|---------|-------|--------|-----------|---------|
| `tools/facturas.js` | 543 | A1: la tasa de ITBIS del comprobante se sigue **deduciendo** como `round4(itbis / subtotal)` | Aviso | Con bases múltiplos de 100 antes salía 0,18 exacto; con el ajuste, el gravado ya no es redondo: 334/1.854 → **0,1802**, 593/3.296 → **0,1799**, 1.020/5.665 → **0,1801**. Esa cifra se guarda en `facturas.itbis_tasa` y la nota de crédito la copia (`tools/facturas.js:656`). El PDF y la plantilla redondean a porcentaje entero (`:292`, `:426`), así que el comprobante sigue diciendo «ITBIS 18 %» y el importe es correcto: no bloquea. Pero el dato fiscal guardado no es la tasa legal, y el lote del contador (fase 12) o un 607 que lea `itbis_tasa` heredarían 0,1802. Ahora `pagos.itbis_tasa` guarda 0,18 exacto: `emitirPorPago` podría preferir `pago.itbis_tasa ?? deducción`. D-01 pedía no tocar `emitirPorPago`, así que decide Victor; lo natural es recogerlo en la 05.3 o en la fase 12. |
| `tools/api.js` / `tools/db.js` | 2366, 2422 / 2783 | A2: el JSON del comprador sigue llevando la base: `GET /api/planes` (`precio`, necesario para que el navegador calcule), y `GET /api/membresias` hace `...s` con `precio_pactado` (ahora la base, p. ej. 3200) y `precio_unitario`, junto a `pagosPendientes[].subtotal` (3296) | Info | Ninguna pantalla lo enseña, así que MOD-02 («el comprador **ve**») se cumple. Pero `cobroPublico` no hace el ajuste invisible a quien abra las herramientas del navegador: con D-01/D-02 (el navegador calcula con la base) es inevitable. Lo digo para que nadie dé por hecho que el 3 % está oculto en la API. |
| `assets/planes.js` | 231 | `<div><dt>…</dt></div>` sin `<dd>` dentro de un `<dl>` | Info | Agrupación de `dl` no válida en HTML. Sin efecto visual probable; conviene ponerle un `<dd>` (p. ej. la vigencia) o sacarlo del `dl`. |

No hay marcadores `TBD/FIXME/XXX/TODO/HACK` en las líneas añadidas.

### Verificación humana requerida

1. **Revisión visual de planes, en claro y oscuro.** Tarjetas con un solo precio final e «ITBIS incluido»; resumen con una sola fila de total. *Por qué humana:* juicio visual.
2. **Confirmaciones de añadir cupo y consola de pagos.** Un solo importe para el comprador; «Base · Ajuste · ITBIS · Total» solo en la consola. *Por qué humana:* diálogos y sesión de personal.
3. **Texto de las Condiciones v2.1, cláusula 2.1** (`legal.html:304`). *Por qué humana:* redacción legal.
4. **Respaldo verificado de producción antes de fusionar**, porque se aplican dos migraciones. *Por qué humana:* operación sobre producción.

Estos puntos no bloquean el estado `passed`, como pedía el encargo.

### Resumen

Se cumple el objetivo de la fase. La fórmula vive en un solo sitio y la usan servidor, navegador y
asistente. Cada pago nuevo guarda el desglose completo y el servidor rechaza uno que no cuadre. Los
precios base cambian por una migración añadida al final, sin reescribir nada vendido. El comprador
solo ve el precio final con «ITBIS incluido», y el comprobante conserva Subtotal/ITBIS/Total con el
ajuste dentro del gravado. No hay brechas bloqueantes. Queda el aviso A1: la tasa de ITBIS guardada
en `facturas` deja de ser 0,18 exacto, aunque el PDF sigue diciendo 18 % y el importe es correcto.
Conviene recogerlo antes del lote del contador.

---

_Verificado: 2026-09-26T05:13:10Z_
_Verificador: Claude (gsd-verifier)_


## Después de la verificación

- **A1 corregido antes de fusionar** (orquestador, 2026-09-26): `tools/facturas.js:emitirPorPago` usa `pago.itbis_tasa` cuando existe y solo deduce la tasa en los pagos anteriores a la 05.1. Motivo: un comprobante emitido no se reescribe, y habría guardado 0,1799 para siempre. `transferencia:probar` §33 comprueba `itbis_tasa === 0.18` (fallaba sin el arreglo).

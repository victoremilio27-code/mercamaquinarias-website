---
phase: 05.1-precio-nico-ajuste-del-3-dentro-del-subtotal-e-itbis-incluid
plan: 04
type: execute
wave: 4
depends_on: ["05.1-02", "05.1-03"]
files_modified:
  - tools/probar-transferencia.js
autonomous: true
requirements: [MOD-01, MOD-02, MOD-03, MOD-04]

must_haves:
  truths:
    - "Por transferencia, de punta a punta: comprar 1 cupo Destacado 30 días deja un pendiente de 3889 (base 3200, ajuste 96, subtotal 3296, ITBIS 593); el correo de datos dice RD$3,889 y no nombra el ajuste; la consola ve base y ajuste; al marcarlo recibido sale un B02 que cuadra 3296 + 593 = 3889."
    - "Anular una transferencia pendiente no mueve la secuencia B02 ni crea factura."
    - "Toda la batería de node (seguridad, pagos, transferencia, facturas, letras, precios, dealer, bitácora, contactos, catálogo, métricas, asistente, codificación, taxonomía) pasa en local."
    - "La revisión visual y lo que solo Victor decide quedan listados en el SUMMARY, sin bloquear el cierre."
  artifacts:
    - path: "tools/probar-transferencia.js"
      provides: "sección 33 «Precio único por transferencia»"
      contains: "33."
  key_links:
    - from: "tools/probar-transferencia.js sección 33"
      to: "POST /api/membresias → registrarCobro → /api/admin/pagos/:id/recibido → pagos.confirmarPago → facturas.emitirPorPago"
      via: "arnés falso de req/res, como la sección 32"
      pattern: "recibido"
---

<objective>
Cerrar la fase: demostrar el precio único por el camino de la transferencia (el que está en
producción, apagado hasta los datos bancarios) de punta a punta, correr la batería completa y dejar
en el SUMMARY la lista de lo que Victor tiene que mirar.

Purpose: los cuatro criterios de éxito de la fase verificados juntos en el camino real de cobro,
antes del PR.
Output: sección 33 en `tools/probar-transferencia.js`; batería en verde; SUMMARY con la lista
para Victor.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05.1-precio-nico-ajuste-del-3-dentro-del-subtotal-e-itbis-incluid/05.1-CONTEXT.md
@.planning/phases/05.1-precio-nico-ajuste-del-3-dentro-del-subtotal-e-itbis-incluid/05.1-02-SUMMARY.md
@.planning/phases/05.1-precio-nico-ajuste-del-3-dentro-del-subtotal-e-itbis-incluid/05.1-03-SUMMARY.md
@CLAUDE.md

<interfaces>
De tools/probar-transferencia.js (sección 32 «Extremo a extremo», ≈942-1040, es el modelo a seguir):
- `encender(cambios)` / `apagar()` encienden y apagan la transferencia con datos de prueba.
- `cuentaConLegales(correo, nombre)` → `{ org, cabeceras }` con los documentos legales aceptados (registra `doc.version`, así que la versión 2.1 de contratación del plan 05.1-03 ya queda aceptada).
- `comprar(cuerpo, quien)`, `misMembresias(quien)`, `pedir({ metodo, url, cuerpo, cabeceras })`, `siguienteB02()`, `consulta`, `ok`, `SELLO`, `dinero(n)` = 'RD$' + toLocaleString('en-US').
- Marcar recibido y anular: las mismas llamadas a `/api/admin/pagos/:id/recibido` y `/api/admin/pagos/:id/anular` con cabeceras de administrador que usa la sección 32 (leerlas allí).
- Los correos se buscan en la bandeja compartida por la referencia de la pasada, nunca por el NCF.
- Después de la sección 32: `apagar()`, el resumen y `process.exit(fallos ? 1 : 0)`.

De tools/db.js tras 05.1-02: `pagoPorId(id)` con `base`, `ajuste`, `ajuste_tasa`, `itbis_tasa`; `pagosParaConsola({ estado })` con esas columnas; `facturaDePago(idPago)`; `facturasDe(idOrg)`.
De tools/facturas.js: `comoHtml(factura)`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarea 1: sección 33, el precio único por transferencia de punta a punta</name>
  <files>tools/probar-transferencia.js</files>
  <read_first>
    - tools/probar-transferencia.js líneas 100-160 (encender, cobroDe), 200-220 (dinero y búsqueda de correos), 480-520 (cuentaConLegales, comprar, misMembresias) y 942-1045 (sección 32 entera y el cierre)
    - .planning/phases/05.1-precio-nico-ajuste-del-3-dentro-del-subtotal-e-itbis-incluid/05.1-CONTEXT.md (Specific Ideas: FISCAL de la sección 39)
  </read_first>
  <behavior>
    - Con la transferencia encendida y una cuenta nueva, `POST /api/membresias` `{ plan: 'destacado', cupo: 1, dias: 30 }` → 202; `d.cobro.total === 3889`; `d.cobro` sin `base`, `ajuste` ni `ajusteTasa`.
    - La fila del pago: pendiente, procesador 'transferencia', base 3200, ajuste 96, ajuste_tasa 0.03, subtotal 3296, itbis 593, itbis_tasa 0.18, total 3889.
    - El correo de datos de transferencia al comprador (buscado por la referencia) contiene «RD$3,889» y no casa con /ajuste|comisi[oó]n|\+ ?3 ?%|RD\$3,200\b/i.
    - `GET /api/membresias` del comprador: `pagosPendientes[0]` sin `base` ni `ajuste`.
    - `pagosParaConsola({ estado: 'pendiente' })` trae la fila con base 3200 y ajuste 96.
    - Marcar recibido por la ruta de administración → comprobante con NCF B02; `siguienteB02()` avanzó exactamente 1; la factura: subtotal 3296, itbis 593, total 3889, subtotal + itbis === total === pago.total; la suscripción creada tiene `precio_pactado === 3200`; `facturas.comoHtml(factura)` no casa con /ajuste|comisi[oó]n|\+ ?3 ?%/i.
    - Una segunda compra de la misma cuenta, `{ plan: 'premium', cupo: 1, dias: 30 }` → pendiente de 6685 (base 5500, ajuste 165, subtotal 5665, itbis 1020); anularla por la ruta de administración → `rechazado`, `siguienteB02()` sin cambio, sin factura para ese pago.
  </behavior>
  <action>
    Añadir en `tools/probar-transferencia.js`, después de la sección 32 y antes de `apagar()` y del resumen, la sección 33 «Precio único por transferencia: el 3 % dentro del subtotal, el comprobante cuadra y anular no gasta NCF» con cada punto de `<behavior>`, calcando la forma de la sección 32 (`encender()`, `cuentaConLegales` con un correo propio de esta sección con `SELLO`, `comprar`, las rutas de administración para marcar y anular, el espía de `PROCESADORES.demo` restaurado en `finally` y la comprobación de que no se llamó). Comentario de cabecera de la sección: es el FISCAL de la sección 39 de `research/modelo-comercial.md` por el camino de cobro que está en producción. Los importes esperados se escriben literales (3889, 3296, 593, 6685…) porque son los de la tabla de la auditoría §3 que Victor aprobó; si no salen, falla la fórmula, no la prueba. Editar con la herramienta de edición (CRLF).
  </action>
  <verify>
    <automated>npm run transferencia:probar</automated>
  </verify>
  <acceptance_criteria>
    - `npm run transferencia:probar` sale con código 0 y su salida contiene «33.» y ninguna línea «FALLA».
    - `grep -c "3889" tools/probar-transferencia.js` ≥ 2 y `grep -c "6685" tools/probar-transferencia.js` ≥ 1.
  </acceptance_criteria>
  <done>El camino de la transferencia demuestra, en CI, el desglose guardado, el precio final en el correo, el ajuste solo en la consola, el B02 que cuadra y la anulación sin NCF.</done>
</task>

<task type="auto">
  <name>Tarea 2: batería completa, barrido de textos del comprador y lista para Victor</name>
  <files>tools/probar-transferencia.js</files>
  <read_first>
    - package.json (scripts)
    - .github/workflows/desplegar.yml (pasos del trabajo `pruebas` y `navegador`)
    - .planning/phases/05-cobro-por-transferencia-bancaria/05-05-SUMMARY.md (cómo se dejó la lista de revisión visual para Victor)
  </read_first>
  <action>
    Correr, uno por uno y en este orden, y corregir en el archivo de origen lo que falle (sin silenciar ni saltarse ninguna comprobación; si una corrección toca un archivo de otro plan, anotarlo como desviación en el SUMMARY): `npm run check:encoding`, `npm run taxonomia`, `npm run facturas:letras`, `npm run precios:probar`, `npm run seguridad:probar`, `npm run dealer:probar`, `npm run bitacora:probar`, `npm run contactos:probar`, `npm run catalogo:probar`, `npm run facturas:probar`, `npm run pagos:probar`, `npm run metricas:probar`, `npm run transferencia:probar`, `npm run chat:probar`.
    Las auditorías con navegador (`npm run auditar`, `npm run check`, `npm run check:motion`) NO se corren en local: en este contenedor de la nube el sandbox de Chrome no se puede activar; las cubre el trabajo `navegador` de CI en el PR. No se añade ningún punto de control humano.
    Barrido de textos del comprador: `grep -rniE "comisi[oó]n|\+ ?3 ?%|ajuste comercial|cargo por servicio" planes.html panel.html publicar.html assets/planes.js assets/panel.js assets/publicar.js tools/correo.js tools/chat.js` no debe devolver nada nuevo respecto de `origin/main` (la «comisión» de «no cobra comisión por venta» ya existía y se queda). `git diff origin/main --stat` no debe tocar ninguna migración anterior de `tools/db.js` ni `tools/facturas.js`.
    Escribir en el SUMMARY de este plan la lista para Victor, sin bloquear el cierre:
    - Revisión visual en claro y oscuro: tarjetas de planes.html (precio final «ITBIS incluido»; Destacado RD$3,889, Premium RD$6,685, Estándar en promoción), resumen del pedido (una sola fila de total), confirmación de ampliar en el panel, línea Base · Ajuste · ITBIS · Total en la consola de pagos.
    - Condiciones de contratación 2.1 reescritas y versión subida a 2.1: todos volverán a aceptarlas antes de su próximo pago. Confirmar la redacción.
    - Al fusionar, el despliegue aplica dos migraciones aditivas (`2026-09-pagos-desglose`, `2026-09-precios-base`); la regla de CLAUDE.md pide respaldo verificado antes de tocar producción: comprobar que el respaldo diario de `tools/tareas.js` es de hoy o correr `npm run respaldo` en el VPS antes de fusionar.
    - Decisiones por defecto que siguen abiertas: P1 pesos enteros (D-03), P3 promoción del Estándar hasta 2026-11-30 (D-11), P9 ampliación al precio de lista (05.4); el estudio `research/precios-mercado.md` pedido por Victor no bloquea.
  </action>
  <verify>
    <automated>npm run check:encoding &amp;&amp; npm run taxonomia &amp;&amp; npm run facturas:letras &amp;&amp; npm run precios:probar &amp;&amp; npm run seguridad:probar &amp;&amp; npm run dealer:probar &amp;&amp; npm run bitacora:probar &amp;&amp; npm run contactos:probar &amp;&amp; npm run catalogo:probar &amp;&amp; npm run facturas:probar &amp;&amp; npm run pagos:probar &amp;&amp; npm run metricas:probar &amp;&amp; npm run transferencia:probar &amp;&amp; npm run chat:probar</automated>
  </verify>
  <acceptance_criteria>
    - Los catorce scripts de `<verify>` salen con código 0.
    - `git diff origin/main -- tools/facturas.js` está vacío.
    - `git diff origin/main -- tools/db.js | grep -E "^-" | grep -vE "^---"` no muestra líneas eliminadas dentro del array `MIGRACIONES` anterior a `'2026-09-pagos-desglose'`.
    - El SUMMARY contiene la sección «Para Victor» con los cuatro puntos de arriba.
  </acceptance_criteria>
  <done>Toda la batería de node está en verde, los textos del comprador no nombran el ajuste, y el SUMMARY deja a Victor la revisión visual, la redacción de 2.1, el respaldo antes de fusionar y las decisiones por defecto.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| comprador → transferencia → consola | El dinero entra fuera del sitio y una persona lo marca |
| despliegue → base de producción | Las migraciones se aplican al fusionar |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.1-12 | Repudiation (fiscal) | B02 emitido al marcar recibido | mitigate | Sección 33: subtotal + ITBIS = total = cobrado, NCF consumido una vez; anular no consume |
| T-05.1-13 | Information disclosure | correo de datos de transferencia | mitigate | Sección 33 comprueba que dice el final (RD$3,889) y no la base ni el ajuste |
| T-05.1-14 | Tampering | base de producción al desplegar | mitigate | Migraciones solo aditivas al final; respaldo verificado antes de fusionar, en la lista para Victor |
| T-05.1-15 | Denial of service | prueba silenciada para publicar | mitigate | Ninguna comprobación se salta ni se desactiva; lo que falle se corrige en origen |
</threat_model>

<verification>
- Batería de node en verde en local; el trabajo `navegador` de CI cubre `auditar`, `check` y `check:motion` en el PR.
- Sección 33 en verde.
</verification>

<success_criteria>
- Los cuatro criterios de éxito de la fase 05.1 del ROADMAP comprobados por arnés: fórmula única (01), precio final a la vista (03), desglose y cuadre fiscal con rechazo sin NCF (02 y 04), precios base con lo vendido intacto (02).
- Lista para Victor escrita en el SUMMARY.
</success_criteria>

<output>
Create `.planning/phases/05.1-precio-nico-ajuste-del-3-dentro-del-subtotal-e-itbis-incluid/05.1-04-SUMMARY.md` when done
</output>

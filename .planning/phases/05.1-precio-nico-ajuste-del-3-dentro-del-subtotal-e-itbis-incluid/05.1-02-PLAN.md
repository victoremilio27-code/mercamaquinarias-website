---
phase: 05.1-precio-nico-ajuste-del-3-dentro-del-subtotal-e-itbis-incluid
plan: 02
type: execute
wave: 2
depends_on: ["05.1-01"]
files_modified:
  - tools/db.js
  - db/schema.sql
  - tools/api.js
  - tools/probar-pagos.js
  - tools/probar-transferencia.js
autonomous: true
requirements: [MOD-01, MOD-02, MOD-03, MOD-04]

must_haves:
  truths:
    - "Cada pago nuevo guarda base, ajuste, ajuste_tasa, subtotal gravado, itbis, itbis_tasa y total; los pagos viejos quedan con NULL y se leen como «sin ajuste» (base = subtotal)."
    - "Comprar 1 cupo Destacado 30 días por la API deja un pago de base 3200, ajuste 96, subtotal 3296, itbis 593, total 3889, y su comprobante con NCF cuadra: 3296 + 593 = 3889 = lo cobrado."
    - "Un pago rechazado no consume NCF: la secuencia B02 y el número de facturas no se mueven."
    - "El servidor recalcula siempre el importe: un `total`, `subtotal` o `precio` en el cuerpo de la petición se ignora."
    - "Los planes quedan en Estándar 1800 y Destacado 3200 de base; Premium sigue en 5500; las suscripciones ya vendidas conservan su `precio_pactado`, que desde ahora es la base (antes del ajuste)."
    - "Ninguna respuesta al comprador (`cobro`, `pagosPendientes`, `membresias[].siguiente`) lleva `base`, `ajuste` ni `ajusteTasa`; la consola (`pagosParaConsola`) sí."
    - "La promoción del Estándar a RD$0 sigue: base 0, ajuste 0, total 0, sin NCF."
  artifacts:
    - path: "tools/db.js"
      provides: "migraciones '2026-09-pagos-desglose' y '2026-09-precios-base' al FINAL de MIGRACIONES; registrarCobro/anotarPago guardan el desglose; pagosParaConsola lo devuelve"
      contains: "'2026-09-precios-base'"
    - path: "tools/api.js"
      provides: "SIN_COSTO desde precios.desglose(0); cobroPublico sin base ni ajuste en las respuestas de compra, ampliación y en `siguiente` de GET /api/membresias"
      contains: "cobroPublico"
    - path: "tools/probar-pagos.js"
      provides: "secciones 23-27: migración, desglose guardado, cuadre fiscal, rechazo sin NCF, servidor recalcula"
  key_links:
    - from: "tools/api.js comprarMembresia/ampliarMembresia"
      to: "precios.precioCompra / precios.precioAmpliacion → desglose"
      via: "el cobro se compone en el servidor y entra entero en registrarCobro"
      pattern: "precios\\.precio(Compra|Ampliacion)\\("
    - from: "tools/db.js registrarCobro"
      to: "pagos.base / pagos.ajuste / pagos.ajuste_tasa / pagos.itbis_tasa"
      via: "INSERT INTO pagos con las columnas nuevas"
      pattern: "ajuste_tasa"
    - from: "tools/db.js aprobarPago (tipo compra)"
      to: "suscripciones.precio_pactado"
      via: "precioPactado = base del pago (o subtotal si el pago es viejo)"
      pattern: "pago\\.base"
---

<objective>
Que cada pago guarde el desglose completo de la fórmula única, que los precios base pasen a
1.800/3.200 por migración, que las respuestas al comprador no lleven el ajuste y que el arnés de
pagos demuestre el cuadre fiscal y que un rechazo no gasta NCF.

Purpose: MOD-03 y MOD-04, y la parte de servidor de MOD-01 (recalcula siempre) y MOD-02 (solo la
consola ve el ajuste).
Output: dos migraciones al final de `MIGRACIONES`, `registrarCobro`/`anotarPago`/`aprobarPago`/
`pagosParaConsola` con el desglose, `cobroPublico` en `tools/api.js`, secciones 23-27 en
`tools/probar-pagos.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-precio-nico-ajuste-del-3-dentro-del-subtotal-e-itbis-incluid/05.1-CONTEXT.md
@.planning/phases/05.1-precio-nico-ajuste-del-3-dentro-del-subtotal-e-itbis-incluid/05.1-01-SUMMARY.md
@.planning/research/auditoria-modelo-comercial.md
@CLAUDE.md

<interfaces>
De assets/precios.js (plan 05.1-01):
- `desglose(base, extra)` → `{ base, ajusteTasa, ajuste, subtotal, itbisTasa, itbis, total, ...extra }`, pesos enteros, `subtotal` = gravado = base + ajuste.
- `precioCompra(...)` y `precioAmpliacion(...)` devuelven ese mismo objeto con sus extras.
- `AJUSTE = 0.03`, `ITBIS = 0.18`.

De tools/db.js (hoy):
- `MIGRACIONES` es un array de `[nombre, [sentencias]]`; la última entrada es `'2026-09-contactos-anuncio'` (termina hacia la línea 1017). `migrar()` corre cada una en transacción y tolera «duplicate column».
- `anotarPago(d, { idOrg, idSusc, cobro, t })` (≈2803): solo importe cero (`soloCero`); INSERT con subtotal, itbis, total, estado 'aprobado', procesador 'sin-costo' si total 0.
- `comprarCupos({ idOrg, idPlan, cupo, dias, cobro })` (≈2829): `otorgarCompra(d, { ..., precioPactado: cobro.subtotal, t })`.
- `registrarCobro({ idOrg, idSusc = null, cobro, intencion })` (≈2881): lanza si `!(cobro.total > 0)`; INSERT pendiente con subtotal, itbis, total, referencia, procesador, intencion.
- `aprobarPago(idPago)` (≈2917): para `intencion.tipo === 'compra'` llama `otorgarCompra(..., precioPactado: pago.subtotal, ...)`.
- `pagosPendientesDe(idOrg)` (≈2991): lo ve el comprador; SELECT id, referencia, total, subtotal, itbis, creado, procesador, intencion. NO se amplía (probar-transferencia comprueba sus campos).
- `pagosParaConsola({ estado, limite })` (≈3016): SELECT de p.subtotal, p.itbis, p.total, ... solo `procesador = 'transferencia'`.
- `rechazarPago(idPago)`, `pagoPorId`, `pagoPorReferencia`, `facturaDePago`, `planPorId`, `planes()` (con `precio_vigente`, `precio_normal`, `en_promo`).

De db/schema.sql: `CREATE TABLE IF NOT EXISTS pagos (...)` (≈línea 540) con subtotal/itbis/total INTEGER NOT NULL, intencion, confirmado, actualizado. Seed de planes (≈794-797): estandar 2000, destacado 3500 con `INSERT OR IGNORE`.

De tools/api.js (hoy):
- `const SIN_COSTO = { subtotal: 0, itbis: 0, total: 0 };` (≈2383), usado como `{ ...SIN_COSTO, referencia, procesador: 'interna' }` para cuentas exentas.
- `comprarMembresia` (≈2527) compone `cobro = { ...precios.precioCompra({ precioUnitario: precioUnitario(plan), cupo, dias }), referencia }` y lo devuelve como `cobro` en 4 `responder(...)` (201 cero, 202 transferencia, 202 en proceso, 201 aprobado).
- `ampliarMembresia` (≈2636) igual con `precios.precioAmpliacion({ precioUnitario: s.precio_unitario, ... })` y 4 `responder(...)` con `cobro`.
- `pagoPublico = (pago) => ({ id, estado })`, `comprobantePublico = (c) => ({ numero, tipo, ncf })` (≈2472).
- `s.precio_unitario` de la ampliación sale del precio de LISTA del plan: NO se toca (P9 se decide en la 05.4).

De tools/facturas.js (no se modifica): `emitirPorPago(pago, ...)` usa `pago.subtotal`, `pago.itbis`, `pago.total` y deduce `itbisTasa = itbis / subtotal`; `comoHtml(factura, opciones)` exportado.
De tools/pagos.js (no se modifica): `lineaDeCupos({ cupo, subtotal })` imprime cantidad × precio unitario = `pago.subtotal` (ya gravado): el ajuste va dentro de la línea, sin línea aparte (D-08).

De tools/probar-pagos.js: helpers `ok`, `lanza`, `consulta`, `ejecuta`, `facturasTotales`, `siguienteB02` (≈294), `cobroDe(subtotal, etiqueta)` (≈97), y dentro del IIFE async `pedir`, `comprar(cuerpo)`, `ampliar(idSusc, cupo)`, `orgApi`, `idSuscApi`, `facturasDelPago`; la última sección es la 22 «Nadie más otorga cupos…», seguida del resumen y `process.exitCode`.
De tools/probar-transferencia.js: helper `cobroDe(subtotal, etiqueta, procesador)` (≈139).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarea 1: migraciones y lo que guarda cada pago, con su prueba en el arnés</name>
  <files>tools/probar-pagos.js, tools/probar-transferencia.js, tools/db.js, db/schema.sql</files>
  <read_first>
    - tools/db.js líneas 40-70 (cabecera de MIGRACIONES), 960-1060 (últimas migraciones y `migrar`), 2785-3050 (`soloCero`, `anotarPago`, `comprarCupos`, `otorgarCompra`, `registrarCobro`, `aprobarPago`, `pagosPendientesDe`, `pagosParaConsola`, `rechazarPago`)
    - db/schema.sql (tabla `pagos` ≈540 y seed de `planes` ≈794)
    - tools/probar-pagos.js líneas 1-140 y 280-400 (helpers, `cobroDe`, `siguienteB02`, secuencia B02) y 600-640 (sección 22 y cierre)
    - tools/probar-transferencia.js líneas 130-160 (`cobroDe`)
    - .planning/phases/05.1-precio-nico-ajuste-del-3-dentro-del-subtotal-e-itbis-incluid/05.1-CONTEXT.md (D-05, D-06, D-10, D-11)
  </read_first>
  <behavior>
    - Tras abrir la base de prueba, `PRAGMA table_info(pagos)` tiene `base`, `ajuste`, `ajuste_tasa`, `itbis_tasa`; la tabla `migraciones` tiene `2026-09-pagos-desglose` y `2026-09-precios-base`.
    - `planPorId('estandar').precio === 1800`, `planPorId('destacado').precio === 3200`, `planPorId('premium').precio === 5500`; el Estándar conserva `precio_promocional = 0` y su `promo_hasta`.
    - `registrarCobro` con `cobro = { ...precios.desglose(3200), referencia, procesador: 'demo' }` guarda base 3200, ajuste 96, ajuste_tasa 0.03, subtotal 3296, itbis 593, itbis_tasa 0.18, total 3889.
    - `registrarCobro` con un cobro sin `base` o incoherente (subtotal ≠ base + ajuste, o total ≠ subtotal + itbis) lanza con `codigo` 500 y no inserta fila.
    - Aprobar ese pago (`db.aprobarPago`) crea la suscripción con `precio_pactado === 3200` (la base, no el gravado).
    - Un pago con `base` NULL (insertado a mano, como los de antes) se aprueba con `precio_pactado === subtotal`.
    - El importe cero (`comprarCupos` con `precios.desglose(0)`) guarda base 0, ajuste 0.
    - `pagosPendientesDe` NO trae `base` ni `ajuste`; `pagosParaConsola` SÍ trae `base`, `ajuste`, `ajuste_tasa`, `itbis_tasa`.
    - `db.rechazarPago` sobre un pendiente con importe deja `rechazado`, `siguienteB02()` igual y `facturasTotales()` igual.
  </behavior>
  <action>
    RED primero, en `tools/probar-pagos.js`:
    - Cambiar el helper `cobroDe(subtotal, etiqueta)` para que componga con la fórmula real: `{ ...precios.desglose(base), referencia: referencia(etiqueta), procesador: 'demo' }` (requerir `../assets/precios.js` junto a los demás `require`, después de fijar las variables de entorno). Renombrar su parámetro a `base`. Las llamadas existentes (7000, 3500, 2000…) pasan a ser bases; la comprobación de la sección de `precio_pactado === 7000` sigue valiendo porque el pactado es ahora la base.
    - Añadir, antes del resumen final y dentro del IIFE, la sección 23 «Precio único: migración y desglose guardado» con cada punto de `<behavior>` (usar `consulta`/`ejecuta` para PRAGMA, la tabla `migraciones` y el INSERT a mano del pago viejo con `base` NULL e `intencion` de compra válida). Comentario de por qué: `research/modelo-comercial.md` §13 pide guardar el desglose en cada transacción y el comprobante tiene que cuadrar ante la DGII.
    - En `tools/probar-transferencia.js`, el mismo cambio en su `cobroDe(subtotal, etiqueta, procesador)`: `{ ...precios.desglose(base), referencia: referencia(etiqueta), procesador }`, con su `require` de `../assets/precios.js` después de las variables de entorno. Nada más en ese archivo.
    Correr `npm run pagos:probar` y ver la sección 23 en rojo.

    GREEN, en `tools/db.js` (per D-05, D-06, D-10):
    - Añadir AL FINAL de `MIGRACIONES` (después de `'2026-09-contactos-anuncio'`, sin tocar ninguna anterior) dos entradas con su comentario de por qué:
      `['2026-09-pagos-desglose', [...]]` con `ALTER TABLE pagos ADD COLUMN base INTEGER`, `... ajuste INTEGER`, `... ajuste_tasa REAL`, `... itbis_tasa REAL`. Comentario: NULL = pago anterior, se lee como sin ajuste (base = subtotal) y su tasa de ITBIS la sigue deduciendo `facturas.emitirPorPago`; nada existente se reescribe.
      `['2026-09-precios-base', [...]]` con `UPDATE planes SET precio = 1800 WHERE id = 'estandar'` y `UPDATE planes SET precio = 3200 WHERE id = 'destacado'`. Comentario: bajada a propósito confirmada por Victor el 2026-09-26 (D-10); Premium y retirados no cambian; lo vendido guarda `precio_pactado`; la promoción del Estándar (`precio_promocional`, `promo_hasta`) no se toca (D-11); el seed de `db/schema.sql` se deja en 2000/3500 porque esta migración corre también sobre una base nueva.
    - `registrarCobro`: además de la guarda actual, lanzar `codigo: 500` si `!Number.isInteger(cobro.base) || !Number.isInteger(cobro.ajuste) || cobro.subtotal !== cobro.base + cobro.ajuste || cobro.total !== cobro.subtotal + cobro.itbis`, con un mensaje que diga que el cobro tiene que salir de `precios.desglose` (el servidor nunca guarda importes armados a mano ni del navegador). Añadir `base, ajuste, ajuste_tasa, itbis_tasa` al INSERT con `cobro.base`, `cobro.ajuste`, `cobro.ajusteTasa ?? null`, `cobro.itbisTasa ?? null`.
    - `anotarPago` (importe cero): añadir las cuatro columnas al INSERT con `cobro.base ?? 0`, `cobro.ajuste ?? 0`, `cobro.ajusteTasa ?? null`, `cobro.itbisTasa ?? null`.
    - `comprarCupos`: `precioPactado: cobro.base ?? cobro.subtotal`. `aprobarPago` (tipo compra): `precioPactado: pago.base ?? pago.subtotal`. Comentario: el pactado es la base antes del ajuste; si guardara el gravado, una renovación o ampliación al pactado (05.3/05.4) cobraría el 3 % dos veces.
    - `pagosParaConsola`: añadir `p.base, p.ajuste, p.ajuste_tasa, p.itbis_tasa` al SELECT. `pagosPendientesDe` NO se toca (es lo que ve el comprador; D-07/D-09).
    - En `db/schema.sql`, añadir las cuatro columnas a `CREATE TABLE IF NOT EXISTS pagos` después de `actualizado`, con un comentario de una línea; `migrar()` ya tolera «duplicate column». El seed de planes NO se toca.
    Editar con la herramienta de edición (CRLF); nada de heredocs.
  </action>
  <verify>
    <automated>npm run pagos:probar &amp;&amp; npm run transferencia:probar &amp;&amp; npm run facturas:probar</automated>
  </verify>
  <acceptance_criteria>
    - `npm run pagos:probar`, `npm run transferencia:probar` y `npm run facturas:probar` salen con código 0.
    - `node -e "const s=require('fs').readFileSync('tools/db.js','utf8');const a=s.indexOf(\"'2026-09-contactos-anuncio'\"),b=s.indexOf(\"'2026-09-pagos-desglose'\"),c=s.indexOf(\"'2026-09-precios-base'\");if(!(a>0&&b>a&&c>b))process.exit(1)"` sale con código 0 (las dos migraciones van al final y en ese orden).
    - `grep -c "UPDATE planes SET precio = 1800 WHERE id = 'estandar'" tools/db.js` = 1 y `grep -c "UPDATE planes SET precio = 3200 WHERE id = 'destacado'" tools/db.js` = 1.
    - `grep -c "pago.base ?? pago.subtotal" tools/db.js` = 1.
    - `git diff --unified=0 tools/db.js` no muestra líneas eliminadas dentro de migraciones anteriores (solo añadidos al final del array y cambios en las funciones de pagos).
    - La salida de `npm run pagos:probar` contiene «23.» y ninguna línea «FALLA».
  </acceptance_criteria>
  <done>Las dos migraciones están al final, cada pago nuevo guarda su desglose, el pactado es la base, la consola recibe el desglose y el comprador no; el arnés lo demuestra.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarea 2: rutas de compra y ampliación con el desglose del servidor, y el cuadre fiscal de punta a punta</name>
  <files>tools/probar-pagos.js, tools/api.js</files>
  <read_first>
    - tools/api.js líneas 2358-2435 (`listarPlanes`, `precioUnitario`, `SIN_COSTO`, `referenciaCobro`, `misPlanes`), 2466-2485 (`pagoPublico`, `comprobantePublico`), 2527-2730 (`comprarMembresia`, `ampliarMembresia`)
    - tools/probar-pagos.js líneas 490-640 (secciones 17-22 por la API y el cierre) y la sección 23 que dejó la tarea 1
    - tools/facturas.js líneas 380-470 (`variablesDe`, `comoHtml`) y 520-560 (`emitirPorPago`)
    - tools/pagos.js líneas 34-100 (`lineaDeCupos`, `emitir`)
  </read_first>
  <behavior>
    - Sección 24: `POST /api/membresias` `{ plan: 'destacado', cupo: 1, dias: 30 }` con demo → 201; la fila del pago: base 3200, ajuste 96, ajuste_tasa 0.03, subtotal 3296, itbis 593, itbis_tasa 0.18, total 3889; `d.cobro.total === 3889`; `d.cobro` NO tiene `base`, `ajuste` ni `ajusteTasa`; la suscripción tiene `precio_pactado === 3200`.
    - Sección 24 (fiscal): la factura del pago tiene NCF B02, `subtotal === 3296`, `itbis === 593`, `total === 3889`, `subtotal + itbis === total === pago.total`; `siguienteB02()` avanzó exactamente 1; `facturas.comoHtml(factura)` no casa con /ajuste|comisi[oó]n|\+ ?3 ?%/i.
    - Sección 25: con `pagos.PROCESADORES.demo` devolviendo rechazado (restaurado en `finally`), `POST /api/membresias` `{ plan: 'premium', cupo: 1, dias: 30 }` → 402; el pago queda `rechazado` con base 5500, ajuste 165, subtotal 5665, itbis 1020, total 6685; `siguienteB02()` y `facturasTotales()` no cambian.
    - Sección 26: el cuerpo `{ plan: 'destacado', cupo: 1, dias: 30, total: 1, subtotal: 1, precio: 1, base: 1, ajuste: 0 }` cobra igual 3889 (el servidor recalcula); la ampliación de 1 a 2 cupos de esa membresía guarda `base === precios.precioAmpliacion({...mismos datos del servidor}).base` y `subtotal === base + ajuste`, y su `d.cobro` no trae `base` ni `ajuste`.
    - Sección 27: el Estándar en promoción → total 0, base 0, ajuste 0, procesador 'sin-costo', sin factura. Con `promo_hasta` puesto en '2020-01-01' (restaurado en `finally`), comprar 1 cupo Estándar 30 días cobra 2188 (base 1800, subtotal 1854, itbis 334) y su factura cuadra.
    - `pagosPendientesDe` de la organización de prueba no tiene la clave `ajuste` en ninguna fila.
    - `GET /api/membresias` de la organización de la API: ninguna `membresias[i].siguiente` tiene `base`, `ajuste` ni `ajusteTasa`, y `siguiente.total` sigue presente.
  </behavior>
  <action>
    RED: añadir a `tools/probar-pagos.js`, después de la sección 23 y antes del resumen, las secciones 24-27 de `<behavior>`, reutilizando `comprar`, `ampliar`, `siguienteB02`, `facturasTotales`, `consulta`, `ejecuta` y el patrón de sustituir `pagos.PROCESADORES.demo` con `try/finally` que ya usan las secciones 18 y 21. Si la B02 de prueba (desde 1 hasta 50) se quedara corta, ampliar el `hasta` donde se carga, no crear otra secuencia. Correr y ver en rojo lo que dependa de `cobroPublico`.

    GREEN, en `tools/api.js` (per D-06, D-07, D-09):
    - `const SIN_COSTO = precios.desglose(0);` en lugar del objeto a mano, con un comentario: así también el importe cero lleva base y ajuste (0) y pasa por la misma fórmula (D-04).
    - Añadir junto a `pagoPublico` una función `cobroPublico` que devuelva el cobro sin `base`, `ajuste` ni `ajusteTasa` (desestructurando y devolviendo el resto). Comentario: la base al lado del subtotal delata el ajuste, y el comprador solo ve el precio final (MOD-02); la consola lo ve por `pagosParaConsola`.
    - En `comprarMembresia` y `ampliarMembresia`, sustituir cada `cobro` que va en un `responder(...)` por `cobro: cobroPublico(cobro)` (son 8: 4 por ruta). El `cobro` completo sigue entrando en `db.registrarCobro`, `db.comprarCupos` y `db.ampliarCupos`.
    - En `misPlanes` (GET /api/membresias, ≈2410), el campo `siguiente` de cada membresía es un `precios.siguienteCupo(...)` y ahora traería base y ajuste al panel del comprador: envolverlo como `cobroPublico(precios.siguienteCupo({...}))` (conserva `total` y `gratuito`, que es lo que pinta `assets/panel.js`).
    - No enviar `AJUSTE` en `listarPlanes` (ruta pública): el navegador ya lo tiene en `assets/precios.js`. No tocar `precioUnitario` ni `s.precio_unitario` de la ampliación (P9, 05.4).
    Editar con la herramienta de edición (CRLF).
  </action>
  <verify>
    <automated>npm run pagos:probar &amp;&amp; npm run seguridad:probar &amp;&amp; npm run transferencia:probar &amp;&amp; npm run dealer:probar</automated>
  </verify>
  <acceptance_criteria>
    - `npm run pagos:probar`, `npm run seguridad:probar`, `npm run transferencia:probar` y `npm run dealer:probar` salen con código 0.
    - `grep -c "cobro: cobroPublico(cobro)" tools/api.js` = 8.
    - `grep -c "const SIN_COSTO = precios.desglose(0)" tools/api.js` = 1.
    - `grep -c "cobroPublico(precios.siguienteCupo" tools/api.js` = 1.
    - `grep -cE '^\s+cobro,\s*$|[:,{] cobro, ' tools/api.js` = 2: solo quedan los dos `cobro,` que entran enteros en `db.registrarCobro({ ... })` (hoy da 10: 8 respuestas + esos 2).
    - La salida de `npm run pagos:probar` contiene «24.», «25.», «26.», «27.» y ninguna línea «FALLA».
  </acceptance_criteria>
  <done>Compra y ampliación guardan el desglose que calcula el servidor, el comprador recibe solo subtotal/ITBIS/total, el comprobante cuadra con el 3 % dentro y un rechazo no gasta NCF, todo probado en el arnés.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → POST /api/membresias y /ampliar | El cuerpo puede traer cualquier importe |
| servidor → comprador (JSON) | Lo que se devuelve no debe enseñar el ajuste |
| migración → base de producción | Se aplica sola al desplegar |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.1-03 | Tampering | comprarMembresia / ampliarMembresia | mitigate | El importe sale de `precios.precioCompra/precioAmpliacion` con el precio de la base; el cuerpo solo aporta plan, cupo, días y método; `registrarCobro` rechaza un cobro que no cuadra con `desglose`; sección 26 lo prueba con `total: 1` |
| T-05.1-04 | Information disclosure | respuestas al comprador | mitigate | `cobroPublico` quita base/ajuste/ajusteTasa; `pagosPendientesDe` no los selecciona; sección 26/27 lo comprueban |
| T-05.1-05 | Repudiation (fiscal) | comprobante con NCF | mitigate | `pagos.subtotal` es el gravado y `emitirPorPago` no cambia; sección 24 y 27 comprueban subtotal + ITBIS = total = cobrado y que el HTML no nombra el ajuste |
| T-05.1-06 | Tampering (fiscal) | rechazo que consume NCF | mitigate | Sección 25: 402, pago rechazado, B02 y facturas quietos |
| T-05.1-07 | Tampering | migración sobre producción | mitigate | Solo `ADD COLUMN` y un `UPDATE planes` de dos filas, al final de `MIGRACIONES`, en transacción; ninguna migración anterior se toca; `precio_pactado` de lo vendido no cambia |
| T-05.1-SC | Tampering | instalación de paquetes | accept | No se instala nada |
</threat_model>

<verification>
- `npm run pagos:probar`, `transferencia:probar`, `facturas:probar`, `seguridad:probar`, `dealer:probar` en verde.
- Las dos migraciones son las dos últimas entradas de `MIGRACIONES`.
</verification>

<success_criteria>
- MOD-03: cada pago guarda base, tasa e importe del ajuste, subtotal gravado, tasa e importe de ITBIS y total; el comprobante cuadra y un rechazo no gasta NCF.
- MOD-04: Estándar 1800 y Destacado 3200 de base; Premium 5500; lo vendido conserva su pactado.
- MOD-01 (servidor): el importe siempre lo recalcula el servidor con la fórmula única.
- MOD-02 (servidor): el ajuste no sale en ninguna respuesta al comprador.
</success_criteria>

<output>
Create `.planning/phases/05.1-precio-nico-ajuste-del-3-dentro-del-subtotal-e-itbis-incluid/05.1-02-SUMMARY.md` when done
</output>

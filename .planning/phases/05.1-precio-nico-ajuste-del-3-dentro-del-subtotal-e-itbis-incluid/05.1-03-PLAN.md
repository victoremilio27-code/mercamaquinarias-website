---
phase: 05.1-precio-nico-ajuste-del-3-dentro-del-subtotal-e-itbis-incluid
plan: 03
type: execute
wave: 3
depends_on: ["05.1-01", "05.1-02"]
files_modified:
  - assets/planes.js
  - assets/panel.js
  - tools/chat.js
  - tools/prueba-chat.js
  - legal.html
  - assets/legales.js
  - assets/admin.js
autonomous: true
requirements: [MOD-01, MOD-02]

must_haves:
  truths:
    - "Cada tarjeta de nivel en planes.html enseña UN precio final (el `total` de la fórmula única) con «ITBIS incluido»; Destacado 30 días sale RD$3,889 y Premium RD$6,685."
    - "El resumen del pedido ya no tiene fila de subtotal ni de ITBIS: un solo «Total · ITBIS incluido» con el `total`, y el botón «Contratar por …» usa ese mismo total."
    - "La confirmación de ampliar cupos del panel dice un solo importe «(ITBIS incluido)», sin «subtotal + ITBIS»."
    - "El asistente anuncia los precios finales con ITBIS incluido, calculados con `precios.desglose`, nunca la base ni el 3 %."
    - "Las Condiciones de contratación (2.1) dicen que los precios que se muestran incluyen el ITBIS, y su versión sube para que se vuelvan a aceptar antes de pagar."
    - "En ninguna pantalla ni texto del comprador aparece «ajuste», «comisión», «cargo» ni «+3 %»; la consola de pagos sí enseña base, ajuste, ITBIS y total."
  artifacts:
    - path: "assets/planes.js"
      provides: "tarjetas y resumen con precio final «ITBIS incluido»"
      contains: "ITBIS incluido"
    - path: "assets/panel.js"
      provides: "confirmación de ampliación con un solo importe"
      contains: "ITBIS incluido"
    - path: "tools/chat.js"
      provides: "tarifas() con precios.desglose(...).total"
      contains: "precios.desglose("
    - path: "assets/legales.js"
      provides: "contratacion versión 2.1, vigente desde 2026-09-26"
    - path: "legal.html"
      provides: "2.1 con precios que incluyen el ITBIS y la línea de versión de contratación en v2.1"
      contains: "incluyen el ITBIS"
    - path: "assets/admin.js"
      provides: "línea Base · Ajuste · ITBIS · Total en cada pago de la consola"
      contains: "Ajuste"
  key_links:
    - from: "assets/planes.js tarjetaNivel/pintarPedido"
      to: "assets/precios.js precioCompra → desglose"
      via: "global cargado por <script src=\"assets/precios.js\"> antes que planes.js"
      pattern: "precioCompra\\("
    - from: "tools/chat.js tarifas"
      to: "assets/precios.js desglose"
      via: "require del módulo compartido"
      pattern: "precios\\.desglose\\("
    - from: "assets/admin.js pagoHTML"
      to: "GET /api/admin/pagos → db.pagosParaConsola (base, ajuste, itbis, total)"
      via: "campos de la fila; los pagos viejos (base NULL) se leen como base = subtotal, sin ajuste"
      pattern: "p\\.ajuste"
---

<objective>
Que el comprador vea siempre un único precio final «ITBIS incluido» —en las tarjetas de planes, el
resumen del pedido, la confirmación de ampliar, el asistente y las condiciones—, que el 3 % no se
nombre nunca ante él, y que la consola de pagos enseñe base, ajuste, ITBIS y total.

Purpose: MOD-02 (lo que se ve) y la parte de pantallas de MOD-01 (el navegador enseña lo que sale de
la misma fórmula que cobra el servidor).
Output: `assets/planes.js`, `assets/panel.js`, `tools/chat.js` (+ su prueba), `legal.html` +
`assets/legales.js` (versión de contratación), `assets/admin.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05.1-precio-nico-ajuste-del-3-dentro-del-subtotal-e-itbis-incluid/05.1-CONTEXT.md
@.planning/phases/05.1-precio-nico-ajuste-del-3-dentro-del-subtotal-e-itbis-incluid/05.1-01-SUMMARY.md
@.planning/phases/05.1-precio-nico-ajuste-del-3-dentro-del-subtotal-e-itbis-incluid/05.1-02-SUMMARY.md
@CLAUDE.md

<interfaces>
De assets/precios.js (plan 05.1-01), global en el navegador y require en node:
- `desglose(base, extra)` → `{ base, ajusteTasa, ajuste, subtotal, itbisTasa, itbis, total, ...extra }`.
- `precioCompra({ precioUnitario, cupo, dias })`, `precioAmpliacion({...})`, `duracion(dias)`, `ITBIS`.

De assets/app.js: `pesos(n)` → 'RD$' + miles con coma (RD$3,889). `esc`, `icono`, `fechaLarga`.

De assets/planes.js (hoy):
- `unitario(n)` (≈64) = precio_vigente o precio.
- `tarjetaNivel(n)` (≈134): `porCupo = Math.round(unitario(n) * duracion(DIAS_PLAN).factor)`; pinta `<span class="plan-op__precio num">${EXENTA_PLAN ? 'Sin costo' : pesos(porCupo)}</span>` y `<span class="plan-op__periodo">por equipo · ${DIAS_PLAN} días</span>`.
- `pedidoPlan()` (≈202) = `{ nivel, ...precioCompra({ precioUnitario: unitario(n), cupo: CUPOS_PEDIDOS, dias: DIAS_PLAN }) }`.
- `pintarPedido()` (≈211): filas «nivel · N equipos → pesos(ped.subtotal)», «Vigencia», «Cupos de regalo», «ITBIS (18 %) → pesos(ped.itbis)», `pedido__total` «Total → pesos(ped.total)»; botón `#btnContratar` «Contratar por pesos(ped.total)» o «Pedir datos para transferir …».
- Líneas ≈106-108 y ≈531-533 ya usan `.total` (siguiente cupo, confirmación de ampliar): se quedan.

De assets/panel.js (hoy, ≈915-927): confirmación de ampliar con `pesos(previo.total)` + «(`pesos(previo.subtotal)` + ITBIS `pesos(previo.itbis)`)». La nota del siguiente cupo (≈101) ya usa `sig.total`.

De tools/chat.js (hoy, `tarifas()` ≈90-115): `pesos = (n) => 'RD$' + Math.round(n).toLocaleString('en-US')`; tres ramas: precio_vigente 0 (promo, «SIN COSTO… (después pesos(precio_normal) por cupo)»), en promoción con precio > 0, y normal «pesos(precio_vigente) por cupo de 30 días (pesos(precio_vigente × (1 + ITBIS)) con ITBIS incluido)».
De tools/prueba-chat.js: `sis = cuerpo.system[0].text`, `comprobar(cond, texto)`; ya exige /SIN COSTO/ y que no salga «RD$0». Usa la base por defecto (no fija MERCA_DB): los precios esperados se calculan con `db.planes()` + `precios.desglose`, no a mano.

De assets/legales.js: documento `{ id: 'contratacion', nombre: 'Condiciones de contratación y pagos', version: '2.0', vigenteDesde: '2026-09-23', obligatorio: false }`; `PARA_PAGAR = ['terminos', 'privacidad', 'contratacion']` hace que al subir la versión se vuelva a pedir la aceptación antes de pagar (`exigirAceptacion` en api.js). Las pruebas registran `doc.version` dinámicamente.
De legal.html: sección de contratación con `<p class="doc-version">v2.0 · vigente desde 2026-09-23</p>` (≈295) y el párrafo 2.1 (≈304) «Los precios se expresan en pesos dominicanos (RD$) y <b>no incluyen el ITBIS</b>, que se agrega conforme a la tasa legal vigente (18 %).»

De assets/admin.js (hoy, ≈797-830): `importePago(n)` con dos decimales; `pagoHTML(p)` pinta concepto e `importePago(p.total)`. `p` viene de `db.pagosParaConsola`, que tras el plan 05.1-02 trae `base`, `ajuste`, `ajuste_tasa`, `subtotal`, `itbis`, `itbis_tasa`, `total` (NULL en `base`/`ajuste` para pagos viejos).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarea 1: planes y panel enseñan un solo precio final «ITBIS incluido»</name>
  <files>assets/planes.js, assets/panel.js</files>
  <read_first>
    - assets/planes.js líneas 50-70, 130-165 y 200-250 (unitario, tarjetaNivel, pedidoPlan, pintarPedido)
    - assets/panel.js líneas 890-935 (confirmación de ampliar)
    - planes.html líneas 245-260 (orden de los script: precios.js antes que planes.js)
    - .planning/phases/05.1-precio-nico-ajuste-del-3-dentro-del-subtotal-e-itbis-incluid/05.1-CONTEXT.md (D-07)
  </read_first>
  <action>
    Per D-07, en `assets/planes.js`:
    - `tarjetaNivel(n)`: sustituir `porCupo` por el `total` de `precioCompra({ precioUnitario: unitario(n), cupo: 1, dias: DIAS_PLAN })` (misma función que cobra; nada de multiplicar a mano). El precio sigue en `.plan-op__precio` (`'Sin costo'` para cuenta exenta, como hoy). En `.plan-op__periodo`, cuando el total sea mayor que 0 y no sea exenta, el texto pasa a «por equipo · N días · ITBIS incluido»; con total 0 (promoción) queda como hoy. Comentario: antes la tarjeta enseñaba el precio antes de ITBIS y el resumen lo desglosaba (auditoría §4.6); ahora es el precio final.
    - `pintarPedido()`: quitar el importe de la fila del nivel (queda solo el rótulo «Nivel · N equipos»; el `dd` puede decir los días o quedar como el nombre del nivel, sin cifra) y quitar la fila «ITBIS (18 %)». La fila `pedido__total` pasa a rotularse «Total · ITBIS incluido» con `pesos(ped.total)`. Vigencia y cupos de regalo se quedan. El botón no cambia (ya usa `ped.total`). Si `ITBIS` deja de usarse en el archivo, no dejar referencias sueltas.
    - No nombrar el 3 %, «ajuste», «cargo» ni «comisión» en ningún texto.
    En `assets/panel.js`, confirmación de ampliar (≈926-927): quitar «(subtotal + ITBIS …)» y dejar «Añadir N cupo(s) cuesta RD$X (ITBIS incluido) por los días que le quedan.\n\n¿Confirma?» con `pesos(previo.total)`.
    No tocar `assets/publicar.js` (ya enseña `p.total`, que ahora lleva el ajuste dentro; su flujo lo rehace la 05.2). Editar con la herramienta de edición (CRLF); sin backticks en heredocs.
  </action>
  <verify>
    <automated>node --check assets/planes.js &amp;&amp; node --check assets/panel.js &amp;&amp; npm run precios:probar</automated>
  </verify>
  <acceptance_criteria>
    - `node --check assets/planes.js` y `node --check assets/panel.js` salen con código 0.
    - `grep -cE "ped\.subtotal|ped\.itbis" assets/planes.js` = 0.
    - `grep -c "previo.subtotal" assets/panel.js` = 0.
    - `grep -c "ITBIS incluido" assets/planes.js` ≥ 2 y `grep -c "ITBIS incluido" assets/panel.js` ≥ 1.
    - `grep -c "porCupo" assets/planes.js` = 0.
    - `grep -niE "comisi[oó]n|\+ ?3 ?%|\.ajuste\b|ajusteTasa|AJUSTE" assets/planes.js assets/panel.js` no devuelve nada.
  </acceptance_criteria>
  <done>Tarjetas, resumen y confirmación de ampliar enseñan un solo precio final con «ITBIS incluido», sacado de la misma función que cobra el servidor.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarea 2: el asistente y las condiciones hablan de precio final con ITBIS incluido</name>
  <files>tools/prueba-chat.js, tools/chat.js, legal.html, assets/legales.js</files>
  <read_first>
    - tools/chat.js líneas 70-125 (`pesos`, `fechaLarga`, `tarifas`)
    - tools/prueba-chat.js líneas 1-110 (arnés, `sis`, comprobaciones de precios)
    - assets/legales.js líneas 1-90 (documentos y final `module.exports`)
    - legal.html líneas 290-310 (sección de contratación)
  </read_first>
  <behavior>
    - Para cada plan de `db.planes()` sin `solo_dealer` con `precio_vigente > 0`, el prompt contiene `pesos(precios.desglose(p.precio_vigente).total)` y NO contiene `pesos(p.precio_vigente) + ' por cupo'` (la base no se anuncia).
    - Para el plan en promoción a 0 con `precio_normal`, el prompt contiene `pesos(precios.desglose(p.precio_normal).total)`.
    - El prompt contiene «ITBIS incluido» y no casa con /ajuste|comisi[oó]n|\+ ?3 ?%/i.
    - Sigue cumpliendo lo que ya se probaba: SIN COSTO, sin «RD$0», sin teléfono, idéntico entre peticiones.
  </behavior>
  <action>
    RED: en `tools/prueba-chat.js`, después de las comprobaciones de «SIN COSTO», añadir las de `<behavior>` calculando lo esperado con `require('../assets/precios.js').desglose` y `require('./db').planes()` (el formato de pesos igual que el de chat.js: 'RD$' + `Math.round(n).toLocaleString('en-US')`). Comentario: antes el asistente multiplicaba a mano por 1,18 —una segunda fórmula— y anunciaba la base; con el ajuste eso habría dado un precio distinto del que cobra el sitio.

    GREEN, en `tools/chat.js` `tarifas()` (per D-01, D-07): el precio anunciado es `precios.desglose(p.precio_vigente).total` y, en la rama de promoción a 0, «después» `precios.desglose(p.precio_normal).total`; en la rama de promoción con precio, el normal también como `desglose(...).total`. Redacción: «RD$X por cupo de 30 días, ITBIS incluido» (y «en promoción (precio normal RD$Y), ITBIS incluido»). Quitar las multiplicaciones `* (1 + precios.ITBIS)`. Nunca escribir la base ni el 3 %.

    En `legal.html`, párrafo 2.1 de contratación: «2.1. Los precios que se muestran en la página de Planes son precios finales en pesos dominicanos (RD$) e <b>incluyen el ITBIS</b> a la tasa legal vigente (18 %). El comprobante detalla el subtotal gravado y el ITBIS.» Sin mencionar el 3 %. Cambiar SOLO la línea `doc-version` de esa sección a «v2.1 · vigente desde 2026-09-26». En `assets/legales.js`, el documento `contratacion` pasa a `version: '2.1'`, `vigenteDesde: '2026-09-26'`, con un comentario de una línea (2.1 decía «no incluyen el ITBIS», contrario a lo que ahora se enseña; subir la versión hace que se vuelva a aceptar antes de pagar por `PARA_PAGAR`). No tocar el final `if (typeof module !== 'undefined' && module.exports)`.
    Editar con la herramienta de edición (CRLF).
  </action>
  <verify>
    <automated>npm run chat:probar &amp;&amp; npm run seguridad:probar &amp;&amp; npm run check:encoding</automated>
  </verify>
  <acceptance_criteria>
    - `npm run chat:probar`, `npm run seguridad:probar` y `npm run check:encoding` salen con código 0.
    - `grep -c "1 + precios.ITBIS" tools/chat.js` = 0 y `grep -c "precios.desglose(" tools/chat.js` ≥ 2.
    - `grep -c "no incluyen el ITBIS" legal.html` = 0 y `grep -c "incluyen el ITBIS" legal.html` = 1.
    - `node -e "const d=require('./assets/legales.js').DOCUMENTOS.find(x=>x.id==='contratacion');if(!d||d.version!=='2.1'||d.vigenteDesde!=='2026-09-26')process.exit(1)"` sale con código 0.
    - `grep -c "v2.1 · vigente desde 2026-09-26" legal.html` = 1.
    - `grep -ciE "ajuste|\+ ?3 ?%" legal.html` = 0 (la «comisión» que ya aparece en 1.1 es «no cobra comisión por venta» y no se toca).
  </acceptance_criteria>
  <done>El asistente y las condiciones anuncian precio final con ITBIS incluido, con la misma fórmula que cobra; la versión de contratación sube para que se acepte de nuevo al pagar.</done>
</task>

<task type="auto">
  <name>Tarea 3: la consola de pagos enseña base, ajuste, ITBIS y total</name>
  <files>assets/admin.js</files>
  <read_first>
    - assets/admin.js líneas 780-835 (`PAGOS_VACIO`, `importePago`, `pagoHTML`)
    - .planning/phases/05.1-precio-nico-ajuste-del-3-dentro-del-subtotal-e-itbis-incluid/05.1-CONTEXT.md (D-09)
  </read_first>
  <action>
    Per D-09, en `pagoHTML(p)` de `assets/admin.js`, añadir debajo de la línea de concepto e importe una línea `<p class="sol__meta num">` con «Base RD$… · Ajuste RD$… · ITBIS RD$… · Total RD$…» usando `importePago`. Para un pago viejo (`p.base == null`), base = `p.subtotal` y el ajuste se escribe «sin ajuste» en lugar de una cifra. Todo lo que venga de la fila pasa por `esc(...)`, como el resto de la función. Comentario: la consola es el único sitio donde se ve el ajuste (MOD-02); el personal lo necesita para cotejar el extracto y el comprobante. No añadir estilos nuevos (reutilizar `sol__meta`). La lista general de todos los pagos por estado es de la 05.4; aquí solo la de transferencias que ya existe.
  </action>
  <verify>
    <automated>node --check assets/admin.js &amp;&amp; npm run transferencia:probar</automated>
  </verify>
  <acceptance_criteria>
    - `node --check assets/admin.js` sale con código 0.
    - `grep -c "Ajuste" assets/admin.js` ≥ 1 y `grep -c "sin ajuste" assets/admin.js` = 1.
    - `grep -cE "p\.base|p\.ajuste" assets/admin.js` ≥ 2.
    - `npm run transferencia:probar` sale con código 0.
  </acceptance_criteria>
  <done>Cada transferencia de la consola enseña base, ajuste, ITBIS y total; los pagos anteriores al cambio salen «sin ajuste».</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| servidor → navegador del comprador | Lo que se pinta y lo que dice el asistente |
| consola (admin) | Único sitio donde se ve el ajuste |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.1-08 | Information disclosure | pantallas del comprador y asistente | mitigate | Solo `total` con «ITBIS incluido»; `grep` de «ajuste/comisión/+3 %» en planes.js, panel.js y legal.html; prueba del prompt en `chat:probar` |
| T-05.1-09 | Tampering | precio enseñado distinto del cobrado | mitigate | Pantallas y asistente usan `precioCompra`/`desglose`, la misma función que el servidor; se quitó la multiplicación por 1,18 del asistente |
| T-05.1-10 | Repudiation | condiciones que contradicen el precio mostrado | mitigate | 2.1 reescrito y versión de `contratacion` subida: `exigirAceptacion(PARA_PAGAR)` pide aceptarla otra vez antes de pagar |
| T-05.1-11 | Tampering (XSS) | fila de pago en la consola | mitigate | Todo campo de la fila pasa por `esc(...)` |
</threat_model>

<verification>
- `node --check` de los tres módulos de navegador; `chat:probar`, `seguridad:probar`, `transferencia:probar`, `check:encoding` en verde.
- La revisión visual de planes.html (tarjetas y resumen), del panel y de la consola en claro y oscuro queda en la lista para Victor (no bloquea: el navegador sin pantalla de la nube no permite activar el sandbox de Chrome; las auditorías puppeteer las corre el trabajo `navegador` de CI).
</verification>

<success_criteria>
- MOD-02: el comprador ve solo el precio final «ITBIS incluido»; el 3 % no aparece en pantallas, asistente ni condiciones; la consola sí lo ve.
- MOD-01 (pantallas): lo que se enseña sale de la misma fórmula que cobra el servidor.
</success_criteria>

<output>
Create `.planning/phases/05.1-precio-nico-ajuste-del-3-dentro-del-subtotal-e-itbis-incluid/05.1-03-SUMMARY.md` when done
</output>

---
phase: 05.1-precio-nico-ajuste-del-3-dentro-del-subtotal-e-itbis-incluid
plan: 02
subsystem: pagos
tags: [precios, itbis, ajuste, pagos, ncf, migraciones]
requires:
  - "05.1-01 (desglose(base) con AJUSTE 0.03)"
provides:
  - "pagos.base, pagos.ajuste, pagos.ajuste_tasa, pagos.itbis_tasa (NULL = pago anterior)"
  - "planes: Estándar 1800 y Destacado 3200 de base por migración; Premium 5500"
  - "registrarCobro rechaza (500) un cobro que no sale de precios.desglose"
  - "precio_pactado = base del pago (o subtotal en pagos viejos)"
  - "cobroPublico en tools/api.js: el comprador nunca recibe base, ajuste ni ajusteTasa"
  - "pagosParaConsola devuelve el desglose para la consola (05.1-03 lo pinta)"
affects:
  - "05.1-03 (consola de pagos: ya recibe base/ajuste; pantallas del comprador sin ajuste)"
  - "05.1-04 (transferencia de punta a punta con el desglose guardado)"
  - "05.3/05.4 (renovación y ampliación al pactado: el pactado ya es la base, sin doble 3 %)"
tech-stack:
  added: []
  patterns:
    - "Guarda de coherencia en registrarCobro: base + ajuste = subtotal y subtotal + itbis = total"
key-files:
  created: []
  modified:
    - tools/db.js
    - db/schema.sql
    - tools/api.js
    - tools/probar-pagos.js
    - tools/probar-transferencia.js
decisions:
  - "precio_pactado guarda la base antes del ajuste; en pagos anteriores (base NULL) el subtotal, que entonces era la base"
  - "registrarCobro exige el desglose entero y coherente; un cobro armado a mano lanza 500 sin insertar"
  - "El seed de planes en db/schema.sql se queda en 2000/3500: la migración 2026-09-precios-base corre también sobre bases nuevas"
metrics:
  duration: "~15 min"
  completed: 2026-09-26
  tasks: 2
  files: 5
---

# Fase 05.1 Plan 02: el desglose en cada pago y el ajuste fuera de la vista del comprador — resumen

Cada pago guarda ahora base, ajuste (importe y tasa), subtotal gravado, ITBIS (importe y tasa) y
total; los precios base pasan a 1.800/3.200 por migración; el pactado de una membresía es la base;
y ninguna respuesta al comprador lleva la base ni el ajuste. El arnés de pagos demuestra de punta a
punta que 1 Destacado de 30 días cobra 3.889 y su B02 dice 3.296 + 593 = 3.889, y que un rechazo no
gasta NCF.

## Qué se hizo

**Tarea 1 (TDD): migraciones y lo que guarda cada pago.**
- RED `79d2519`: `cobroDe` de `probar-pagos.js` y `probar-transferencia.js` compone con
  `precios.desglose(base)`; sección 23 nueva con columnas, migraciones, precios base, cobro
  incoherente rechazado, pactado = base, pago viejo sin base, consola sí / comprador no y rechazo
  sin NCF (12 fallos en rojo).
- GREEN `18beba5` en `tools/db.js`: al final de `MIGRACIONES`, `2026-09-pagos-desglose` (cuatro
  `ADD COLUMN`) y `2026-09-precios-base` (dos `UPDATE planes`), con sus comentarios; ninguna
  migración anterior tocada. `registrarCobro` valida y guarda el desglose; `anotarPago` guarda base
  y ajuste (0 en el importe cero); `comprarCupos` y `aprobarPago` pactan `base ?? subtotal`;
  `pagosParaConsola` selecciona las cuatro columnas. `db/schema.sql` las declara en la tabla.

**Tarea 2 (TDD): rutas y cuadre fiscal.**
- RED `93de996`: secciones 24-27 (Destacado 3200 → 3889 con B02 que cuadra y HTML que no nombra
  el ajuste; Premium rechazado 402 con 5500/165/5665/1020/6685 y B02 quieta; el servidor ignora
  `total/subtotal/precio/base/ajuste` del cuerpo y la ampliación sale de `precioAmpliacion`;
  Estándar en promoción a 0 sin NCF y, vencida, 1800 → 2188 con comprobante que cuadra;
  `pagosPendientesDe` y `GET /api/membresias` sin ajuste). 4 fallos en rojo, todos de `cobroPublico`.
- GREEN `fd7d0aa` en `tools/api.js`: `SIN_COSTO = precios.desglose(0)`, `cobroPublico` junto a
  `pagoPublico`, en las 8 respuestas de compra/ampliación y en `siguiente` de `GET /api/membresias`.
  El cobro completo sigue entrando en `registrarCobro`, `comprarCupos` y `ampliarCupos`.

## Verificación

- `pagos:probar` 112 comprobaciones en verde (antes 75), `transferencia:probar` 196, `facturas:probar`,
  `seguridad:probar`, `dealer:probar`, y además `precios:probar`, `bitacora:probar`,
  `metricas:probar`, `contactos:probar`, `catalogo:probar`, `chat:probar`, `facturas:letras`: todos
  con código 0.
- Criterios de aceptación: orden de las migraciones comprobado con el `node -e` del plan; los dos
  `UPDATE` y `pago.base ?? pago.subtotal` aparecen una vez; `git diff` de `tools/db.js` solo borra
  líneas de las funciones de pagos; `cobro: cobroPublico(cobro)` = 8, el patrón de `cobro,` sueltos
  = 2 (los de `registrarCobro`), `SIN_COSTO` y `cobroPublico(precios.siguienteCupo` = 1 cada uno.
- Las auditorías de navegador no arrancan Chrome en este contenedor; corren en el CI.

## Commits

| Tarea | Commit | Mensaje |
|-------|--------|---------|
| 1 (RED) | 79d2519 | test(05.1-02): el arnés de pagos exige el desglose guardado y los precios base |
| 1 (GREEN) | 18beba5 | feat(05.1-02): cada pago guarda el desglose y los precios base pasan a 1800/3200 |
| 2 (RED) | 93de996 | test(05.1-02): cuadre fiscal de punta a punta y ajuste oculto al comprador |
| 2 (GREEN) | fd7d0aa | feat(05.1-02): el comprador recibe el cobro sin base ni ajuste |

## Desviaciones del plan

**1. [Regla 1 - Prueba] La sección 15 de `probar-pagos.js` comparaba la línea de la factura con 3000**
- **Encontrado en:** tarea 1, al pasar `cobroDe` a la fórmula real.
- **Problema:** con `cobroDe(3000)` el subtotal facturado es ya el gravado (3090), así que
  `cantidad × precio_unitario === 3000` dejaba de valer aunque la factura fuera correcta.
- **Arreglo:** se compara con `p.subtotal` del propio pago, con un comentario de que el ajuste va
  dentro de la línea (D-08). No se tocó `tools/pagos.js`.
- **Commit:** 79d2519

Nada más: el resto se ejecutó tal cual.

## Para los planes siguientes

- La consola ya recibe `base`, `ajuste`, `ajuste_tasa`, `itbis_tasa` en `pagosParaConsola`; falta
  pintarlos (05.1-03). En pagos viejos llegan como `null`.
- `tools/seed.js` ya componía con `precios.precioCompra`, así que pasa la guarda nueva sin cambios.
- `s.precio_unitario` de la ampliación sigue saliendo del precio de lista del plan (P9, 05.4).

## TDD Gate Compliance

Tarea 1: RED `79d2519` → GREEN `18beba5`. Tarea 2: RED `93de996` → GREEN `fd7d0aa`. Sin refactor.

## Self-Check: PASSED

- FOUND: tools/db.js, db/schema.sql, tools/api.js, tools/probar-pagos.js, tools/probar-transferencia.js
- FOUND: 79d2519, 18beba5, 93de996, fd7d0aa

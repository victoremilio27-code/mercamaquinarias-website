---
phase: 06-cardnet-construido-probado-y-apagado
plan: 08
type: execute
wave: 7
depends_on: ["06-07"]
files_modified:
  - tools/probar-cardnet.js
  - deploy/README.md
autonomous: true
requirements: [PAGO-04, PAGO-05, PAGO-06, PAGO-07, PAGO-08]

must_haves:
  truths:
    - "Una sección de extremo a extremo del arnés recorre, contra el doble y por las rutas HTTP, los cinco criterios de la fase en el orden del ROADMAP, y cada uno imprime su número."
    - "La misma sección, con CardNet apagado, demuestra que compra, panel, notificación, CSP, tareas e informe se comportan como tras la fase 5."
    - "`deploy/README.md` dice exactamente qué pedir a CardNet, cómo encender `lab`, qué URL de notificación registrar, cómo instalar el temporizador de conciliación, cómo pasar a `produccion` y cómo apagar, sin ningún valor de clave."
    - "La batería completa del job `pruebas` y las auditorías con navegador pasan con CardNet apagado."
  artifacts:
    - path: "tools/probar-cardnet.js"
      provides: "sección extremo a extremo con los cinco criterios"
      contains: "extremo a extremo"
    - path: "deploy/README.md"
      provides: "sección Cobro con tarjeta (CardNet)"
      contains: "MERCA_CARDNET"
  key_links:
    - from: "tools/probar-cardnet.js extremo a extremo"
      to: "POST /api/pagos/cardnet/notificacion"
      via: "api.manejar con Basic correcto, dos veces"
      pattern: "cardnet/notificacion"
---

<objective>
Cerrar la fase demostrando los cinco criterios de principio a fin con el doble de
CardNet, y dejar escrito el procedimiento para que el día de la afiliación sea
encender: qué pedir, qué configurar, cómo comprobar y cómo apagar.

Purpose: los cinco criterios del ROADMAP y PAGO-04 a PAGO-08 juntos, más la
condición de la fase: construido, probado y apagado.
Output: sección final de `tools/probar-cardnet.js` y sección de `deploy/README.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/phases/06-cardnet-construido-probado-y-apagado/06-CONTEXT.md
@.planning/phases/06-cardnet-construido-probado-y-apagado/06-07-SUMMARY.md
@.planning/phases/06-cardnet-construido-probado-y-apagado/06-VALIDATION.md
@deploy/README.md
@.planning/research/cardnet.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarea 1: los cinco criterios de extremo a extremo</name>
  <files>tools/probar-cardnet.js</files>
  <read_first>
    - tools/probar-cardnet.js (entero: helpers, doble, secciones de 06-01 a 06-07)
    - .planning/ROADMAP.md (Phase 6, Success Criteria)
    - tools/probar-transferencia.js (su sección de extremo a extremo, como modelo)
  </read_first>
  <behavior>
    - Criterio 1: barrera de PCI en verde; un recorrido completo de compra con tarjeta nueva (compra → 202 con `urlCaptura` → confirmar con un cuerpo que incluye propiedades inventadas → aprobado) no escribe en la base ni en `pagos_eventos` ninguna cadena de 13 a 19 dígitos seguidos, y el token usado es el del `Customer` del doble.
    - Criterio 2: con CardNet apagado, `GET /api/planes`, `POST /api/membresias`, `GET /api/membresias`, la notificación, la CSP, `reconciliar`, `renovar` e informe responden como la referencia de la fase 5 (sin propiedades `cardnet`/`tarjetas`, 404 en la notificación, CSP idéntica, tareas sin acciones, informe idéntico).
    - Criterio 3: en `lab` con el doble aprobando, una organización nueva compra cupos con tarjeta, obtiene cupos y un comprobante con NCF, y publica un anuncio; con el doble rechazando, otra compra deja 402, sin cupos, sin factura y la secuencia B02 igual.
    - Criterio 4: la misma notificación entregada dos veces → una factura y B02 +1; el cuerpo de `purchase` que vio el doble para RD$2.000 de subtotal lleva `Amount` = `aCentavos(total)` y, para un total de 2000, `200000`.
    - Criterio 5: un pago aprobado en el doble y pendiente en la base (sin notificación ni confirmación) queda aprobado tras `pagos.reconciliar`, con una factura, y `componerInforme` sobre `db.informe` del día lista su referencia en «Pasarela de pago».
    - El procesador `demo` no se llama ni una vez con CardNet activo.
  </behavior>
  <action>
Sección final `console.log('\nN. Extremo a extremo: los cinco criterios de la fase')`
con subsecciones «Criterio 1» a «Criterio 5» y «Apagado», sobre una base limpia
dentro de la desechable (organizaciones y usuarios nuevos creados por la API o con
las funciones de `db.js` que ya usan las secciones anteriores). Reutiliza los
helpers existentes; si alguno falta, se añade aquí, no en otra prueba. Envolver
`PROCESADORES.demo` para contar llamadas. Si al escribirla aparece un fallo real
en el código de los planes anteriores, se corrige en el archivo correspondiente
en un commit `fix(06-08): ...` y se anota como desviación: no se ajusta la prueba
para que pase.
  </action>
  <verify>
    <automated>npm run cardnet:probar</automated>
  </verify>
  <acceptance_criteria>
    - `tools/probar-cardnet.js` contiene `extremo a extremo` y las cadenas `Criterio 1` … `Criterio 5`.
    - `npm run cardnet:probar` sale 0.
  </acceptance_criteria>
  <done>Los cinco criterios del ROADMAP tienen una prueba que los recorre enteros y corre en CI.</done>
</task>

<task type="auto">
  <name>Tarea 2: el procedimiento de encendido y la batería completa</name>
  <files>deploy/README.md</files>
  <read_first>
    - deploy/README.md (secciones de Anthropic y de la transferencia, como modelo de estilo)
    - .planning/research/cardnet.md («Qué hace falta para contratarlo» y «Resumen de lo que hay que preguntarle a CardNet»)
    - .planning/phases/06-cardnet-construido-probado-y-apagado/06-CONTEXT.md (Preguntas abiertas)
  </read_first>
  <action>
**`deploy/README.md`** (LF, como todo `deploy/`): sección «Cobro con tarjeta
(CardNet)» después de la de la transferencia, con:
1. Qué es y por qué está apagado (una línea; remite a `.planning/research/cardnet.md`).
2. Lo que hay que tener de CardNet antes de encender: las preguntas abiertas del
   CONTEXT, con la de `DataDo.Invoice` primera y en negrita (si la respuesta es
   que va el NCF, NO se enciende: hay que rediseñar).
3. Encender en `lab`: las tres líneas `MERCA_CARDNET=lab`,
   `MERCA_CARDNET_LLAVE_PUB=<…>`, `MERCA_CARDNET_LLAVE_PRIV=<…>` para
   `/etc/mercamaquinarias.env` (marcadores entre ángulos, ningún valor),
   `systemctl restart mercamaquinarias`, y cómo comprobarlo: el aviso de arranque
   ya no sale, `planes.html` ofrece «Tarjeta», la CSP lleva `frame-src` de lab.
4. Pedir a CardNet que registre la URL de notificación
   `https://mercamaquinarias.com/api/pagos/cardnet/notificacion` (comprobar el
   dominio real en `deploy/nginx.conf`).
5. Instalar el temporizador: copiar `mercamaquinarias-pagos.service` y `.timer` a
   `/etc/systemd/system/`, `systemctl daemon-reload`, `systemctl enable --now
   mercamaquinarias-pagos.timer`, `systemctl list-timers | grep pagos`. Inocuo con
   CardNet apagado.
6. Lista de comprobación en lab con tarjetas de prueba de CardNet: compra aprobada
   (NCF emitido), rechazada (sin NCF), activación de perfil si aparece, la
   notificación llega (`pagos_eventos`), conciliación (parar el servicio a mitad y
   ver que el temporizador lo recupera), renovación forzando `renovacion_proxima`
   en una membresía de prueba. Lo que no cuadre con lo supuesto se corrige en la
   función aislada correspondiente (`normalizar`, `perfilesDe`, `cuerpoCliente`,
   `compraDeNotificacion`, `escucharFin`).
7. Pasar a `produccion` tras la certificación: cambiar el modo y las dos llaves,
   reiniciar; antes, respaldo verificado de la base (`VACUUM INTO` +
   `integrity_check`, CLAUDE.md).
8. Apagar: `MERCA_CARDNET=apagado` (o borrarla) y reiniciar; los pagos con tarjeta
   pendientes se quedan como están y la conciliación no los toca mientras esté
   apagado; se vuelven a conciliar al encender.
Ningún número de teléfono; soporte por correo.

**Batería completa** (sin cambios de código si todo pasa): correr en orden
`check:encoding`, `taxonomia`, `facturas:letras`, `seguridad:probar`,
`dealer:probar`, `bitacora:probar`, `facturas:probar`, `pagos:probar`,
`transferencia:probar`, `cardnet:probar`, `chat:probar`; después, con
`MERCA_DB` desechable, `npm run db:demo`, el sitio arrancado, `npm run auditar`,
`npm run check` y `npm run check:motion`. Anotar el resultado de cada uno en el
SUMMARY. Y la búsqueda del criterio 1:
`git grep -n -i -E "c[v]v" -- tools assets db deploy '*.html'` → vacío.
  </action>
  <verify>
    <automated>npm run cardnet:probar &amp;&amp; npm run pagos:probar &amp;&amp; npm run transferencia:probar &amp;&amp; npm run facturas:probar &amp;&amp; npm run seguridad:probar &amp;&amp; npm run bitacora:probar &amp;&amp; npm run dealer:probar &amp;&amp; node -e "const s=require('fs').readFileSync('deploy/README.md','utf8');if(s.includes('\r'))throw new Error('README con CRLF');for(const t of ['MERCA_CARDNET_LLAVE_PRIV','cardnet/notificacion','mercamaquinarias-pagos.timer','DataDo.Invoice']){if(!s.includes(t))throw new Error('falta '+t)}if(/MERCA_CARDNET_LLAVE_[A-Z]+=[^<\s]/.test(s))throw new Error('valor de llave en el README');console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `deploy/README.md` contiene la sección con los ocho puntos y sigue en LF.
    - Ninguna línea de `deploy/README.md` asigna un valor real a una `MERCA_CARDNET_LLAVE_*`.
    - La batería completa y las auditorías pasan con CardNet apagado (resultado anotado en el SUMMARY).
  </acceptance_criteria>
  <done>Encender CardNet es seguir una lista; apagado, todo el sitio pasa las mismas pruebas que antes de la fase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentación → operador | Quien enciende sigue el README en el VPS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-38 | Information disclosure | README | mitigate | Solo marcadores entre ángulos; comprobado por la verificación |
| T-06-39 | Tampering | encendido en producción sin certificar | mitigate | El README ordena `lab` primero, la lista de comprobación y la pregunta de `DataDo.Invoice` como condición para encender |
| T-06-40 | Denial of service | base de producción | mitigate | Respaldo verificado antes de pasar a producción (CLAUDE.md) |
</threat_model>

<verification>
- Batería completa del job `pruebas` y auditorías con navegador en verde con CardNet apagado.
- `git grep -n -i -E "c[v]v" -- tools assets db deploy '*.html'` vacío.
- La prueba en lab con el iframe real y la notificación real queda como verificación manual (06-VALIDATION) para cuando haya credenciales; no bloquea el cierre.
</verification>

<success_criteria>
Los cinco criterios de la fase pasan de extremo a extremo contra el doble, el sitio apagado es el de la fase 5, y el encendido está escrito paso a paso.
</success_criteria>

<output>
Crear `.planning/phases/06-cardnet-construido-probado-y-apagado/06-08-SUMMARY.md` con la tabla de la batería, la correspondencia criterio → sección del arnés y la lista de lo pendiente de Victor y de CardNet.
</output>

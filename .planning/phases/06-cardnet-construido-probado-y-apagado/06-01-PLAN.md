---
phase: 06-cardnet-construido-probado-y-apagado
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/cardnet.js
  - tools/probar-cardnet.js
  - package.json
  - .github/workflows/desplegar.yml
  - .env.example
autonomous: true
requirements: [PAGO-04, PAGO-05, PAGO-06, PAGO-08]
user_setup:
  - service: CardNet (plataforma de Tokenización)
    why: "Sin las dos llaves y el modo, CardNet queda apagado y el sitio se comporta como antes de la fase."
    env_vars:
      - name: MERCA_CARDNET
        source: "Victor: `lab` con las credenciales de certificación, `produccion` tras certificar. Vacía = apagado."
      - name: MERCA_CARDNET_LLAVE_PUB
        source: "CardNet: PublicAccountKey de Tokenización (en /etc/mercamaquinarias.env)"
      - name: MERCA_CARDNET_LLAVE_PRIV
        source: "CardNet: PrivateAccountKey de Tokenización (en /etc/mercamaquinarias.env, chmod 600)"

must_haves:
  truths:
    - "Sin `MERCA_CARDNET` (o con `apagado`, vacía o un valor desconocido), o sin alguna de las dos llaves, `cardnet.activo()` es false y `cardnet.faltantes()` nombra lo que falta sin mostrar ningún valor."
    - "`cardnet.aCentavos(2000)` devuelve `200000`; con 2000.5, -1, '2000' o NaN lanza. Es la única multiplicación por 100 de `tools/cardnet.js`."
    - "El cuerpo de `cobrar` que ve el doble de transporte lleva `Amount: 200000`, `Currency: 'DOP'`, `Order`, `UniqueID` y `DataDo.Invoice` iguales a la referencia del pago, y `DataDo.Tax` en centavos."
    - "`normalizar` solo da `aprobado` ante un estado de aprobación explícito; una respuesta rara o vacía es `pendiente`."
    - "Ningún archivo de `tools/`, `assets/`, `db/`, `deploy/` ni un `.html` de la raíz nombra el código de seguridad de la tarjeta ni los campos de número o vencimiento en claro; la prueba falla si aparece uno."
    - "Las pruebas no tocan la red: el transporte real lanza si se llama durante `cardnet:probar`."
  artifacts:
    - path: "tools/cardnet.js"
      provides: "modo, activo, faltantes, urlBase, origenCaptura, autorizacionEsperada, aCentavos, normalizar, limpiar, mensajeDeRechazo, cuerpoCliente, perfilesDe, cuerpoCompra, compraDeNotificacion, crearCliente, verCliente, cobrar, consultarCompra, devolver, activarPerfil, borrarPerfil, _transporte"
      contains: "aCentavos"
    - path: "tools/probar-cardnet.js"
      provides: "Arnés de la fase 6 con contadores y process.exit"
      min_lines: 150
  key_links:
    - from: "tools/cardnet.js cobrar"
      to: "cardnet._transporte"
      via: "todas las llamadas pasan por el objeto del módulo"
      pattern: "_transporte"
    - from: ".github/workflows/desplegar.yml"
      to: "npm run cardnet:probar"
      via: "paso CardNet del job pruebas, tras Transferencia"
      pattern: "cardnet:probar"
---

<objective>
El cliente de CardNet, completo y apagado: configuración con interruptor, la
única conversión a centavos, la normalización de respuestas, el filtro de
registros de PCI, las ocho llamadas de la API de Tokenización con una costura
para pruebas, y la barrera que impide que el código nombre campos de tarjeta.

Purpose: PAGO-04 (tokenización y `POST /v1/api/purchase`), PAGO-05 (la tarjeta
nunca en nuestro servidor), PAGO-06 (centavos en un solo sitio), PAGO-08
(construido y apagado). Todo lo demás de la fase se apoya en este módulo.
Output: `tools/cardnet.js`, `tools/probar-cardnet.js` con su script y su paso de
CI, y las variables vacías en `.env.example`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/06-cardnet-construido-probado-y-apagado/06-CONTEXT.md
@.planning/phases/06-cardnet-construido-probado-y-apagado/06-RESEARCH.md
@.planning/research/cardnet.md
@tools/transferencia.js
@tools/chat.js
@tools/probar-transferencia.js

La fase 5 tiene que estar fusionada en la rama base: si `tools/transferencia.js`
o `pagos.metodosDeCobro` no existen, PARA. No se reconstruye aquí nada de otra
fase.

<interfaces>
Rutas y campos de CardNet (literales, `cardnet.md` [VERIFICADO]):
- URL base lab `https://labservicios.cardnet.com.do/servicios/tokens/`,
  producción `https://servicios.cardnet.com.do/servicios/tokens/`.
- `POST {base}/v1/api/customer` → `CustomerId`.
- `GET {base}/v1/api/customer/{CustomerId}` → `CaptureURL`, `UniqueID`, `PaymentProfiles`.
- `POST {base}/v1/api/customer/{CustomerId}/activate` con `Token` y `ActivationCode`.
- `POST {base}/v1/api/customer/{CustomerId}/PaymentProfileDelete`.
- `POST {base}/v1/api/purchase` con `TrxToken`, `Order`, `Amount`, `Currency`, `Capture`, `DataDo { Invoice, Tax }`, y `UniqueID` de idempotencia.
- `GET {base}/v1/api/purchase/{PurchaseID}`; `POST {base}/v1/api/purchase/{PurchaseID}/refund`.
- Autenticación HTTP Basic: la llave privada como usuario, contraseña vacía.
- Notificación: `Notification { ResourceType, ResourceUrl, ResourceObject }`.
- Captura: `{CaptureURL}?key={PublicAccountKey}&session_id={UniqueID}`.
- Error dedicado `CS012 PROFILE_MUST_BE_ACTIVATED_FIRST`.

Patrón de `tools/transferencia.js` (fase 5): lectura de `process.env` en cada
llamada; `faltantes()` devuelve nombres de variables, nunca valores.
Patrón de `tools/chat.js` `pedir()`: `https.request` con `timeout`, resuelve
siempre `{ estado, crudo, fallo? }` y nunca lanza.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarea 1: arnés en rojo y el núcleo puro de tools/cardnet.js</name>
  <files>tools/probar-cardnet.js, tools/cardnet.js, package.json</files>
  <read_first>
    - tools/probar-transferencia.js (cabecera, entorno antes del require, ok(), secciones, cierre)
    - tools/transferencia.js (forma del interruptor y de faltantes)
    - tools/chat.js líneas 200-235 (cliente https a mano)
    - .planning/phases/06-cardnet-construido-probado-y-apagado/06-CONTEXT.md (D-01 a D-09)
    - package.json (bloque scripts)
  </read_first>
  <behavior>
    - Sin ninguna `MERCA_CARDNET*`: `modo()` es 'apagado', `activo()` false, `faltantes()` incluye 'MERCA_CARDNET', 'MERCA_CARDNET_LLAVE_PUB' y 'MERCA_CARDNET_LLAVE_PRIV'.
    - `MERCA_CARDNET=lab` con las dos llaves de prueba: `activo()` true, `urlBase()` es la de lab con barra final, `origenCaptura()` es `https://labservicios.cardnet.com.do`.
    - `MERCA_CARDNET=produccion`: `urlBase()` es la de producción. `MERCA_CARDNET=encendido` o `LAB`: apagado.
    - `MERCA_CARDNET_URL=http://x` se ignora (no es https); `https://otra.prueba/tokens/` se usa.
    - Cambiar el entorno en la misma ejecución cambia el resultado (lectura en cada llamada).
    - `aCentavos(2000) === 200000`, `aCentavos(0) === 0`; lanza con 2000.5, -1, '2000', NaN, null.
    - `normalizar({ Status: 'Approved', ResponseCode: '00', PurchaseId: 'P1', AuthorizationCode: 'A1', Order: 'TE-2026-AAAAAA' })` → `{ resultado: 'aprobado', codigo: '00', procesadorId: 'P1', autorizacion: 'A1', referencia: 'TE-2026-AAAAAA' }`.
    - Rechazo explícito con `ResponseCode: '51'` → `resultado: 'rechazado'`, `motivo` = `mensajeDeRechazo('51')` (fondos insuficientes).
    - `normalizar(null)`, `normalizar({})`, `normalizar({ Status: 'Algo' })` → `pendiente`.
    - `normalizar` con `CommerceAction: { ActionType: 1, Url: <https del origen de CardNet> }` → `pendiente` con `redireccion`; con una URL de otro origen, sin `redireccion`.
    - `limpiar` conserva `Company` y `Brand`, quita `Token`, `TrxToken` y `CreditCardNumber`, y enmascara cualquier secuencia de 13 a 19 dígitos dentro de cadenas, a cualquier profundidad; no modifica el objeto original.
  </behavior>
  <action>
Primero el arnés en rojo (D-31), luego el código.

**`tools/probar-cardnet.js`** (nuevo): cabecera con el «por qué existe» al estilo
de `probar-transferencia.js`: cada comprobación es un fallo que costaría dinero
real o que metería el sitio en el alcance de PCI. Antes de cualquier `require`
de `./db`: `BANCO = .tmp/prueba-cardnet` borrado y recreado, `MERCA_DB`,
`MERCA_FACTURAS`, `MERCA_CORREO=archivo`, `MERCA_SECRETO` de prueba,
`MERCA_ENV` apuntando a un archivo inexistente dentro de `BANCO` (así
`tools/entorno.js`, que cargan `tareas.js` y otros, no puede reencender CardNet
desde un `.env` local), y BORRAR del entorno toda variable que empiece por
`MERCA_CARDNET` o `MERCA_TRANSFERENCIA`.
Copia `ok()` y los contadores de `probar-transferencia.js`. Funciones
`encender(modo = 'lab')` / `apagar()` que fijan o borran `MERCA_CARDNET`,
`MERCA_CARDNET_LLAVE_PUB` = `llave-publica-de-prueba` y
`MERCA_CARDNET_LLAVE_PRIV` = `llave-privada-de-prueba` (nunca valores que se
parezcan a las claves reales de QA que publica CardNet). Al cargar `./cardnet`,
sustituir inmediatamente `cardnet._transporte` por una función que lanza «la
prueba intentó salir a la red»; cada sección que necesite respuestas instala su
doble con `respuestas` programadas y una lista `llamadas` donde guarda
`{ metodo, url, cabeceras, cuerpo }`. Secciones numeradas con
`console.log('\nN. ...')`; al final `N comprobaciones, M fallos` y
`process.exit(fallos ? 1 : 0)`. Script
`"cardnet:probar": "node tools/probar-cardnet.js"` en `package.json`, junto a
`transferencia:probar`.

**`tools/cardnet.js`** (nuevo, solo módulos de Node): cabecera que explique qué
es (la única pieza que habla con CardNet; no abre la base, no sabe de cupos ni
de NCF, D-04), por qué va apagado (D-01, patrón Brevo/Anthropic), por qué las
claves no están en el repositorio aunque CardNet publique las de QA (D-03) y
por qué nunca se usa un modo «sin pantalla» (PCI, `cardnet.md`). Exporta:
- `modo()` → 'apagado' | 'lab' | 'produccion' según D-01 (comparación exacta en
  minúsculas; lo demás, 'apagado').
- `activo()` → modo distinto de 'apagado' y las dos llaves no vacías.
- `faltantes()` → nombres de variables que faltan o no valen (D-01), sin valores.
- `urlBase()` → D-02; termina siempre en `/`.
- `origenCaptura()` → `new URL(urlBase()).origin`, o null si no está activo.
- `aCentavos(pesos)` → D-05: `Number.isInteger(pesos) && pesos >= 0` o lanza
  `Error('Importe inválido para CardNet')`; devuelve `pesos * 100`. Comentario:
  el proyecto guarda pesos enteros, CardNet espera centavos («100 → 10000»), y
  un factor escrito dos veces cobra cien veces de más o de menos.
- `normalizar(respuesta)` → D-07. Aprobado: `Status` en minúsculas es
  'approved' o 'aprobada' o `ResponseCode === '00'` con `Status` ausente;
  rechazado: `Status` 'rejected'/'declined'/'rechazada' o un `ResponseCode`
  presente distinto de '00' con `Status` ausente; lo demás pendiente. Lee
  `PurchaseId` (o `PurchaseID`), `AuthorizationCode`, `ResponseCode`, `Order`.
  Si trae `CommerceAction` con `ActionType` 1 (Redirect) y una URL https cuyo
  origen es el de `urlBase()`, añade `redireccion: <url>` y el resultado es
  `pendiente` (D-34); una URL de otro origen se descarta.
  Comentario [POR CONFIRMAR EN LAB]: los nombres exactos se ajustan aquí y en
  ningún otro sitio.
- `mensajeDeRechazo(codigo)` → tabla `RECHAZOS` con al menos '05' (no
  autorizada por el banco), '14' (número inválido), '51' (fondos
  insuficientes), '54' (tarjeta vencida), '57' (operación no permitida para esa
  tarjeta), '61' (excede el límite), '91' (el banco no respondió; intente más
  tarde), 'CS012' (hay que activar la tarjeta con el código del banco); cada
  texto dice qué hacer. Desconocido: «El banco no aprobó el pago. No se le
  cobró nada, no se añadió ningún cupo y no se emitió comprobante.»
- `limpiar(obj)` → D-09: copia profunda; elimina claves cuyo nombre, en
  minúsculas, contenga 'card', 'tarjeta', 'expir', 'venc', 'token' o el nombre
  del código de seguridad, o sea exactamente 'pan', salvo 'brand', 'last4',
  'marca', 'ultimos4' (no por subcadena 'pan': borraría `Company`); los tokens
  reutilizables tampoco van a registros ni eventos; en cada cadena sustituye
  `\d{13,19}` por asteriscos. El nombre del código de
  seguridad se arma sin escribirlo literal (por ejemplo, con `String.fromCharCode`)
  para no romper la barrera de la tarea 3.
- `autorizacionEsperada()` → `'Basic ' + base64(llavePrivada + ':')`, la misma
  cabecera con la que CardNet firma sus notificaciones (la usará 06-04 para
  compararla con `timingSafeEqual`); null si no está activo.
- `_transporte(opciones)` real: `https.request` sobre la URL resultante, con
  `Authorization: Basic base64(llavePrivada + ':')`, `Content-Type:
  application/json`, `timeout: 20000`; resuelve siempre
  `{ estado, cuerpo (JSON o null), fallo? }`, nunca lanza. Nunca imprime la
  llave ni el cuerpo.
  </action>
  <verify>
    <automated>npm run cardnet:probar</automated>
  </verify>
  <acceptance_criteria>
    - `tools/cardnet.js` contiene `function aCentavos(` y `module.exports`.
    - `node -e "const c=require('./tools/cardnet');if(c.aCentavos(2000)!==200000)process.exit(1)"` sale con 0.
    - `grep -c "\* *100\|100 *\*\|/ *100" tools/cardnet.js` devuelve 1 (la de `aCentavos`).
    - `npm run cardnet:probar` sale 0 e imprime la línea `comprobaciones`.
    - `package.json` solo cambia en `scripts` (sin dependencias nuevas).
  </acceptance_criteria>
  <done>Configuración, centavos, normalización, mensajes y limpieza probados sin red; el arnés existe y corre.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarea 2: las llamadas a la API de Tokenización</name>
  <files>tools/cardnet.js, tools/probar-cardnet.js</files>
  <read_first>
    - tools/cardnet.js (lo escrito en la tarea 1)
    - .planning/research/cardnet.md (secciones «Plataforma 2», «El flujo que hay que implementar», «Cómo cuadra con la facturación»)
    - .planning/phases/06-cardnet-construido-probado-y-apagado/06-RESEARCH.md (tabla «POR CONFIRMAR EN LAB»)
  </read_first>
  <behavior>
    - Con CardNet apagado, toda llamada devuelve `{ ok: false, motivo: 'apagado' }` sin tocar `_transporte`.
    - `crearCliente({ correo, nombre, rnc })` hace POST a `{base}v1/api/customer` y devuelve `{ ok: true, clienteId }` leyendo `CustomerId`.
    - `verCliente(id)` hace GET a `{base}v1/api/customer/{id}` y devuelve `{ ok, clienteId, urlCaptura, sesion, perfiles }` con `urlCaptura = CaptureURL + '?key=' + llave pública + '&session_id=' + UniqueID` (codificados).
    - `perfilesDe(cliente)` devuelve `[{ perfilId, token, marca, ultimos4, venceMes, venceAnio, activo }]` y nada más de cada perfil.
    - `cobrar({ token, pago })` hace POST a `{base}v1/api/purchase` con el cuerpo de `cuerpoCompra`: `TrxToken`, `Order` = referencia, `UniqueID` = referencia, `Amount` = `aCentavos(total)`, `Currency: 'DOP'`, `Capture: true`, `DataDo: { Invoice: referencia, Tax: aCentavos(itbis) }`; devuelve `{ ok, ...normalizar(cuerpo), crudo: limpiar(cuerpo) }`.
    - Un pago de RD$2.000 + ITBIS 360 viaja como `Amount: 236000` y `Tax: 36000`; uno de total 2000 como `Amount: 200000`.
    - Estado 0 (red caída) o 5xx en `cobrar` → `resultado: 'pendiente'` con `fallo` descrito; nunca 'rechazado'.
    - `consultarCompra(id)`, `devolver(id)`, `activarPerfil({ clienteId, token, codigo })`, `borrarPerfil({ clienteId, perfilId })` llaman al método y la ruta documentados.
    - `compraDeNotificacion(cuerpo)` devuelve el id de compra de un `Notification` (del `ResourceObject` o del final de `ResourceUrl`) o null.
    - La cabecera `Authorization` que ve el doble es `Basic ` + base64 de `llave-privada-de-prueba:`.
  </behavior>
  <action>
Secciones nuevas del arnés primero, luego el código.

En `tools/cardnet.js`, una función interna `llamar(metodo, ruta, cuerpo)` que
comprueba `activo()` (si no, `{ ok: false, motivo: 'apagado' }`), compone la URL
con `urlBase()` y llama `module.exports._transporte(...)` —por el objeto del
módulo, como `facturas.emitirPorPago`, para que la prueba pueda sustituirlo—.
Sobre ella: `crearCliente`, `verCliente`, `cobrar`, `consultarCompra`,
`devolver`, `activarPerfil`, `borrarPerfil`, con las rutas de `<interfaces>`.

Funciones puras separadas, exportadas, cada una con su comentario
[POR CONFIRMAR EN LAB] donde el campo no está copiado literalmente en
`cardnet.md`: `cuerpoCliente({ correo, nombre, rnc })` (D-04; mínimo `Email`,
`FirstName` y `DocumentNumber` si hay RNC), `perfilesDe(cliente)`,
`cuerpoCompra({ token, pago })` (D-05 y D-06; comentario con la pregunta abierta
de `DataDo.Invoice`: si CardNet exigiera el NCF aquí, NO se reserva un NCF antes
de cobrar, se para y se rediseña) y `compraDeNotificacion(cuerpo)`.

`devolver` queda construida y probada sin ninguna ruta que la use (diferido en
CONTEXT). Nada de este archivo escribe en consola el cuerpo de una respuesta sin
`limpiar`.
  </action>
  <verify>
    <automated>npm run cardnet:probar</automated>
  </verify>
  <acceptance_criteria>
    - `tools/cardnet.js` contiene `v1/api/purchase`, `v1/api/customer`, `/activate`, `PaymentProfileDelete`, `/refund`.
    - `tools/cardnet.js` contiene `module.exports._transporte(` (llamada por el objeto del módulo).
    - `tools/cardnet.js` no contiene `console.log(` y ningún `console.error(` recibe un cuerpo sin `limpiar(`.
    - La sección del arnés que comprueba `Amount: 200000` pasa.
  </acceptance_criteria>
  <done>Las ocho llamadas existen, pasan por la costura, y su forma se prueba contra el doble sin red.</done>
</task>

<task type="auto">
  <name>Tarea 3: la barrera de PCI, el paso de CI y las variables vacías</name>
  <files>tools/probar-cardnet.js, .github/workflows/desplegar.yml, .env.example</files>
  <read_first>
    - .github/workflows/desplegar.yml (job pruebas, paso Transferencia)
    - .env.example (bloque de la transferencia de la fase 5, como modelo)
    - .gitattributes (desplegar.yml va en LF)
  </read_first>
  <action>
**Barrera (D-08)**, sección nueva del arnés: recorre recursivamente `tools/`,
`assets/`, `db/`, `deploy/` y los `.html` de la raíz (excluye `node_modules`,
`.tmp`, `.planning` y archivos binarios por extensión: png, jpg, webp, ico, pdf,
mp4, db). Para cada archivo de texto busca, sin distinguir mayúsculas, los
nombres prohibidos armados SIN escribirlos literalmente en el propio archivo:
el nombre del código de seguridad de tres letras (c-v-v, y también c-v-c),
`card-number`, `cardnumber`, `expiration-date` y `numero_tarjeta` (construye
cada uno con `['c','v','v'].join('')`, `['card','number'].join('-')`, etc.).
Cada hallazgo es un fallo con ruta y línea. Comentario: es la regla de revisión
de PAGO-05 («si el código nombra una variable así, está mal»), y la prueba se
excluye a sí misma solo porque arma los nombres por partes.

**`.github/workflows/desplegar.yml`** (LF): paso `- name: CardNet` con
`run: npm run cardnet:probar` en el job `pruebas`, justo después de
`- name: Transferencia`, misma sangría. Nada más cambia. El CI no define
ninguna `MERCA_CARDNET*`: todo lo demás corre con CardNet apagado.

**`.env.example`**: bloque «Cobro con tarjeta (CardNet)» con
`MERCA_CARDNET=`, `MERCA_CARDNET_LLAVE_PUB=`, `MERCA_CARDNET_LLAVE_PRIV=` vacías
y `# MERCA_CARDNET_URL=` comentada, explicando: qué modo es cada valor, que
vacía = apagado y el sitio sigue como antes, que las llaves son de la
plataforma de Tokenización (distintas de las del Botón de Pago), que las de QA
publicadas por CardNet tampoco van aquí, y que en el VPS van en
`/etc/mercamaquinarias.env` con permisos 600. Ningún valor, ni en comentarios.
  </action>
  <verify>
    <automated>npm run cardnet:probar &amp;&amp; node -e "const f=require('fs');const y=f.readFileSync('.github/workflows/desplegar.yml','utf8');if(y.includes('\r'))throw new Error('yml con CRLF');const i=y.indexOf('transferencia:probar'),j=y.indexOf('cardnet:probar');if(i<0||j<i)throw new Error('paso fuera de sitio');const e=f.readFileSync('.env.example','utf8');const m=e.match(/^MERCA_CARDNET[A-Z_]*=.+$/m);if(m)throw new Error('valor en .env.example');console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -n -i -E "c[v]v|card-number|expiration-date" -- tools assets db deploy '*.html'` no devuelve nada.
    - `.github/workflows/desplegar.yml` contiene `run: npm run cardnet:probar` después de `transferencia:probar` y sigue en LF.
    - `.env.example` contiene `MERCA_CARDNET_LLAVE_PRIV=` sin valor.
  </acceptance_criteria>
  <done>La barrera de PCI bloquea la fusión, la prueba corre en CI, y `.env.example` documenta las tres variables vacías.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| servidor → CardNet | Salida HTTPS con la llave privada en cabecera Basic |
| entorno → servidor | Las llaves llegan por variables de entorno que solo controla quien administra el VPS |
| repositorio → mundo | Todo lo que se commitea es público para quien tenga acceso al repositorio |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information disclosure | llave privada | mitigate | Solo en `/etc/mercamaquinarias.env` (600); `.env.example` vacío (verificado); nunca se imprime; `faltantes()` da nombres, no valores |
| T-06-02 | Information disclosure | datos de tarjeta en registros | mitigate | `limpiar()` elimina claves de tarjeta y enmascara 13-19 dígitos; ningún `console.error` con cuerpo crudo |
| T-06-03 | Elevation of privilege (alcance PCI) | código que nombra campos de tarjeta | mitigate | Barrera de la tarea 3 en CI: cero apariciones en el código |
| T-06-04 | Tampering | importe | mitigate | `aCentavos` único, entero ≥ 0, probado con 2000 → 200000; otra multiplicación por 100 hace fallar la aceptación |
| T-06-05 | Spoofing | URL base | mitigate | Solo https; por defecto las URL verificadas; `MERCA_CARDNET_URL` solo la cambia quien controla el entorno |
| T-06-06 | Denial of service | CardNet lento | mitigate | `timeout` de 20 s; red caída o 5xx = `pendiente`, nunca rechazo ni aprobación |
</threat_model>

<verification>
- `npm run cardnet:probar` en verde; `pagos:probar` y `transferencia:probar` siguen verdes sin tocarlos.
- `git grep -n "MERCA_CARDNET_LLAVE" -- .` solo devuelve nombres de variable.
- Sin dependencias nuevas.
</verification>

<success_criteria>
`tools/cardnet.js` habla con las ocho rutas de la API de Tokenización a través de una costura, convierte a centavos en un solo punto, nunca aprueba por duda y está apagado por defecto; la barrera de PCI y la prueba corren en CI.
</success_criteria>

<output>
Crear `.planning/phases/06-cardnet-construido-probado-y-apagado/06-01-SUMMARY.md` con las firmas finales de `tools/cardnet.js`, la forma del doble de transporte que usarán los planes siguientes y el número de comprobaciones.
</output>
